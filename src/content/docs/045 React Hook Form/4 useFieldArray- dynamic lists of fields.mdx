---
title: 'useFieldArray: dynamic lists of fields'
tagline: React Hook Form's useFieldArray hook, the tool for forms that own a variable-length list of rows the user adds, removes, and reorders.
chapter-id: 45
course-progress: 0.435
sidebar:
  order: 4
  label: useFieldArray
---

import { Aside, CardGrid } from '@astrojs/starlight/components';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import CodeTooltips from '../../../components/code/CodeTooltips.astro';
import Dropdowns from '../../../components/exercises/dropdowns/Dropdowns.astro';
import Sequence from '../../../components/exercises/sequence/Sequence.astro';
import Step from '../../../components/exercises/sequence/Step.astro';
import KeyBugScrub from '../../../components/lessons/045/4/KeyBugScrub.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

The invoice form you built over the last two lessons has three scalar fields — a customer, an email, a total. A real invoice doesn't. It has a *list* of line items, and the user decides how many: one line for a quick consultation, twelve for a month of itemized work. A form that owns a variable-length list like that is one of the four triggers from the start of this chapter, and this is the lesson where it finally gets its tool.

By the end, that same form grows an add-and-remove line-items section: its total is computed from the rows, it validates each row *and* "at least one line," it reorders, and it saves through the same `createInvoice` action — turning the list the user edited into the right mix of database inserts, updates, and deletes. The hook that makes all of that tractable is `useFieldArray`, and the case for it lands hardest if you first picture doing it the hard way.

## What a variable-length list costs the native way

Picture line items built on the native pattern from the last chapter — uncontrolled inputs, identified by `name`, read out of a flat `FormData`. The trouble is that `FormData` is flat and a list is not, so you fake the nesting with indexed `name` strings you generate from a counter:

```tsx
{rowIds.map((rowId, index) => (
  <div key={rowId}>
    <input name={`lineItems[${index}].description`} defaultValue="" />
    <input name={`lineItems[${index}].amount`} defaultValue="" />
  </div>
))}
```

That single map hides four separate bookkeeping jobs, and you own all four by hand:

1. **The indexed `name` strings.** `lineItems[0].amount`, `lineItems[1].amount`, and so on, each spliced together from the row's position. The server has to parse that string convention back into an array on the other side.
2. **A parallel `useState` array of row keys** — the `rowIds` above — that exists for no reason except to give React a stable `key` and to drive the add and remove buttons. It holds no data the user cares about; it's pure scaffolding for the list's identity.
3. **Manual re-indexing when a middle row is removed.** Delete row 1 of three and rows 2 and 3 have to renumber to 1 and 2, or their `name` strings collide and the `FormData` you read on submit is wrong.
4. **A second parallel effort to render each row's error.** The schema validates `lineItems` as an array, so a bad amount on row 2 lives at the array path `lineItems[2].amount`. Matching that path back to the right input, by hand, for every row, is its own pile of work.

That's four bookkeeping jobs for *one* conceptual thing — "a list of rows." None of them is about the invoice; all of them are about wrestling a flat, string-keyed form into holding a nested, growable structure. This is exactly the threshold the chapter named: when one form owns a set of repeated rows whose count the user controls, the native pattern's coordination cost crosses the line, and <Term definition="The React Hook Form hook that manages a variable-length array field: it owns the rows' identity and ordering and coordinates their re-renders, so add / remove / reorder stops being manual bookkeeping.">`useFieldArray`</Term> is the canonical reach. It owns all four jobs — the array structure, the identity keys, the re-indexing, and the per-row error wiring — behind one hook call.

One thing it does *not* change: the trust boundary. `useFieldArray` is a client-side convenience for editing the list, exactly as the resolver was a client-side convenience for validating it. The action still runs `InvoiceSchema.safeParse(input)` on entry, array and all. Adopting the hook makes the *editing* pleasant; it does not move the gate. That line has held for every lesson in this chapter and it holds here.

## The hook: fields, append, remove, and the rest

`useFieldArray` does not create a form. It plugs into the one you already have. You hand it the `control` off the `form` object the page created back in the [primitives lesson](/045-react-hook-form/2-the-five-primitives--useform-register-controller-handlesubmit-formstate/), name the array field it should manage, and it returns an array to render plus a set of operations to mutate that array. Here's the whole call:

<AnnotatedCode lang="tsx" code={`
const { fields, append, remove, move, replace } = useFieldArray({
  control: form.control,
  name: 'lineItems',
});

const addLine = () => append({ description: '', amount: 0 });
`}>
  <AnnotatedStep meta={`"fields" "append" "remove" "move" "replace"`} color="blue">
    The hook returns the array RHF tracks (`fields`) plus a toolbox of operations — this lesson reaches for `append`, `remove`, `move`, and `replace`. There are a few more (`prepend`, `insert`, `swap`, `update`), summarized just below.
  </AnnotatedStep>

  <AnnotatedStep meta={`"control: form.control" "name: 'lineItems'"`} color="green">
    `control` is the same one off the `form` you already created — that's how the hook reads and writes *this* form, not a new one. `name` is the array field's path in the schema; it must match the `lineItems` key exactly.
  </AnnotatedStep>

  <AnnotatedStep meta={`"fields"`} color="orange">
    `fields` is a render-time *snapshot* of the array, each entry carrying RHF's own `id` plus the row's values. It is **not** the live value — to read what the user has typed right now, you'll use `useWatch` a few sections down. Map over it to render rows.
  </AnnotatedStep>

  <AnnotatedStep meta={`"append({ description: '', amount: 0 })"`} color="violet">
    `append` adds a row. Pass the **full default shape** — every field the schema requires. This is the same `defaultValues` discipline from the primitives lesson, applied per row; an empty `append({})` would leave the new inputs uncontrolled.
  </AnnotatedStep>
</AnnotatedCode>

Read the operations as a small imperative API you call from event handlers — a click adds a row, a click removes one. The names say what they do, and you'll reach for a handful of them:

<CodeTooltips tooltips={{
  append: 'Add a row at the end.',
  prepend: 'Add a row at the start.',
  insert: 'Add a row at a given index.',
  remove: 'Remove the row at an index (or all rows if called with no argument).',
  swap: 'Exchange two rows by index.',
  move: 'Move a row from one index to another, shifting the rest.',
  replace: 'Swap the entire array for a new one in a single call.',
}}>
```ts
const { append, prepend, insert, remove, swap, move, replace } = fieldArray;
```
</CodeTooltips>

The job these operations quietly absorb is the third on the painful list above: re-indexing. When you `remove(1)` from a three-row list, RHF slides rows up, migrates each row's validation and dirty state to its new position, and re-keys everything internally. You never splice an array or renumber a `name` string. That whole category of bug — the one where a deleted middle row leaves the form's data misaligned — is gone, because you stopped owning the array's mechanics.

One rule from that walkthrough is worth pulling out and underlining, because it's the most common first mistake with the hook.

<Aside type="caution">
Always pass `append` (and `prepend` / `insert`) the **complete default row**, with every field the schema declares: `append({ description: '', amount: 0 })`, never `append({})`. An empty object gives the new inputs no value to start from, so they render uncontrolled and flip to controlled the instant the user types — the same uncontrolled-to-controlled warning `defaultValues` exists to prevent, now firing one row at a time.
</Aside>

## Rendering rows, and why the key must be field.id

Rendering the list is a `map` over `fields`. Each entry becomes a row, each row renders its inputs through the `Field` + `Controller` layer this chapter standardized on, and the row carries a "Remove" button; a single "Add line" button sits below the list. Here's the section, built on the form file from the last two lessons:

<div data-mark-color="green">

```tsx title="app/invoices/new-invoice-form.tsx" "key={field.id}"
{fields.map((field, index) => (
  <FieldGroup key={field.id}>
    <Controller
      name={`lineItems.${index}.description`}
      control={form.control}
      render={({ field, fieldState }) => (
        <Field data-invalid={fieldState.invalid}>
          <FieldLabel htmlFor={field.name}>Description</FieldLabel>
          <Input {...field} id={field.name} aria-invalid={fieldState.invalid} />
          {fieldState.invalid && <FieldError errors={[fieldState.error]} />}
        </Field>
      )}
    />
    <Button type="button" variant="ghost" onClick={() => remove(index)}>
      Remove
    </Button>
  </FieldGroup>
))}
<Button type="button" variant="outline" onClick={addLine}>
  Add line
</Button>
```

</div>

Everything in that snippet you've seen before — the `Controller` render prop, the `field` spread, `fieldState` driving the error row — except the field path is now templated with the row index (`lineItems.${index}.description`) and the row is keyed by `field.id`. One thing to read carefully: there are *two* different `field`s here. The outer one, from `fields.map((field, index) => …)`, is the array row — that's the one whose `id` becomes the key. The inner one, from `Controller`'s `render={({ field }) => …}`, is the per-input wiring bundle you already know. They share a name but they are different objects, and only the outer one carries the `id`. That key is the single most important character in the whole lesson, and getting it wrong produces a bug that is invisible in the code and obvious the moment you click "Remove."

<VideoCallout videoId="tvEeNPy7OVA" videoTitle="React Hook Form Tutorial - Dynamic Fields">
  Codevolution walks the same wiring end to end in 9 minutes — `useFieldArray` with `control`, mapping `fields` with `key={field.id}`, and `append` / `remove`.
</VideoCallout>

Here is the trap. Reaching for `key={index}` *looks* fine — the rows render, add works, remove works. It only breaks when you remove a row that *isn't last*.

<CodeVariants>
  <CodeVariant label="key={index} — breaks on remove">
    <div data-mark-color="red">

    ```tsx del={2}
    {fields.map((field, index) => (
      <FieldGroup key={index}>
        {/* row inputs */}
    ```

    </div>
    **Breaks on remove.** The key is the row's *position*, not its identity. Remove a middle row and every row below it shifts up by one — so React sees the key it had at that position unchanged, keeps the old DOM node, and stuffs the next row's data into it. Focus jumps, half-typed text lands on the wrong line, dirty flags migrate, exit animations fire on the wrong node.
  </CodeVariant>

  <CodeVariant label="key={field.id} — correct">
    <div data-mark-color="green">

    ```tsx ins={2}
    {fields.map((field, index) => (
      <FieldGroup key={field.id}>
        {/* row inputs */}
    ```

    </div>
    **Correct.** `field.id` is the row's stable identity — RHF assigns it once and it follows the row through every reorder and removal. Now React tracks each row's DOM node by *which row it is*, not where it sits, so removing one leaves the survivors untouched.
  </CodeVariant>
</CodeVariants>

This is one of the oldest rules in React, and you met it as a code convention earlier: *lists need a stable key tied to data identity; never the array index for reorderable lists.* The reason it bites so hard here is that `useFieldArray` is built for the reorderable case — adding, removing, and moving rows is the whole point — so the index key is wrong on every operation that isn't "append to the end." That is precisely why <Term definition="A stable key RHF assigns to each field-array row. It survives reorders and removals and is meant to be used as the React key. It is the row's render identity, not its domain ID — it is never persisted.">`field.id`</Term> exists: it's a render key RHF generates for exactly this purpose. Use it, and forget the array index for this map.

Read carefully and the breakage is plausible; *watch* it and it becomes unforgettable. Scrub through the sequence below. Three rows, A, B, and C; the user has focus and half-typed text in row C, then removes row B.

<KeyBugScrub />

The left column is the shipped-bug class: a user editing line 3 of an invoice clicks "remove line 1," and their cursor and half-typed amount silently teleport to a different row. The right column is one character's difference. That's the leverage of `field.id`.

Pin down the two characters that make this list correct. The snippet below is a minimal line-items list with `useFieldArray` already wired — plain `<input>`s, no resolver and no `Field` layer (that's the real lesson's shape, simplified here so the keying point stands alone). Two spots are left blank: the row's `key`, and the argument to `append`. Get both right and add / remove / reorder are correct; get the key wrong and a removed middle row silently teleports the survivor's text and focus.

<Dropdowns
  instructions="Two blanks: the row's `key` and the argument to `append`. Pick the stable identity for the key, and the full default row for `append`."
  answers={[
    { answer: 'field.id', options: ['field.id', 'index', 'field.description'] },
    { answer: "{ description: '', amount: 0 }", options: ["{ description: '', amount: 0 }", '{}', 'undefined'] },
  ]}
>

```tsx
export function App() {
  const { control, register } = useForm({
    defaultValues: { lineItems: [{ description: '', amount: 0 }] },
  });
  const { fields, append, remove } = useFieldArray({
    control,
    name: 'lineItems',
  });

  return (
    <div>
      {fields.map((field, index) => (
        <div key={___}>
          <input {...register(`lineItems.${index}.description`)} />
          <button type="button" onClick={() => remove(index)}>
            Remove
          </button>
        </div>
      ))}
      <button type="button" onClick={() => append(___)}>
        Add line
      </button>
    </div>
  );
}
```

</Dropdowns>

## The Zod array schema and its two error paths

The hook manages the array; the schema describes its shape. And per the discipline this chapter installed — one schema, two importers — that shape lives in exactly one place: the feature's `invoice-schema.ts`, imported by both the action and the form. So extending the form to hold line items starts by extending the schema, and everything else follows from there.

Add a `lineItems` array to `InvoiceSchema`:

```ts title="app/invoices/_lib/invoice-schema.ts" ins={5-14}
export const InvoiceSchema = z.object({
  customer: z.string().min(1, 'Customer is required'),
  email: z.email('Enter a valid email'),
  total: z.coerce.number<number>().positive(),
  lineItems: z
    .array(
      z.object({
        id: z.uuid().optional(),
        description: z.string().min(1, 'Describe the line'),
        amount: z.coerce.number<number>().positive('Amount must be positive'),
      }),
    )
    .min(1, 'Add at least one line'),
});
```

Three details in that block carry weight. The format builders follow the Zod 4 conventions you've been using — top-level `z.uuid()`, not the deprecated string chain. The amount uses `z.coerce.number<number>()`, the same generic-pinned coercion from the [resolver lesson](/045-react-hook-form/3-zodresolver--one-schema-both-sides-of-the-wire/): it accepts the string an `<input>` produces, produces a `number`, and the pinned generic keeps the *input* type a clean `number` so `defaultValues` and the registered field type-check. And the `.min(1, 'Add at least one line')` on the array is the "you can't ship an empty invoice" rule.

The one to notice is `id: z.uuid().optional()`. This is not the same as `field.id`. This is the line item's *domain* ID — the primary key it has in the database, if it has one. A row the user just added has no domain ID yet (`undefined`); a row loaded from an existing invoice carries the one the database assigned. That present-or-absent distinction is the hinge for saving the array later: present means "update this existing line," absent means "insert this new one." Plant that now; it pays off in the final section.

Now the part the chapter outline flagged as the lesson's second footgun: **where the errors land.** A field array produces errors at two genuinely different paths, and beginners reliably look for both in the wrong place.

The first is the **per-row error**. A bad amount on row 2 lands at `errors.lineItems[2].amount`. You almost never read that raw path, though, because each row renders through a `Controller` scoped to `lineItems.${index}.amount`, and the `fieldState` the render prop hands you is *already* the error for that exact row. So `<FieldError errors={[fieldState.error]} />` inside the row renders it with no path arithmetic — the same wiring you've used for every field this chapter. You just need to recognize the shape when you see it in `formState.errors`.

The second is the **array-level error** — the `.min(1, 'Add at least one line')` one. This is the trap. That error does *not* attach to any row, so it never shows up inside the `Controller`s. And it is *not* at `errors.lineItems.message`, which is where most people look second. It lives at `errors.lineItems.root.message`:

<div data-mark-color="orange">

```tsx "errors.lineItems?.root"
{form.formState.errors.lineItems?.root != null && (
  <FieldError errors={[form.formState.errors.lineItems.root]} />
)}
```

</div>

The <Term definition="The error attached to the whole array field rather than to any element. Array-level rules like .min(1) report here, at errors.<name>.root, not on any row and not at errors.<name>.message.">root error</Term> slot is specifically where RHF parks errors that belong to the array as a whole rather than to any one element. Render it once, as the list's own error row — usually right below "Add line," so an empty list shows "Add at least one line" exactly where the user would go to fix it.

Both of these are the resolver's *client-side* reads of the same schema the action parses with. The rules — the per-row `.min(1)`, the array `.min(1)`, the messages — are written once, in the schema file, and both sides read them. You are not maintaining two copies of "an invoice needs at least one line."

Quick recall check on the two paths, since prose washes over the exact characters and these are the characters that bite. Fill in the blanks.

<Dropdowns
  instructions="Fill the blanks. The first read is the per-row field error; the second is the array-level 'add at least one line' error. They land in different places."
  answers={[
    { answer: '[index]', options: ['[index]', 'root', '.amount'] },
    { answer: 'root', options: ['root', '[0]', 'message'] },
    { answer: 'message', options: ['message', 'amount', 'error'] },
  ]}
>

```tsx
const rowError =
  form.formState.errors.lineItems?.___?.amount?.message;

const listError =
  form.formState.errors.lineItems?.___?.___;
```

</Dropdowns>

## The live total: useWatch over the array

The form's `total` was a field the user typed. On a real invoice it shouldn't be — the total *is* the sum of the line amounts, and asking the user to keep it in sync with the lines they're editing is asking for a wrong number. So the senior move is to derive it: make `total` read-only and computed from the rows, updating live as the user edits amounts. That requires reading the array's *current* values, which is the moment the snapshot caveat from earlier comes due.

Recall that `fields` is a render-time snapshot, not the live value. Mapping over it renders the rows, but its `amount`s are frozen at the last render — useless for a running sum. To read what the user has typed *right now*, you have two tools, and the choice is about whether you need to react to changes:

- `form.getValues('lineItems')` reads the current array once, imperatively, and does **not** subscribe — no re-render when it changes. Right for a read inside an event handler.
- `useWatch({ control: form.control, name: 'lineItems' })` returns the current array *and* subscribes, re-rendering the caller whenever any amount changes. Right for a live-updating total.

For the total you want the subscription. But you remember the lever from the primitives lesson: a subscription in the form root re-renders the *entire* form on every keystroke. So you scope it — drop the `useWatch` into a small leaf component that renders only the total, exactly as the `CharCount` example did. The form root stays still; only the total ticks.

<AnnotatedCode lang="tsx" code={`
const InvoiceTotal = ({ control }: { control: Control<InvoiceInput> }) => {
  const lineItems = useWatch({ control, name: 'lineItems' });
  const total = lineItems.reduce(
    (sum, line) => sum + (Number(line.amount) || 0),
    0,
  );
  return <output>{total.toFixed(2)}</output>;
};
`}>
  <AnnotatedStep meta={`"useWatch({ control, name: 'lineItems' })"`} color="blue">
    `useWatch` subscribes *this leaf* to the live `lineItems` array. Because the subscription lives here and not in the form root, only this component re-renders when an amount changes — the form stays still. Same scope-the-subscription move from the primitives lesson, applied to the array.
  </AnnotatedStep>

  <AnnotatedStep meta={`{3-6} "Number(line.amount) || 0"`} color="green">
    Sum the amounts. The defensive `Number(line.amount) || 0` matters: the watched value is the *input* shape, so an amount mid-edit can be the empty string `''` (or `NaN` after `Number`). The `|| 0` keeps the running total a number while the user types — the schema's coercion is what cleans it on submit.
  </AnnotatedStep>

  <AnnotatedStep meta={`"<output>{total.toFixed(2)}</output>"`} color="orange">
    Render it read-only. `<output>` is the semantic element for a calculated result, and there's no input here for the user to fight with — the total is a consequence of the lines, not a field.
  </AnnotatedStep>
</AnnotatedCode>

You drop `<InvoiceTotal control={form.control} />` into the form wherever the total should display, and you delete the old `total` input entirely. The watched value being the *input* type is the same `z.input` versus `z.output` distinction from the resolver lesson, surfacing one more time: RHF tracks what goes in (a string mid-edit), the schema produces what comes out (a coerced number on submit). The leaf reads the input side, so it guards; the action receives the output side, already clean. *Track this, transform, receive that* — the total just happens to be reading the "track this" end.

This is the same `useWatch`-in-a-leaf isolation the primitives lesson made literal with render badges, so there's nothing new in the *mechanism* — only a new, motivated reason to reach for it. Scope the subscription, don't memoize the tree.

## Reordering rows with move

Sometimes the order of the rows matters — the line order prints on the invoice, or rows carry a priority. `move(from, to)` handles it: it slides a row to a new index and re-indexes everyone else, migrating each row's field state along with it. You don't splice the array, and because your key is `field.id` and not the index, the surviving rows keep their identity — so a reorder animates cleanly instead of flickering. (That's the keying section paying off a second time: `field.id` makes both removal *and* reordering correct.)

The honest default for "occasionally reorder a handful of rows" needs no new dependency — two buttons per row:

<div data-mark-color="blue">

```tsx title="app/invoices/new-invoice-form.tsx" "move(index, index - 1)" "move(index, index + 1)"
<Button
  type="button"
  variant="ghost"
  disabled={index === 0}
  onClick={() => move(index, index - 1)}
>
  Move up
</Button>
<Button
  type="button"
  variant="ghost"
  disabled={index === fields.length - 1}
  onClick={() => move(index, index + 1)}
>
  Move down
</Button>
```

</div>

The `disabled` guards keep the indices in bounds — no "move up" on the first row, no "move down" on the last. That's the whole feature for a list a user nudges now and then.

When the product genuinely needs drag-to-reorder — a long list the user rearranges constantly — the seam is the same. You pair `move` with a drag-and-drop library: the library fires a callback when a drag ends, handing you the source and target indices, and your handler is one line, `move(from, to)`. RHF re-renders the new order and you're done. The 2026 reach for the library itself is `@dnd-kit`; you don't need its tutorial to see the integration point, which is just `onDragEnd → move`. The reference below has the full drag example when you reach for it.

## Saving the array: the insert, update, delete diff

The form now edits a list. The last question is what the *server* does with it, and this is where the `id?` field you planted in the schema earns its place. The senior framing: the form owns the row set the user wants; the action <Term definition="To compute the INSERT / UPDATE / DELETE writes by comparing the submitted list against the rows currently in the database. The action's job after it parses the submitted array.">reconciles</Term> that set against what's currently in the database. The submitted list is the new source of truth; the action makes the database match it.

The call is unchanged from the resolver lesson: RHF holds the typed object, so `onSubmit(values)` hands it straight to `await createInvoice(values)`, and the action's first line is still `InvoiceSchema.safeParse(input)`. (The chapter's project keeps `FormData` because it's deliberately built on the native pattern; this RHF form is the typed-object caller. That decision was settled last lesson — not relitigating it here.)

What's new is the action's body. Once it has parsed the array, it computes a three-way diff by looking at each row's domain `id`:

<AnnotatedCode lang="tsx" code={`
export async function createInvoice(input: Invoice) {
  const parsed = InvoiceSchema.safeParse(input);
  if (!parsed.success) {
    return err(
      'validation',
      'Check the highlighted fields.',
      z.flattenError(parsed.error).fieldErrors,
    );
  }

  const submitted = parsed.data.lineItems;
  const existingIds = await listLineItemIds(parsed.data.id);
  const submittedIds = new Set(submitted.map((line) => line.id));

  const saved = await db.transaction(async (tx) => {
    const toInsert = submitted.filter((line) => line.id == null);
    const toUpdate = submitted.filter((line) => line.id != null);
    const toDelete = existingIds.filter((id) => !submittedIds.has(id));
    // tx.insert(toInsert) · tx.update(toUpdate) · tx.delete(toDelete)
  });

  revalidateTag('invoices');
  return ok(saved);
}
`}>
  <AnnotatedStep meta={`{2-10}`} color="blue">
    Parse first, always — the same gate from every lesson. The array crossed the wire, so it gets the same `safeParse` treatment as every other field. Bail on failure before touching the database, returning the flat `fieldErrors` the `Result` contract expects.
  </AnnotatedStep>

  <AnnotatedStep meta={`{13-14}`} color="violet">
    Load the line IDs currently in the database for this invoice, and gather the IDs the form submitted into a set. These two collections are what the diff compares — current truth versus desired truth.
  </AnnotatedStep>

  <AnnotatedStep meta={`{17-19}`} color="green">
    The three-way split, all keyed on the domain `id`. No `id` → INSERT (a row the user appended). Has an `id` → UPDATE (an existing row edited in place). In the database but absent from the submission → DELETE (a row the user removed). The `remove(index)` clicks become deletes here.
  </AnnotatedStep>

  <AnnotatedStep meta={`"db.transaction" "revalidateTag('invoices')"`} color="orange">
    All three writes run in one transaction — more than one row changes, so it's atomic or nothing (the transaction mechanics are from the Drizzle chapter). Revalidate *after* the write, before the return. External calls would sit outside the transaction; there are none here.
  </AnnotatedStep>
</AnnotatedCode>

Read the three-way split slowly, because it's the idea the whole `id?` field was for. Every row the user added with `append` has no `id`, so it's an INSERT. Every row that came from an existing invoice has its `id`, so it's an UPDATE. And every row that *was* in the database but is *missing* from what the form submitted is one the user deleted with `remove` — so it's a DELETE, found by set difference. The form never sends a "delete this" instruction; deletion is inferred from absence. That's why the submitted list has to be the complete, current desired state: the action trusts it as the full picture.

There's a satisfying loop to close after a successful save. The rows the action just *inserted* now have real database IDs — but the form still holds them as `id`-less rows, because that's how the user left them. If the user edits and saves again, those rows would look new a second time and get inserted twice. The fix is to reconcile the form to server truth: the action returns the canonical line list *with the persisted IDs*, and the form swaps its whole array for that list in one call — `replace`:

<div data-mark-color="green">

```tsx title="app/invoices/new-invoice-form.tsx" "replace(result.data.lineItems)"
const onSubmit = async (values: Invoice) => {
  const result = await createInvoice(values);
  if (result.ok) {
    replace(result.data.lineItems);
    return;
  }
  applyServerErrors(form, result);
};
```

</div>

`replace` swaps the entire array at once — cheaper and more targeted than a full `form.reset()` when only the lines changed, and crucially it stamps the inserted rows with their new `id`s. So the loop closes: the user `append`ed an `id`-less row → the action inserted it and returned it *with* an `id` → `replace` writes that `id` back into the form → the next save sees a row with an `id` and correctly UPDATEs it instead of inserting a duplicate. The form and the database stay in agreement across repeated saves.

This is the one place the action returns more than `{ id }`. The Server Action discipline from the [`Result` lesson](/043-server-actions/3-result-or-throw/) is that success returns the minimal payload — `ok({ id })` — and the client re-reads through the revalidated cache. Here the action returns the canonical line list instead, because `replace` can only reconcile the form if it gets the persisted line IDs back; a bare `{ id }` wouldn't carry them. It's a deliberate, narrow exception to the minimal-payload rule, not a license to start returning full rows everywhere.

That `applyServerErrors(form, result)` on the failure branch is the helper from the resolver lesson, reused as-is. It routes any `fieldErrors` the action returns back onto the right fields through `setError` — and it works for array paths too. The one wiring subtlety: a server-pushed error on a line has to be keyed in RHF's dotted-path shape, `lineItems.0.amount`, so that `setError` lands it on the row's registered `Controller` and the existing `<FieldError>` renders it. Match that path shape and a business-rule failure the client couldn't check — say, a line referencing an archived product — surfaces on the exact row, through the same error UI as everything else.

The save lifecycle has a precise order, and getting it out of order is the bug. Put these steps in sequence.

<Sequence instructions="Order the steps the save takes, from the form's submit to the form catching back up with the database.">
  <Step>`onSubmit` hands the typed values to `createInvoice(values)`</Step>
  <Step>The action runs `InvoiceSchema.safeParse(input)` first</Step>
  <Step>Load the line IDs currently in the database for this invoice</Step>
  <Step>Diff the submitted rows into insert / update / delete by their `id`</Step>
  <Step>Apply all three writes inside one `db.transaction`</Step>
  <Step>`revalidateTag` the invoices, then return the `Result` with persisted line IDs</Step>
  <Step>The form calls `replace` with the returned rows, stamping the new `id`s</Step>
</Sequence>

## When the array is the wrong shape

`useFieldArray` scales further than you'd guess, because of the same isolation that powers the rest of RHF: editing one row re-renders that row, not the other forty-nine. A fifty-row invoice stays smooth. But there's a ceiling, and it's worth knowing where it is so you don't drive a `useFieldArray` off a cliff.

<Aside type="note">
Past a few hundred rows, a single client form is the wrong shape — not because RHF gets slow, but because the *architecture* does. Every row ships to the client, lives in browser memory, and submits as one payload. At that scale the senior reach is a different UI entirely: a paginated list or a bulk-edit table with row-level saves, where the client only ever holds one page. That's a list-and-table pattern from later in the course, not a `useFieldArray`. Recognize the threshold; reach for `useFieldArray` for the bounded list a single form should own, and reach past it when the list is really a dataset.
</Aside>

## What you built

The invoice form that started this chapter with three scalar fields now owns a real line-items list. You added a `lineItems` array to the one shared schema; `useFieldArray` gave you `append`, `remove`, and `move` over it without a single hand-spliced array or hand-numbered `name` string; `key={field.id}` made add, remove, and reorder correct where the array index would have silently corrupted them; the two error paths — per-row through `Controller`, array-level at `errors.lineItems.root` — render through the same `<FieldError>` rows as every other field; a scoped `useWatch` in an `<InvoiceTotal>` leaf derives the total live without churning the form; and the action turns the submitted list into the right inserts, updates, and deletes by diffing on the domain `id`, with `replace` closing the loop so repeated saves stay honest.

The one mental model to keep: **`useFieldArray` owns the rows' identity and ordering — not their values.** The values live in the same `form` instance you've had all along, read with `useWatch`, written with `register` and `Controller`. The `fields` array is a keyed render snapshot, and `field.id` is its render key, distinct from the line's domain `id` that decides insert-versus-update on the server. Hold those two `id`s apart and the whole pattern stays clear.

Next, the chapter's last production pattern: carrying one form's state across many components for a multi-step wizard.

## Going further

The lesson is self-contained; these fill in the corners — the full operation reference and the drag-to-reorder example named above.

<CardGrid>
  <ExternalResource
    title="React Hook Form — useFieldArray"
    href="https://react-hook-form.com/docs/usefieldarray"
    icon="simple-icons:reacthookform"
    iconColor="#EC5990"
    description="Every operation, the field.id keying rule, and the drag-and-drop reorder example."
  />
  <ExternalResource
    title="dnd kit"
    href="https://dndkit.com/"
    icon="lucide:grip-vertical"
    description="The 2026 drag-and-drop toolkit; pair its onDragEnd with move(from, to) for sortable rows."
  />
  <ExternalResource
    title="React Hook Form — useWatch"
    href="https://react-hook-form.com/docs/usewatch"
    icon="simple-icons:reacthookform"
    iconColor="#EC5990"
    description="The hook behind the live total — subscribe to a field and isolate the re-render to one leaf."
  />
  <ExternalResource
    title="Zod — defining schemas"
    href="https://zod.dev/api"
    icon="simple-icons:zod"
    iconColor="#408AFF"
    description="The array() reference — .min() for 'at least one line' and where array errors land."
  />
</CardGrid>
