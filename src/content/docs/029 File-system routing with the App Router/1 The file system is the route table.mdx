---
title: The file system is the route table
tagline: Your first look at Next.js, how the App Router turns the folder tree under app/ into your app's URLs, and where every other file belongs.
chapter-id: 29
course-progress: 0.00005
sidebar:
  order: 1
  label: File tree and page.tsx
---

import Figure from '../../../components/figures/Figure.astro';
import TabbedContent from '../../../components/figures/tabbed-content/TabbedContent.astro';
import TabbedItem from '../../../components/figures/tabbed-content/TabbedItem.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import StateMachineWalker from '../../../components/figures/state-machine-walker/StateMachineWalker.astro';
import Question from '../../../components/figures/state-machine-walker/Question.astro';
import Branch from '../../../components/figures/state-machine-walker/Branch.astro';
import Leaf from '../../../components/figures/state-machine-walker/Leaf.astro';
import Matching from '../../../components/exercises/matching/Matching.astro';
import Pair from '../../../components/exercises/matching/Pair.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { CardGrid, FileTree } from '@astrojs/starlight/components';
import AppTreeToUrls from '../../../components/lessons/029/1/AppTreeToUrls.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

The starter you'll build on gives you a `src/app/` folder with exactly two files: a `layout.tsx` and a `page.tsx`. Open the app and you see a page at `/`. Now product hands you the next ticket: ship `/dashboard`, and a `/dashboard/invoices` page underneath it.

In most frameworks you'd go looking for the routing config — a file with an array of route objects, a `createRouter` call, something where you register the URL and point it at a component. In Next.js there is no such file. There is no route table to edit, because **the folder tree under `app/` *is* the route table.** You don't register a route; you create a folder. By the end of this lesson you'll be able to look at any `app/` tree and read off the list of URLs it serves, look at a target URL and create the files that serve it, and know the one rule that decides where every other file in the project lives.

## A folder is a URL segment, a page makes it a page

Two rules carry this whole system, and once they click, everything else is a footnote.

The first: **every folder under `app/` is a <Term definition="One slash-separated piece of a URL path. The folder app/dashboard/ owns the dashboard segment.">route segment</Term>** — one slash-separated piece of the URL. A folder named `dashboard` contributes `/dashboard`; a folder named `invoices` nested inside it contributes the `invoices` piece, giving `/dashboard/invoices`. The folder nesting *is* the URL nesting. There's nothing to wire up.

The second: **a segment is only *visitable* once it contains a `page.tsx`.** A `page.tsx` is the file whose default-exported component renders at that URL. A folder with no `page.tsx` is perfectly legal — Next.js just won't serve a page there. Visit it directly and you get a 404; its only job is to hold deeper segments that *do* have a page.

So the mapping is mechanical. `app/page.tsx` — a `page.tsx` with no folder around it — is the index route, `/`. `app/dashboard/page.tsx` is `/dashboard`. `app/dashboard/invoices/page.tsx` is `/dashboard/invoices`. That's the answer to the ticket: to ship those two routes you create `src/app/dashboard/page.tsx` and `src/app/dashboard/invoices/page.tsx`. No registration step exists because the files *are* the registration.

The diagram below makes the mapping literal. On the left is the `app/` tree; on the right, the URL each `page.tsx` produces. Read it as a function: feed in a folder path ending in `page.tsx`, read out a URL.

<Figure>
  <AppTreeToUrls />
  <Fragment slot="caption">
    Every `page.tsx` is one URL. The folder path to it, minus the filename, is the path of the URL — the numbers tie each leaf to the route it serves.
  </Fragment>
</Figure>

Now the file itself. A `page.tsx` is the smallest thing that can be a page: a component, default-exported.

```tsx
export default function DashboardPage() {
  return <h1>Dashboard</h1>;
}
```

Three facts about this file are load-bearing, and each one tends to trip people up.

The export is `default`, and that is *not* optional. Next.js imports the default export of every `page.tsx` to find the component to render — it has no other way to know which thing in the file is the page. This is worth flagging because it cuts against a rule you'll see everywhere else in this course: prefer named exports, always. The App Router is one of the few places that rule yields, because the framework mandates a default export from its convention files (`page.tsx`, `layout.tsx`, `route.ts`, and a handful of others). Default exports are deliberate *here* and a code smell *everywhere else*; the convention files are the documented exception, not a loophole.

The component's *name* is irrelevant to routing. `DashboardPage`, `Page`, `Anything` — Next.js never reads the name; it reads the default export and the folder it sits in. The **folder** names the route, the file just provides the component. Name it for the humans reading the code (`DashboardPage` is clearer than a bare `Page`), not for the router.

And it's a <Term definition="A component that runs on the server during the request and ships zero JavaScript to the browser by default. The opposite is a Client Component, marked with 'use client'.">Server Component</Term> by default — no `'use client'` at the top. For now that means: it renders on the server and ships no client-side JavaScript unless something asks for it. The full weight of what "Server Component" means is the next chapter's job; here, just register that `page.tsx` starts on the server side of the line.

<VideoCallout videoId="9602Yzvd7ik" videoTitle="Next.js Routing — file-based routing explained">
  Codevolution builds the same folder-to-URL mapping from scratch in 6 minutes — including the 404 you get when a folder has no `page.tsx`.
</VideoCallout>

## Read the tree, write the tree

Before anything gets layered on top, lock in the core mapping in both directions. The whole rest of the lesson assumes you can do this fluently.

Match each `page.tsx` path on the left to the URL it produces on the right.

<Matching instructions="Match each `page.tsx` file to the URL where it renders.">
  <Pair>
    <Fragment slot="left">`app/page.tsx`</Fragment>
    <Fragment slot="right">`/`</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">`app/settings/page.tsx`</Fragment>
    <Fragment slot="right">`/settings`</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">`app/settings/billing/page.tsx`</Fragment>
    <Fragment slot="right">`/settings/billing`</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">`app/dashboard/page.tsx`</Fragment>
    <Fragment slot="right">`/dashboard`</Fragment>
  </Pair>
</Matching>

Now reverse it, in your head: product wants a page at `/team/members`. Which files do you create? Work it out before you open the answer.

<details>
<summary>Answer</summary>

Two files: `src/app/team/page.tsx` (serves `/team`) and `src/app/team/members/page.tsx` (serves `/team/members`). If product only asked for `/team/members` and never `/team`, you'd still create the `team/` folder — it's the segment that holds `members/` — but you could leave it without a `page.tsx`, so `/team` would 404 while `/team/members` works.

</details>

## The starter is the source of truth, not the wizard

Every Next.js tutorial, repository, and screenshot you'll ever meet was scaffolded by one command: `pnpm create next-app`. It's the official project generator, and you should be able to recognize what it produces — but in this course you won't run it. The course ships its own starter, and *that* is canonical. Knowing the wizard's output matters only so its artifacts look familiar when you see them in the wild.

Here's the shape it generates — with the same choices the course's starter makes (Biome, a `src/` directory), so this is also the tree you'll be working in:

<FileTree>
- src/
  - app/
    - **layout.tsx** root layout — owns `<html>`/`<body>` (next lesson)
    - **page.tsx** the `/` route
    - globals.css global stylesheet, imported by the root layout
- next.config.ts framework config — Turbopack, React Compiler, etc.
- tsconfig.json TypeScript config — defines the `@/*` import alias
- biome.json linter + formatter config
- package.json
- AGENTS.md instructions for coding agents
</FileTree>

In Next.js 16 the wizard offers two paths. The fast one is a single keystroke for **recommended defaults**: TypeScript, ESLint, Tailwind v4, the App Router, and an `AGENTS.md`. The other is a **customize** path that additionally asks you to choose a <Term definition="A tool that catches code problems (linting) and enforces consistent formatting. Biome is a single Rust binary that does both, fast.">linter</Term> — ESLint, <Term definition="A fast Rust-based linter and formatter, a single binary replacing ESLint + Prettier. The course's pick.">Biome</Term>, or none — whether to enable the React Compiler, whether to put your code under a `src/` directory, and what import alias to use. There is no bundler question: <Term definition="Next.js 16's default bundler, written in Rust. Replaces Webpack with no flag to set.">Turbopack</Term> is the default in 16, with nothing to opt into.

The course's starter picks Biome (the wizard now offers it as a first-class choice, not the default) and the `src/` directory — which is why your tree is `src/app/…` rather than a bare `app/…` at the repo root. That's the only structural difference you'll notice: same routing rules, one extra folder at the top. The `AGENTS.md` in the template is there specifically to steer coding agents toward current Next.js patterns rather than stale ones — a small nod to how this code actually gets written in 2026.

:::note
When you read Next.js docs or a random GitHub repo, you'll often see bare `app/` at the project root instead of `src/app/`. They're the same thing — that project just skipped the `src/` directory. Every routing rule in this lesson applies identically; mentally prepend `src/` and move on.
:::

## Co-locate by feature, not by layer

You can now create routes. The bigger question — the one that shapes the entire codebase, not just one URL — is where the *rest* of the code goes. And the App Router has a strong, opinionated answer that's worth treating as the first real architectural principle of this course.

Make it concrete. The dashboard needs three supporting pieces, each used *only* by the dashboard: a `RevenueChart` component, a `formatCents` helper that turns `4250` into `$42.50`, and an `archiveInvoice` server action. Where do those three files go?

There are two answers, and the difference between them is the whole point.

<TabbedContent syncKey="code-organization">
  <TabbedItem label="By layer" caption="One feature, scattered across five folders — each shared with every other feature. To understand the dashboard you open all five and pick its files out of the pile.">
    <FileTree>
    - src/
      - app/
        - dashboard/
          - page.tsx
      - components/
        - revenue-chart.tsx the dashboard's
        - …
      - hooks/
        - use-dashboard-filters.ts the dashboard's
        - …
      - lib/
        - format-cents.ts the dashboard's
        - …
      - services/
        - archive-invoice.ts the dashboard's
        - …
    </FileTree>
  </TabbedItem>

  <TabbedItem label="By feature" caption="One feature, one folder. To understand the dashboard you open one place — and to delete it, you delete one folder.">
    <FileTree>
    - src/
      - app/
        - dashboard/
          - **page.tsx**
          - revenue-chart.tsx
          - use-dashboard-filters.ts
          - format-cents.ts
          - archive-invoice.ts
    </FileTree>
  </TabbedItem>
</TabbedContent>

The first tab organizes **by layer**: a folder per *kind* of file — all components together, all hooks together, all helpers together. It's a structure you'll recognize from older codebases, and it's not wrong everywhere. But for code that belongs to one route, it's the wrong default, and the second tab shows why: organizing **by feature** puts everything the dashboard needs *inside* `app/dashboard/`. One feature, one folder.

This is **Architectural Principle #1: organize by purpose, not by file kind.** A feature's components, helpers, and actions live next to the page they serve, not in a `components/` or `hooks/` or `services/` bucket on the other side of the repo.

You already have the instinct for this, even if you've never named it. When you write a function with a couple of private helpers, you put the helpers right next to it, not in a `helpers.ts` file three directories away — the things that change together should *sit* together. The App Router just extends that instinct to the file system. And it's especially natural here because the file system is *already feature-shaped* — it's the route tree. Co-locating with the route means your folders mirror your product.

The stakes are about *change*, which is most of what you do to a codebase after the first week. When the dashboard needs work, the by-feature layout gives you one folder to open to understand it and one folder to delete to remove it. The by-layer layout spreads a single feature's entire lifecycle across five directories: every change is a five-folder scavenger hunt, and deleting the feature cleanly means hunting down its scattered files one by one and hoping you got them all. *To delete a feature, delete its folder* — that's the test, and only one of these layouts passes it.

## Private folders keep your files out of the router

Co-location raises an obvious worry. If *every folder under `app/` is a route segment*, then doesn't putting a `revenue-chart.tsx` next to `page.tsx` risk turning it into a URL? Or worse — if I make a `charts/` folder to group a few of them, is `/dashboard/charts` now a broken half-page?

Two things resolve this, and the distinction between them is a genuinely experienced one.

First, the safe default you already half-know: only `page.tsx` (and one other file we'll meet shortly) creates a route. A bare `revenue-chart.tsx` sitting next to `page.tsx` is **already non-routable** — it's just a module you import, never a URL. Files in `app/` are safely co-located by default. So a stray component file isn't going to leak as a page.

<VideoCallout videoId="fpOinVG85Pk" videoTitle="File Colocation in Next.js">
  Codevolution demonstrates the safe-by-default rule live (3 min) — dropping a component next to `page.tsx` and confirming it never becomes a URL.
</VideoCallout>

But a *folder* is a segment, so `charts/` is a real concern, and that's where the underscore comes in. **A folder whose name starts with `_` is invisible to the router.** `app/dashboard/_components/` and everything inside it opt out of routing entirely — `app/dashboard/_components/revenue-chart.tsx` is reachable by import but can never, ever be a URL. The underscore is the explicit "this is supporting code, not a route" marker.

So if bare files are already safe, why bother with the underscore at all? Three reasons, in ascending order of how much they matter:

- **Readability.** Grouping the supporting files under `_components/` and `_lib/` keeps the route folder itself scannable — `page.tsx` and a couple of named folders, instead of a dozen loose files you have to sort by eye.
- **Convention.** It's what the ecosystem expects. Other developers and tooling read `_components/` instantly as "co-located, non-routable." Following the shared convention is free legibility.
- **Future-proofing.** This is the real one. Next.js keeps adding reserved filenames. A bare `default.tsx` you wrote as a component would silently become a parallel-route fallback — a framework convention you'll meet later in this chapter — and you'd have an extremely confusing bug. A file at `_lib/default.ts` can never collide with a framework name, because the whole `_lib/` folder is off the router's radar.

Think of the underscore as cheap insurance, not a hard requirement. Here's the canonical shape of a real feature folder — the structure you'll reach for over and over for the rest of the course:

<Figure>
  <FileTree>
  - src/
    - app/
      - dashboard/
        - **page.tsx** the route — `/dashboard`
        - layout.tsx the dashboard shell (next lesson)
        - `_components/` *— not a route, imported never visited*
          - revenue-chart.tsx
          - kpi-card.tsx
        - `_lib/` *— not a route*
          - format-cents.ts
        - `_actions.ts` *— not a route, the dashboard's server actions*
  </FileTree>
  <Fragment slot="caption">
    The canonical feature folder. Only the bold `page.tsx` is a URL; the `_`-prefixed folders and `_actions.ts` are *invisible to the router* — reachable by import, never a page.
  </Fragment>
</Figure>

A few conventions are baked into that tree, and they line up with the project's code standards. Filenames are kebab-case (`revenue-chart.tsx`, `format-cents.ts`) — the only exceptions are framework-mandated names like `page.tsx` and `layout.tsx`. The `_actions.ts` file is where a route's <Term definition="Functions that run on the server, called from the client like normal functions. Covered in depth in a later chapter.">server actions</Term> live; you'll meet them properly much later, so for now just file it as "the dashboard's server-side mutations."

One more standard worth stating now: when you import from `_lib/`, you import the *specific file* you need — `_lib/format-cents` — and there is no `index.ts` that re-exports everything from the folder. Those re-export files (barrels) are banned in this course, because a barrel pulls every file in the folder into the module graph at once and defeats the careful Server/Client split you'll spend the next chapter learning. Import the file, not the folder.

## What lives outside the route folder

Co-location has a natural counter-question: surely not *everything* lives inside a route folder? A `Button`, a `formatCents` you end up needing in three different features, the database client — those aren't owned by any single route. Where do they go, and when?

One rule resolves every instance of this question:

> Co-locate under `_components/` or `_lib/` **until a second feature imports it. Then promote it** to the top-level `components/` or `lib/`.

The threshold is "used by two or more features." A helper the dashboard alone uses stays in `app/dashboard/_lib/`. The moment the invoices page *also* needs it, it graduates to `src/lib/` where both can reach it. Start local, promote on the second use. That single heuristic is the answer to "where does this go" for shared code.

Here's the project-root surface those promotions land in — the top-level shape you'll inhabit for the rest of the course:

<FileTree>
- src/
  - **app/** all routes live here
  - **components/** app-wide UI — used by 2+ features
    - ui/ shadcn primitives *(Ch 027)*
  - **lib/** shared helpers — used by 2+ features
    - utils.ts `cn()` and other tiny helpers
    - result.ts the `Result` type and its helpers
  - db/ the database layer *(a later unit)*
  - env.ts typed, validated env vars *(a later chapter)*
- next.config.ts framework config
- tsconfig.json defines the `@/*` alias
- proxy.ts the request gate *(a later chapter)*
</FileTree>

Don't try to absorb that whole tree. Most of it is owned by chapters you haven't reached — `db/`, `env.ts`, `proxy.ts` all get their own lessons. The point of showing it now is orientation: `app/` is routes, `components/` is shared UI, `lib/` is shared logic, and everything else is a labeled box you'll open later. It's a map, not a syllabus.

There's one mechanical detail in those imports worth pinning down, because you'll type it hundreds of times. Look at how a route reaches a shared helper.

<CodeVariants>
  <CodeVariant label="Deep relative">
    ```tsx title="src/app/dashboard/invoices/page.tsx"
    import { formatCents } from '../../../lib/format-cents';
    ```
    **Fragile.** Count the dots — three levels up just to reach `lib/`. Move this page one folder deeper or one folder over and every `../` is now wrong. The path describes where the file *is*, not what it *wants*.
  </CodeVariant>

  <CodeVariant label="Alias">
    ```tsx title="src/app/dashboard/invoices/page.tsx"
    import { formatCents } from '@/lib/format-cents';
    ```
    **Stable.** `@/` always means the source root, no matter how deep this file sits. Move the page anywhere and the import is unchanged — it names the *destination*, not a relative walk.
  </CodeVariant>
</CodeVariants>

The `@/` is an <Term definition="A tsconfig.json path mapping that turns @/x into an absolute import from the source root, so it never depends on where the importing file sits.">import alias</Term> — a single line in `tsconfig.json` maps `@/*` to your source root, and from then on `@/lib/format-cents` resolves the same from anywhere in the project. The contract an experienced developer follows: **`@/` for everything that crosses a feature boundary; relative paths only for intra-feature, same-folder or sibling imports.** Inside `app/dashboard/`, the page imports its own chart with `./_components/revenue-chart` — they're one feature, they move together, the relative path is correct and reads as "right here." But the moment an import reaches *out* of the feature — to `lib/`, to `components/`, to another route's code — it goes through `@/`.

This dovetails with the import-ordering rule from the code standards: imports come in three groups — external packages first, then `@/` aliases, then relative imports — separated by blank lines. So the alias isn't just ergonomics; it's the visual cue that says "this dependency lives outside my feature."

## Where does this file go?

You now have both halves of the placement decision — co-locate by feature, promote on the second use. The skill is running them as a single reflex every time you create a file. Walk the decision below; it's the exact question-order an experienced developer runs in their head when they drop a new file into the project.

<StateMachineWalker title="Where does this file go?">
  <Question id="routable" prompt="Is this file a routable page — something with its own URL?">
    <Branch label="Yes — it renders at a URL" to="leaf-page" />
    <Branch label="No — it's supporting code" to="features" />
  </Question>

  <Question id="features" prompt="How many features use it?">
    <Branch label="Only one feature" to="kind-local" />
    <Branch label="Two or more features" to="kind-shared" />
  </Question>

  <Question id="kind-local" prompt="Is it a component, or a helper / action?">
    <Branch label="A component (renders UI)" to="leaf-feature-components" />
    <Branch label="A helper or a server action" to="leaf-feature-lib" />
  </Question>

  <Question id="kind-shared" prompt="Is it UI, or logic?">
    <Branch label="UI (a shared component)" to="leaf-components" />
    <Branch label="Logic (a shared helper)" to="leaf-lib" />
  </Question>

  <Leaf id="leaf-page" verdict="page.tsx in its route folder">
    Create the folder for the URL segment and drop a `page.tsx` in it. The folder names the route; the default export is what renders.
  </Leaf>

  <Leaf id="leaf-feature-components" verdict="app/<feature>/_components/">
    A component only this feature uses belongs in the feature's private `_components/` folder — co-located, non-routable, reachable by import.
  </Leaf>

  <Leaf id="leaf-feature-lib" verdict="app/<feature>/_lib/ (or _actions.ts)">
    A single-feature helper goes in the feature's `_lib/`; a single-feature server action goes in its `_actions.ts`. Co-located, private to the feature.
  </Leaf>

  <Leaf id="leaf-components" verdict="components/">
    Two features need this UI, so it's no longer feature-scoped. Promote it to the top-level `components/`.
  </Leaf>

  <Leaf id="leaf-lib" verdict="lib/">
    Two features need this logic. Promote it to the top-level `lib/`, where both can import it through `@/lib/…`.
  </Leaf>
</StateMachineWalker>

That walk is the entire placement model compressed into a question order. Internalize the *order* — routable first, then feature-count, then kind — and you'll never again stare at a new file wondering where it belongs.

## Files the framework reads at the top of app/

A `page.tsx` isn't the only filename Next.js treats specially. There's a small vocabulary of reserved names the framework looks for, and the only reason to meet them now is so you don't confuse them with ordinary files — or accidentally name one of your own files into a framework behavior. Each gets a sentence here and a full lesson later.

**`route.ts` — the other routable file.** A segment becomes visitable with a `page.tsx`, but it can *also* become visitable with a `route.ts` instead. The difference: `page.tsx` renders UI at a URL, while `route.ts` returns raw HTTP responses at a URL — JSON, a redirect, a file — with no UI at all. It's how you build an API endpoint inside the App Router, following the same folder-is-a-segment rule with a different leaf file. So `app/api/health/route.ts` serves `/api/health` as an endpoint. That's all you need for now; a later chapter owns route handlers in full. The one line to keep: **`page.tsx` = UI at a URL, `route.ts` = HTTP endpoint at a URL.**

**Reserved files at `app/`'s root.** A handful of filenames generate behavior simply by *existing* at the right path. `globals.css` is the global stylesheet the root layout imports. `favicon.ico` becomes the browser-tab icon with no configuration. And a set of metadata files — `robots.ts`, `sitemap.ts`, `opengraph-image.png`, `icon.png`, and their siblings — generate SEO and social-sharing artifacts just by being present. You don't need to know any of them in depth yet; a later chapter owns the SEO set. The takeaway is narrow: **some filenames at `app/` are framework-reserved — don't reuse those names for your own files.**

**The Pages Router — one line.** Before the App Router, Next.js routed through a `pages/` directory with different rules. A legacy codebase may still have one; this course teaches only the App Router, and migrating off `pages/` is out of scope. If you see a `pages/` folder, read it as "the old system" and keep moving.

## Sorting routable from non-routable

This whole lesson comes down to one question you should now be able to answer instantly for any file: *does it create a URL, or not?* The sort below is every rule from the lesson in one drill — `page.tsx` and `route.ts` make URLs; underscores, missing pages, and anything outside `app/` don't. Drop each item in the right bucket.

<Buckets twoCol instructions="Sort each path by whether visiting it serves a URL.">
  <Bucket name="url" label="Creates a URL" description="Visiting this path serves something." />
  <Bucket name="no-url" label="Never a URL" description="Reachable by import at most, never visited." />

  <Item bucket="url">`app/page.tsx`</Item>
  <Item bucket="url">`app/blog/page.tsx`</Item>
  <Item bucket="url">`app/api/health/route.ts`</Item>
  <Item bucket="url">`app/(marketing)/page.tsx`</Item>
  <Item bucket="no-url">`app/dashboard/_components/chart.tsx`</Item>
  <Item bucket="no-url">`app/dashboard/` *(folder, no `page.tsx`)*</Item>
  <Item bucket="no-url">`src/lib/utils.ts`</Item>
  <Item bucket="no-url">`app/_lib/format.ts`</Item>
</Buckets>

The `app/(marketing)/page.tsx` item is a deliberate teaser. A folder wrapped in parentheses is a *route group* — it organizes files without contributing a segment to the URL, so that `page.tsx` still renders at `/`, not `/(marketing)`. That's the next lesson's territory; for now, just file it under "creates a URL, the parentheses vanish from the path."

## External resources

The official Next.js documentation has a dedicated page on these conventions — the canonical reference for the folder, file, and co-location rules this lesson covered. Keep it bookmarked; it's the page you'll come back to when you're unsure whether a filename is reserved. The free Next.js Learn course walks the same routing rules hands-on, and the App Router Playground lets you click through them live.

<CardGrid>
  <ExternalResource
    href="https://nextjs.org/learn/dashboard-app/creating-layouts-and-pages"
    title="Next.js Learn: Creating Layouts and Pages"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="Vercel's free interactive course builds nested routes with page.tsx and co-located files step by step — the same rules, done by hand."
  />
  <ExternalResource
    href="https://nextjs.org/docs/app/getting-started/project-structure"
    title="Project structure and organization"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="The canonical reference for App Router folder conventions, reserved filenames, and co-location — the source behind every rule in this lesson."
  />
  <ExternalResource
    href="https://nextjs.org/docs/app/getting-started/installation"
    title="Getting Started: Installation"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="What pnpm create next-app generates and the choices it prompts for — useful context for the wizard output you'll recognize elsewhere."
  />
  <ExternalResource
    href="https://app-router.vercel.app/"
    title="Next.js App Router Playground"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="Vercel's live playground for App Router features — click through routes, layouts, and file conventions, with source linked for each example."
  />
</CardGrid>
