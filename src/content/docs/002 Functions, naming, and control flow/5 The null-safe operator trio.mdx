---
title: "The null-safe operators: ?., ??, ??="
tagline: "JavaScript's operators for missing values: optional chaining, nullish coalescing, and nullish assignment, without defensive chains or falsy-coercion bugs."
chapter-id: 2
course-progress: 0.0145
rewrites: 2
sidebar:
  order: 5
  label: Null-safe operators
---

import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import MultipleChoice from '../../../components/exercises/multiple-choice/MultipleChoice.astro';
import McqChoice from '../../../components/exercises/multiple-choice/McqChoice.astro';
import McqWhy from '../../../components/exercises/multiple-choice/McqWhy.astro';
import PredictOutput from '../../../components/exercises/predict-output/PredictOutput.astro';
import PredictWhy from '../../../components/exercises/predict-output/PredictWhy.astro';
import ScriptCoding from '../../../components/live-coding/ScriptCoding/ScriptCoding.astro';
import TSPlaygroundCallout from '../../../components/embeds/TSPlaygroundCallout.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import Term from '../../../components/ui/Term.astro';
import { CardGrid } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

Two bugs share a root cause: a value that might be missing. The first is the defensive chain `if (user && user.profile && user.profile.address) { … }`, retyped on every page until someone reaches for a shorter fix. The second is `const pageSize = input.pageSize || 20`, which works until a caller passes `pageSize: 0` to mean "show no rows" and your function silently renders twenty. A trio of operators fixes both precisely.

**`?.`**, optional chaining, reads a property or calls a method that might not be there. **`??`**, nullish coalescing, supplies a default only when the value is actually missing. **`??=`**, nullish coalescing assignment, fills a slot the first time it's needed. They come with one rule: JavaScript rejects `??` mixed with `||` or `&&` unless you add parentheses.

Last lesson's guard clauses handle these cases with an `if` and a return; these operators handle them inline, no extra indentation.

## `?.` for nullable property and call access

The first operator collapses a defensive chain into a single line.

```ts
const city = user?.profile?.address?.city;
```

Each `?.` checks the value to its **left** before stepping right. The first nullish link <Term definition="The expression stops evaluating at the first link that returns the early value. For ?., that's the first nullish link returning undefined; for ??, that's a non-nullish left returning itself.">short-circuits</Term> the whole expression to `undefined`, so the chain replaces nested `&&` guards and never throws `Cannot read property 'address' of undefined`. <Term definition="The ?. operator family — ?., ?.(), ?.[…] — for accessing a property, calling a method, or indexing when the receiver might be null or undefined.">**Optional chaining**</Term> has three forms, each with its own punctuation.

<CodeVariants>
  <CodeVariant label="Property access">
    ```ts
    const city = user?.profile?.address?.city;
    ```
    Use the dotted form at any access where the receiver might be missing.
  </CodeVariant>

  <CodeVariant label="Method call">
    ```ts
    const greeting = user?.greet?.();
    ```
    The `?.()` form checks that `user.greet` is not nullish before calling: an optional `onSuccess?.()` prop, a `cleanup?.()` from an effect, a feature-flagged method on a third-party SDK.
  </CodeVariant>

  <CodeVariant label="Indexed access">
    ```ts
    const first = invoices?.[0];
    ```
    The `?.[…]` form checks `invoices` before indexing. Use it wherever the key needs brackets: dynamic keys, numeric indices, anything not expressible as a static dotted path.
  </CodeVariant>
</CodeVariants>

### Each `?.` only guards the link to its left

A `?.` checks exactly one step, so one `?.` at the front of a chain leaves every later link unguarded.

```ts
const city = user?.profile.address.city;
```

This looks defensive but isn't. The `?.` only guards the access to `profile`; if `user.profile` is nullish, `.address` throws the same `Cannot read property 'address' of undefined` you were avoiding. The fix is a `?.` at every link that could be missing:

```ts
const city = user?.profile?.address?.city;
```

When you review a chain with one `?.` followed by plain dots, check whether the types allow the middle links to be nullish. If they do, the chain is one missing field away from a runtime error.

### The overuse trap: don't silence the type system

The opposite reflex causes more trouble: adding `?.` wherever you're unsure. Every unnecessary `?.` silences the type system on its line. Optional chaining is for the nullables the type acknowledges; TypeScript is meant to fail loudly when a field that was non-nullable becomes nullable later. Reach for `?.` on a field the type says is required, and you turn that future warning off.

```ts
type Order = {
  customer: Customer;
  lines: Line[];
};

const orderTotal = order.customer?.lines.length;
```

The type marks `customer` as required, so its type is `Customer`, not `Customer | undefined`. The `?.` reads as cautious but isn't: the day someone marks `customer` optional to support draft orders, every call site silently returns `undefined` instead of failing at the line that now needs a second look. The rule:

**`?.` where the type acknowledges nullable; let TypeScript fail at the call site otherwise.**

A `?.` on a non-nullable field is a hidden hole in your type coverage, not extra safety. The narrowing chapter later in this unit covers when a guard clause that narrows before the access beats a `?.` after it.

:::tip
Biome's `useOptionalChain` rule (in the recommended preset) flags long `&&` chains that only guard a property access and autofixes them to the `?.` form. It handles the form; which links genuinely need a `?.` is still your call.
:::

The exercise below checks the per-link rule on a realistic chain.

<MultipleChoice>
  Given the types below, which expression fetches the city without throwing when any nullable link is missing — and without silencing a future type error?

  ```ts
  type Address = { city: string };
  type Customer = { address?: Address };
  type Order = { customer?: Customer; lines: Line[] };

  declare const order: Order;
  ```

  <McqChoice>
    ```ts
    order.customer?.address.city
    ```
  </McqChoice>
  <McqChoice correct>
    ```ts
    order.customer?.address?.city
    ```
  </McqChoice>
  <McqChoice>
    ```ts
    order?.customer?.address?.city
    ```
  </McqChoice>
  <McqChoice>
    ```ts
    order.customer.address.city
    ```
  </McqChoice>

  <McqWhy>The second is the right shape: both nullable links (`customer?` and `address?`) carry a `?.`, and the final `.city` reads safely because the chain has already short-circuited to `undefined` if anything before was missing. The first is the per-link trap — `customer?.address` guards `customer`, but the plain `.city` still throws the moment `address` is missing. The third is the overuse trap: `order` is non-nullable per the type, so the leading `?.` silences a future signal — the day someone marks `order` optional, every caller silently returns `undefined` instead of the compiler pointing at the chain. The fourth is the original defensive-chain bug: no guards at all, throws on the first missing link.</McqWhy>
</MultipleChoice>

## `??` for "use a default when the value is missing"

The rule fits in one sentence: **`??` returns the right operand only when the left is `null` or `undefined`, while `||` returns it for any falsy left.**

**Nullish** means genuinely absent: `null` or `undefined`. **Falsy** is wider, also counting legitimate-but-empty values as missing: the empty string, zero, `false`, and `NaN`. They overlap on `null` and `undefined` and diverge everywhere else. `||` defaults on every zero, empty string, or `false` the caller might pass, not just on absence, and you almost never want both behaviors at once.

### Three concrete bugs `||` ships

All three tabs share one shape: the right operand is the default, the left can legitimately be falsy, and `||` overwrites the value the caller meant.

<CodeVariants>
  <CodeVariant label="`pageSize || 20` swallows 0">
    <div data-mark-color="orange">

    ```ts {1}
    const pageSize = input.pageSize || 20;
    ```

    </div>

    <div data-mark-color="green">

    ```ts {1}
    const pageSize = input.pageSize ?? 20;
    ```

    </div>

    A caller passing `pageSize: 0` means "show no rows," a valid request for an empty result. `||` reads `0` as falsy and overwrites it with `20`; `??` keeps the zero and defaults only when the field is absent.
  </CodeVariant>

  <CodeVariant label="`name || 'Anonymous'` swallows empty string">
    <div data-mark-color="orange">

    ```ts {1}
    const displayName = form.name || 'Anonymous';
    ```

    </div>

    <div data-mark-color="green">

    ```ts {1}
    const displayName = form.name ?? 'Anonymous';
    ```

    </div>

    An empty string is often a field the user deliberately cleared before submit. `||` treats it as missing; `??` respects the choice and defaults only when the field wasn't submitted at all.
  </CodeVariant>

  <CodeVariant label="`enabled || true` swallows false">
    <div data-mark-color="orange">

    ```ts {1}
    const enabled = settings.enabled || true;
    ```

    </div>

    <div data-mark-color="green">

    ```ts {1}
    const enabled = settings.enabled ?? true;
    ```

    </div>

    A boolean literal on the right is the clearest sign `||` is wrong. When the left is also boolean, falsy-versus-nullish is the whole question, and `||` overwrites every deliberate `false` the user sets.
  </CodeVariant>
</CodeVariants>

### The whole table, by hand

Work the full table by hand to fix the distinction. The program runs six inputs, five falsy and one ordinary string, through both `||` and `??` for twelve outputs. The three rows where the operators disagree are the three bugs from the tabs above.

<PredictOutput
  instructions="Predict every line this program prints. Six inputs crossed against `||` and `??` — twelve lines total. The rows where the two operators diverge are the three bugs `||` ships and `??` fixes."
  expected={`0 || 'D' = D
0 ?? 'D' = 0
"" || 'D' = D
"" ?? 'D' = 
false || 'D' = D
false ?? 'D' = false
null || 'D' = D
null ?? 'D' = D
undefined || 'D' = D
undefined ?? 'D' = D
"value" || 'D' = value
"value" ?? 'D' = value`}
>

```ts
const cases = [0, '', false, null, undefined, 'value'];
for (const v of cases) {
  console.log(`${JSON.stringify(v)} || 'D' = ${v || 'D'}`);
  console.log(`${JSON.stringify(v)} ?? 'D' = ${v ?? 'D'}`);
}
```

<PredictWhy>The operators disagree on three rows: `0`, `''`, and `false`. For each, `||` reads the left as falsy and substitutes the default, while `??` reads it as non-nullish and keeps it. On the other three (`null`, `undefined`, `'value'`) they agree, which is why the bug stays invisible in casual reading. It fires only when a caller deliberately passes a legitimate falsy value, exactly when the user's intent matters most.</PredictWhy>

</PredictOutput>

### When to reach for each

**For "use a default when the value is missing," reach for `??`. Reach for `||` only when "any truthy value short-circuits" is exactly what you want.** That case is rare, so write `??` by reflex and treat every `||` as a deliberate choice. This is the same semantics as the parameter defaults from "Options objects," which fire only on `undefined`; `??` gives you that behavior as an operator anywhere in a function body.

<VideoCallout videoId="i6CsAoSnalQ" videoTitle="JavaScript nullish operators — coalescing and optional chaining">
  basarat (author of *TypeScript Deep Dive*) covers both operators in six minutes: the `||` falsy-versus-nullish trap on `0` and `''`, then the three `?.` access forms.
</VideoCallout>

:::note
Many "default to 20 if missing" decisions belong at the schema boundary, not the call site. Zod 4's `.default(20)` applies during parse, so downstream code never sees the missing case. Reach for `??` on values that *don't* pass through a schema: local state, derived values, lazy initialization.
:::

## `??=` for lazy initialization

**`x ??= y` assigns `y` to `x` only when `x` is nullish.** Its canonical use is the cache-or-compute pattern, in three characters.

```ts
const cache: Record<string, number> = {};

const getOrCompute = (key: string) => {
  cache[key] ??= computeExpensive(key);
  return cache[key];
};
```

That one line fills a key's slot on the first call and reads the cached value on every call after, saying "fill this slot if it's empty" instead of an explicit `if (cache[key] === undefined) …`. Reach for it in any initialize-once, reuse-after pattern.

### The right-hand side is evaluated lazily

`??=` evaluates its right operand only when the left is nullish, so `computeExpensive(key)` never runs on a cache hit — which is the point when that operand is a function call, an API request, or a database read. The long-hand `cache[key] = cache[key] ?? computeExpensive(key)` also short-circuits to the same result, but reads the cache twice and writes every time.

### `||=` and `&&=` exist, but you write `??=`

Two siblings round out the set: `||=` assigns when the left is *falsy*, carrying the same `0`/`''`/`false` traps as `||`, and `&&=` assigns when the left is *truthy*. Reach for `??=` by default, for the same reason you reach for `??` over `||`: nullish-not-falsy is almost always what you want. Knowing the other two exist is enough to read them.

### Put the trio together

This exercise uses all three operators. `getConfig` reads a deeply optional configuration object and returns a normalized `{ pageSize, theme }`, treating `0` as a real `pageSize` and `''` as a real `theme` rather than missing values to overwrite. The `||` reflex fails both; the `??` reflex passes.

<ScriptCoding
  instructions="Implement getConfig(input). Read input.user.preferences.pageSize and default to 20 when the field is missing — treat 0 as a legitimate user choice, not as 'missing'. Read input.user.preferences.theme and default to 'light' when missing — treat '' as a legitimate cleared value. Return { pageSize, theme }. The expected solution uses ?. for the nested access and ?? for the two defaults."
  starter={`const getConfig = (input) => {
  // your code here
};
`}
  tests={`
test('returns defaults when input has no user', () => {
  expect(getConfig({})).toEqual({ pageSize: 20, theme: 'light' });
});

test('returns defaults when preferences is missing', () => {
  expect(getConfig({ user: {} })).toEqual({ pageSize: 20, theme: 'light' });
});

test('returns 0 when pageSize is deliberately 0 (not 20)', () => {
  const input = { user: { preferences: { pageSize: 0, theme: 'dark' } } };
  expect(getConfig(input)).toEqual({ pageSize: 0, theme: 'dark' });
});

test('returns empty string when theme is deliberately cleared (not light)', () => {
  const input = { user: { preferences: { pageSize: 50, theme: '' } } };
  expect(getConfig(input)).toEqual({ pageSize: 50, theme: '' });
});
`}
/>

## Mixing `??` with `||` and `&&`

Combine `??` with `||` or `&&` without parentheses and JavaScript refuses to compile it. `a || b ?? c` is a syntax error.

```ts
const value = a || b ?? c;
```

A reader can't tell whether `||` binds tighter than `??` or the reverse, so rather than pick an answer half of them would read wrong, the language makes you group explicitly. One set of parens states the grouping you meant:

```ts
const value = (a || b) ?? c;

const value = a || (b ?? c);
```

The two groupings differ. The first runs `||` first: take the truthy of `a` or `b`, then fall back to `c` only if that is nullish. The second runs `??` first: use `a` when truthy, otherwise compute `b ?? c`. Same operators, two meanings.

<TSPlaygroundCallout
  code={`declare const a: string | null;
declare const b: string | null;
declare const c: string;

const value = a || b ?? c;`}
  flags={{ strict: true, target: 99 }}
  title="?? mixed with || without parens"
  label="Open the parens-required error"
  height={400}
>
  Watch TypeScript reject the unparenthesized expression; add parens around either grouping and the error clears.
</TSPlaygroundCallout>

### The broader parens reflex

JavaScript's precedence table has too many operators to memorize, and typing parens is cheaper than recalling them. So **whenever an expression mixes different kinds of operator, add parens that state the intent**, even where the parser doesn't require them.

```ts
const score = a ?? b + 1;

const score = (a ?? b) + 1;
```

The first line parses as `a ?? (b + 1)`, almost certainly not what was meant; the second says it out loud: take `a` if it's set, otherwise `b`, then add one. A misread precedence costs a runtime bug that casual review won't catch, while the parens cost nothing. Any time `??` shares an expression with `||`, `&&`, `+`, or another binary operator, parenthesize.

## External resources

<CardGrid>
  <ExternalResource
    title="MDN — Nullish coalescing operator (??)"
    href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="The reference for `??`: the falsy-vs-nullish table and the rule for mixing it with `||`/`&&`."
  />
  <ExternalResource
    title="MDN — Optional chaining (?.)"
    href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="The reference for `?.`: property, call, and indexed access forms, plus per-link short-circuiting."
  />
  <ExternalResource
    title="javascript.info — Nullish coalescing operator '??'"
    href="https://javascript.info/nullish-coalescing-operator"
    icon="simple-icons:javascript"
    iconColor="#F7DF1E"
    description="A second explainer for `??`, with worked examples and the parentheses rule for mixed expressions."
  />
  <ExternalResource
    title="Biome — useOptionalChain"
    href="https://biomejs.dev/linter/rules/use-optional-chain/"
    icon="simple-icons:biome"
    iconColor="#60A5FA"
    description="The Biome lint rule that rewrites defensive `&&` chains into `?.` with an autofix."
  />
</CardGrid>
