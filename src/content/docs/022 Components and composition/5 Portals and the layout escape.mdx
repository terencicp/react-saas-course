---
title: Portals and the layout escape
tagline: Use React's createPortal to render modals, toasts, and popovers outside the CSS traps that would otherwise cage them on the page.
chapter-id: 22
course-progress: 0.00005
sidebar:
  order: 5
  label: Portals
---

import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import Figure from '../../../components/figures/Figure.astro';
import TabbedContent from '../../../components/figures/tabbed-content/TabbedContent.astro';
import TabbedItem from '../../../components/figures/tabbed-content/TabbedItem.astro';
import ReactCoding from '../../../components/live-coding/ReactCoding/ReactCoding.astro';
import PredictOutput from '../../../components/exercises/predict-output/PredictOutput.astro';
import PredictWhy from '../../../components/exercises/predict-output/PredictWhy.astro';
import MultipleChoice from '../../../components/exercises/multiple-choice/MultipleChoice.astro';
import McqChoice from '../../../components/exercises/multiple-choice/McqChoice.astro';
import McqWhy from '../../../components/exercises/multiple-choice/McqWhy.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { Card, CardGrid, Steps } from '@astrojs/starlight/components';
import TrapIllustration from '../../../components/lessons/022/5/TrapIllustration.astro';
import DomVsReactTree from '../../../components/lessons/022/5/DomVsReactTree.astro';
import StackingVsTopLayer from '../../../components/lessons/022/5/StackingVsTopLayer.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

For four lessons you have written components that render exactly where you put them.
You drop a `<Button>` inside a `<Card>`, and its DOM lands inside the card's DOM. That correspondence between where a component sits in your code and where its markup sits in the page is what makes JSX feel like writing HTML. For one specific kind of UI, that same correspondence becomes a wall.

Take the hover-lift `<Card>` from earlier in this chapter, the one with the subtle `transform` that nudges it up two pixels on hover. Inside it you put a "Delete account?" confirmation dialog, and you give that dialog `fixed inset-0` so it covers the whole screen, the way every modal does. It doesn't. The dialog is trapped inside the card: clipped by the card's edges, sized against the card instead of the viewport, sitting two pixels too high because the card moved. Everything about your code is correct. The dialog is simply in the wrong place in the *DOM*.

That is the entire problem this lesson solves. A component can be in the right place in your code and the wrong place in the rendered page, and the fix is a **portal**. We start with the trap, so the fix has something to act on. Then `createPortal` itself, then the part that catches everyone off guard: the portal moves the DOM but leaves the React tree completely intact. From there we cover the responsibilities the browser quietly stops handling once you cover the page, and the platform features that are starting to make parts of this work obsolete. By the end you'll open shadcn's `Dialog`, `Sheet`, `Popover`, and `Sonner` and know exactly what every line is doing, because every one of them is built on what's in this lesson.

## The layout trap that portals exist to solve

We start with the bug rather than the API, because the bug is what makes the API worth learning. `createPortal` is a trivial function, and the only durable thing to learn about it is *when* you reach for it.

Recall from when you learned about stacking contexts that a handful of CSS properties on an ancestor, namely `transform`, `filter`, `perspective`, `will-change`, and `contain`, turn that ancestor into a <Term definition="The rectangle a positioned element's top/right/bottom/left offsets resolve against. Normally the viewport for position: fixed, but a transform, filter, or similar property on an ancestor makes that ancestor the containing block instead.">containing block</Term> for any `position: fixed` descendant. Once that happens, `fixed` stops resolving against the viewport and resolves against *that ancestor* instead. A hover-lift card with `transform: translateY(-2px)` is exactly such an ancestor, so the dialog's `fixed inset-0` pins it to the card's box, not the screen's.

Here is the bug as real CSS, the example we'll carry through the whole lesson:

<TrapIllustration />

That `transform` is one of three traps that all share one fix. The first is the **containing-block trap** you just saw: a `transform` (or `filter`, `perspective`, and so on) ancestor steals `fixed` away from the viewport. The second is the **overflow clip**: an ancestor with `overflow: hidden` or `overflow: auto`, such as a scrollable table or a card with rounded corners, clips anything that tries to escape its box, so a dropdown rendered inside it gets its bottom sliced off. The third is **stacking-context burial**: a <Term definition="A self-contained z-index layer. Once an element creates one (via transform, opacity < 1, z-index on a positioned element, and more), its descendants' z-index values only compete inside it — they can never out-stack content in a sibling context, no matter how high the number.">stacking context</Term> traps its children's `z-index`, so your overlay loses to a sibling's content even at `z-index: 9999`.

You *can* patch each of these one at a time. Raise the `z-index`, add `isolation: isolate` to force a clean stacking context for the burial case, or restructure the DOM so the troublesome ancestor isn't in the way. But `isolation` does nothing for the containing-block trap, since it fixes stacking, not where `fixed` measures from, and restructuring the DOM fights the component boundaries you spent this chapter drawing. There is one fix that escapes all three at once without fighting anything: render the overlay's DOM *outside* the trapping subtree entirely.

Hold onto this idea, because it is the bridge to the whole lesson. The component belongs in the tree where you wrote it, because that's where its props, its state, and its event handlers live; only its DOM needs to move. That split, between where the component lives and where its markup lands, is precisely what a portal gives you.

First, feel the wall. The exercise below hands you the trapped setup. Try to free the dialog with CSS alone.

<ReactCoding
  live
  maxHeight={460}
  instructions="The dialog here should cover the whole screen the way every modal does, but the card's transform traps it: clipped at the card's edge, sized to the card instead of the viewport. Try to free it with CSS alone: raise the z-index, switch positioning, reach for anything. You'll find you can't get there from inside the card. The fix in the next section isn't CSS."
  target={`export function App() {
  return (
    <div className="min-h-screen bg-zinc-100 p-6">
      <article className="relative -translate-y-0.5 overflow-hidden rounded-xl border border-zinc-300 bg-white p-6 shadow-sm">
        <h3 className="text-base font-semibold text-zinc-900">Account</h3>
        <p className="mt-1 text-sm text-zinc-500">Permanently remove this workspace and all its data.</p>
        <button className="mt-4 rounded-md bg-red-600 px-3 py-1.5 text-sm font-medium text-white">
          Delete account
        </button>
      </article>

      {/* The dialog is a sibling of the card, so "fixed inset-0" measures
          against the viewport — a full-screen backdrop, panel centered. */}
      <div className="fixed inset-0 grid place-items-center bg-black/50">
        <div className="w-72 rounded-xl bg-white p-5 shadow-xl">
          <h2 className="text-base font-semibold text-zinc-900">Delete account?</h2>
          <p className="mt-1 text-sm text-zinc-500">This cannot be undone.</p>
          <div className="mt-4 flex justify-end gap-2">
            <button className="rounded-md px-3 py-1.5 text-sm font-medium text-zinc-600">
              Cancel
            </button>
            <button className="rounded-md bg-red-600 px-3 py-1.5 text-sm font-medium text-white">
              Delete
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}`}
  starter={`export function App() {
  return (
    <div className="min-h-screen bg-zinc-100 p-6">
      {/* The hover-lift card: its -translate-y-0.5 is a transform, which makes
          it the containing block for the fixed dialog nested inside it. */}
      <article className="relative -translate-y-0.5 overflow-hidden rounded-xl border border-zinc-300 bg-white p-6 shadow-sm">
        <h3 className="text-base font-semibold text-zinc-900">Account</h3>
        <p className="mt-1 text-sm text-zinc-500">Permanently remove this workspace and all its data.</p>
        <button className="mt-4 rounded-md bg-red-600 px-3 py-1.5 text-sm font-medium text-white">
          Delete account
        </button>

        {/* Edit these classes all you like — you can't make this reach the
            viewport while it lives inside the transformed card above. */}
        <div className="fixed inset-0 grid place-items-center bg-black/50">
          <div className="w-72 rounded-xl bg-white p-5 shadow-xl">
            <h2 className="text-base font-semibold text-zinc-900">Delete account?</h2>
            <p className="mt-1 text-sm text-zinc-500">This cannot be undone.</p>
            <div className="mt-4 flex justify-end gap-2">
              <button className="rounded-md px-3 py-1.5 text-sm font-medium text-zinc-600">
                Cancel
              </button>
              <button className="rounded-md bg-red-600 px-3 py-1.5 text-sm font-medium text-white">
                Delete
              </button>
            </div>
          </div>
        </div>
      </article>
    </div>
  );
}`}
/>

The reference reaches the target by a move CSS can't make from the editor: it renders the dialog as a *sibling* of the card, not a child, so nothing transformed sits between the dialog and the root. That structural move, with the DOM moved out and the React tree untouched, is the portal, and it's next.

## `createPortal`: rendering into a different part of the DOM

`createPortal` lives in `react-dom`, not `react`. That import line trips people up constantly, so read it twice:

```tsx
import { createPortal } from 'react-dom';
```

The signature is `createPortal(children, domNode, key?)`. You call it inside a component's render and return what it gives back, the same way you return JSX. The difference is the second argument: instead of letting React place `children`'s DOM where the component sits, you name the DOM node you want them placed *under* instead.

```tsx
createPortal(<DialogContent />, document.body);
```

This renders `<DialogContent />`'s markup as a child of `<body>`, while the component calling `createPortal` stays exactly where it is in the React tree. Apply it to the trapped dialog and the trap dissolves. The dialog's DOM ancestors are now `<body>` and nothing else, with no transformed card in the chain, so `position: fixed` measures against the viewport again, the `overflow: hidden` that was clipping it is gone from the path, and there's no buried stacking context to lose to. The dialog covers the screen, not because you changed any of its own CSS, but because you changed where its DOM lives.

The second argument is the **portal target**, the node the content attaches to. `document.body` is the daily reach for modals and toasts, and it's the one you'll use 95% of the time.

:::note
A minority of projects render a dedicated `<div id="portal-root" />` in the root layout and target that instead, so portal content sits under a specific ancestor and inherits its CSS variables. It's rarely worth the ceremony: dark mode lives as `.dark` on `<html>`, which already reaches `<body>`-attached portals, so the theme tokens flow into a `document.body` portal for free. Reach for a named root only when you have a concrete reason; otherwise use `<body>`.
:::

The optional third argument is a `key`. You need it only when you render a *list* of portals into the same container and React needs a stable identity for each, which is almost never the case for a single modal. Keys are the next chapter's topic; for now, know the argument exists and move on.

There is one failure to watch for once the target is anything other than `document.body`: `createPortal` throws if the target node is `null`. If you reach for `document.getElementById('portal-root')` before that element has rendered, you get `null`, and the render crashes. Default to `document.body`, which always exists, or guarantee the node is mounted before you portal into it.

Here's the smallest `Modal` that puts this together. The file would be `modal.tsx`, exporting `Modal`. Step through it.

<AnnotatedCode lang="tsx" maxLines={14} code={`
'use client';

import { createPortal } from 'react-dom';
import { cn } from '@/lib/utils';

type ModalProps = {
  children: ReactNode;
  className?: string;
  onClose: () => void;
};

export const Modal = ({ children, className, onClose }: ModalProps) =>
  createPortal(
    <div onClick={onClose} className="fixed inset-0 grid place-items-center bg-black/50">
      <div className={cn('rounded-xl bg-background p-6 shadow-xl', className)}>
        {children}
      </div>
    </div>,
    document.body,
  );
`}>
  <AnnotatedStep meta={`{1} "'use client'"`} color="blue">
    A portal touches `document`, which only exists in the browser, so the file is a Client Component. We'll come back to why this directive matters for portals at the end of the section.
  </AnnotatedStep>

  <AnnotatedStep meta="{6-10}" color="blue">
    A plain component: `children` to show, an `onClose` callback, and an optional `className` for the panel. Nothing here hints that the DOM is about to relocate.
  </AnnotatedStep>

  <AnnotatedStep meta="{14-18}" color="blue">
    A full-screen backdrop (`fixed inset-0`, semi-transparent black) centering a panel. This is the JSX that was getting trapped, and clicking the backdrop calls `onClose`.
  </AnnotatedStep>

  <AnnotatedStep meta={`{13} "createPortal" "document.body"`} color="blue">
    The line that does the escaping. `createPortal` takes that JSX and renders its DOM under `document.body`, far from any transformed or overflow-clipped ancestor. The same JSX as before lands in a different DOM home.
  </AnnotatedStep>

  <AnnotatedStep meta={`{3} "react-dom"`} color="blue">
    The import that trips everyone: `createPortal` comes from `react-dom`, never from `react`.
  </AnnotatedStep>
</AnnotatedCode>

That `Modal` is intentionally missing things: no focus handling, no `Esc` key, no scroll lock. That's not an oversight; it's the subject of two sections from now. For the file itself, the convention is a kebab-case name, `modal.tsx`, exporting `Modal`.

Now to the `'use client'` at the top. `createPortal(<X />, document.body)` reads `document`, and `document` does not exist on the server. In a Next.js app every component is a Server Component until you say otherwise, and rendering this one on the server would crash on that missing `document`. The `'use client'` directive marks the boundary where this component runs in the browser instead. You'll learn that boundary properly in a later chapter on Server and Client Components; for now, treat it as a fact: a file that touches `document`, which means every portal, needs `'use client'` at the top. shadcn's portal-based components all carry it, which is part of why you can drop them in without thinking about it.

## Portals escape the DOM, not React

Here is the part everyone gets wrong, so it goes first and in bold: **the DOM node moves; the React tree does not.** A portaled component is still, for every single thing React cares about, a child of the component that rendered it. Its markup landed under `<body>`, but in the tree React reasons about, it never left home.

That one fact has three consequences, and each is something beginners assume a portal breaks.

**Context flows through it.** A theme or auth provider sitting above the portal in the React tree still reaches the portaled content, even though that content's DOM lives far away under `<body>`. Context follows the tree, and the tree is unchanged. Context is a topic for a couple of chapters from now; the only thing to take here is that a portal does not cut a component off from the providers above it.

**State lives where it's declared.** Portaling does not create a new component scope. State declared in the parent still drives the portaled UI exactly as it would if the markup had rendered in place. The portal is a destination for DOM, not a boundary for anything on the React side.

**Events bubble through the React tree, not the DOM tree.** This is the surprising part. Picture a `<div onClick={…}>` that renders a portaled `<Tooltip>` among its children. In the DOM, that tooltip is now a sibling of the div, sitting under `<body>`, so a real DOM `click` on the tooltip would never bubble to the div, because they aren't ancestor and descendant anymore. But React replays the event along the tree *it* knows, the tree where the tooltip is still a child of the div, so the div's `onClick` fires. This is deliberate: React keeps event propagation tied to the structure you wrote, so handlers behave the same no matter where the DOM physically landed.

This tree-stays-intact property is the whole point of portals, and it's worth hearing argued from the architecture angle rather than the CSS one.

<VideoCallout videoId="5F7GrlIPdJ4" videoTitle="Senior-Level Understanding of React Portals">
UI Engineering reframes portals (5 min) as a way to *colocate* a modal's JSX and state next to its trigger while React still renders it elsewhere in the DOM, which is exactly the "tree where you wrote it" idea above.
</VideoCallout>

The diagram below shows the same tooltip in both trees at once. Watch where the highlighted node sits as you flip between the tabs.

<DomVsReactTree />

The event-bubbling rule has a sharp edge worth naming. A common pattern is "click outside to close": you put an `onClick` on a wrapper that closes a menu when anything outside the menu is clicked. If that menu is portaled and you rely on React's bubbling, clicks *inside* the portaled menu will bubble up through the React tree to that wrapper and close the menu the instant the user touches it, the opposite of what you want. There are two fixes: call `e.stopPropagation()` inside the portal so the click doesn't climb the React tree, or hang the outside-click listener on `document`, a real DOM listener that sees the portal as the `<body>` sibling it actually is. Production menu libraries handle this for you; the point is to recognize *why* the bug happens when you see it.

Predict what this prints. A parent `<div>` logs on click; it renders a portaled button that also logs on click. The user clicks the button.

<PredictOutput expected={`button clicked
parent clicked`}>

The button's DOM lands under `<body>` — a *sibling* of the parent `<div>`, not a descendant. A native DOM `click` would never travel between two siblings. The user clicks the button.

```tsx
const Parent = () => (
  <div onClick={() => console.log('parent clicked')}>
    {createPortal(
      <button onClick={() => console.log('button clicked')}>
        Open
      </button>,
      document.body,
    )}
  </div>
);
```

<PredictWhy>Even though the button's DOM sits under `<body>` as a sibling of the parent `<div>` — where a native DOM event would never bubble between them — React replays the event along the *React* tree, where the button is still the div's child. So the button's own handler runs first, then the click bubbles up to the parent's. Move the DOM, and the events still follow the tree you wrote.</PredictWhy>

</PredictOutput>

## The responsibilities you inherit when you cover the page

Step back and notice what you just did. You took a piece of UI out of the normal document flow and dropped it on top of the page. The browser has expectations about that, and they're worth respecting, because they're the same expectations a keyboard user and a screen-reader user are relying on.

In normal flow, the platform hands you a lot for free. Tab moves focus through the page in a sensible order. The page scrolls. `Esc` and the back button do what users expect. The moment you portal an overlay on top of everything, you step *outside* that safety net, and an overlay that traps a keyboard or a screen-reader user isn't merely rough around the edges, it's broken. So a portal doesn't just move DOM. It quietly hands you a contract.

That contract has a name and a canonical spec: the <Term definition="WAI-ARIA Authoring Practices Guide — the W3C's reference patterns for accessible widgets, with the exact keyboard, focus, and ARIA expectations for each. The Dialog (Modal) pattern is the checklist for any overlay that covers the page.">WAI-ARIA APG</Term> modal-dialog pattern. Here's the checklist it asks for, and what a real user loses when each line is missing.

<Steps>

1. **`role="dialog"` and `aria-modal="true"`.** Without these, a screen reader announces your overlay as a generic chunk of page and never tells the user the rest of the page is now inert.

2. **`aria-labelledby` pointing at the title, `aria-describedby` at the body.** These give the dialog an accessible name and description, so a screen-reader user hears "Delete account, dialog" instead of silence on open.

3. **Move focus into the dialog when it opens.** If focus stays on the trigger behind the overlay, a keyboard user is typing into a page they can't see.

4. **Trap focus inside while it's open**, so Tab and Shift+Tab cycle within the dialog and never reach the page behind. Skip this and Tab walks the user straight out into the frozen content underneath, with no way back.

5. **Restore focus to the trigger on close.** Otherwise focus snaps back to the top of the document and the user has to find their place again.

6. **`Esc` closes it.** This is the universal "get me out," and keyboard users reach for it first.

7. **A click on the backdrop closes it**, except for destructive or data-loss dialogs, where you *want* a stray click to do nothing. Your "Delete account?" dialog is exactly the case to leave out.

8. **Lock body scroll while it's open.** If the page behind scrolls under the modal, the user loses their place and the overlay feels unanchored.

</Steps>

That is a real list with real engineering behind it, and the <Term definition="Keeping Tab and Shift+Tab cycling within the open dialog so keyboard focus can never reach the page behind it. Surprisingly fiddly to do correctly: it has to find the first and last focusable elements and wrap focus between them.">focus trap</Term> alone is genuinely fiddly to get right. So here is the senior move, and it's the refrain of this whole lesson: **you do not hand-build this.** shadcn's `Dialog`, with Radix underneath, ships every one of these guarantees, correctly, today. Your job is not to reimplement the focus trap. Your job is to *know the checklist*, so when you audit a modal in a real codebase you can tell at a glance whether it's accessible or quietly broken. Reading the list is the deliverable; the from-scratch focus trap and scroll lock are built up from primitives later, in the project chapter, not now.

:::caution
Scroll locking looks like one line, setting `overflow: hidden` on `<body>` while the modal is open, and on the desktop it nearly is. iOS Safari is the catch: `overflow: hidden` doesn't fully stop touch-scrolling, so you also have to pin the scroll position (`position: fixed` on the body, then restore `scrollY` on close) or the page jumps to the top when the modal opens. Recognize it when you see it; the real implementation comes with the project chapter.
:::

Now a quick audit. Here's a stripped modal that ships markup but skips two contract items. What breaks for the user?

<MultipleChoice>
  This modal is portaled to `<body>` and, on open, runs `panelRef.current.focus()` to move focus onto the panel. The backdrop and panel carry `role="dialog"`, `aria-modal`, and an `aria-labelledby` wired to the title. There is no `keydown` listener and no logic that confines Tab to the panel.

  ```tsx
  createPortal(
    <div className="fixed inset-0 grid place-items-center bg-black/50">
      <div ref={panelRef} tabIndex={-1} role="dialog" aria-modal="true" aria-labelledby="t">
        <h2 id="t">Rename project</h2>
        <input className="..." />
        <button onClick={onClose}>Close</button>
      </div>
    </div>,
    document.body,
  )
  ```

  A keyboard-only user opens it. Which problems do they actually hit? Select all that apply.

  <McqChoice correct>After a couple of Tab presses they land on a link in the page underneath — which is still sitting there, behind the backdrop, fully reachable.</McqChoice>
  <McqChoice correct>The reflex of tapping the key in the top-left to bail out does nothing; the only exit is to Tab around until they reach the Close button.</McqChoice>
  <McqChoice>On open, the keystrokes they type go to whatever was focused before, not to the panel.</McqChoice>
  <McqChoice>A screen reader reaches the panel and can't tell the user what this overlay is called.</McqChoice>

  <McqWhy>The two gaps are the **focus trap** and the **`Esc` handler**. With nothing confining Tab to the panel, focus walks out into the frozen page behind the backdrop (choice 1); with no `keydown` listener, `Esc` is dead and the keyboard exit is a manual Tab to Close (choice 2). The other two are already handled here: focus *is* moved onto the panel on open via `panelRef.current.focus()`, and the title wired through `aria-labelledby` gives the dialog its accessible name.</McqWhy>
</MultipleChoice>

## The three places portals earn their keep

Almost every portal you'll meet is one of three shapes. You will *import* all three from a library rather than write them, but knowing which library owns which shape, and why each needs a portal, is what lets you read that code.

<CardGrid>

<Card title="Modals and dialogs">
An overlay that covers the page regardless of what `transform`, `overflow`, or `z-index` traps sit between the trigger and the root: the central example of this lesson, generalized. The production answer is shadcn's `Dialog`, `AlertDialog`, and `Sheet` (the side-anchored variant). You compose them; the portal and the whole accessibility contract are already inside.
</Card>

<Card title="Toasts">
A container portaled to `<body>` once, with each toast `fixed` to a corner of the viewport, stacking with `gap`, auto-dismissing, and animating in and out via the `data-state` and `tw-animate-css` pattern from the last chapter. The 2026 shadcn default is **`sonner`**: you add a single `<Toaster />` to the root layout and call `toast('Saved')` from anywhere. Because the container lives on `<body>`, toasts survive route changes, which is exactly what you want for "your export is ready" landing after the user has navigated on.
</Card>

<Card title="Popovers, dropdowns, tooltips">
These have to escape a parent's `overflow` *and* follow the trigger as the page scrolls. `position: absolute` inside the trigger dies to `overflow: hidden`; `fixed` plus a portal escapes the clip but then needs JavaScript to track the trigger's position. There are two production answers: `@floating-ui/react` (the library Radix leans on) does the positioning math in JS, and CSS anchor positioning, covered just below, increasingly does it with no JS at all. You'll meet the shadcn surface for these in a later chapter.
</Card>

</CardGrid>

## The platform is catching up

Portals plus manual focus and scroll management were always a workaround. They existed because the web platform lacked two things: a way to render above every stacking context, and a way to anchor one element to another without JavaScript. In 2026 the browser is closing both gaps, and reading new code well means knowing which problems are now the platform's job.

**Native `<dialog>` and `showModal()`.** The `<dialog>` element has been Baseline since 2022 and sits near 97% support today. Call `dialog.showModal()` and the browser renders it in the <Term definition="A browser-managed layer that paints above the entire page, ignoring z-index and every stacking context. Top-layer elements escape the containing-block and overflow traps by construction. dialog.showModal() and elements using the popover attribute render here.">top layer</Term>, above *every* stacking context, with no portal, no `z-index` to manage, and no containing-block trap, because the top layer sits outside the normal painting order entirely. You also get `Esc`-to-close and a built-in focus trap for free, and the <Term definition="The pseudo-element that styles the scrim behind a top-layer element. ::backdrop on a <dialog> or popover lets you set the dimming/blur behind it with plain CSS, no extra element.">`::backdrop`</Term> pseudo-element styles the scrim with plain CSS. That's the platform doing natively most of what the portal-plus-accessibility dance used to do by hand.

The diagram makes the central argument visible: why the top layer makes `z-index` management for overlays obsolete.

<StackingVsTopLayer />

So why does shadcn still ship a portal-based `Dialog` instead of wrapping native `<dialog>`? Because the two solve slightly different problems, and the choice is a real one:

- Reach for **native `<dialog>` + `showModal()`** when you want platform behavior and standard styling is fine. It's the lean option: no library, no portal, focus trap and `Esc` included.
- Reach for **shadcn's `Dialog`** (portal plus Radix-managed focus) when you need *animated entrance and exit choreography*, custom backdrop interaction, or the open/closed state wired tightly into your app's React state. Radix keeps the DOM mounted through the exit transition using the `data-state` pattern, so the modal can animate out before it unmounts, which raw `<dialog>` doesn't orchestrate for you. shadcn's `Dialog` is the project's daily reach; native `<dialog>` is the leaner alternative worth knowing for the simple cases.

**CSS anchor positioning** is the platform's answer to the popover-tracking problem, and it reached Baseline in 2026 (Chrome 125+, Firefox 132+, Safari 18.2+). You put `anchor-name` on the trigger, `position-anchor` and `position-area` on the popover, and `@position-try` to flip it when it would collide with a viewport edge, and the popover tracks its trigger with no JavaScript at all. Pair it with the `popover` attribute (and `<button popovertarget>` to wire a trigger to it), and you get a popover that renders in the top layer, never gets clipped by an ancestor's `overflow`, and needs no portal. You'll see the shadcn surface for this in a later chapter; here, just register that the JavaScript positioning math is becoming the browser's job.

<VideoCallout videoId="DNXEORSk4GU" videoTitle="CSS Popover + Anchor Positioning is Magical">
Kevin Powell builds a trigger-anchored menu (20 min) with the `popover` attribute, `anchor()`, and `@position-try` for edge collisions: the no-JavaScript replacement for the portal-plus-Floating-UI dance, live in code.
</VideoCallout>

Here is the durable takeaway. Portals and hand-rolled focus and scroll management were the workaround for a platform missing a top layer and anchored positioning. In 2026 the platform is filling both gaps: `<dialog>`'s top layer removes the stacking and containing-block traps, and anchor positioning removes the popover math. Portals stay essential for what the platform doesn't cover yet, namely arbitrary portaled content, animated overlays, and state controlled by your framework, and they're still what shadcn ships, so you'll read them daily. But you read them as an experienced engineer now, knowing which of those problems the browser has quietly taken off your plate.

## External resources

The reference set, in the order you'll reach for it: the React API, the platform features that are replacing parts of it, the contract you audit against, and the two shadcn surfaces you'll actually import.

<CardGrid>

<ExternalResource
  title="React — createPortal"
  href="https://react.dev/reference/react-dom/createPortal"
  icon="simple-icons:react"
  iconColor="#61DAFB"
  description="The full API reference, including the event-bubbling-through-the-React-tree behavior."
/>

<ExternalResource
  title="Josh Comeau — What The Heck, z-index??"
  href="https://www.joshwcomeau.com/css/stacking-contexts/"
  icon="simple-icons:css3"
  iconColor="#1572B6"
  description="An interactive walk through stacking contexts: the trap that makes z-index: 9999 lose."
/>

<ExternalResource
  title="MDN — the <dialog> element"
  href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog"
  icon="simple-icons:mdnwebdocs"
  description="showModal(), the top layer, ::backdrop, and the built-in focus and Esc behavior."
/>

<ExternalResource
  title="WAI-ARIA APG — Dialog (Modal) pattern"
  href="https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/"
  icon="lucide:accessibility"
  description="The authoritative accessible-modal contract: the checklist this lesson turned into an audit."
/>

<ExternalResource
  title="MDN — using CSS anchor positioning"
  href="https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_anchor_positioning/Using"
  icon="simple-icons:mdnwebdocs"
  description="anchor-name, position-area, and @position-try: popover positioning with no JavaScript."
/>

<ExternalResource
  title="shadcn/ui — Dialog"
  href="https://ui.shadcn.com/docs/components/dialog"
  icon="simple-icons:shadcnui"
  description="The portal-based modal you'll actually import, with the full contract already satisfied."
/>

<ExternalResource
  title="Radix Primitives — Dialog"
  href="https://www.radix-ui.com/primitives/docs/components/dialog"
  icon="simple-icons:radixui"
  iconColor="#FFFFFF"
  description="The engine under shadcn's Dialog: the focus trap, Esc, and ARIA contract, documented."
/>

<ExternalResource
  title="shadcn/ui — Sonner"
  href="https://ui.shadcn.com/docs/components/sonner"
  icon="simple-icons:shadcnui"
  description="The 2026 toast default: one <Toaster /> in the layout, toast() from anywhere."
/>

</CardGrid>
