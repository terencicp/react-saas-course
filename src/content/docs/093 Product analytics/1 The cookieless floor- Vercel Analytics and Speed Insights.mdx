---
title: 'The cookieless floor: Vercel Analytics and Speed Insights'
tagline: Ship Vercel Web Analytics and Speed Insights as the cookieless, no-consent-needed baseline that answers your traffic and real-user performance questions before you reach for a product-analytics platform.
chapter-id: 93
course-progress: 0.00005
sidebar:
  order: 1
  label: The cookieless floor
---

import { Steps } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';
import Figure from '../../../components/figures/Figure.astro';
import TwoTierStack from '../../../components/lessons/093/1/TwoTierStack.astro';
import WebAnalyticsOverview from '../../../components/lessons/093/1/WebAnalyticsOverview.astro';
import ConsentFork from '../../../components/lessons/093/1/ConsentFork.astro';
import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import MultipleChoice from '../../../components/exercises/multiple-choice/MultipleChoice.astro';
import McqChoice from '../../../components/exercises/multiple-choice/McqChoice.astro';
import McqWhy from '../../../components/exercises/multiple-choice/McqWhy.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import Term from '../../../components/ui/Term.astro';
import { CardGrid } from '@astrojs/starlight/components';

<CourseProgressBar value={frontmatter['course-progress']} />

The product is live. You followed the deployment chapter, pushed to Vercel, and the marketing site and the app are both serving real traffic right now. That is the moment this lesson is about — not because something is broken, but because three questions just came due, and you can't answer any of them yet.

How much traffic is the app getting, and where is it coming from? Which pages turn visitors into sign-ups, and which ones quietly leak them? And is the experience fast enough that Google's search ranking and your own conversion rate aren't being punished for it? You don't need an event schema to answer these. You don't need to decide on a product-analytics platform. You need two installs — `@vercel/analytics` and `@vercel/speed-insights` — and two lines of JSX in a file you already have. That's it. By the end of this lesson all three questions are answered in a dashboard, with zero event code written.

You already have everything this builds on: a Next.js App Router app deployed on Vercel, an `app/layout.tsx` at the root, and the consent gate you built back in the security chapter — the one that holds non-essential tracking behind an explicit yes. That consent gate is going to matter here, but not in the way your instinct expects. Hold onto that thought; it's the single most important correction in this lesson, and we'll get to it deliberately.

## Three questions, one floor

Let's name those three questions properly, because they're not one-time curiosities — they're the recurring questions every team running a SaaS asks from the first day of production onward.

- **Traffic.** How many people, and from where? Raw volume and referrers tell you whether your launch tweet did anything and whether that one blog post is actually sending humans.
- **Behavior, lightly.** Which pages do people land on, and which do they leave from? You don't need a funnel yet to notice that the pricing page gets traffic but the sign-up that should follow it doesn't.
- **Performance.** Is the page fast for *real* users on *real* devices? Not "fast on your laptop" — fast on a mid-range phone on a hotel wifi, which is what Google scores you on.

Two installs answer all three. That's the **floor** — the analytics layer that is correct to ship before you've decided anything else, because it costs you nothing to have and answers questions you have today.

Here's the shape of the whole chapter in one picture. There are two tiers to a SaaS analytics stack, and this lesson ships the bottom one.

<Figure caption="Two tiers. The floor ships unconditionally; the top tier is an additive layer you reach for only when a real need pays for it.">
  <TwoTierStack />
</Figure>

The bottom tier — the cookieless floor — is what you ship today, unconditionally, on every Vercel project. The top tier is <Term definition="A product-analytics platform that folds events, feature flags, session replay, and experiments into one tool.">PostHog</Term>, and unlike the floor it has a trigger. You add it only when a specific kind of question shows up, and there are exactly five of them. You cross the threshold the moment you need to know:

- **Who** — a specific identified user, not an anonymous visitor.
- **What they did** — an event with properties (`plan: 'pro'`, `seats: 12`), not just a page view.
- **Across sessions** — a funnel that stitches a sign-up today to a paid invoice next week.
- **Gated by a flag** — rolling a new feature out to 10% of orgs first.
- **In replay** — watching the actual session where a user rage-clicked and bailed.

Until at least one of those is a real, present need, the floor is your entire analytics stack. We'll come back to this list at the end of the lesson as the closing decision; the next lesson treats it in full. For now, just notice that none of these five is "how much traffic do I get" — that's the floor's job, and the floor does it for free.

## Why cookieless is the default, not the conditional path

Most tools in this course are conditional. You reach for them when a threshold is crossed, and a big part of each lesson is teaching you *not* to reach for them too early. This one is the inverse. Vercel Web Analytics is the thing you ship by reflex, on every project, the way you'd add a favicon. It's worth understanding exactly why, because the reasons are also what makes it safe to stop here.

The biggest reason is one word: **cookieless**. Vercel Web Analytics sets no cookies and does no <Term definition="Identifying a returning visitor by combining device and browser signals into a stable hash, without a cookie.">fingerprinting</Term>. The data is aggregated and anonymized the instant it's collected. That property has a direct, concrete payoff on your marketing site: it is not blocked by your consent banner, so you keep the traffic data from every visitor — including the large majority who close the banner without ever clicking accept. We'll unpack the legal shape of this in its own section, because it's the lesson's key correction. For now, hold the headline: cookieless is *why* this can run for everyone, and running for everyone is most of its value.

The second reason is **cost**. Vercel Web Analytics is free on every plan, including Hobby. No separate contract, no per-event invoice landing at the end of the month. There is a monthly cap on captured events, and it's honest to say so — on Hobby, collection pauses when you hit the cap rather than billing you for overflow, and raising it means upgrading to Pro. But for a pre-product-market-fit app that cap is generous, and the practical experience is "free." The thing to internalize is that even the cost is a *volume* cost, not an engineering one. There's no event taxonomy to design and budget for. The price of the floor, in both money and effort, is close to zero.

That zero-effort point deserves its own line, because it's what makes the floor durable. The whole install is two `<script>` injections via two official packages. The data model is fixed — page views, referrers, geography, device — so there is no schema to design, no schema to keep aligned across your codebase, and nothing to rot six months from now when someone renames a thing and forgets the analytics call.

Contrast that with the top tier. PostHog is genuinely powerful, but everything it gives you is *weight*: an SDK in your bundle, an event schema your team has to govern, a consent gate to route every call through, a quota to watch so a stray `capture()` in a render loop doesn't burn your month. None of that is wrong — it's the right cost to pay when a real need shows up. But it *is* a cost. The floor has none of it.

:::tip
For a pre-PMF product or a pure marketing site, this *is* your entire analytics stack. Shipping the floor and stopping there isn't cutting a corner — it's the correct call. Adding a product-analytics platform before you have a product-analytics question is premature, and the schema you build will rot before anyone reads it.
:::

## What Vercel Web Analytics gives you

So what exactly do those two scripts buy you? Web Analytics gives you a focused set of traffic metrics:

- **Page views** and **unique visitors** — your volume.
- **Top pages** and **top referrers** — what's getting attention and what's sending it.
- **Country**, **OS**, **browser**, and **device class** — the shape of your audience.

Every one of those is computed from data that's aggregated and anonymized at the point of ingest. No personal data leaves the visitor's device — that's the mechanism underneath the cookieless guarantee, not a separate feature. It's also worth knowing the script is built to be resilient: the current `@vercel/analytics` package discovers its ingest endpoints rather than hard-coding one, which is how it survives the URL patterns ad-blockers reach for. You don't need to configure any of that; it's just why your numbers hold up better than a naive analytics tag's would.

Here's what that looks like once it's live and verified — the overview panel you'll be watching:

<WebAnalyticsOverview />

There's one boundary to draw clearly so you don't try to push this tool past what it's for. Web Analytics does support **custom events** — but treat them as out of scope, for two reasons. First, the data model around them is shallow: no funnels, no cohorts, no cross-session identity. Second, custom events are a Pro and Enterprise feature; they're not on the free Hobby tier at all. So the moment you find yourself wanting to fire a custom event, that's not a signal to upgrade your Vercel plan — it's the threshold to the top tier. Don't try to bend Web Analytics' custom events into a product-analytics layer. The right reach is PostHog, and that's the next lesson.

## What Speed Insights gives you

The second install, `@vercel/speed-insights`, answers the third question: is the page actually fast for real people? It does that by sampling <Term definition="Google's standardized real-user performance metrics — loading, interactivity, and visual stability — scored at the 75th percentile.">Core Web Vitals</Term> from your live production traffic.

You'll see these metric names on the dashboard, so meet them now as vocabulary — we are not defining them here. The performance chapter coming later owns their precise definitions, thresholds, and how to fix each one. For now: <Term definition="Largest Contentful Paint — how long the biggest piece of content takes to appear. Full treatment in the performance chapter.">LCP</Term>, <Term definition="Interaction to Next Paint — how quickly the page responds to a tap or click. Full treatment in the performance chapter.">INP</Term>, <Term definition="Cumulative Layout Shift — how much the page jumps around as it loads. Full treatment in the performance chapter.">CLS</Term>, TTFB, and FCP.

The one idea worth holding from this lesson is the distinction between two kinds of performance data. Lab data comes from a synthetic test — one run, one machine, controlled conditions. **Field data** comes from your actual users, on their actual devices and networks, and Speed Insights is field data. That distinction is not academic: field data, scored at the 75th percentile, is what Google uses to rank you in Search. A perfect lab score means little if real users on real phones are having a slow time. Speed Insights shows you the truth that actually affects your ranking.

<VideoCallout videoId="4pI8nBt98ys" videoTitle="Google Core Web Vitals: A Complete Guide For Beginners">
  DebugBear's 13-minute primer on LCP, INP, and CLS — and the field-vs-lab distinction this section turns on, with the 75th-percentile field data Speed Insights reports.
</VideoCallout>

This lesson ships the *wire*. The performance chapter later consumes this exact dashboard as its starting input — so when you get there, you won't be installing anything new; you'll be reading the data you turned on today. Think of the two lessons as one continuous thread: turn on the sensor now, learn to act on it later.

## Installing the floor

Now the hands-on part. It's short: two packages, two components, one dashboard toggle, one verification.

<Steps>

1. Install both packages with pnpm.

   ```bash
   pnpm add @vercel/analytics @vercel/speed-insights
   ```

2. Add both components to your root layout, inside `<body>`, after `{children}`. This is the only code in the lesson — walked below.

3. Enable Web Analytics and Speed Insights in the Vercel project dashboard, per environment. This is a separate step, and it's the one people forget. The package ships the script either way, but until you flip this toggle in the dashboard, the script runs and the data goes nowhere. No error, no warning — just an empty dashboard. Turn it on the same time you ship the code.

4. Verify in production. Load a page on your live site and watch the dashboard. Give it time — there's a first-load latency of roughly thirty seconds to a minute before the first data shows up, so an empty panel for the first minute is expected, not a bug. Once a view lands, you're done.

</Steps>

The code in step two is the whole thing. Two imports and two self-closing components in your root layout:

<AnnotatedCode lang="tsx" maxLines={16} code={`
import { Analytics } from '@vercel/analytics/next';
import { SpeedInsights } from '@vercel/speed-insights/next';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        {children}
        <Analytics />
        <SpeedInsights />
      </body>
    </html>
  );
}
`}>
  <AnnotatedStep meta="{1-2}" color="blue">
    Both imports come from the Next.js-specific subpath entry of each package — the entry points built for the App Router. One import per capability, nothing more.
  </AnnotatedStep>

  <AnnotatedStep meta="{4}" color="blue">
    This is your existing root layout, and it stays a server component. You're not adding `'use client'` and you're not wrapping it in a provider — each component handles its own client behavior internally.
  </AnnotatedStep>

  <AnnotatedStep meta={`{9-10} "<Analytics />" "<SpeedInsights />"`} color="green">
    Drop both once, at the root, after `{children}`. Because every route renders inside the root layout, this single placement covers your entire app — every page, automatically. There is nothing to add per-route.
  </AnnotatedStep>
</AnnotatedCode>

:::note
In 2026, Vercel's dashboard tooling can perform this whole install for you end-to-end. We walk the manual version anyway, because the point of this lesson is to know exactly what gets wired and why — not to watch it happen.
:::

## Why these two skip the consent gate

Here is the correction I promised at the start, and it's the most important paragraph in the lesson. You just spent a whole lesson in the security chapter learning that nothing non-essential fires before the user consents — that every tracker routes through your `useConsent()` source of truth, and stays dark until the user explicitly accepts. That lesson did its job a little too well. The natural next move, having just installed two analytics tools, is to wrap `<Analytics />` and `<SpeedInsights />` in that same gate. Do not do that.

The reason these two are different comes straight back to the property we opened with. The consent gate exists to protect the user's personal data. Vercel Web Analytics and Speed Insights collect none — no cookies, no fingerprinting, everything aggregated and anonymized at ingest. Under most jurisdictions that puts them in the "essential" or "legitimate interest" category for traffic analytics, which is exactly the category that does not require consent. So they sit *outside* the gate, by design. The cookieless guarantee isn't just a nice-to-have; it's the entire reason these tools are allowed to run before anyone clicks accept.

Now picture what happens if you gate them anyway. On a marketing page, most visitors never click accept — they read what they came for and leave, or they dismiss the banner. If `<Analytics />` is behind the gate, every one of those visitors is invisible to you. You've thrown away the bulk of your traffic data, the exact data you installed this for, in exchange for zero additional compliance benefit — because the tool didn't need consent in the first place. It's a pure loss. Gate by *what the tool collects*, never by reflex.

And to be clear, the rule isn't "analytics never needs consent" — it's "gate by what's collected." PostHog, the top tier, *does* handle personal data: distinct user IDs, IP addresses, event properties that carry user state. PostHog absolutely routes through the consent gate, and a later lesson in this chapter wires exactly that. Naming both halves is what makes the principle stick: the floor skips the gate because it's cookieless; PostHog goes through it because it isn't.

<Figure caption="Two lanes. The cookieless floor goes straight to ingest; PostHog passes through the consent gate and fires only after the user accepts.">
  <ConsentFork />
</Figure>

One honest caveat, then we move on: "most jurisdictions" is doing real work in that sentence. A few stricter regimes draw the line differently, and the final call on what your specific product needs is your legal team's, not your framework's. The engineering default in 2026 is no-gate for these two, cookieless tools — but it's a default, not a guarantee.

Let's check that both boundaries landed — the capability boundary (what the floor can answer) and the consent boundary (what skips the gate) are the two load-bearing ideas of this lesson. Sort each concern into where it belongs.

<Buckets twoCol instructions="Sort each analytics concern into where it belongs.">
  <Bucket name="floor" label="Cookieless floor" description="Ships now, no consent" />
  <Bucket name="threshold" label="Crosses the threshold" description="Needs PostHog, consent-gated" />

  <Item bucket="floor">Page views by country</Item>
  <Item bucket="floor">Top referrers</Item>
  <Item bucket="floor">Unique visitor count</Item>
  <Item bucket="floor">Real-user LCP</Item>
  <Item bucket="threshold">Which users abandoned the onboarding funnel</Item>
  <Item bucket="threshold">Roll the new billing UI out to 10% of orgs</Item>
  <Item bucket="threshold">Replay the session where a user rage-clicked</Item>
  <Item bucket="threshold">Did variant B lift trial-to-paid</Item>
</Buckets>

## Where to enable it: production, preview, dev

The dashboard toggle from the install is per-environment, which turns "is this on?" into a small but real decision across your three environments. The defaults are easy:

- **Production: on.** This is where real traffic lives and where the data matters. Always on.
- **Preview deployments: opt in.** Turn it on for a preview URL when you specifically want to QA against real traffic on that deploy. Otherwise leave it off — you don't want your QA clicks muddying production-shaped data.
- **Development: off.** Your local clicking around is noise, not signal. There's nothing useful to capture from `localhost`.

There's one dependency hiding under all of this that's worth stating plainly, because it's a silent failure waiting to happen. `@vercel/analytics` needs Vercel's edge network to ingest its data. The course's stack is Vercel-hosted, so this is the happy default — but if you ever ship this package on a static export hosted somewhere else, the script will load, run, and catch absolutely nothing. There's no edge endpoint to send to. It looks installed and it collects nothing.

:::caution
Two traps, both of the "looks installed, collects nothing" variety — watch for them:

1. **You added the package but never flipped the dashboard toggle.** The script ships, the data sinkholes. Enabling Web Analytics and Speed Insights in the project dashboard is a separate, required step.
2. **You shipped `@vercel/analytics` on a static export hosted off Vercel.** No Vercel edge means no ingest endpoint, so nothing is captured no matter what the dashboard says.

Both fail quietly — an empty dashboard, no error in the console. If your numbers stay at zero, check these two first.
:::

## When to reach past the floor

You've shipped the floor. The last thing to leave with isn't an install — it's the decision of when to add the next tier, so you reach for it on purpose rather than by anxiety.

Here are those five threshold signals again, now as your closing checklist. Each one names a question the floor structurally *cannot* answer, which is what makes the line real rather than arbitrary:

- **Who** — the floor has no identity. It counts anonymous visitors; it can't tell you what *this signed-in user* did.
- **What they did** — the floor has no event schema. It records page views, not "clicked upgrade" with `plan: 'pro'` attached.
- **Across sessions** — the floor doesn't stitch sessions. It can't follow one person from sign-up today to first paid invoice next week.
- **Gated by a flag** — the floor has no flags. It can't roll a feature out to 10% of orgs or kill-switch a broken one.
- **In replay** — the floor has no replay. It can't show you the actual session behind a bug that never threw an error.

If zero of those are real for your product right now, you are done. Stay on the floor. The moment one of them becomes a genuine, present need, that's your signal — and the next lesson lands exactly that decision.

A quick word on the tool you might expect to see here and won't: **Google Analytics 4**. It was the historical default for a decade, and this course doesn't reach for it. Its data model — sessions, hits, audiences — is an awkward fit for product analytics, its consent-banner UX is heavier than what we just built, and its export shape is rigid. The 2026 SaaS split is clean: Vercel for traffic, PostHog for product analytics. GA4 stays a marketing-team conversation, not an engineering one. We name it once, here, so you can recognize it and move past it.

That's the handoff. The next lesson lands the PostHog decision in full — the moment it earns its weight, and the one-platform-four-primitives play that makes it the right reach when the threshold finally arrives.

<MultipleChoice>
  A pre-PMF marketing site with one pricing page wants to know how many visitors it gets and which posts drive sign-ups. What's the right analytics stack?

  <McqChoice correct>The cookieless floor on its own.</McqChoice>
  <McqChoice>PostHog from day one, floor optional.</McqChoice>
  <McqChoice>GA4 for the marketing numbers.</McqChoice>
  <McqChoice>The floor plus PostHog's four primitives — events, flags, replay, experiments — wired up now so they're ready later.</McqChoice>

  <McqWhy>"How many visitors" and "which posts drive sign-ups" are pure volume and referrer questions, and that's exactly what Vercel Web Analytics and Speed Insights answer for free, before any consent click. None of the five threshold signals — *who*, *what they did*, *across sessions*, *gated by a flag*, *in replay* — is in play, so there's nothing for the top tier to do yet. Reaching for **PostHog** (or worse, all four of its primitives) before a single trigger is real is the exact over-build this lesson warns against: an SDK in the bundle, a schema to govern, a consent gate to route through — all weight, no answer it gives you that the floor doesn't. And **GA4** isn't the 2026 reach at all; its session/hit model and heavier consent UX don't fit, and these questions are squarely the floor's job.</McqWhy>
</MultipleChoice>

## External resources

The install is short, so the official quickstarts cover the wiring — and two deeper references back the two load-bearing ideas: why these tools skip the consent gate, and what the Speed Insights metrics actually mean.

<CardGrid>
  <ExternalResource
    title="Vercel Web Analytics — Quickstart"
    href="https://vercel.com/docs/analytics/quickstart"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="Official setup guide for the traffic floor."
  />
  <ExternalResource
    title="Vercel Speed Insights — Quickstart"
    href="https://vercel.com/docs/speed-insights/quickstart"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="Official setup guide for real-user Core Web Vitals."
  />
  <ExternalResource
    title="Vercel Web Analytics — Privacy and Compliance"
    href="https://vercel.com/docs/analytics/privacy-policy"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="The cookieless, no-fingerprinting mechanics behind why the floor skips the consent gate."
  />
  <ExternalResource
    title="web.dev — Web Vitals"
    href="https://web.dev/articles/vitals"
    icon="simple-icons:google"
    iconColor="#4285F4"
    description="Google's own reference on LCP, INP, CLS, and the 75th-percentile field-data threshold."
  />
</CardGrid>
