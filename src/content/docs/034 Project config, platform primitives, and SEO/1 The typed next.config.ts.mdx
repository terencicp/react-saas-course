---
title: The typed next.config.ts
tagline: How Next.js reads next.config.ts, the typed project-level configuration file that shapes how your app is built and served before any route runs.
chapter-id: 34
course-progress: 0.00005
sidebar:
  order: 1
  label: next.config.ts
---

import Figure from '../../../components/figures/Figure.astro';
import ArrowDiagram from '../../../components/figures/ArrowDiagram.astro';
import DiagramSequence from '../../../components/figures/diagram-sequence/DiagramSequence.astro';
import DiagramStep from '../../../components/figures/diagram-sequence/DiagramStep.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import StateMachineWalker from '../../../components/figures/state-machine-walker/StateMachineWalker.astro';
import Question from '../../../components/figures/state-machine-walker/Question.astro';
import Branch from '../../../components/figures/state-machine-walker/Branch.astro';
import Leaf from '../../../components/figures/state-machine-walker/Leaf.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import MultipleChoice from '../../../components/exercises/multiple-choice/MultipleChoice.astro';
import McqChoice from '../../../components/exercises/multiple-choice/McqChoice.astro';
import McqWhy from '../../../components/exercises/multiple-choice/McqWhy.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { Aside, CardGrid } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';
import ConfigSurfaceMap from '../../../components/lessons/034/1/config-surface-map.astro';
import ServerExternalRelocation from '../../../components/lessons/034/1/serverexternal-relocation.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

The day you scaffold a Next.js project, `next.config.ts` is almost empty — a couple of flags and an export. It looks like a file you'll never think about again.

Fast-forward a year on a real SaaS. That same file now declares the image domains your CDN serves from, holds a permanent redirect from a URL scheme you retired, carries a stack of security headers, and has one stubborn line for an SDK that flat-out refuses to bundle. It grew because the product grew, one concern at a time.

So the question this file quietly asks every time you open it is the one an experienced engineer asks of any config: *what earns a line here, and what does each line cost?* Some entries are free — flags you flip on at the start and never reconsider. Others are levers with a price tag, and you only reach for them when something specific breaks.

The last two chapters lived *inside* the route tree — how routes render under Cache Components, and how a request flows through `cookies()`, `headers()`, and `proxy.ts`. This file is the layer that wraps all of that: the project-level surface, read once before any of your routes run. The rest of this chapter then walks it one key at a time — images, redirects, headers — so this opening lesson does two jobs: it hands you a one-screen **map** of everything the surface touches and which lesson owns each piece, and it teaches one entry to real depth — `serverExternalPackages`, the canonical "this SDK won't bundle" lever.

## The file Next reads before your routes

`next.config.ts` is a plain Node module. <Term definition="Next.js's Rust-based bundler and dev server — the tool that compiles your TypeScript and combines your code into the files the server actually runs.">Turbopack</Term> loads it once when you start the dev server and once at build time, reads the single config object it exports, and that's it. It never ships to the browser; it's pure server-and-build-time configuration. Nothing in this file affects a user's bundle — it shapes how your app is *built* and *served*.

Here's the whole shape:

```ts title="next.config.ts"
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // config goes here
};

export default nextConfig;
```

That's the canonical skeleton. A typed object literal, annotated with `NextConfig`, exported as the default. Every key you'll meet in this lesson goes inside those braces.

The reason for the type annotation is the whole reason this course always writes the config in TypeScript. A config typo is otherwise *invisible*. Misspell a key — `cacheComponent` instead of `cacheComponents` — and nothing complains; Next just doesn't see the option and silently ignores it. You've changed nothing and you have no idea. With `NextConfig` annotating the object, that same typo is a red squiggle in your editor and an error at build time. The type turns "silently does nothing" into "won't compile," which is exactly the trade you want for a file this load-bearing. You also get autocomplete over the entire surface, so you can discover what's configurable without leaving the editor.

A few rules that aren't choices. Always `.ts`, never `.js` or `.mjs` — this is an ESM project and TypeScript is the form you write. Next even refuses `.cts` and `.cjs` outright, which is a hint that the file format isn't a free decision. And don't keep both a `.js` and a `.ts` config side by side: Next picks one and the other silently does nothing — the same invisible-failure trap, one level up. Use `import type` for `NextConfig`, not a plain `import` — it's a type-only import, and the course's TypeScript settings enforce that distinction. This is a file you'll copy into every project, so it's worth getting the small things right once.

## A one-screen map of the surface

Before any single key, here's the map. The point of this section isn't to teach the keys — most of them have their own lessons — it's so you always know *where a given concern lives*. When you need to add an image domain or a redirect six weeks from now, you should be able to picture exactly which box of this file you're reaching into.

I've grouped the keys by **kind**, because the kind is what determines how you think about each one — and, conveniently, the kind is usually the lesson too.

The following diagram is the chapter's table of contents drawn as a map. Skim it, don't study it.

<Figure expandable={false} caption="Everything `next.config.ts` touches in a 2026 SaaS — and where this chapter teaches each piece.">
  <ConfigSurfaceMap />
</Figure>

Five kinds, and they behave very differently. **Always-on flags** you set once and leave on — they're scaffolding, not decisions, and we'll dispatch both of them in the next section. **Platform-pipeline config** (`images`) tunes how Next processes your assets; that's the next lesson. **Edge routing rules** (`redirects`, `rewrites`, `headers`, `trailingSlash`) describe how Next handles URLs and responses before they reach your route handlers; a later lesson in this chapter owns those — except the *contents* of `headers()`, the security baseline (CSP, HSTS, and friends), which gets its full treatment later still, when we harden the app before launch. **Bundling levers** are the conditional power-tools, and `serverExternalPackages` is the one we'll go deep on below.

That last box, `experimental`, deserves a warning right here. It's the holding pen for options Next hasn't stabilized yet, and its keys *come and go between minor releases*. An `experimental` block you copy from a blog post can simply stop existing after an upgrade — the option gets promoted to a top-level key, renamed, or dropped. Never paste an `experimental` block without checking it still exists in the version you're on. (This is exactly what happened to the two flags we're about to set: both used to live under `experimental` and are now plain top-level keys. Older tutorials still show the old location.)

That's the whole map. No deep dives here — this section is the index, and from here on we only open two of those boxes.

## Two flags every new project turns on

Two keys go on at the start of every new project in this course and stay on. They're not decisions you weigh per project; they're defaults. But each earns its place for a reason worth one tight beat.

**`cacheComponents: true`** opts you into the Cache Components rendering model from an earlier chapter — routes become dynamic by default, and you cache deliberately with an explicit `use cache` where it pays off. That model is the whole subject of its own chapter, so the one thing to know here is operational: it's a single flag, it's on, and it stays on. We're not re-teaching the model; we're just flipping its switch.

**`typedRoutes: true`** is the one worth actually seeing, because it's a small flag that buys a real engineering reflex for free. It takes every route in your app and turns the set of valid paths into a typed union — the type system now knows that `/dashboard` is a real route and `/dashbord` isn't. The payoff is where a broken link gets *caught*.

Compare the two worlds. The following tabs show the exact same line of code with the flag off and on.

<CodeVariants>
  <CodeVariant label="Without typedRoutes">
    <div data-mark-color="red">

    ```tsx "/dashbord"
    <Link href="/dashbord">Dashboard</Link>
    ```

    </div>
    **Compiles fine, breaks at runtime.** `href` is just a `string`, so nothing checks the typo. The user discovers the dead link when they click it and hit a 404 — in production, where you find out from an error report, not an editor.
  </CodeVariant>

  <CodeVariant label="With typedRoutes">
    <div data-mark-color="green">

    ```tsx "/dashbord"
    <Link href="/dashbord">Dashboard</Link>
    ```

    </div>
    **Caught at build time.** Identical line, but now `href` only accepts known routes. `"/dashbord"` isn't one, so it's a type error — a red squiggle in the editor and a failed build. The dead link never ships.
  </CodeVariant>
</CodeVariants>

Same line, different moment of truth. Without the flag, a mistyped internal link is a runtime 404 your *user* finds. With it, it's a build error *you* find, before the code is anywhere near production. That shift — from "fails at runtime, in front of the user" to "fails at compile time, in front of you" — is the entire value of the flag, and it costs you nothing but one line of config. (`<Link>` is the App Router's client-side navigation component you met in an earlier chapter; if it's hazy, that's fine — the only thing that matters here is that its `href` is what `typedRoutes` makes type-safe.)

Both flags now go into the config object, and it starts to accumulate:

```ts title="next.config.ts" ins={2-3}
const nextConfig: NextConfig = {
  cacheComponents: true,
  typedRoutes: true,
};
```

<VideoCallout videoId="rVdR0_Ujgq4" videoTitle="NEW Next.js TypeScript Features — Web Dev Simplified">
  Kyle (Web Dev Simplified) flips `typedRoutes: true` and watches a bad `<Link href>` turn red, then tours the related route-prop helper types — 7 min.
</VideoCallout>

## When an SDK won't bundle: serverExternalPackages

This is the lever — the conditional power-tool of the lesson. Everything so far has been a flag you turn on and forget. `serverExternalPackages` is the opposite: a line you add *only* when a specific failure shows up, and the real skill is recognizing that failure on sight and knowing whether this is even the right fix. So we'll do it in order — the trigger, then the mechanism, then the decision rule, then the cost.

### The wall you hit

Picture a normal afternoon. Your SaaS generates PDF invoices, so you install a vendor SDK that rasterizes documents — we'll call it `@acme/pdf-engine`, a stand-in for the kind of native package real apps pull in (think a PDF renderer, a barcode generator, a native-crypto module). You import it in a route handler, run the dev server, hit the route — and it crashes. The error looks something like this:

```bash frame="terminal"
 ⨯ Error: Could not load the native binding for @acme/pdf-engine
   Cannot find module './pdf_engine.linux-x64.node'
   Require stack:
   - .next/server/chunks/[turbopack]/acme-pdf-engine.js
   - .next/server/app/api/invoices/[id]/route.js
```

The exact wording varies by package — you'll see "Could not load the native binding," "Module not found," "Cannot find module './something.node'," sometimes a raw `MODULE_NOT_FOUND`. What they share is the shape: at runtime, inside the `.next` build output, something the package needs at its side can't be found.

Here's why it happens. Turbopack's job is to take your Server Component or route-handler code and the packages it imports and **bundle** them — trace every `import`, pull the relevant code together, and emit the files the server runs. That works beautifully for ordinary JavaScript, where every dependency is statically visible in the import graph. It breaks for two kinds of package. The first depends on a <Term definition="A compiled .node module — machine code built for a specific platform. A bundler can't read or rewrite it, only load it at runtime through require.">native binding</Term>: a `.node` file, compiled machine code that the bundler can't read, rewrite, or fold into a JavaScript chunk — it can only be loaded at runtime. The second reaches for its dependencies *dynamically*, with a `require()` computed at runtime instead of a static `import` the bundler can see ahead of time. Either way the bundler quietly loses the trail, and the missing piece surfaces as a crash when the code finally runs. You don't need the bundler internals — the category is what matters: **native-binding and dynamic-`require` packages resist bundling.**

### What the flag actually does

`serverExternalPackages` is the escape hatch:

<div data-mark-color="orange">

```ts title="next.config.ts" "'@acme/pdf-engine'"
const nextConfig: NextConfig = {
  serverExternalPackages: ['@acme/pdf-engine'],
};
```

</div>

That line tells Next one thing: *don't bundle this package — leave it alone and `require` it natively at runtime.* The package is opted out of Server Component bundling entirely. Instead of folding `@acme/pdf-engine` into a chunk, Next emits a thin `require('@acme/pdf-engine')` in the server output, and Node's ordinary module resolution loads the real package — native binding and all — from `node_modules` at runtime, exactly the way it would in a plain Node script. The package goes from "compiled into the bundle, broken" to "required from `node_modules`, working."

The following sequence shows that relocation. Scrub through it.

<ServerExternalRelocation />

Now the cost, stated honestly, because this is where judgment lives. Externalizing isn't free. The package no longer gets bundled and tree-shaken with the rest of your server code, so the function output is a little **larger**, and because the `require` happens when the function runs rather than being inlined ahead of time, the <Term definition="The first-request latency when a serverless function spins up from a cold (idle) state. Larger function output and runtime requires make this slower.">cold start</Term> is marginally **slower**. When the SDK genuinely needs externalizing, that's a fine trade — a slightly heavier function in exchange for a working SDK. When it *doesn't*, you've paid the tax for nothing. Which is exactly why the next part matters.

### Check the default list first

Here's the part that trips people up, and the genuinely current reflex for 2026. Next.js already ships with a **large built-in list** of packages it externalizes for you. Most of the popular Node SDKs a SaaS reaches for are already on it — `@prisma/client`, `sharp`, `pg`, `mongodb`, `better-sqlite3`, `@aws-sdk/client-s3`, `puppeteer`, and roughly eighty more. For any of those, you do nothing. You install the package, import it, and it works, because Next already knew it shouldn't be bundled.

So the worked example matters precisely *because* `@acme/pdf-engine` is the rare package that *isn't* on that list. That's the only situation where you touch this config. The decision rule is a short procedure, and it runs in this order:

<StateMachineWalker title="Should this package go in serverExternalPackages?">
  <Question id="crashes" prompt="You installed an SDK and used it. Does it crash at build or runtime?">
    <Branch label="No — it just works" to="do-nothing" />
    <Branch label="Yes — it throws" to="error-kind" />
  </Question>

  <Question id="error-kind" prompt="What does the error look like?">
    <Branch
      label="Native binding / dynamic require"
      to="on-list"
      rationale="'Could not load the native binding', 'Cannot find module ./x.node' — a bundling failure."
    />
    <Branch
      label="A package you forgot to install"
      to="real-bug"
      rationale="'Cannot find module the-sdk' with no native or .node detail — the package isn't there at all."
    />
  </Question>

  <Question
    id="on-list"
    prompt="Is the package already on Next's default external list?"
    description="The serverExternalPackages docs page carries the live list — check it before touching the config."
  >
    <Branch label="Yes, it's on the list" to="already-handled" />
    <Branch label="No, it's not listed" to="add-it" />
  </Question>

  <Leaf id="do-nothing" verdict="Do nothing">
    Most packages bundle fine or are already externalized. Don't pre-configure anything — a config line you didn't need is pure cold-start tax.
  </Leaf>

  <Leaf id="real-bug" verdict="Fix the real problem — install the dependency">
    This isn't a bundling problem. Externalizing it would hide a genuinely missing package behind a runtime `require` that still fails, just later and more confusingly. Install what's actually missing.
  </Leaf>

  <Leaf id="already-handled" verdict="Do nothing — Next already externalizes it">
    It's on the default list, so Next already emits a runtime `require` for it. Adding it yourself is redundant.
  </Leaf>

  <Leaf id="add-it" verdict="Add it to serverExternalPackages">
    A native or dynamic-`require` package that isn't on the default list. This is the one case the config key exists for: add the single entry and restart the dev server.
  </Leaf>
</StateMachineWalker>

Two anti-patterns to name, because both are common and both are costly. First, **don't preemptively externalize.** Don't add a package "to be safe" because you read somewhere that database drivers can be tricky. If it bundles fine, externalizing it just buys you a slower cold start for no reason — and if it's a popular SDK, it's probably on the default list anyway. Try it first; configure only on a real failure.

Second, **don't use `serverExternalPackages` to silence a missing-dependency error.** This is the subtle one. A package you forgot to install also crashes with "Cannot find module" — and externalizing it can look like it helps, because the error shape is similar. It doesn't help; it just moves the same failure from build time to runtime, where it's harder to diagnose. If the real problem is a package that isn't installed, the fix is to install it, not to teach the bundler to skip it.

### serverExternalPackages vs transpilePackages

There's a neighboring key that's easy to confuse with this one, and naming the difference once will keep you from reaching for the wrong tool: `transpilePackages`. They live in the same part of the config and they both deal with how Next treats a dependency, but they solve *opposite* problems and push code in *opposite* directions.

`serverExternalPackages` is for a **finished, compiled** Node package that breaks when bundled — you push it *out* of the bundle and `require` it at runtime. `transpilePackages` is for a **raw, un-compiled** package — typically one of your own monorepo packages shipped as plain `.ts`/`.tsx` that hasn't been through a build step — that Next must pull *in* and compile before it can bundle the rest of your app. One package is too finished to bundle; the other isn't finished enough. Out versus in.

Sort these to lock in the distinction — the skill is classifying a package by its symptom, which is exactly what you'll do in real life.

<Buckets twoCol instructions="Each package breaks Next's default bundling for a different reason. Sort it into the key that fixes it.">
  <Bucket name="external" label="serverExternalPackages" description="Bundle out — require at runtime" />
  <Bucket name="transpile" label="transpilePackages" description="Compile in before bundling" />

  <Item bucket="external">A native image-processing library with a `.node` binary</Item>
  <Item bucket="external">A database driver that does a dynamic `require()` at runtime</Item>
  <Item bucket="external">A vendor PDF SDK shipped with a compiled native addon</Item>
  <Item bucket="transpile">Your monorepo's `@acme/ui`, shipped as raw `.tsx`</Item>
  <Item bucket="transpile">An internal `@acme/utils` package published as un-compiled TypeScript</Item>
</Buckets>

### Transitive dependencies are handled for you

One short forward-looking note, so an older pattern doesn't trip you up when you read other people's configs. When you externalize a package, you also want *its* dependencies externalized — a native SDK usually pulls in helper packages of its own, and bundling those while externalizing the parent is its own kind of broken. In older Next.js versions you had to hand-list every one of those child dependencies, which was tedious and easy to get wrong.

As of **Next.js 16.1**, you don't. When you put a package in `serverExternalPackages`, Turbopack externalizes its transitive dependencies automatically — you name the one package, and its dependency tree comes along. So if you find a config or a blog post that carefully lists out an SDK's internal dependencies one by one, you're looking at an obsolete pattern. One entry is enough now.

## A real config edit does nothing until you restart

This one isn't about any single key — it applies to *every* line in the file, and it burns hours in practice, so it gets its own beat.

`next.config.ts` is read **at server startup**. Once. If you edit the file while the dev server is running, the server keeps using the config it loaded when it booted — your change sits on disk doing absolutely nothing. The symptom is maddening precisely because it's silent: you add a flag, save, refresh, and the behavior is unchanged, so you assume you got the config *wrong* and start second-guessing a line that was perfectly correct.

<Aside type="caution">
After any edit to `next.config.ts`, restart the dev server. Make "I changed the config and nothing happened — did I restart?" the very first thing you check. It's the single most common false alarm with this file, and it's a five-second fix, not a config bug.
</Aside>

You'll meet this same gotcha again when we get to redirects: "I added a redirect and it doesn't fire" is, nine times out of ten, a dev server still running the old config. Same file, same rule.

## A production next.config.ts, end to end

Let's put it together. Here's the config you'd actually ship for a new SaaS at this stage — small, honest, every line defensible. Step through it; each step explains why that line earns its place.

<AnnotatedCode lang="ts" code={`
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  cacheComponents: true,
  typedRoutes: true,
  serverExternalPackages: ['@acme/pdf-engine'],
  // Security headers (CSP, HSTS) go here once the hardening pass lands.
  // async headers() { ... },
  // images / redirects / rewrites get added here as the product grows.
};

export default nextConfig;
`}>
  <AnnotatedStep meta={`{1} "import type"`} color="blue">
    The typed import. `import type` because it's type-only, which the course's TypeScript settings require; `NextConfig` is what turns a misspelled key into a build error instead of a silently-ignored option. It's the reason the whole file is `.ts`.
  </AnnotatedStep>

  <AnnotatedStep meta="{4-5}" color="green">
    The two always-on flags. `cacheComponents` opts into the Cache Components rendering model; `typedRoutes` makes a bad `<Link href>` a build error. Set once, left on, no per-project thought.
  </AnnotatedStep>

  <AnnotatedStep meta="{6}" color="orange">
    The single conditional entry. It's here *only* because `@acme/pdf-engine` is native, crashed when bundled, and isn't on Next's default list. If any of those weren't true, this line wouldn't exist — orange marks it as a lever that earns its cost.
  </AnnotatedStep>

  <AnnotatedStep meta="{7-9}" color="blue">
    Signposts, not code. The commented `headers()` marks where the security baseline will land later; the trailing comment marks the seams where the image config and routing rules from the next lessons will grow the file. The file is honest about what it doesn't do yet.
  </AnnotatedStep>
</AnnotatedCode>

That's the mental model to carry out of here, in one line: `next.config.ts` is a **typed object, read once at startup**, holding a few always-on flags plus a handful of conditional levers — and every lever you add carries a cost, so you add it only when a real trigger forces your hand. The typed import is what keeps the whole thing honest.

## Check your map

Two quick checks on the two things this lesson had to leave behind — the map and the `serverExternalPackages` judgment call.

<MultipleChoice>
  An SDK you just installed crashes at build with "Could not load the native binding." What's the right *first* move?

  <McqChoice correct>Check whether Next already externalizes it on its default list, and confirm it actually needs externalizing, before touching the config.</McqChoice>
  <McqChoice>Add it to `serverExternalPackages` straight away — a native-binding crash is exactly what that key is for.</McqChoice>
  <McqChoice>Move it to `transpilePackages` so Next compiles the package for you.</McqChoice>
  <McqChoice>Set the package to `unoptimized` in the config to skip the bundling step.</McqChoice>

  <McqWhy>A native-binding crash is the signature of a package that resists bundling — but jumping straight to the config is the wrong reflex. Most popular native SDKs are already on Next's default external list and need no config at all, so the first move is to check that list and confirm the package genuinely isn't handled. You only add a line for the package that truly isn't covered. `transpilePackages` solves the opposite problem (raw TypeScript to compile *in*), and `unoptimized` belongs to the `images` config — it does nothing for bundling.</McqWhy>
</MultipleChoice>

<MultipleChoice>
  You add `typedRoutes: true` to a `next.config.ts` that's typed with `NextConfig`, save, and refresh the page in your already-running `next dev` — but a `<Link href="/dashbord">` still compiles without a squiggle. Before you touch anything else, what's the one thing to check?

  <McqChoice correct>Whether you've bounced the dev server since the edit — it's still serving the config it parsed when it booted.</McqChoice>
  <McqChoice>Whether the key name got mistyped, which would let Next ignore it without complaint.</McqChoice>
  <McqChoice>Whether the file should be `.mjs`, since `.ts` configs can't carry stable flags.</McqChoice>
  <McqChoice>Whether `typedRoutes` still needs to be nested under an `experimental` block.</McqChoice>

  <McqWhy>The config is parsed once, at startup. An edit to a server that's already up changes the file on disk but not the config in memory, so the fix is a restart — and "did I restart?" is the first thing to rule out. The other three are real-sounding but ruled out here: a mistyped key can't slip past the `NextConfig` annotation (it'd be a type error, not a silent no-op), `.ts` is the correct and only supported form for this ESM project, and `typedRoutes` graduated to a stable top-level key — nesting it under `experimental` would be the actual mistake.</McqWhy>
</MultipleChoice>

## External resources

The single most useful page to bookmark is the `serverExternalPackages` reference — it carries the live default list, which is the thing you'll actually want to check before adding a package by hand.

<CardGrid>
  <ExternalResource
    title="Next.js — serverExternalPackages"
    href="https://nextjs.org/docs/app/api-reference/config/next-config-js/serverExternalPackages"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="The config key reference — and the live list of packages Next externalizes by default."
  />
  <ExternalResource
    title="Next.js — Package Bundling guide"
    href="https://nextjs.org/docs/app/guides/package-bundling"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="When and how Next bundles dependencies — externalizing, transpiling, and analyzing what ships."
  />
  <ExternalResource
    title="Next.js — typedRoutes"
    href="https://nextjs.org/docs/app/api-reference/config/next-config-js/typedRoutes"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="The flag that turns your route set into a typed union, so a bad <Link href> won't compile."
  />
  <ExternalResource
    title="Next.js — next.config.js reference"
    href="https://nextjs.org/docs/app/api-reference/config/next-config-js"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="Every configuration option in one place, each linking to its own page."
  />
</CardGrid>
