---
title: 'Diataxis: the four jobs a doc can do'
chapter-id: 101
course-progress: 0.00005
tagline: Your introduction to Diataxis, the framework that sorts every doc in a SaaS repo into one of four jobs so each reader finds what they came for.
sidebar:
  order: 1
  label: The four jobs of docs
---

import Figure from '../../../components/figures/Figure.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { Card, CardGrid } from '@astrojs/starlight/components';
import Matching from '../../../components/exercises/matching/Matching.astro';
import Pair from '../../../components/exercises/matching/Pair.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import DiataxisQuadrantGrid from '../../../components/lessons/101/1/DiataxisQuadrantGrid.astro';
import ReadmeMixingTrap from '../../../components/lessons/101/1/ReadmeMixingTrap.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

You have read bad documentation. You have probably written some too. The README that opens like a friendly tutorial, drifts into a list of commands you'll never run, then spends forty lines justifying a database choice, then trails off into a half-finished "Architecture" section that was last accurate two years ago. By line fifty nobody is reading. Lines fifty-one through four hundred are wrong, and no one knows, because no one got there.

This is the gap between "we have docs" and "we have docs anybody actually uses." A team can produce a thousand lines of documentation and still leave every reader unserved, and the reason is almost never effort. It's that one document was asked to do four different jobs at once, and a reader who showed up for one of those jobs has to wade past the other three to find their part. The docs aren't bad because they're short or long. They're bad because they're *mixed*.

<Term definition="A documentation framework (Procida, 2017) that sorts all technical docs into four types by the reader's need. From Greek dia- (across) and taxis (arrangement).">Diataxis</Term> is the vocabulary that names those four jobs. It's a four-word framework, and once you have the four words, the rest of this chapter becomes a single question asked four times: *which artifact in the repo owns which job?* You've already lived inside three of the four types without naming them. This course is one of them. The repo you've been building is held together by another. By the end of this lesson you'll be able to look at any document — or any paragraph inside one — and say which of four jobs it's doing, and spot the moment it fails because it's quietly trying to do two.

## Why one document can't do four jobs

Start with the reader, not the document. Whenever someone opens a doc, they arrive with exactly one of four needs, and here is the uncomfortable part: those four needs actively interfere with each other. Optimizing a paragraph for one of them makes it *worse* for the other three. That's not a failure of writing skill. It's structural, and it's the whole reason the framework exists.

Picture the four readers. The first is brand new to the codebase and needs to be taken by the hand down one path until something works — they don't want options or completeness, options are paralyzing when you don't yet know what any of them mean. The second already knows the codebase and needs to accomplish one specific task right now; they want to scan, find the one answer, and leave — narrative prose is friction to them. The third needs to look up a single fact, like the exact columns on a table, and wants it dry and complete with zero story attached. The fourth wants to understand *why* a decision was made, and for that they need exactly the prose and rationale the other three were trying to skip.

Now watch them collide. Hand the first reader the completeness the third one craves and you've buried them. Hand the third reader the narrative the fourth one needs and you've slowed them to a crawl at 2am when they had one question. The needs don't just differ — they pull in opposite directions, and a single paragraph cannot face four directions at once. That is why good documentation splits by what the reader came to do, and it's the spine the four definitions hang on.

## The four jobs, one definition each

Here are the four. Read each one by its reader, not by its file format — the type is defined by *what the reader came to do*, never by where the bytes live. The order is canonical; learn it in this order.

<CardGrid>
  <Card title="Tutorial — learning" icon="open-book">
    For a stranger to the codebase. It takes them by the hand down **one** path to a working end state: no decisions, no alternatives, success guaranteed if they follow along. You already know this one intimately — *this course is a tutorial.* In a SaaS repo the only tutorial is the README's "Getting started" path, if it has one, and nothing else.
  </Card>
  <Card title="How-to — doing a task" icon="setting">
    For someone who already knows the codebase and wants to accomplish one specific thing: "add a Server Action," "seed a tenant for local dev," "rotate the Stripe webhook secret." Goal-directed, assumes competence, built to be scanned. Its home is a "Common tasks" list, or a `/docs/how-to/` folder for the heavy ones.
  </Card>
  <Card title="Reference — looking up a fact" icon="list-format">
    Dry, complete, structurally organized; it describes the surface exactly *as it is*, with no interpretation. The reader wants one fact and nothing around it. You've met this one too: the Drizzle schema file **is** reference; `env.ts` **is** reference; a TSDoc comment on a public function **is** reference.
  </Card>
  <Card title="Explanation — understanding why" icon="comment">
    For someone who wants the background, the rationale, the trade-offs — the *why* behind a choice rather than the steps to do a task. Read at leisure, away from the keyboard. An ADR is explanation; a note on "why we chose Server Actions over a REST layer" is explanation.
  </Card>
</CardGrid>

A few of those anchors lean on names you'll meet properly later. A <Term definition="TypeScript-flavoured JSDoc comment block (/** … */) above a declaration; editors surface the first sentence as hover text. Syntax covered in the next chapter.">TSDoc</Term> comment is the doc-block that editors turn into hover text, and you'll write them in the next chapter. An <Term definition="Architecture Decision Record — one short markdown file capturing one architectural choice and the reasoning behind it. Full template in this chapter's ADR lesson.">ADR</Term> is a one-decision-per-file record of an architectural choice; it gets its own lesson later in this chapter. For now they're just the canonical homes of reference and explanation — name them and move on.

Notice what these four definitions have in common: every one of them is phrased by the reader's intent. Not "a tutorial is a markdown file with numbered steps." A tutorial is *the doc a stranger follows to a first success.* The format is downstream of the job.

<VideoCallout videoId="0BqucaRwHhA" videoTitle="Better Docs with Diátaxis">
  The Scaling DevTools podcast walks the four types against real dev-tool product docs and untangles the tutorial-vs-how-to confusion — roughly 24 minutes.
</VideoCallout>

## The 2x2 behind the four

So why exactly four? Not three, not five? Because two independent questions, each with two answers, produce four combinations — and Diataxis is built on exactly those two questions.

The first question is about the reader's **mode**: are they *acquiring* a skill they don't yet have (study mode), or *applying* a skill they already have (work mode)? A newcomer learning the codebase is acquiring. An engineer rotating a secret is applying. The second question is about the doc's **content**: is it about *action* — the practical steps of doing something — or about *cognition* — theoretical knowledge, the understanding behind it? "Run these commands" is action. "Here's why the architecture is shaped this way" is cognition.

Two questions, two answers each, and the four document types fall straight out of the grid. A tutorial is acquisition plus action: you're learning, by doing. A how-to is application plus action: you're working, by doing. Reference is application plus cognition: you're working, by knowing a fact. Explanation is acquisition plus cognition: you're learning, by understanding. The following diagram places all four on those two axes.

<Figure caption="Four readers, four quadrants. A doc that lands on the diagonal between two cells is the doc nobody finishes.">
  <DiataxisQuadrantGrid />
</Figure>

There's a payoff hiding in this grid, and it's the one senior move of the whole lesson. When a document *fights itself* — when it feels bloated and unfocused and you can't say why — it has almost always straddled two quadrants. The diagonal tension between, say, tutorial and reference is exactly the smell of a doc trying to teach a newcomer and serve a fact-lookup in the same breath. Once you can see the grid, you can name the smell.

## The mixing trap that wrecks most READMEs

Now point the framework at the document you've seen fail most often: the README. Walk a typical one top to bottom and watch it cross every quadrant in turn.

It opens as a tutorial — "clone this, install that, run this" — aimed at the newcomer. Then it slides into how-to: "to add a feature, do X, then Y." Then it hardens into reference: a table of environment variables and their types. Then it drifts into explanation: "we chose Postgres because…" Four sections, four quadrants, four different readers — and *every* reader has to scan past three jobs that aren't theirs to reach the one that is. The newcomer hits the env-var table and stalls. The engineer who just wanted the test command reads a paragraph about Postgres. The doc isn't too long; it's four documents wearing one trenchcoat. The following diagram shows the same README two ways — as the mixed novel, and split into its proper homes.

<Figure caption="Same content, two layouts. On the left, one document doing four jobs; on the right, each job sent to the artifact that owns it — which is the shape the rest of this chapter builds.">
  <ReadmeMixingTrap />
</Figure>

The senior call is the one the right side of that diagram makes: don't write one document that does four jobs — *route each job to its right home.* Keep the README a thin tutorial-and-pointer doc. Send reference down into the schema file and the TSDoc, where it can't go stale. Send explanation to ADRs. Send how-to to `AGENTS.md` or a `/docs/how-to/` folder. The README that survives is the one that does one job and points at the rest.

## Where each type lives in a 2026 SaaS repo

That routing isn't abstract. In a 2026 SaaS repo the four types land on a small, specific set of files, and each of the next three lessons in this chapter takes one row of this map and builds it out properly.

- **Tutorial → the `README.md` "Getting started" section** — and only that section. The next lesson builds the thin README.
- **How-to → `AGENTS.md`'s "Common tasks"**, plus a `/docs/how-to/` folder for the heavyweight procedures. The `AGENTS.md` lesson later in this chapter owns this.
- **Reference → the code itself** — the Drizzle schema, your Zod schemas, TSDoc on public functions, the `env.ts` file. The truth and its documentation are the same bytes.
- **Explanation → `/docs/adr/`** for architectural decisions, plus the occasional inline `// why` comment for narrow rationale. The ADR lesson later in this chapter owns this.

Now the single most important caveat in this lesson, and the one beginners get wrong first: **Diataxis is a thinking vocabulary, not a folder mandate.** The four types are how you decide what a given doc is *for*. They are not four directories every repo must create. Most SaaS repos need a `README.md`, an `AGENTS.md`, and a `/docs/adr/` folder — and nothing else. Those few files (plus the source code itself) carry all four types between them. A team that scaffolds empty `/tutorials`, `/how-to`, `/reference`, and `/explanation` folders on day one hasn't applied the framework; they've cargo-culted it, and now they own four directories that will sit empty or, worse, fill with the wrong things.

:::tip
Don't read the four types as four folders. Read them as four questions you ask about a doc you're already writing: *who's reading this, and what did they come to do?* The answer tells you which existing file it belongs in.
:::

Before moving on, bind the abstract types to concrete reader sentences. Try this drill — match each reader's intent, phrased the way they'd actually say it, to the Diataxis type that serves them.

<Matching instructions="Match each reader's request to the Diataxis type that serves it.">
  <Pair>
    <Fragment slot="left">"I've never seen this codebase — get me to a running dev server."</Fragment>
    <Fragment slot="right">Tutorial</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">"I know the repo. How do I rotate the Stripe webhook secret?"</Fragment>
    <Fragment slot="right">How-to</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">"What columns does the `invoices` table have?"</Fragment>
    <Fragment slot="right">Reference</Fragment>
  </Pair>
  <Pair>
    <Fragment slot="left">"Why did we pick Drizzle instead of Prisma?"</Fragment>
    <Fragment slot="right">Explanation</Fragment>
  </Pair>
</Matching>

Now run it the other way — the direction you'll actually use on the job. Here are real artifacts from a SaaS repo. Drop each one into the documentation job it does. This is the core skill: look at a file and name its job.

<Buckets twoCol instructions="Sort each real repo artifact into the documentation job it does.">
  <Bucket name="tutorial" label="Tutorial" description="Learning — take me by the hand" />
  <Bucket name="howto" label="How-to" description="Doing — help me do this task" />
  <Bucket name="reference" label="Reference" description="Fact — tell me exactly what" />
  <Bucket name="explanation" label="Explanation" description="Why — help me understand" />

  <Item bucket="tutorial">The `README.md` "Getting started" steps</Item>
  <Item bucket="howto">An `AGENTS.md` "Common tasks" entry</Item>
  <Item bucket="howto">`/docs/how-to/deploy.md`</Item>
  <Item bucket="reference">`src/db/schema.ts`</Item>
  <Item bucket="reference">`env.ts`</Item>
  <Item bucket="reference">TSDoc on a Server Action</Item>
  <Item bucket="explanation">`/docs/adr/0001-use-drizzle.md`</Item>
  <Item bucket="explanation">A `// why we avoid useEffect here` comment</Item>
</Buckets>

If sorting the reference bucket felt automatic — schema, env, TSDoc, all *code* — hold onto that. It's the spine of the whole chapter.

## The "could this be a link?" reflex

Look again at where reference lives: in the code. That points at the reflex that ties this whole chapter together, and it's small enough to carry in your head.

Whenever you're about to *paraphrase* canonical truth in a doc — a function signature, a schema's column list, a config value's type — stop, and link to the source instead.

The reason is mechanical, not stylistic. A paraphrase has nothing holding it in sync with the thing it paraphrases. The moment the code changes, the paraphrase is silently wrong, and "silently" is the dangerous word — no test fails, no build breaks, the doc just quietly starts lying. A link can't lie like that. It resolves to the <Term definition="The one canonical place a fact lives. Every other mention should point at it, not copy it, so the fact can't drift out of sync.">source of truth</Term> itself, so it's correct by construction; when the code changes, the link still points at the current truth. This is what turns the rest of the chapter's rules from aspirations into mechanics. "Keep the README thin" and "let the source be the doc" aren't discipline you have to remember — they're what falls out of asking, before you type a paragraph, *could this just be a link?* The next lesson takes this exact reflex and turns it into the README's design.

## What "good" looks like for each type

One last thing makes the framework operational instead of merely tidy: each type passes a *different* test. Knowing the four tests is how you check your own docs.

- **A tutorial passes if** a new hire follows it to a green checkmark in under thirty minutes without asking anyone. The trap here is the "comprehensive" instinct — bolt how-to and reference onto the tutorial "for completeness" and the newcomer stalls; a doc that tries to pass all four tests passes none.
- **A how-to passes if** the reader doing the task scan-finds the answer in under a minute. If they have to read a paragraph of background to get to the step, you've leaked explanation into it.
- **A reference passes if** an engineer debugging at 2am finds the exact fact without reading one thing they don't need. The "the code is self-documenting" line is half true and half dangerous here: the *schema* genuinely is self-documenting, but a gnarly Server Action's TSDoc is not optional — "self-documenting" justifies skipping reference only for code that's actually trivial.
- **An explanation passes if** a future maintainer grasps the trade-off well enough to decide whether the decision still holds under new constraints. The failure mode is writing explanation before any reference exists — a "philosophy" doc floating above an undocumented codebase is decoration, not documentation.

Notice that no single document could pass all four tests at once, because the tests pull in the same opposite directions the readers did. That's the deepest version of the lesson: "good docs" isn't one bar. It's four bars, and the first job is knowing which one you're aiming at.

## Recap

Four readers, four jobs, one job per doc. Before you write a paragraph, ask which of the four the reader came for — learning, doing a task, looking up a fact, or understanding why — and write for that one reader only. When you catch yourself about to copy a fact that lives somewhere canonical, link to it instead of paraphrasing it. The four types are a vocabulary for deciding what a doc is *for*; they are not four folders to create.

The next lesson takes the first artifact on the map — the README — and builds it as a deliberately thin tutorial-and-pointer document, the first concrete payoff of everything you just named.

## External resources

<CardGrid>
  <ExternalResource
    title="Diátaxis — the framework"
    href="https://diataxis.fr"
    icon="lucide:compass"
    iconColor="#16A34A"
    description="The canonical site for the four-type framework. A focused, roughly five-minute read."
  />
  <ExternalResource
    title="We fixed our docs with Diátaxis"
    href="https://blog.sequinstream.com/we-fixed-our-documentation-with-the-diataxis-framework/"
    icon="lucide:git-compare"
    iconColor="#7C3AED"
    description="A SaaS dev-tool team's concrete before/after of restructuring product docs into the four types."
  />
  <ExternalResource
    title="Should you use Diátaxis?"
    href="https://idratherbewriting.com/blog/what-is-diataxis-documentation-framework"
    icon="lucide:pen-line"
    iconColor="#D97706"
    description="Tom Johnson's balanced evaluation — why it caught on, and where its edges are."
  />
</CardGrid>
