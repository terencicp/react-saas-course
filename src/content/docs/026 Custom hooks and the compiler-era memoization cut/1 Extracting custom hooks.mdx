---
title: Extracting custom hooks
description: When composing built-in hooks into a reusable named behavior earns its weight, how to name and shape it, and the 2026 catalog worth recognizing.
chapter-id: 26
course-progress: 0.5
sidebar:
  order: 1
  label: Custom hooks
---

import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import CodeTooltips from '../../../components/code/CodeTooltips.astro';
import RecipeNotState from '../../../components/lessons/026/1/RecipeNotState.astro';
import StateMachineWalker from '../../../components/figures/state-machine-walker/StateMachineWalker.astro';
import Question from '../../../components/figures/state-machine-walker/Question.astro';
import Branch from '../../../components/figures/state-machine-walker/Branch.astro';
import Leaf from '../../../components/figures/state-machine-walker/Leaf.astro';
import ReactCoding from '../../../components/live-coding/ReactCoding/ReactCoding.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { Card, CardGrid } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

A dashboard you're working on lazy-loads images as they scroll into view. The product grid does it, the activity feed does it, the team-members list does it. In all three, the same six lines show up: a `useRef` for the element, a `useEffect` that wires up an `IntersectionObserver`, and a `useState` flag that flips to `true` the moment the element crosses the viewport. Three components, one block of wiring, copied verbatim.

Then a fourth screen needs the same behavior. A junior reaches for the clipboard. An experienced engineer doesn't — they pull those six lines into a function called `useIntersection`, and now every screen calls `useIntersection(ref)` and reads back a boolean. That function is a custom hook, and the decision to write it is the entire subject of this lesson.

Custom hooks are where React's hook system stops being a fixed menu of built-ins and becomes something you extend. But extraction is a decision, not a reflex — pulled too early it buries simple code under indirection, pulled at the right moment it turns a tangle into a name. By the end of this lesson you'll be able to decide *whether* a behavior is worth extracting, name the hook so it reads right, choose what it returns, and recognize the handful of custom hooks a 2026 SaaS codebase reaches for again and again.

You already have every piece this lesson composes — `useState`, `useReducer`, `useRef`, `useEffect`, `useEffectEvent`, `useContext`, the concurrent hooks, and the rules that govern all of them. Nothing new gets added to the menu here. What's new is packaging: taking the primitives you know and wrapping them into named, reusable behaviors.

## A custom hook is a function that calls hooks

Here is the whole definition: a custom hook is an ordinary JavaScript function whose name starts with `use` and that calls one or more hooks inside it — built-in hooks, or other custom hooks. That's it. There is no special API, no registration step, no `createHook` factory, nothing to import beyond the hooks you were already importing. If you can write a function, you can write a custom hook.

The part that trips people up is the name. The `use` prefix is not a stylistic convention or a label you add for tidiness — it is a contract, and two different audiences read it. The first is the <Term definition={"react-hooks/rules-of-hooks. The ESLint rule that enforces the rules of hooks: hooks must be called at the top level of a component or another hook, never inside conditions, loops, or nested functions."}>linter</Term>: the `react-hooks` ESLint plugin treats any `use`-named function as a hook and holds it to the <Term definition={"Hooks must be called at the top level of a component or another hook, in the same order on every render. Enforced by react-hooks/rules-of-hooks."}>rules of hooks</Term>. The second is the next person who reads your code: the prefix tells them "this function obeys those rules — call it at the top level, unconditionally, never inside an `if`." You met those rules a couple of chapters back, when you saw that React tracks each hook call by its position in a fixed call order; the prefix is how that guarantee travels from a built-in hook out to the functions you write yourself.

So let's make the `useIntersection` extraction from a moment ago concrete. The following comparison puts the copied-everywhere version next to the extracted one — the same behavior, before and after it earns a name.

<CodeVariants maxLines={16}>
  <CodeVariant label="Inline (×3)">
    <div data-mark-color="orange">

    ```tsx {5-13}
    const ProductCard = ({ product }: { product: Product }) => {
      const ref = useRef<HTMLDivElement>(null);
      const [isVisible, setIsVisible] = useState(false);

      useEffect(() => {
        const el = ref.current;
        if (!el) return;
        const observer = new IntersectionObserver(([entry]) => {
          if (entry.isIntersecting) setIsVisible(true);
        });
        observer.observe(el);
        return () => observer.disconnect();
      }, []);

      return (
        <div ref={ref}>{isVisible ? <img src={product.image} /> : null}</div>
      );
    };
    ```

    </div>
    **Repeated verbatim in three components.** Each one carries the full `IntersectionObserver` dance — the ref, the effect, the cleanup, the state flag.
  </CodeVariant>

  <CodeVariant label="Extracted">
    <div data-mark-color="green">

    ```tsx {21}
    const useIntersection = (ref: RefObject<Element | null>): boolean => {
      const [isVisible, setIsVisible] = useState(false);

      useEffect(() => {
        const el = ref.current;
        if (!el) return;
        const observer = new IntersectionObserver(([entry]) => {
          if (entry.isIntersecting) setIsVisible(true);
        });
        observer.observe(el);
        return () => observer.disconnect();
      }, [ref]);

      return isVisible;
    };

    const ProductCard = ({ product }: { product: Product }) => {
      const ref = useRef<HTMLDivElement>(null);
      const isVisible = useIntersection(ref);
      return (
        <div ref={ref}>{isVisible ? <img src={product.image} /> : null}</div>
      );
    };
    ```

    </div>
    **One recipe, called wherever the behavior is needed.** The component shrinks to the line that matters — *is this element on screen yet?*
  </CodeVariant>
</CodeVariants>

Nothing moved into the hook that you haven't written a hundred times. The hook *is* the effect, the state, and the ref — lifted out of the component and given a name that says what the wiring is for. The component that's left reads like a sentence: hold a ref, ask whether it's on screen, render accordingly.

One corollary falls straight out of the definition, and it's the most common way the contract gets abused. A `use`-named function that calls **no** hooks inside it is misnamed. If all your function does is shape some data — reformat a price, build a CSS class string, sort an array — and it touches no `useState`, no `useEffect`, no other hook, then it is a plain function, not a hook. Drop the `use` prefix and ship it from `lib/`. Naming a hookless function `useFormatPrice` lies to the linter (which will now police it for rules it doesn't need) and lies to the reader (who will expect it to behave like a hook). The prefix is a promise; only make it when it's true.

## Same hook, separate state

This is the single most important thing to internalize about custom hooks, and it's where almost everyone's intuition is wrong the first time.

When you call the same custom hook in two different components, they share the **code** — the wiring recipe — and nothing else. They do not share state. Each call site gets its own `useState` cell, its own `useEffect` subscription, its own ref. Two components calling `useIntersection(ref)` are not watching the same element and they are not pooling a single boolean; each one has a completely independent visibility flag that knows nothing about the other.

The clean way to hold this: a custom hook is a recipe, and each component that calls it cooks its own dish. Writing down a recipe once doesn't mean two cooks following it end up sharing a single plate of food — they each produce their own. The recipe is shared; the meal is not. A custom hook shares instructions for wiring state and effects; it never shares the state and effects those instructions produce.

The diagram below makes the structure literal. One `useDebounced` recipe sits at the top. Two sibling components each call it, and each call produces its own private `[value, setValue]` cell. Type into the search field and only its cell changes; the filter field's debounced value sits there untouched.

<RecipeNotState />

This matters because the misconception causes real bugs. A developer writes `useToggle()` expecting that calling it in the header and in the sidebar will keep a single shared open/closed flag, ships it, and is baffled when toggling one does nothing to the other. The hook was never the problem — it was the wrong tool for the job. A custom hook is the right tool when you want the *same behavior* in many places; it is the wrong tool when you want the *same state* in many places.

When you genuinely need state shared across components, you already know the tools that do that, and a custom hook is none of them. Lift the state up to a common parent and pass it down. Put it in a context when the consumers are far apart in the tree. Or, for app-wide state that lives outside the tree entirely, reach for an external store — a class of tool you'll meet later in the course. The job of those tools is to *share* a single piece of state. The job of a custom hook is to *reproduce* a behavior. Keep the two intentions separate and the bugs above never happen.

<VideoCallout videoId="I2Bgi0Qcdvc" videoTitle="Custom Hooks in React (Design Patterns)">
  Darius Cosden walks the whole extraction motion in 13 minutes — taking a tangled component apart into named hooks and composing them back together.
</VideoCallout>

## Shaping the hook: arguments in, tuple or object out

A custom hook is just a function, so its shape is just a function signature — what goes in, what comes out. A few small conventions make a hook feel native instead of homemade, and they're worth holding to across a codebase.

**Everything the hook needs comes in as an argument.** If `useIntersection` needs an element, you pass it the ref. If `useDebounced` needs a value and a delay, you pass both. The watch-out here is subtle: a hook that reaches out and reads a value from the component that called it — anything other than through its own parameters — has leaked the consumer's scope into itself. It now silently depends on a variable that the call site can't see in the signature, and it breaks the moment a second component with a different layout tries to use it. If the hook needs it, pass it in.

**Everything the hook produces comes out as the return value**, and there are three shapes worth knowing, chosen by how many things come back. The following block shows all three side by side.

```tsx
const isOnline = useOnlineStatus();
const [draft, setDraft] = useLocalStorage('draft', '');
const { data, isLoading, error } = useFetch('/api/invoices');
```

When there's exactly one output, return it bare — `const isOnline = useOnlineStatus()` reads perfectly and a tuple or object would just be ceremony. When the hook hands back a value paired with a way to change it, return a **tuple**, deliberately mirroring `useState` so the destructuring feels familiar — `const [draft, setDraft] = useLocalStorage('draft', '')` reads exactly like the built-in it's built on. When three or more things come back, return an **object**, so each call site names what it pulls out and the order stops mattering — `const { data, isLoading, error } = useFetch(url)` lets a caller take only `data` without counting commas. The rule of thumb is the same one the code conventions use everywhere: tuples when the order is the meaning, objects when the names are.

The one discipline that matters more than which shape you pick is **picking one and holding it.** A hook that returns a tuple in one case and an object in another breaks every call site that guessed wrong, and it forces readers to check the implementation to remember which they're getting. Decide the shape once, when you write the hook, and never make it conditional.

### Generics let the type flow from the call site

A hook that passes a value *through* it — takes something in, hands the same kind of thing back — should be <Term definition={"A type parameter (written <T>) that lets one function work over many types, with the concrete type filled in from how the function is called."}>generic</Term>, so its type is decided by whoever calls it rather than frozen when you write it. `useLocalStorage` is the textbook case: it can store a string draft, a number, a boolean preference — and the caller shouldn't have to cast or annotate. A type parameter lets one definition serve all of them.

<CodeTooltips tooltips={{
  '<T,>': 'A type parameter. In a .tsx file the trailing comma is required — it tells the parser this is a generic, not the start of a JSX tag.',
  'initial: T': 'The initial value pins T. Pass a string and T is string; pass a number and T is number — no annotation needed at the call site.',
  '[T, (value: T) => void]': 'The return tuple: the current value, and a setter that only accepts the same type T.',
}}>
```tsx
const useLocalStorage = <T,>(key: string, initial: T): [T, (value: T) => void] => {
  // ...read and write localStorage under the key...
};

const [draft, setDraft] = useLocalStorage('draft', '');
const [count, setCount] = useLocalStorage('count', 0);
```
</CodeTooltips>

The payoff is at the call sites. `useLocalStorage('draft', '')` infers `T = string`, so `setDraft` only accepts strings and `draft` is typed as a string everywhere you read it. `useLocalStorage('count', 0)` infers `T = number` from the same definition. You wrote the hook once; the types specialize themselves per call.

Two notes on how that signature is written, both from the code conventions. A hook is bound to a `const` as an arrow function — that's the default form for hooks in this codebase, the same as components and callbacks. And the return type is annotated explicitly here even though TypeScript could infer it, because in this case the signature *is* the lesson: spelling out `[T, (value: T) => void]` documents the contract at a glance. The convention is to annotate the return type of an exported function, and to annotate any time the signature itself is the thing worth reading.

:::caution
That trailing comma in `<T,>` is not a typo. In a `.tsx` file, `<T>` on its own looks like the opening of a JSX tag to the parser, and you'll get a confusing syntax error. The comma — `<T,>` — disambiguates it as a type parameter. You only hit this in `.tsx`; plain `.ts` files don't need it.
:::

## When extraction earns its weight

Now the judgment, which is the real content of this lesson. Anyone can move six lines into a function. Knowing *when* that move pays for itself — and when it just adds a layer you have to read through later — is what separates a codebase that's a pleasure to change from one that's a maze of one-line wrappers.

Extract a custom hook when **any one** of these three conditions holds.

**Reuse.** The behavior shows up in two or more components, and the wiring is non-trivial — more than a line or two. This is the `useIntersection` case from the start of the lesson: six lines of observer wiring, copied across three screens, about to be copied to a fourth. Extraction stops the copying and gives the behavior a single home to fix bugs in.

**Clarity.** A single component has a tangled knot of coordinated hooks — an effect, some state, a ref, all working together toward one job — and lifting that knot into a named hook reveals what the component is actually doing, even if this is the *only* component that will ever call it. Reuse is not a prerequisite here. Readability is a legitimate reason to extract on its own. A 120-line component with three intertwined effects becomes legible the moment two of them move behind `useAutosave()` and `usePresence()`.

**Encapsulation.** A behavior wraps an external system — a browser API, a third-party SDK, anything that needs effect plus state plus ref wiring and a cleanup function — and that machinery should be hidden behind a clean interface. `useMediaQuery` hides a `matchMedia` subscription; `useIntersection` hides an `IntersectionObserver`; anything wrapping `addEventListener` fits here. The component shouldn't have to know the external API exists; it should just ask a question and get an answer.

Now the inverse, which matters just as much — because over-extraction is its own bug, and it's the one juniors commit while thinking they're being tidy.

Do **not** extract when there's nothing to reveal. A single `useState(false)` does not become better by being renamed `useToggle` and called once; you've added a file, an import, and a layer of indirection to hide a single line that was already clear. "A hook for everything" is the same mistake as premature optimization — effort spent buying a benefit that isn't there. And do not extract a function that calls no hooks — that's not a hook at all, it's a utility, and it belongs in `lib/` without the `use` prefix, exactly as the first section warned.

There's one more case that lives on this boundary and gets its own section next: a hook that takes a *callback* as an argument and calls it from inside an effect. That pattern is worth extracting, but it has a trap, and handling the trap is what makes the difference between a hook that works and one that thrashes.

The walkthrough below is the question order an experienced engineer runs through, fastest cut first. Click through it on a behavior you're unsure about.

<StateMachineWalker kind="decision" root="calls-hooks" title="Should I extract this into a custom hook?">
  <Question
    id="calls-hooks"
    prompt="Does the block call at least one hook?"
    description="useState, useEffect, useRef, or any other hook — built-in or custom."
  >
    <Branch label="No — it's pure data-shaping" to="leaf-utility" />
    <Branch label="Yes, it calls hooks" to="external" />
  </Question>

  <Question
    id="external"
    prompt="Is it wiring a browser API or SDK with effect + ref + cleanup?"
    description="matchMedia, IntersectionObserver, a map widget — anything with a subscribe/unsubscribe lifecycle worth hiding."
  >
    <Branch label="Yes" to="leaf-encapsulate" rationale="Hide the external API up front; reuse doesn't even matter yet." />
    <Branch label="No" to="reuse" />
  </Question>

  <Question id="reuse" prompt="Is the behavior used in two or more components?">
    <Branch label="Yes, and it's more than a line or two of wiring" to="leaf-reuse" rationale="Copying it again is the smell." />
    <Branch label="No — only one component uses it" to="clarity" />
  </Question>

  <Question
    id="clarity"
    prompt="Does the hook block tangle the component's intent?"
    description="A knot of coordinated effects/state/refs that obscures what the component is for."
  >
    <Branch label="Yes — a name would reveal what it does" to="leaf-clarity" />
    <Branch label="No — it's a one-liner like a single useState" to="leaf-inline" />
  </Question>

  <Leaf id="leaf-utility" verdict="Ship it as a /lib utility">
    No hooks means it isn't a hook. The `use` prefix would lie to the linter and the reader. Give it a verb-led name and export it from `lib/`.
  </Leaf>

  <Leaf id="leaf-encapsulate" verdict="Extract to encapsulate">
    The encapsulation condition. Hide the external API's subscribe/cleanup machinery behind a clean question-and-answer interface so the component never sees it.
  </Leaf>

  <Leaf id="leaf-reuse" verdict="Extract a custom hook">
    The reuse condition. A behavior wired in two-plus places, non-trivial enough to copy, has earned a single home. Fix bugs once, not three times.
  </Leaf>

  <Leaf id="leaf-clarity" verdict="Extract for clarity">
    A single caller is fine. If naming the block makes the component readable, that alone justifies it. Readability is a first-class reason.
  </Leaf>

  <Leaf id="leaf-inline" verdict="Leave it inline">
    The over-extraction guard. A one-line `useState` wrapped in a hook adds indirection without revelation. Keep simple state where it's used.
  </Leaf>
</StateMachineWalker>

Notice what the funnel does that a bulleted list can't: it makes "leave it inline" and "it's just a utility" feel like real answers, arrived at by asking the right questions in the right order — not like failures to extract. That order is the skill. Hooks-or-not first because it's the cheapest cut. Then reuse, then clarity, and the inline floor underneath both. The encapsulation question is one you can ask up front the moment you see a browser API: wiring `addEventListener` or a third-party widget is almost always worth hiding, even before you know whether you'll reuse it.

## Callbacks that should not re-fire the effect

Here's the one composition pattern that's genuinely subtle, and the reason a hook called `useOnClickOutside` is worth studying closely.

The behavior is the dropdown-and-modal staple: close when the user clicks anywhere outside this element. So you write `useOnClickOutside(ref, handler)` — it attaches a <Term definition={"A DOM event that fires on mouse, touch, or pen press. The unified successor to mousedown and touchstart."}>`pointerdown`</Term> listener in an effect and, when a click lands outside the element `ref` points at, it calls `handler`. The call site looks like this, and it's exactly as ergonomic as it should be:

```tsx
useOnClickOutside(menuRef, () => setOpen(false));
```

Look at that handler: a fresh inline arrow, written right at the call site, recreated on every single render. That's the right way for a caller to use this hook — nobody should have to wrap it in anything or think about its identity. Pushing that ceremony onto every caller would make the hook miserable to use.

But it sets a trap inside the hook. The naive implementation puts `handler` in the effect's dependency array, because the effect closes over it and the exhaustive-deps rule asks for everything the effect reads. And since the caller passes a brand-new arrow every render, `handler`'s identity changes every render, so the effect tears down and re-attaches its listener every render. That's wasteful, and worse, there's a window between removing the old listener and adding the new one where a click can fall through and be missed entirely.

The fix is the bright line you learned a couple of chapters back, applied *inside the hook*: wrap the caller's `handler` with <Term definition={"A React hook that wraps a callback so it always reads the latest props and state, but is excluded from effect dependency arrays. Stable since React 19.2 — the linter understands it and won't ask for it in deps."}>`useEffectEvent`</Term>. The wrapped version always calls the latest `handler` the caller passed, but it has a *stable identity*, so the effect can depend only on `ref` and never on the handler. The listener attaches once and stays put. The caller still passes any inline arrow they like, and now it costs nothing.

Step through the implementation. The relationship between the wrap line and the dependency array is the whole point — watch how they connect.

<AnnotatedCode lang="tsx" maxLines={16} code={`
const useOnClickOutside = (
  ref: RefObject<HTMLElement | null>,
  handler: (event: PointerEvent) => void,
) => {
  const onClickOutside = useEffectEvent(handler);

  useEffect(() => {
    const listener = (event: PointerEvent) => {
      const el = ref.current;
      if (!el || el.contains(event.target as Node)) return;
      onClickOutside(event);
    };
    document.addEventListener('pointerdown', listener);
    return () => document.removeEventListener('pointerdown', listener);
  }, [ref]);
};
`}>
  <AnnotatedStep meta="{1-4}" color="blue">
    The signature. The hook takes a `ref` and a plain `handler`. Consumers pass an inline arrow; they never wrap it themselves.
  </AnnotatedStep>

  <AnnotatedStep meta={`"useEffectEvent"`} color="green">
    Wrap the consumer's handler. `onClickOutside` always calls the latest handler, but its identity is stable across renders.
  </AnnotatedStep>

  <AnnotatedStep meta="{7-15}" color="blue">
    The effect attaches one `pointerdown` listener. The listener checks whether the click landed outside the element before firing.
  </AnnotatedStep>

  <AnnotatedStep meta={`"[ref]"`} color="blue">
    Only `ref` is a dependency. Because `onClickOutside` is stable, it's correctly left out — the listener attaches once and never thrashes on re-renders.
  </AnnotatedStep>

  <AnnotatedStep meta="{14}" color="orange">
    Cleanup removes the listener when the component unmounts or the ref changes. Standard effect hygiene.
  </AnnotatedStep>
</AnnotatedCode>

This is the pattern that makes any callback-accepting hook feel right. The shape generalizes: whenever a hook takes a function from its caller and calls that function from inside an effect, wrap it with `useEffectEvent` so the effect doesn't re-run on the caller's render churn. The caller stays free to pass inline functions; the hook stays stable underneath.

:::note
`useEffectEvent` is stable as of React 19.2, and the `react-hooks` linter understands it — it will not flag the wrapped event as a missing dependency, which is exactly why `[ref]` above passes the exhaustive-deps rule cleanly.
:::

## Composing hooks from other hooks

A custom hook can call other custom hooks. This isn't an advanced technique you graduate to — it's the daily mode, and it falls straight out of the definition. A custom hook is a function that may call hooks; "hooks" includes the ones you wrote. The whole point of the `use` contract is that composition comes for free.

Two quick examples show the shape. Read them as illustrations, not implementations to memorize.

```tsx
const useFilteredList = (items: Item[], query: string) => {
  const deferredQuery = useDeferredValue(query);
  return items.filter((item) => item.name.includes(deferredQuery));
};

const usePaginatedData = (query: string) => {
  const page = Number(useSearchParams().get('page') ?? '1');
  return useFetch(`/api/search?q=${query}&page=${page}`);
};
```

`useFilteredList` composes a built-in concurrent hook into a domain behavior: it takes `useDeferredValue` — the hook that lets an expensive update lag behind without blocking typing — and wraps it into "give me the filtered list, kept responsive." The component calling `useFilteredList(items, query)` never has to know `useDeferredValue` is involved. `usePaginatedData` composes a data-fetching hook with `useSearchParams`, the Next.js hook that reads the current page out of the URL's query string — you'll meet that one properly when the course reaches the App Router; here it's just another hook being composed in.

The rule for composition is short: compose freely, and **flatten only when nesting hides intent.** Stacking hooks inside hooks is fine right up until you can no longer tell where a given piece of state actually lives — when you're three hooks deep chasing which layer owns the value you're debugging. That's the signal to inline one layer back. Until then, let composition do its job.

## The 2026 custom-hook catalog

There's a set of custom hooks that a 2026 SaaS codebase reaches for over and over. The value in knowing them is not memorizing implementations — it's *recognizing the shape* the moment a behavior calls for it, so you reach for the named hook (or a vetted one from a library) instead of pasting a raw `useEffect` block for the fifth time. We'll build one in full to see how everything in this lesson comes together, then card the rest as a recognition reference.

### Flagship: `useLocalStorage`

`useLocalStorage(key, initial)` keeps a piece of state synchronized with the browser's `localStorage`, so a value survives reloads and stays in sync across tabs. It's the flagship for this lesson because it exercises everything you've just learned — it's generic, it returns a tuple, and it has one detail that experienced engineers get right and the obvious implementation gets wrong.

That detail is server rendering. The tempting version is `useState(() => localStorage.getItem(key))`. It has two problems. On the server, during SSR, there is no `localStorage` — `window` doesn't exist — so that line throws and the render dies. And even if you guard it, the server and the first client render would disagree about the value, and React would flag a <Term definition={"The client re-attaching React to the server-rendered HTML on first load. If the first client render produces different output than the server sent, React warns about a mismatch."}>hydration</Term> mismatch. The fix is to build the hook on <Term definition={"React's primitive for subscribing a component to a value that lives outside React, safely under concurrent rendering and server-side rendering."}>`useSyncExternalStore`</Term> — the primitive for subscribing React to a value that lives outside it — which takes a dedicated server-snapshot function precisely so server and client can agree.

Step through the implementation. `useSyncExternalStore` takes three functions, and each one earns its place.

<AnnotatedCode lang="tsx" maxLines={18} code={`
const subscribe = (onChange: () => void) => {
  window.addEventListener('storage', onChange);
  return () => window.removeEventListener('storage', onChange);
};

const cache = new Map<string, { raw: string | null; value: unknown }>();

const useLocalStorage = <T,>(key: string, initial: T): [T, (value: T) => void] => {
  const getSnapshot = (): T => {
    let raw: string | null;
    try {
      raw = window.localStorage.getItem(key);
    } catch {
      return initial;
    }
    const cached = cache.get(key);
    if (cached && cached.raw === raw) return cached.value as T;
    const value = raw === null ? initial : (JSON.parse(raw) as T);
    cache.set(key, { raw, value });
    return value;
  };

  const getServerSnapshot = (): T => initial;

  const value = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);

  const setValue = (next: T) => {
    window.localStorage.setItem(key, JSON.stringify(next));
    window.dispatchEvent(new StorageEvent('storage', { key }));
  };

  return [value, setValue];
};
`}>
  <AnnotatedStep meta="{1-4}" color="blue">
    `subscribe`: the first `useSyncExternalStore` argument. It registers a listener and returns the unsubscribe function. It depends on nothing inside the hook, so it lives at module scope — a single stable reference, never recreated per render, so React never needlessly re-subscribes. The `storage` event fires when `localStorage` changes, including writes from other tabs, which is how cross-tab sync comes for free.
  </AnnotatedStep>

  <AnnotatedStep meta="{6}" color="violet">
    The snapshot cache. `useSyncExternalStore` compares snapshots with `Object.is`, so `getSnapshot` must return the *same reference* when the underlying data hasn't changed — otherwise React loops forever. This module-level map remembers, per key, the last raw string and the value it parsed to.
  </AnnotatedStep>

  <AnnotatedStep meta="{8}" color="green">
    The generic signature: takes a `key` and a typed `initial` value, returns the familiar `[value, setValue]` tuple. `T` flows from the call site, exactly like the generic section showed.
  </AnnotatedStep>

  <AnnotatedStep meta="{9-21}" color="blue">
    `getSnapshot`: reads the raw string and returns a *cached* parsed value. If the raw string matches what the cache last saw, it hands back the very same reference — so an object or array value stays referentially stable across renders. Only a changed raw string triggers a fresh `JSON.parse`. The read is wrapped in try/catch so a missing or corrupt value falls back to `initial` rather than throwing.
  </AnnotatedStep>

  <AnnotatedStep meta="{23}" color="violet">
    `getServerSnapshot`: the detail that matters. During SSR and the first client render there is no `localStorage`, so this returns `initial`, guaranteeing server and client agree and hydration doesn't mismatch.
  </AnnotatedStep>

  <AnnotatedStep meta="{27-30}" color="orange">
    `setValue`: writes the new value as JSON and dispatches a `storage` event so same-tab subscribers re-read immediately (the native event only fires in *other* tabs).
  </AnnotatedStep>
</AnnotatedCode>

Read back what each piece bought. The generic means a draft string and a saved count share one definition. The tuple means the call site destructures it like `useState`. The three store functions mean the hook is safe on the server, reactive to other tabs, and never throws on bad data — and the cached snapshot keeps an object or array value referentially stable, so `useSyncExternalStore` doesn't spin. This one hook is a tour of the whole lesson — and it's the one a project later in the course will hand you to build with, so you'll see it again.

<VideoCallout videoId="WBPUz1u2rZ8" videoTitle="The Most Underrated React Hook You've Never Used">
  Cosden Solutions on `useSyncExternalStore` — the subscribe/getSnapshot/getServerSnapshot contract that makes the hook above SSR-safe, in 20 minutes.
</VideoCallout>

### The rest of the catalog

The following hooks round out the set worth recognizing. Each card names the hook, its return shape, and the failure it prevents — not its implementation. When you feel one of these behaviors coming on, reach for the named hook.

<CardGrid>
  <Card title="useMediaQuery(query)" icon="laptop">
    Returns a `boolean`. A `matchMedia` subscription with cleanup. Reach for it instead of polling `window.innerWidth` on every resize event.
  </Card>
  <Card title="useIntersection(ref, options)" icon="seti:image">
    Returns visibility / the observer entry. Wraps `IntersectionObserver`. Replaces hand-rolled scroll-handler position math for lazy-loading and scroll-spy.
  </Card>
  <Card title="useDebounced(value, delay)" icon="seti:clock">
    Returns the deferred value. Value-shaped, not callback-shaped, so the result flows through dependency arrays naturally — unlike the old debounced-callback pattern. (`useThrottled` is its rate-limited sibling.)
  </Card>
  <Card title="useLockBodyScroll()" icon="seti:lock">
    Returns nothing. Toggles `overflow: hidden` on `<body>` with cleanup. Stops the page behind an open modal from scrolling. (A project later in the course consumes this one.)
  </Card>
  <Card title="usePrevious(value)" icon="left-arrow">
    Returns the value from the previous render. The one legitimate effect-driven previous-value pattern. Saves you from reaching for extra state just to remember what a value was last render.
  </Card>
  <Card title="useCopyToClipboard()" icon="document">
    Returns `[copy, copied]`. Wraps the Clipboard API with a transient `copied` flag that resets itself. Stops you hand-rolling the reset timeout every time you build a copy button.
  </Card>
  <Card title="useOnClickOutside(ref, handler)" icon="random">
    Returns nothing. The `useEffectEvent`-inside hook from earlier. Kills the dropdown/modal "click outside to close" boilerplate.
  </Card>
</CardGrid>

The thread under all seven: the win is recognition, not recall. You don't need these memorized line for line. You need to see "I'm about to wire `matchMedia` by hand" and stop, because there's a named hook — yours or a library's — that already encapsulates it correctly, cleanup and all.

## Hooks that take a dependency array, and the lint

Most custom hooks should *not* take a dependency array. The experienced-engineer default is a focused API: `useDebounced(value, delay)` takes a value, not a list of dependencies, and that's deliberate — the caller passes the thing, the hook owns the wiring. Value-shaped hooks like that are easier to use correctly and have one fewer footgun.

But occasionally a hook does accept a dependency array — something shaped like `useInterval(callback, deps)`, where the caller controls when the interval resets. That hook has a problem the built-ins don't: the `exhaustive-deps` lint rule won't check its dependency array, because the rule only knows about React's own hooks. Your custom dep-array hook can silently drift out of sync and the linter says nothing.

The fix is one line of config — you tell the rule the names of your dep-array hooks so it polices them too.

<div data-mark-color="blue">

```ts {2}
'react-hooks/exhaustive-deps': ['warn', {
  additionalHooks: '(useInterval|useIsomorphicLayoutEffect)',
}],
```

</div>

The `additionalHooks` regex lists the custom hooks whose final argument is a dependency array, and from then on the lint enforces exhaustive deps on them exactly as it does on `useEffect`. You met the `react-hooks` rules a couple of chapters back; this is the seam where you extend them to your own hooks.

But the better instinct is to avoid needing this at all. Whenever you can express a hook as value-shaped (`useDebounced(value, delay)`) rather than dep-array-shaped, do — there's no `additionalHooks` upkeep and no dependency array for a caller to get wrong. The React team's own guidance points the same way: most custom hooks should expose a higher-level API, not a raw dependency array. Keep the dep-array shape for the rare hook that genuinely needs the caller to control re-runs.

## Custom hooks under the React Compiler

:::note[A bridge to the next lesson]
You may be wondering whether the React Compiler — the build-time optimization this chapter turns to next — changes how you write custom hooks. It doesn't. The compiler analyzes a custom hook's body like any other function, and a *pure* one gets auto-memoization for free, with no work from you. The rules of hooks still hold, dependency arrays still hold, the shapes in this lesson still hold. Nothing here changes; the compiler simply does more underneath it. The next lesson is where you'll see what it does and how to turn it on.
:::

## Practice: extract a hook from a tangled component

Time to do the core motion yourself. The component below shows an online/offline status badge with its logic wired inline — `navigator.onLine` read into state, an effect subscribing to the `online` and `offline` window events, all tangled in among the JSX. This is the encapsulation case: a browser API with subscribe-and-cleanup wiring that belongs behind a clean interface.

Extract that logic into a `useOnlineStatus()` hook defined in the same file, returning a boolean, and have `App` consume it. The rendered output should stay identical — the refactor changes structure, not behavior.

<ReactCoding
  maxHeight={360}
  instructions="This badge wires navigator.onLine plus the online/offline window events straight into App. Extract that logic into a custom hook named useOnlineStatus, defined above App in this same file, that returns a boolean — then have App call it and render the same badge. The rendered output must not change."
  starter={`import { useState, useEffect } from 'react';

export function App() {
  const [isOnline, setIsOnline] = useState(
    typeof navigator === 'undefined' ? true : navigator.onLine,
  );

  useEffect(() => {
    const goOnline = () => setIsOnline(true);
    const goOffline = () => setIsOnline(false);
    window.addEventListener('online', goOnline);
    window.addEventListener('offline', goOffline);
    return () => {
      window.removeEventListener('online', goOnline);
      window.removeEventListener('offline', goOffline);
    };
  }, []);

  return (
    <div className="p-4">
      <span className={isOnline ? 'text-green-600' : 'text-red-600'}>
        {isOnline ? 'Online' : 'Offline'}
      </span>
    </div>
  );
}`}
  tests={`
test('renders the current online status text', () => {
  const expected = navigator.onLine ? 'Online' : 'Offline';
  expect(document.body.textContent).toContain(expected);
});

test('the status text carries the matching color class', () => {
  const span = document.querySelector('span');
  const cls = span?.className ?? '';
  expect(cls).toContain(navigator.onLine ? 'text-green-600' : 'text-red-600');
});
`}
/>

If you get stuck, the reference solution is below. Try the extraction first — the whole skill is in the doing.

<details>
<summary>Reference solution</summary>

```tsx
import { useState, useEffect } from 'react';

const useOnlineStatus = (): boolean => {
  const [isOnline, setIsOnline] = useState(
    typeof navigator === 'undefined' ? true : navigator.onLine,
  );

  useEffect(() => {
    const goOnline = () => setIsOnline(true);
    const goOffline = () => setIsOnline(false);
    window.addEventListener('online', goOnline);
    window.addEventListener('offline', goOffline);
    return () => {
      window.removeEventListener('online', goOnline);
      window.removeEventListener('offline', goOffline);
    };
  }, []);

  return isOnline;
};

export function App() {
  const isOnline = useOnlineStatus();
  return (
    <div className="p-4">
      <span className={isOnline ? 'text-green-600' : 'text-red-600'}>
        {isOnline ? 'Online' : 'Offline'}
      </span>
    </div>
  );
}
```

The component now reads as a single question — *am I online?* — and the wiring lives behind the name. That's the extraction, end to end.

</details>

## Quick check: hook, utility, or inline?

One last sort to harden the central decision. Drag each snippet into where it belongs — extract a custom hook, ship it from `lib/` as a plain utility, or leave it inline in the component. The line between the three is the judgment this whole lesson was about.

<Buckets twoCol instructions="Sort each snippet by what you'd do with it: extract a hook, ship a /lib utility, or leave it inline.">
  <Bucket name="hook" label="Extract a custom hook" description="Calls hooks, and reuse or clarity or encapsulation applies" />
  <Bucket name="utility" label="Ship from /lib" description="No hooks inside — a plain function" />
  <Bucket name="inline" label="Leave it inline" description="One trivial line, one component" />

  <Item bucket="hook">Six lines of `IntersectionObserver` wiring used in three components</Item>
  <Item bucket="inline">A one-line `useState(false)` toggle used in one component</Item>
  <Item bucket="utility">A pure `formatCurrency(cents)` with no hooks</Item>
  <Item bucket="hook">A `matchMedia` subscription with cleanup, used by the responsive nav</Item>
  <Item bucket="utility">A `slugify(title)` string transform</Item>
  <Item bucket="inline">A single `useRef` for a DOM node read in one event handler</Item>
</Buckets>

## What you can do now

You can look at a block of hook wiring and decide whether it's worth a name — reuse, clarity, or encapsulation tips it toward extraction; a single trivial line keeps it inline; no hooks at all sends it to `lib/`. You can name a hook so the `use` prefix tells the truth, shape its return as a value, tuple, or object and hold that shape, and make it generic when a type should flow from the call site. You can wrap a caller's callback with `useEffectEvent` so a callback-accepting hook doesn't thrash, compose hooks out of other hooks, and recognize the 2026 catalog on sight. The next lesson turns to the React Compiler — the piece that quietly auto-memoizes all of this and reshapes when you'd ever reach for manual memoization at all.

## External resources

<CardGrid>
  <ExternalResource
    title="React docs — Reusing Logic with Custom Hooks"
    href="https://react.dev/learn/reusing-logic-with-custom-hooks"
    icon="simple-icons:react"
    iconColor="#61DAFB"
    description="The official guide to extracting and composing custom hooks, including the share-code-not-state model."
  />
  <ExternalResource
    title="React docs — Separating Events from Effects"
    href="https://react.dev/learn/separating-events-from-effects"
    icon="simple-icons:react"
    iconColor="#61DAFB"
    description="The reasoning behind the useEffectEvent wrap that keeps callback-accepting hooks from thrashing."
  />
  <ExternalResource
    title="React docs — useSyncExternalStore"
    href="https://react.dev/reference/react/useSyncExternalStore"
    icon="simple-icons:react"
    iconColor="#61DAFB"
    description="Reference for the primitive behind the SSR-safe useLocalStorage, including the server-snapshot contract."
  />
  <ExternalResource
    title="usehooks-ts"
    href="https://usehooks-ts.com"
    icon="simple-icons:typescript"
    iconColor="#3178C6"
    description="A vetted, typed catalog of the hooks in this lesson — reach for these before re-deriving the wiring."
  />
</CardGrid>
