---
title: The Next.js root layout owns the document shell
tagline: How the Next.js App Router builds the HTML document around your React app, and the rules for what belongs in the root layout that wraps every page.
chapter-id: 17
course-progress: 0.00005
sidebar:
  order: 2
  label: The root layout
---

import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import Figure from '../../../components/figures/Figure.astro';
import ArrowDiagram from '../../../components/figures/ArrowDiagram.astro';
import Term from '../../../components/ui/Term.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import TrueFalse from '../../../components/exercises/true-false/TrueFalse.astro';
import Statement from '../../../components/exercises/true-false/Statement.astro';
import TfWhy from '../../../components/exercises/true-false/TfWhy.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { FileTree, CardGrid } from '@astrojs/starlight/components';
import DocumentSkeleton from '../../../components/lessons/017/2/DocumentSkeleton.astro';
import MetadataToHead from '../../../components/lessons/017/2/MetadataToHead.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

Every component you've written so far returns a fragment of UI — a heading, a list, a button. Your homepage might be nothing more than this:

```tsx title="app/page.tsx"
export default function Home() {
  return <h1>Welcome</h1>;
}
```

Now run `pnpm dev`, open the page, and choose "View Source" from the browser menu. You don't see `<h1>Welcome</h1>` sitting alone. You see a complete HTML document:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Acme</title>
  </head>
  <body>
    <h1>Welcome</h1>
    <script src="/_next/static/chunks/main.js"></script>
  </body>
</html>
```

Your `<h1>` is in there — buried near the bottom, inside a `<body>`, inside an `<html lang="en">`, under a `<head>` full of tags you never typed, next to a `<script>` you didn't add. So here's the question this lesson answers: **who writes the rest of the page?**

In the <Term definition="A React framework that handles routing, rendering, and the server for you.">Next.js</Term> <Term definition="Next.js's file-based routing system — folders under app/ define your URLs, and special files like layout.tsx and page.tsx define what renders.">App Router</Term>, the document around your components has exactly three authors, and learning which one owns which piece is the whole lesson:

- One file — `app/layout.tsx` — writes the `<html>` and `<body>` tags. The shell.
- The **metadata API** writes everything inside `<head>` — the `<title>`, the `<meta>` tags, the favicon link.
- A small `<Providers>` component writes the client-side machinery that lives inside `<body>`.

That split is not bureaucracy. The document shell is a set of decisions read by machines you never see — a search crawler reads your `<title>`, a screen reader reads `lang="en"`, the browser reads `<meta charset>` before it can decode a single byte of your page. Get the shell right and those machines do their jobs. Get it wrong, or put the wrong thing in it, and you break SEO, accessibility, or rendering for *every page at once*, because this is the one file the whole app shares.

By the end you'll be able to write a correct `app/layout.tsx` from memory and — just as important — name what must never go in it and exactly what breaks when it does. You'll also finally meet `children` as a real, load-bearing prop: in the last lesson it was a name on a list; here it's the slot your entire app renders into.

## Every page is the same HTML document

Before we can talk about "the layout owns this part" and "the metadata API owns that part," we need a shared picture of the parts. So, a fast recap — you met the <Term definition="Document Object Model — the tree of objects the browser builds from your HTML.">DOM</Term> as a tree of nodes back in the DOM chapter; this is the document frame that sits on top of that tree.

Every web page ever served, in any framework, has the same outer skeleton. A `<!DOCTYPE html>` declaration, then a single `<html>` element wrapping exactly two children: a `<head>` and a `<body>`.

<Figure>
  <DocumentSkeleton />
  <Fragment slot="caption">
    Every page shares this skeleton. `<head>` holds metadata machines read; `<body>` holds what people see. The rest of the lesson is just: which file writes which box.
  </Fragment>
</Figure>

Four pieces carry all the weight here. Learn what each is for once and the names stop being noise:

- **`<!DOCTYPE html>`** is the standards-mode switch. It isn't an element and it has no closing tag — it's a one-line instruction that tells the browser "render this page by modern web standards." Without it, browsers fall back to <Term definition="A legacy browser rendering mode that emulates 1990s browser bugs for compatibility with very old pages. You never want it.">quirks mode</Term> and emulate decades-old bugs. Next.js emits this line for you; you will never write it.
- **`<html lang="en">`** is the root element — the single box everything else lives inside. Its `lang` attribute declares the document's language, and three different consumers read it: a screen reader uses it to pick the right pronunciation, the browser uses it for hyphenation, and translation tools use it to know what they're translating *from*. You always set it.
- **`<head>`** holds metadata — information *about* the page that isn't drawn on the page itself: the `<title>`, the character encoding, the viewport settings, the favicon link, social-share tags.
- **`<body>`** holds everything visible. This is where your React tree mounts — your `<h1>` from a moment ago landed here.

Here's the part to carry through the whole lesson. The contents of `<head>` look invisible because nothing in there renders on the page — but they are the most-read part of your document, just not by humans. Your `<title>` is the text on the browser tab *and* the blue heading of your search result on Google. Your `<meta charset>` tells the browser which character encoding to use to turn raw bytes into text — get it wrong or omit it and your page renders <Term definition="Garbled text produced when bytes are decoded with the wrong character encoding — café becomes cafÃ©.">mojibake</Term>. Your `<meta name="description">` is the grey snippet under that search result. None of this is decoration. It's a set of contracts with the browser and with every crawler that visits, and the root layout is where you sign them.

## app/layout.tsx is the shell

This is the structural heart of the lesson, so let's slow down here.

In the App Router, a file named `app/layout.tsx` is the <Term definition="The App Router's outermost layout, at app/layout.tsx — rendered once around every route. It owns the html and body tags.">root layout</Term>. It's the outermost component in your app, and Next.js renders it once, wrapping it around every single page. It is also *required*: Next.js refuses to build an App Router project without one. There is no app without this file.

Here's the entire thing — the minimal correct root layout, top to bottom.

<AnnotatedCode lang="tsx" code={`
import './globals.css';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
`}>
  <AnnotatedStep meta="{1}" color="violet">
    The very first line imports `globals.css`. Notice there's no name on the left — you're not importing a value, you're importing the file *for its side effect*: pulling the app's single global stylesheet (the Tailwind entry point) into the build. It lives here, in the root layout, so those styles apply to every page. We'll come back to it.
  </AnnotatedStep>

  <AnnotatedStep meta="{3-7}" color="blue">
    The component is a `default export` named `RootLayout`. You learned the rule that this course uses named exports almost everywhere — this is one of the handful of exceptions, because the App Router *finds* this component by its default export. The framework dictates it; the rule still holds. Its only prop is `children`, typed `React.ReactNode`.
  </AnnotatedStep>

  <AnnotatedStep meta={`{4} "React.ReactNode"`} color="blue">
    `children` is the prop the last lesson named and left as a forward reference — here it pays off. Next.js injects your page into this slot through the framework convention; `RootLayout` never imports it. The type `React.ReactNode` covers anything React can render — JSX, a string, a number, an array of those, even `null`. The full story of `children` as a component API comes in a later chapter; for now, just read it as "the page goes here."
  </AnnotatedStep>

  <AnnotatedStep meta="{10}" color="green">
    `<html lang="en">` — the layout literally renders the root element and sets the document language. No other file in your app renders this tag.
  </AnnotatedStep>

  <AnnotatedStep meta="{11}" color="green">
    `<body>{children}</body>` — and inside the body, the `children` slot. This single line is where your entire visible app mounts. Read top to bottom, this tiny file *is* the document shell: it opens `<html>`, opens `<body>`, drops the page in, closes both.
  </AnnotatedStep>
</AnnotatedCode>

Three facts about this file are worth more than the rest. They're the difference between a layout an experienced engineer trusts and one that quietly breaks.

**It's a Server Component, and that's the most important fact about the file.** Notice what's *absent*: no `'use client'` line at the top, no hooks, no `window` or `localStorage`. By default, every component in the App Router is a <Term definition="A component that runs only on the server and ships no JavaScript to the browser — the Next.js default. Covered in full in a later chapter.">Server Component</Term> — it runs on the server, renders to HTML, and ships *zero* JavaScript to the browser for itself. You opt a component *into* the browser with a directive you'll meet shortly; the root layout never does. The full Server/Client model gets a whole chapter later on — for now, hold one sentence: the layout runs on the server and sends down HTML.

**It owns `<html>` and `<body>`, and nothing else may render them.** This is a hard rule. As your app grows you'll add *nested* layouts — layouts for a specific section, like everything under `/dashboard` — and those are covered in a later chapter. The thing to know now is that a nested layout returns JSX that slots *inside* `<body>` through its own `children`; it never renders `<html>` or `<body>` again. The reason is blunt: two `<html>` tags in one document is invalid HTML. The root layout is the one and only place these two tags exist.

**`lang` belongs here, and forgetting it is a real bug.** For a single-language SaaS, hardcode `lang="en"` exactly as shown. (When you later build a multi-language app, that value gets rendered dynamically from the URL or the user's locale — a later chapter handles it; ignore it for now.) Leaving `lang` off isn't a style nit: a screen reader then has to *guess* the language, and it often guesses wrong, reading English content with, say, Spanish pronunciation rules. That's why "every root layout sets `lang`" is a reflex worth building now — it costs one attribute and it's an accessibility regression to skip it.

A quick map of where this file sits and who its neighbors are:

<FileTree>
- app/
  - **layout.tsx** the root layout — owns the html and body tags
  - page.tsx the homepage, rendered into children
  - globals.css the single global stylesheet
  - `_components/`
    - providers.tsx the client wrapper (next section)
</FileTree>

## The metadata API writes the head

So the layout writes `<html>` and `<body>`. But look back at the document from the start of the lesson — there was a whole `<head>` full of tags, and the layout we just wrote doesn't contain a single one of them. Where does `<head>` come from?

Here's the instinct to unlearn before it forms: you might expect to write `<head><title>Acme</title></head>` inside the layout's JSX, the way you would in a plain HTML file. **You don't.** Next.js gives you a different, better tool for `<head>` — the <Term definition="Next.js's declarative way to author the head — you export a metadata object (or a generateMetadata function) and Next.js renders the corresponding head tags for you.">metadata API</Term>. Instead of writing tags, you *export a description* of them, and Next.js renders the actual `<head>` tags for you.

It looks like this — a `metadata` object, typed with `Metadata` from `next`, exported right alongside `RootLayout` in the same file:

```tsx title="app/layout.tsx"
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Acme',
  description: 'Invoicing for small teams.',
  icons: { icon: '/favicon.ico' },
};
```

That object isn't markup, but Next.js turns it into markup. Each field becomes the matching `<head>` tag:

<Figure expandable={false}>
  <ArrowDiagram arrows={[]}>
    <MetadataToHead />
  </ArrowDiagram>
  <Fragment slot="caption">
    You describe the head as a plain object; Next.js renders the tags. Same color, same tag.
  </Fragment>
</Figure>

Those are the three fields almost every SaaS sets at the root: a `title`, a `description`, and `icons` for the favicon. (`title` can also be a template object so every page gets a "— Acme" suffix automatically, but that's a refinement for a later chapter; one string is the right start.)

There are two ways to provide metadata, and the difference is *when the values are known*:

- **`metadata`** — a plain constant object, as above — when the values are fixed at build time. Your app's name, your default description. This is what the root layout uses.
- **`generateMetadata`** — an `async` function that returns a `Metadata` object — when the values depend on the route's data. An invoice page can't hardcode its title; it has to fetch invoice #4019 and return `{ title: 'Invoice #4019' }`. You'll reach for `generateMetadata` constantly once you're building real pages; the full surface (social-share cards, dynamic preview images) is a later chapter. For now, just know the name and that it's the dynamic sibling of `metadata`.

You might have noticed two tags in that first document — `<meta charset>` and `<meta viewport>` — that aren't in our `metadata` object at all. **Next.js emits both automatically.** UTF-8 charset and the standard responsive viewport are there on every page without you lifting a finger, and you almost never override them. (There's a separate `viewport` export for the rare case where you need per-page variation, but reaching for it is unusual; a later chapter covers it.)

Now the part that matters most — *why* a declarative object instead of just letting you write `<head>` tags yourself? Because the metadata API does two things hand-written tags can't. It **deduplicates and orders** the head tags across your whole app, and it lets a page **override** the layout's defaults — a specific page can export its own `title` and it cleanly *wins* over the layout's. Hand-write `<title>` as raw JSX in two places and you get two `<title>` tags fighting, with no rule for which one a crawler honors. The API is the single source of truth precisely so that conflict can't happen. That's the reasoning behind a rule you'll see again at the end of this lesson: never put `<head>` tags in your JSX.

There's exactly one honest exception, named so you recognize it later: a performance hint like `<link rel="preconnect">` that the typed fields don't directly cover goes through the metadata object's `other` field — still the API, still not raw JSX.

Let's lock in the split, because it's the spine of everything here. Sort each of these into who authors it in the document.

<Buckets twoCol instructions="Each item below is part of the rendered HTML document. Sort it into the author responsible for it.">
  <Bucket name="metadata" label="Metadata API" description="writes the head" />
  <Bucket name="layout" label="Root layout JSX" description="writes the html and body tags" />

  <Item bucket="metadata">The page `<title>`</Item>
  <Item bucket="metadata">The `<meta name="description">`</Item>
  <Item bucket="metadata">The favicon `<link rel="icon">`</Item>
  <Item bucket="metadata">The `<meta charset="utf-8">` (framework's job, never hand-written JSX)</Item>
  <Item bucket="layout">The `lang` attribute</Item>
  <Item bucket="layout">The `<body>` element</Item>
  <Item bucket="layout">Wrapping the page in `{children}`</Item>
</Buckets>

## What lives inside the body

The layout we wrote put just one thing inside `<body>`: `{children}`. In a real app, a few other things legitimately live there, wrapped around the page. There are exactly three patterns, and it's worth knowing all three so you recognize them — and knowing the discipline that keeps the list short.

The first you've already met: **the `{children}` slot itself.** Always present, it's where the current page renders. Every root layout has it.

The second is **global providers wrapping `{children}`.** Some things every page needs aren't UI you can see — they're shared context: the current theme (light or dark), a data cache, the active language. In React these are supplied by a <Term definition="A component that supplies app-wide context — theme, data cache, locale — to everything rendered beneath it.">provider</Term> component that wraps the tree, so anything rendered beneath it can read that context. Because *every* page needs them, providers wrap `{children}` at the root. You'll wire real ones in later chapters — a theme provider, a query client, an i18n provider — but the shape is always the same: a wrapper around the children.

The third is **persistent UI** — chrome that should survive navigation instead of unmounting and remounting on every page change. The classic example is a <Term definition="A fixed DOM node, like #toast-root, that React renders overlay UI (toasts, modals) into from anywhere in the tree.">portal target</Term> for toast notifications: an empty `<div id="toast-root" />` that stays mounted so a "Saved!" toast can render into it from anywhere.

Putting those together, a realistic `<body>` looks like this — still tiny:

```tsx title="app/layout.tsx"
<body>
  <Providers>{children}</Providers>
  <div id="toast-root" />
</body>
```

That `<Providers>` wrapper is doing real work, and the next section is entirely about why it has to be its own component. For now, read it as "the box the providers live in."

Now, you might wonder: what about a navigation bar? A site-wide navbar is a *candidate* for the root layout — but be careful, and this is the discipline that matters. The root layout is shared by **every** route: your marketing homepage, your sign-in screen, your logged-in dashboard. A navbar that only makes sense inside the app doesn't belong in a layout the sign-in page also renders. Section-specific UI like that belongs in a *nested* layout (a later chapter), not the root.

That points at a reflex experienced engineers build, and it's the principle behind half the warnings later in this lesson: **keep the root layout lean.** Everything in it runs on every navigation, so everything in it is a cost paid globally, on every page transition. Put the shell, the providers, and genuinely-global chrome here — and push everything route-specific down into nested layouts. When in doubt, the root layout is too important to be the junk drawer.

## Fonts and global styles load at the root

Two integrations have the root layout as their natural home, for the exact same reason `<html lang>` does: they're "load once, apply everywhere." You won't learn either subsystem in depth here — just recognize where they live and why.

The first is **`next/font`**, Next.js's font loader. You want a font to apply to the *whole* document, so you load it in the root layout. The shape: import a font, call it once at the top of the module with some options, then apply the `className` it hands back to `<html>`. Here's the same `app/layout.tsx`, grown to load a font and the global stylesheet:

<AnnotatedCode lang="tsx" code={`
import './globals.css';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
`}>
  <AnnotatedStep meta="{1}" color="violet">
    The `globals.css` import again — the single global stylesheet, the Tailwind entry point. No name on the left because it's imported purely for its side effect: its styles join the build and apply to every page. It always sits first.
  </AnnotatedStep>

  <AnnotatedStep meta="{2-4}" color="blue">
    Import the `Inter` font, then call it *once* at module scope — not inside the component — with options. `subsets: ['latin']` tells Next.js to ship only the Latin glyphs. The call returns an object; we keep it in `inter`. Because it runs at the module top level, Next.js self-hosts and preloads the font at build time — there's no request to Google at runtime and no flash of the wrong font.
  </AnnotatedStep>

  <AnnotatedStep meta={`{12} "inter.className"`} color="blue">
    Apply `inter.className` to `<html>`. That's the same `inter` from step 2 — the font and the class are one connected thing. With the class on the root element, the font cascades down to the entire document.
  </AnnotatedStep>
</AnnotatedCode>

The payoff of `next/font` — worth naming even though the depth is a later chapter — is that Next.js *self-hosts and subsets* the font at build time and preloads it. <Term definition="Shipping only the glyphs a language actually needs — for example, just the Latin characters — to shrink the font file.">Subsetting</Term> shrinks the file, self-hosting kills the runtime round-trip to Google's servers, and preloading prevents the <Term definition="Content jumping as a late-loading font or image arrives. next/font prevents the font-driven case by reserving space and preloading.">layout shift</Term> where text visibly reflows when a webfont finally arrives.

The second integration is **`globals.css`** — that side-effect import you've seen on line one of both versions. It's the single global stylesheet for the whole app, and it's the entry point for Tailwind, the styling system the next chapter is entirely about. You import it in the root layout for the same "apply everywhere" reason, and you import it for its *side effect* — it has no named export to grab, it just needs to be in the build. The next chapter unpacks what's actually inside it.

Both belong at the root because both are document-wide. The font and the global stylesheet apply to every page, just like `lang` does — so they're authored once, in the one file every page shares.

## Client code belongs in a Providers child, not the root

This is the highest-stakes rule in the lesson, so let's build it as cause and effect rather than a commandment to memorize.

You've seen that the root layout is a Server Component — it runs on the server and ships no JavaScript. The escape hatch into the browser is a single line: <Term definition="A directive at the very top of a file marking that module — and everything it imports — as Client Component code that also runs in the browser. The opt-out of the Server Component default.">`'use client'`</Term> at the top of a file. That directive marks the file, *and everything it imports*, as code that also runs in the browser. It's how you opt a component into being interactive.

Now the trap. It's tempting, when you need a provider that uses React state (like a theme provider), to just slap `'use client'` at the top of `app/layout.tsx` and be done. Here's why that one line is a disaster: **the directive is contagious downward.** A `'use client'` layout doesn't just make the layout a Client Component — it turns *every page beneath it*, which is your entire app, into a <Term definition="The portion of the React tree below a 'use client' boundary that ships to the browser and runs as JavaScript.">client subgraph</Term>. One word at the top of one file and you've shipped your whole application to the browser as client JavaScript, forfeiting the Server Components default — server-only data access, zero-JS rendering — for every route at once. It is the most expensive single line you can write in a Next.js app.

The fix is the `<Providers>` pattern, and it rests on one insight that surprises most people: **a Server Component can render a Client Component as a child and hand it `children`.** The boundary isn't all-or-nothing at the top of the tree — you put it exactly where it's needed and no higher.

So the client concerns move into their own file. That file — `app/_components/providers.tsx` — carries `'use client'` at *its* top, takes `children`, and wraps them in whatever providers need browser state. The root layout stays a pure Server Component and simply renders `<Providers>` around the page. The boundary lands on `<Providers>`, deep in the tree; everything above it, including the entire layout, stays on the server.

<CodeVariants>
  <CodeVariant label="'use client' on the layout (wrong)" icon="lucide:x">
    <div data-mark-color="red">

    ```tsx title="app/layout.tsx" del={1}
    'use client';

    import { ThemeProvider } from 'next-themes';

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en">
          <body>
            <ThemeProvider>{children}</ThemeProvider>
          </body>
        </html>
      );
    }
    ```

    </div>
    **One directive, whole app shipped to the browser.** Putting `'use client'` on the layout turns every route beneath it into a client subgraph — the entire app loses the Server Components default. The layout itself doesn't even need browser state; only the provider does.
  </CodeVariant>

  <CodeVariant label="<Providers> child (right)" icon="lucide:check">
    <div data-mark-color="green">

    ```tsx title="app/layout.tsx"
    import { Providers } from './_components/providers';

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en">
          <body>
            <Providers>{children}</Providers>
          </body>
        </html>
      );
    }
    ```

    ```tsx title="app/_components/providers.tsx" {1}
    'use client';

    import { ThemeProvider } from 'next-themes';

    export function Providers({ children }: { children: React.ReactNode }) {
      return <ThemeProvider>{children}</ThemeProvider>;
    }
    ```

    </div>
    **The boundary sits on the leaf that needs it.** `'use client'` lives in `providers.tsx`, so only the provider and its children become client code. The layout stays a Server Component — it just renders `<Providers>` and passes the page through as `children`.
  </CodeVariant>
</CodeVariants>

The repository convention is exactly this: one provider shell, mounted as a `<Providers>` Client Component at the root layout. When you add a theme later, or a data cache, or anything that needs React state, it goes inside that file — never on the layout itself.

<VideoCallout videoId="ebOgXUPG3_k" videoTitle="Next.js 15 Tutorial - 62 - Context Providers">
  Codevolution builds the exact `<Providers>` pattern in ~5 minutes — a theme context fails in the layout, so it moves into a dedicated `'use client'` provider that wraps `children` while the layout stays a Server Component.
</VideoCallout>

## The root layout and hydration mismatches

The second high-stakes footgun is subtler, and it follows directly from "the layout runs on the server." You don't need the full theory yet — just enough to recognize the failure and reach for the fix.

Here's the shallow mechanism. The root layout renders to HTML on the server. Then that HTML arrives in the browser and React performs <Term definition="The browser-side step where React reuses the server-rendered HTML and attaches event handlers by re-rendering the same tree. It expects the markup to match the server's.">hydration</Term>: it walks the very same tree again, in the browser, attaching event handlers as it goes — and it *expects the markup it produces to match the server's exactly*. When the two don't match, you get a <Term definition="When the browser's render differs from the server's. React warns, and may throw away the server HTML and re-render from scratch.">hydration mismatch</Term> — React warns in the console and may discard the server HTML entirely.

<VideoCallout videoId="KAjemAivU24" videoTitle="Next.js Hydration failed because the initial UI does not match what was rendered on the server (FIX)">
  ByteGrad walks through the causes of this exact mismatch error in ~9 minutes — including dates and random values — and the two fixes this section names: scoping the dynamic part to a client component, and `suppressHydrationWarning`.
</VideoCallout>

So picture this innocent-looking line in your root layout:

```tsx
<p>{new Date().toLocaleTimeString()}</p>
```

The server renders it at, say, `14:30:01`. The HTML ships. A few hundred milliseconds later the browser hydrates and runs the *same* line — but now the clock reads `14:30:02`. The server said one thing, the browser computed another, and React can't reconcile them. **Anything that differs between the server render and the client render** — `Date.now()`, `Math.random()`, `crypto.randomUUID()`, anything reading the current moment or per-request state — produces this. And because it's in the *root layout*, which wraps every page, the mismatch is global.

There are two fixes, in order of preference:

The default reach is to **scope the dynamic bit to a Client Component.** Move the time, the random value, the per-request read into a small `'use client'` leaf that computes it in the browser (often after hydration, in an effect) — so the server render and the first browser render agree, and the changing value appears only afterward. The mismatch never happens because the server never tried to render the moving part.

The second, surgical fix is **`suppressHydrationWarning`** on the one specific element whose mismatch is genuinely expected and harmless:

```tsx
<html lang="en" suppressHydrationWarning>
```

This tells React "I know this one element's content will differ between server and client — don't warn." The textbook legitimate use is a theme library setting the `dark` class on `<html>` via a tiny inline script *before* React hydrates, so the page doesn't flash the wrong theme — the server couldn't have known the user's saved theme, so a mismatch on `<html>` is expected and fine. You'll see exactly this in a later chapter. Two cautions, though: it suppresses the warning for that *one element only*, not its whole subtree, and reaching for it to silence a *real* mismatch doesn't fix the bug — it hides it. It's a scalpel, not a mute button.

The full hydration-mismatch story is a later chapter. Here you just need to recognize the failure on sight and know the two ways out.

## What does not belong in the root layout

Everything above converges on a short checklist. The root layout is the one file shared by your whole app, so the cost of a mistake here is global — which is exactly why it's worth knowing what *not* to put in it. Each item below ties back to the section that explained why:

- **No `'use client'`.** It poisons the entire tree into a client subgraph and forfeits Server Components for every route. Push client concerns into `<Providers>`.
- **No raw `<head>` JSX.** It bypasses the metadata API's deduplication, ordering, and override behavior. Use the `metadata` export; `<title>` especially is never inline JSX. (The lone exception: a `<link rel="preconnect">` performance hint goes through the metadata `other` field.)
- **No per-request randomness or current time.** It causes a hydration mismatch, globally. Scope it to a Client Component, or — only when the mismatch is expected and benign — `suppressHydrationWarning` on that one element.
- **No heavy or server-only data fetching.** The layout runs on every navigation, so any fetch here is paid on every page transition. Fetch close to the page that actually needs the data.
- **No per-page UI or per-page metadata.** Both belong to a nested layout or the page's own `metadata` export. The root is shared by every route, including sign-in and marketing pages.
- **Don't forget `lang`.** This is the one thing people *omit* rather than misplace — and missing it is an accessibility regression, because the screen reader is left guessing the language.

A quick self-check on the three watch-outs that bite hardest:

<TrueFalse instructions="Each statement is about the Next.js root layout.">
  <Statement answer="false">
    Adding `'use client'` to the top of `app/layout.tsx` is a clean way to use a theme provider that needs React state.
    <TfWhy>**False.** The directive is contagious downward: a `'use client'` layout turns the entire app beneath it into a client subgraph, forfeiting the Server Components default for *every* route. Put `'use client'` on a `<Providers>` child instead — a Server Component can render a Client Component and pass it `children`.</TfWhy>
  </Statement>

  <Statement answer="true">
    The page `<title>` should be set through the exported `metadata` object, not written as a `<title>` tag in the layout's JSX.
    <TfWhy>**True.** The metadata API deduplicates, orders, and lets a page override the layout's head tags. Hand-written `<title>` JSX bypasses all of that and risks duplicate, conflicting tags with no rule for which one a crawler honors.</TfWhy>
  </Statement>

  <Statement answer="false">
    Rendering `{new Date().toLocaleTimeString()}` directly in the root layout is fine because it's just a string.
    <TfWhy>**False.** The server renders one time, the browser hydrates a moment later and computes a different one — a hydration mismatch, and because it's the root layout, it's global. Scope the clock to a `'use client'` Component that computes it in the browser.</TfWhy>
  </Statement>
</TrueFalse>

The mental model to walk away with is compact: the root layout is a Server Component that renders once around every page, owns `<html lang>` and `<body>` exclusively, delegates `<head>` to the metadata API, and pushes anything client-only into a `<Providers>` child. Three authors, one document. Get that split right and the document machinery — crawlers, screen readers, the browser, React's hydration — all read what they need.

## External resources

The references below are the canonical source for each piece of the document this lesson split apart — the layout file, the metadata API, the `<head>` it produces, and the Server/Client boundary the `<Providers>` rule rests on. You don't need any of them to write a correct root layout, but they're worth a bookmark for the day you reach past what this lesson covered.

<CardGrid>
  <ExternalResource
    title="Next.js — layout.js file convention"
    href="https://nextjs.org/docs/app/api-reference/file-conventions/layout"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="The root layout, nested layouts, and the props Next.js passes them."
  />
  <ExternalResource
    title="Next.js — Metadata and generateMetadata"
    href="https://nextjs.org/docs/app/api-reference/functions/generate-metadata"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="Every metadata field, the title template, and the dynamic generateMetadata function."
  />
  <ExternalResource
    title="React — 'use client' directive"
    href="https://react.dev/reference/rsc/use-client"
    icon="simple-icons:react"
    iconColor="#61DAFB"
    description="The canonical rule for the server/client boundary the Providers pattern relies on."
  />
  <ExternalResource
    title="MDN — What's in the head? Web page metadata"
    href="https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Structuring_content/Webpage_metadata"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="The plain-HTML tour of title, charset, favicon, and description — what the metadata API renders for you."
  />
</CardGrid>
