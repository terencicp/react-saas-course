---
title: Buttons, links, and lists
tagline: Picking the semantic HTML element, button, link, or list, that matches what each control does.
chapter-id: 17
course-progress: 0.1229
sidebar:
  order: 4
  label: Buttons, links, and lists
rewrites: 2
---

import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import CodeTooltips from '../../../components/code/CodeTooltips.astro';
import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import Figure from '../../../components/figures/Figure.astro';
import StateMachineWalker from '../../../components/figures/state-machine-walker/StateMachineWalker.astro';
import Question from '../../../components/figures/state-machine-walker/Question.astro';
import Branch from '../../../components/figures/state-machine-walker/Branch.astro';
import Leaf from '../../../components/figures/state-machine-walker/Leaf.astro';
import Term from '../../../components/ui/Term.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import ReactCoding from '../../../components/live-coding/ReactCoding/ReactCoding.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import FreeBehaviorMap from '../../../components/lessons/017/4/FreeBehaviorMap.astro';
import { CardGrid } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

The last lesson gave you the page's rooms: `<header>`, `<nav>`, `<main>`, `<footer>`, and the heading outline inside them. Now you wire up what the user clicks. Take one screen in the Acme invoicing app: a **Save** button that writes the open invoice, a **Go to dashboard** action that takes you to `/dashboard`, and a short **list of plan features** in the sidebar. Three controls, and it is tempting to reach for whatever tag is nearest.

Here is that screen written two ways. Read the first sentence under each tab before the code.

<CodeVariants>
  <CodeVariant label="Naive">
    ```tsx
    <div onClick={save}>Save</div>
    <button onClick={() => router.push('/dashboard')}>Dashboard</button>
    <div>Unlimited invoices</div>
    <div>Priority support</div>
    ```
    Each control is a `<div>` or `<button>` chosen by habit, not by what it does.
  </CodeVariant>

  <CodeVariant label="Semantic">
    <div data-mark-color="green">

    ```tsx "button" "Link" "ul" "li"
    <button type="button" onClick={save}>Save</button>
    <Link href="/dashboard">Dashboard</Link>
    <ul>
      <li>Unlimited invoices</li>
      <li>Priority support</li>
    </ul>
    ```

    </div>
    Three elements, each chosen by what the control does: act, go, or list.
  </CodeVariant>
</CodeVariants>

The two render the same pixels, so in a browser you cannot tell them apart. But they behave completely differently the moment anyone reaches for a keyboard, a screen reader, or a right-click. This lesson teaches the decision that separates them: **behavior picks the element, the class picks the look.** A control that *performs an action* is a `<button>` even when styled to look like a link; a control that *goes to a URL* is a link even when styled to look like a button. The look is a separate knob, one you'll reach for with Tailwind in the next chapter, and it never changes which element is correct. Last lesson the element carried the page outline; here it carries the page's interactivity, across three families: things that **do**, things that **go**, and things that form a **sequence**.

## Buttons perform actions

A <Term definition="The HTML element for a control that performs an action in place — submitting, toggling, opening.">`<button>`</Term> performs an action on the current page: submit a form, open a modal, delete a row, toggle a sort order. It acts and you stay put; it never navigates. If a click does something but doesn't take the user to a URL, it's a button.

So why not a `<div onClick>`, which fires the same handler? Because the browser gives a real `<button>` five behaviors you would otherwise write by hand:

- **Keyboard focus.** A `<button>` is in the `Tab` order automatically.
- **Keyboard activation.** `Enter` *and* `Space` fire its click, the convention every keyboard user expects.
- **A focus ring.** The browser draws a visible focus indicator when the button is tabbed to.
- **The role.** A screen reader announces it as "button," so a non-sighted user knows it's actionable.
- **Disabled handling.** Setting `disabled` removes the button from the activation path and dims it, in one attribute.

Reach for `<div onClick>` and you rebuild all five yourself.

:::note
Tailwind's <Term definition="Tailwind's base layer, applied automatically. It zeroes default browser styling — button chrome, list bullets, heading sizes — so every element starts visually neutral and your utility classes paint it.">Preflight</Term> reset strips the grey native button chrome, so buttons here start visually bare. The element is still a button with all five behaviors; only the paint is gone.
:::

### `<button type>` is not optional

A `<button>` **inside a `<form>` defaults to `type="submit"`**, so a click submits the form whether or not you intended it. The attribute takes one of three lowercase values; `type="Submit"` does nothing and reports no error, the same casing trap you saw with JSX attributes.

- `type="submit"` submits the form. This is the default inside a form.
- `type="button"` is inert: it runs only its own `onClick` and submits nothing.
- `type="reset"` clears the form to its initial values. Rare and surprising to users, so know it exists and move on.

The reflex to build: **every `<button>` declares its `type` explicitly.** The default isn't wrong, but it's invisible, and the failure it causes is expensive: a Cancel button with no `type` submits the half-finished form instead of abandoning it.

<AnnotatedCode lang="tsx" maxLines={12} code={`
function InvoiceActions() {
  return (
    <form action={saveInvoice}>
      <button type="submit">Save</button>
      <button type="button" onClick={discard}>Cancel</button>
    </form>
  );
}
`}>
  <AnnotatedStep meta="{4}" color="blue">
    `action` is the Server Action that runs on submit. The next lesson wires it up; for now, watch the buttons.
  </AnnotatedStep>

  <AnnotatedStep meta="{5}" color="green">
    Save is the primary action, so `type="submit"` is right. Clicking it, or pressing `Enter` in a field, submits the form.
  </AnnotatedStep>

  <AnnotatedStep meta="{6}" color="red">
    With no `type`, Cancel would default to `submit` and send the half-finished invoice. Nothing warns you.
  </AnnotatedStep>

  <AnnotatedStep meta={`"type=\\"button\\""`} color="green">
    `type="button"` makes Cancel inert to the form: it runs only its `onClick`.
  </AnnotatedStep>
</AnnotatedCode>

### Disabled buttons must say why

`disabled` is a boolean attribute: write `disabled` to turn it on, omit it to leave it off. A disabled button can't be activated, the browser dims it, and it drops out of the activation path.

The cost is to the user. A disabled button is often skipped in the `Tab` order and gives no reason it won't work, so a customer can stare at a greyed-out **Send invoice** button without knowing it's waiting on a required field three rows up. Never hide a critical action behind a bare `disabled`; surface the reason where the user can find it, with an inline message, a tooltip, or a hint wired to the button with `aria-describedby` (covered in "data-\*, aria-\*, and tables").

### Icon-only buttons need a name

Many buttons in a real app have no text: a trash can to delete a row, a copy glyph, an X to dismiss. They look obvious to a sighted user, but a `<button>` whose only child is an icon has no <Term definition="The label assistive technology announces for a control. It comes from the element's text content, or from an aria-label / aria-labelledby when there's no visible text.">accessible name</Term>, so a screen reader user hears "button" and nothing more. `aria-label` gives it a name assistive tech reads aloud, without putting text on screen.

<CodeTooltips tooltips={{
  'aria-label': "The accessible name a screen reader announces. Required when the button has no visible text child — without it the user just hears 'button'.",
  type: 'Always declare it. button keeps this inert to any surrounding form.',
}}>
```tsx
<button type="button" aria-label="Delete invoice" onClick={remove}>
  <TrashIcon />
</button>
```
</CodeTooltips>

`aria-label` replaces the accessible name, so on a button that already has visible text it overrides those words. Reach for it only when there's no visible text to begin with.

## Links navigate to URLs

A link is the mirror image of a button: a button acts and keeps you in place, while a <Term definition="The anchor element — a control that navigates to a URL.">link</Term> **takes the user to a URL** — another page, an external site, a file, or a spot further down the current page. If the job is to go somewhere, it's a link.

### `href` is what makes a link a link

The `href` is what makes an `<a>` a link. With one, it's the real thing: focusable, activated by `Enter`, openable in a new tab via `Cmd`/`Ctrl`-click, right-clickable for "Open in new tab" and "Copy link," visible to crawlers. Without one, the `<a>` is inert — no focus, no role, no behavior. It looks like a link and does nothing.

So if you catch yourself writing an `<a>` with an `onClick` and no `href`, stop. A control that acts on click is a `<button>`; the `href` is the tell that a control goes somewhere.

### `target="_blank"` travels with `rel`

By default a link replaces the current page. Add `target="_blank"` and it opens in a new tab instead, the right call for an external site. But `target="_blank"` carries a security obligation, so pair it with `rel="noopener noreferrer"`:

- `noopener` cuts the new page's handle back to yours. Without it, the opened site can reach through `window.opener` and silently redirect your original tab to a phishing clone, an attack called <Term definition="An attack where a page opened via target=_blank uses window.opener to redirect the original tab to a look-alike phishing page while the user is reading the new one.">tabnabbing</Term>.
- `noreferrer` strips the `Referer` header, so the destination isn't told which of your pages sent the visitor.

Modern browsers imply `noopener` here on their own, but write the `rel` explicitly anyway: defaults shift and older webviews lag. On user-generated links such as a comment or bio, add `nofollow` so crawlers don't lend your ranking to whatever a stranger pasted.

<VideoCallout videoId="wWzlN096DvA" videoTitle="This is How rel=&quot;noopener&quot; Protects Your Outgoing Links">
  Tony Teaches Tech demos the tabnabbing attack live, with `window.opener.location` rewriting the original tab (6 min).
</VideoCallout>

### Files and in-page jumps

Two smaller link jobs round this out. A `download` attribute turns a link into a file download: `<a download href="/report.pdf">`, and a value (`download="invoice.pdf"`) renames the saved file.

An `href` starting with `#` points at an element on the *same* page by its `id`. So `<a href="#pricing">` scrolls to whatever has `id="pricing"` and updates the URL hash, with no page load — how a table of contents and the "skip to main content" link work. The target just needs a unique `id`, and one CSS line softens the jump (`scroll-behavior: smooth`, exposed by Tailwind as `scroll-smooth`).

<CodeTooltips tooltips={{
  target: 'Opens the link in a new browser tab instead of replacing the current page.',
  rel: 'noopener severs window.opener (stops tabnabbing); noreferrer strips the Referer header. The required pair for target=_blank.',
  '#faq': "A fragment link — scrolls to the element with id='faq' on this same page and updates the URL hash. No page load.",
}}>
```tsx
<a href="/pricing">Pricing</a>
<a href="https://stripe.com" target="_blank" rel="noopener noreferrer">Stripe</a>
<a href="#faq">Jump to FAQ</a>
```
</CodeTooltips>

### `<Link>` is `<a>` plus client-side navigation

For links inside your own app, Next.js gives you a better anchor. <Term definition="Next.js's navigation component, imported from next/link.">`<Link>`</Term> renders to a plain `<a href>` in the HTML the browser receives, so everything you just learned still holds: crawlers follow it, `Cmd`-click opens a new tab, "Copy link" works, and if JavaScript fails to load the link still navigates.

What it adds is <Term definition="A route change handled by JavaScript on the client, swapping only the parts of the page that changed — no full-document reload, no white flash.">soft navigation</Term>: clicking it changes the route without a full reload — no white flash, just the parts that changed. The rule is simple:

> Internal routes use `<Link>`. External links use a plain `<a target="_blank" rel="noopener noreferrer">`.

Watch for a navigation dressed up as a button. Real codebases are full of `<button onClick={() => router.push('/dashboard')}>`, a button whose entire job is to go to a URL. It works on click but loses copy-link, middle-click, and crawlability. If the control goes to a URL, it's a `<Link>`, however button-like it looks.

<CodeVariants>
  <CodeVariant label="Internal — Link">
    <div data-mark-color="green">

    ```tsx "Link"
    <Link href="/dashboard">Dashboard</Link>
    ```

    </div>
    **An internal route.** Renders to a real `<a href>` and adds soft navigation.
  </CodeVariant>

  <CodeVariant label="External — a">
    <div data-mark-color="blue">

    ```tsx "target" "rel"
    <a href="https://stripe.com" target="_blank" rel="noopener noreferrer">Stripe</a>
    ```

    </div>
    **An external site.** A plain anchor, new tab, with the security `rel` pair.
  </CodeVariant>

  <CodeVariant label="Navigation in button clothes">
    ```tsx del={1}
    <button onClick={() => router.push('/dashboard')}>Dashboard</button>
    ```
    **Avoid this.** A navigation pretending to be a button — it loses copy-link, middle-click, and crawlability.
  </CodeVariant>
</CodeVariants>

`<Link>` also prefetches routes and restores scroll position, covered with the App Router later.

<VideoCallout videoId="9y7E44zHiGM" videoTitle="Next.js 15 Tutorial - Link Component">
  Codevolution walks through `next/link` in practice — `href`, dynamic routes, and the `replace` prop (5 min).
</VideoCallout>

## Choosing the element: button, link, or div

Both halves point to one rule: **match the element to the behavior, not the look.** A `<button>` styled to look like a link still acts; an `<a>` styled to look like a button still navigates. Restyle freely, but the element has to encode what the control does.

### What `<div role="button">` really costs

To see why the semantic element wins, try to fake one. Making a `<div>` behave like a button takes all of this:

- `role="button"` so assistive tech announces it correctly.
- `tabIndex={0}` so it's reachable by `Tab`.
- An `onKeyDown` handler that fires on `Enter` *and* `Space`, and calls `preventDefault` on `Space`, or the page scrolls instead of activating.
- Focus-ring CSS, because you've lost the browser's default one.
- `aria-pressed` if it's a toggle.
- `cursor-pointer`, because a `<div>` doesn't get the pointer cursor.

That is everything `<button>` gives you for free, rebuilt by hand, with more places for bugs to hide. Reach for `<button>` and restyle it. The same logic rules out `<div role="link">`: if it navigates, it's an `<a>`.

Run the decision one question at a time.

<StateMachineWalker title="Pick the element">
  <Question
    id="intent"
    prompt="What does this control do?"
    description="Start from behavior, never from how it should look."
  >
    <Branch label="Performs an action — submit, open, delete, toggle" to="leaf-button" />
    <Branch label="Goes to a URL" to="destination" />
    <Branch label="I'll just use a <div> with onClick" to="leaf-div" />
  </Question>

  <Question id="destination" prompt="Where does it go?">
    <Branch label="An internal route in this app" to="leaf-link" />
    <Branch label="An external site" to="leaf-anchor" />
  </Question>

  <Leaf id="leaf-button" verdict={'<button type="...">'}>
    A real button gives you keyboard focus, `Enter` and `Space` activation, the focus ring, and the "button" role, all for free. Declare the `type` explicitly so a surrounding form can't hijack the click.
  </Leaf>

  <Leaf id="leaf-link" verdict={'<Link href="...">'}>
    Soft client-side navigation, and it still renders to a plain `<a href>`, so copy-link, `Cmd`-click, and crawlers all keep working.
  </Leaf>

  <Leaf id="leaf-anchor" verdict={'<a target="_blank" rel="noopener noreferrer">'}>
    A plain anchor to the outside world. New tab, with the `rel` pair that severs `window.opener` and strips the referrer.
  </Leaf>

  <Leaf id="leaf-div" verdict="Don't — you'd rebuild a button by hand">
    You'd hand-wire the whole list above — role, `Tab` reach, keyboard activation, focus ring, cursor — to get what `<button>` already gives you. Use `<button>`.
  </Leaf>
</StateMachineWalker>

Here is the bare `<button>` tag, no props or handlers, and everything it hands you.

<Figure expandable={false}>
  <FreeBehaviorMap />
  <Fragment slot="caption">
    Five behaviors from the bare `<button>` tag. A `<div>` makes every one your code to own.
  </Fragment>
</Figure>

Now fix three planted bugs: a delete control built as a `<div>`, a settings link faked with a navigating button, and a Cancel button inside a form with no `type`. Repair each so it uses the right element and attributes.

<ReactCoding
  hidePreview
  instructions={`Three controls are built with the wrong element. Fix each one: Delete should be a real <button> (not a <div>), Settings should navigate to /settings as a link, and the Cancel button inside the form must not submit it. This sandbox has no router, and <Link> renders to exactly the <a href> you'd write — so use a plain <a href="/settings"> for the navigation.`}
  starter={`export function App() {
  return (
    <div className="flex flex-col gap-3">
      <div role="button" onClick={() => alert('deleted')}>Delete</div>
      <button onClick={() => location.assign('/settings')}>Settings</button>
      <form>
        <button>Save</button>
        <button>Cancel</button>
      </form>
    </div>
  );
}`}
  tests={`
const buttonByText = (label) =>
  [...document.querySelectorAll('button')].find(
    (b) => b.textContent?.trim() === label
  );

test('Delete is a real <button>, not a <div role="button">', () => {
  expect(document.querySelector('div[role="button"]')).toBeNull();
  expect(buttonByText('Delete')).not.toBeUndefined();
});

test('Settings is an <a href="/settings">, not a button', () => {
  const link = document.querySelector('a[href="/settings"]');
  expect(link).not.toBeNull();
  expect(link?.textContent?.trim()).toBe('Settings');
  expect(buttonByText('Settings')).toBeUndefined();
});

test('Cancel has type="button" so it cannot submit the form', () => {
  const cancel = buttonByText('Cancel');
  expect(cancel).not.toBeUndefined();
  expect(cancel?.getAttribute('type')).toBe('button');
});

test('Save still submits — its type is "submit"', () => {
  const save = buttonByText('Save');
  expect(save).not.toBeUndefined();
  expect(save?.getAttribute('type')).toBe('submit');
});
`}
/>

<details>
<summary>Reveal the fixed version</summary>

```tsx
export function App() {
  return (
    <div className="flex flex-col gap-3">
      <button type="button" onClick={() => alert('deleted')}>Delete</button>
      <a href="/settings">Settings</a>
      <form>
        <button type="submit">Save</button>
        <button type="button">Cancel</button>
      </form>
    </div>
  );
}
```

Delete acts, so it's a `<button type="button">`. Settings goes to a URL, so it's an `<a href="/settings">`, what `<Link>` would render. Both form buttons declare a `type`: `submit` for Save, `button` for Cancel so it can't silently submit.

</details>

## Lists group related items

The third family is the lightest. A <Term definition="An unordered list — a bullet list where order doesn't carry meaning.">`<ul>`</Term> holds an unordered sequence, an <Term definition="An ordered list — a numbered list where order is meaningful.">`<ol>`</Term> an ordered one, and an <Term definition="A list item — the only valid direct child of a <ul> or <ol>.">`<li>`</Term> wraps each item. Any sequence of related, parallel items is a list: navigation links, a feature grid, a comment thread, audit-log entries.

You render them with the `.map` pattern from "JSX as property syntax", and the key rule carries over: **one `key` per `<li>`, tied to the data's identity, never the array index.**

<div data-mark-color="green">

```tsx "key={feature.id}"
<ul>
  {features.map((feature) => (
    <li key={feature.id}>{feature.name}</li>
  ))}
</ul>
```

</div>

A `<ul>` can nest inside an `<li>` for a file tree or threaded comment, and `<ol>` takes `start`, `reversed`, and `type` to control numbering. You'll rarely reach for either.

### When a row of items is a list

Overusing `<ul>` is as much a problem as underusing it. The test: **would a screen-reader user usefully hear "list, three items" here?** Navigation links, feature cards, and comments under a post are related and parallel, so the count helps; they're a list. A logo beside a sign-in button just sits near it, so it's not.

Build a nav as a bare row of `<a>`s and it renders fine, but silently drops the "list of N items" count that tells a screen-reader user how many destinations there are.

### The nav-list pattern

The last lesson named the primary navigation as a list. Here it is: a `<nav>` landmark wrapping a `<ul>` of `<li>`, each holding a `<Link>`.

<AnnotatedCode lang="tsx" maxLines={14} code={`
const links = [
  { href: '/dashboard', label: 'Dashboard' },
  { href: '/invoices', label: 'Invoices' },
  { href: '/customers', label: 'Customers' },
];

function PrimaryNav() {
  return (
    <nav aria-label="Primary">
      <ul className="flex gap-4 list-none">
        {links.map((link) => (
          <li key={link.href}>
            <Link href={link.href}>{link.label}</Link>
          </li>
        ))}
      </ul>
    </nav>
  );
}
`}>
  <AnnotatedStep meta="{9}" color="blue">
    The `<nav>` landmark, named with `aria-label` so a screen reader can tell it from any other nav on the page.
  </AnnotatedStep>

  <AnnotatedStep meta="{10}" color="green">
    A real semantic list, so assistive tech announces "list, 3 items." `flex gap-4` lays it out horizontally and `list-none` drops the bullets; the semantics survive the styling.
  </AnnotatedStep>

  <AnnotatedStep meta={`{11-12} "key={link.href}"`} color="violet">
    `.map` over the data, one `<li>` per link, each keyed by its `href` rather than the array index.
  </AnnotatedStep>

  <AnnotatedStep meta="{13}" color="orange">
    An internal `<Link>` inside each `<li>`: soft navigation, still a real `<a href>` underneath.
  </AnnotatedStep>
</AnnotatedCode>

:::note
That `flex gap-4 list-none` is the one styled example in this lesson: it shows a semantic `<ul>` keeping its list semantics under a custom horizontal look. Real components reach for shared utilities and a `<Button>` component, both of which come later.
:::

Now sort the three families apart. Drop each control or piece of content into the element family it should use.

<Buckets twoCol instructions="Sort each control or piece of content into the element family it should use.">
  <Bucket name="button" label="<button>" description="Performs an action in place" />
  <Bucket name="link" label="<a> / <Link>" description="Goes to a URL" />
  <Bucket name="list" label="<ul> / <ol>" description="A sequence of related items" />

  <Item bucket="button">Submit the sign-up form</Item>
  <Item bucket="button">Open a confirmation modal</Item>
  <Item bucket="button">Delete this invoice row</Item>
  <Item bucket="link">Go to the billing page</Item>
  <Item bucket="link">Open the docs in a new tab</Item>
  <Item bucket="link">Jump to the FAQ section</Item>
  <Item bucket="list">The app's primary nav items</Item>
  <Item bucket="list">Comments under a post</Item>
  <Item bucket="list">Plan features in the pricing card</Item>
</Buckets>

## Button types inside a form

Inside a form, a `<button>` defaults to `type="submit"`, and pressing `Enter` in a text input submits too. So any unmarked button, and any stray `Enter`, fires the form unless every non-submit button is marked `type="button"`.

A submit button can also carry its own `formAction`, `formMethod`, `formEncType`, `formNoValidate`, or `formTarget` to override the form's matching attribute for that one button; recognize them, you'll rarely need them. The inputs, labels, and the `name`-to-data contract come in the next lesson.

## External resources

<CardGrid>
  <ExternalResource
    title="MDN — the <button> element"
    href="https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="The full attribute surface, including the type values and form association."
  />
  <ExternalResource
    title="MDN — the <a> element"
    href="https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/a"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="href, target, download, and the rel values for new-tab links."
  />
  <ExternalResource
    title="Next.js — <Link> API reference"
    href="https://nextjs.org/docs/app/api-reference/components/link"
    icon="simple-icons:nextdotjs"
    iconColor="#000000"
    description="The component's full prop surface — prefetch, scroll, replace."
  />
  <ExternalResource
    title="A Web Developer's Guide to Buttons vs Links"
    href="https://www.a11y-collective.com/blog/button-vs-link/"
    icon="lucide:accessibility"
    iconColor="#7C3AED"
    description="The A11Y Collective on the same decision this lesson hangs on, from the screen-reader angle."
  />
</CardGrid>
