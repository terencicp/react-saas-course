---
title: The push-is-the-deploy model
tagline: The mental model behind shipping to Vercel, how a git push becomes a deployment and why production is just an alias pointing at one immutable build.
chapter-id: 98
course-progress: 0.00005
sidebar:
  order: 1
  label: Push is the deploy
---

import { Aside } from '@astrojs/starlight/components';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import { CardGrid } from '@astrojs/starlight/components';
import Term from '../../../components/ui/Term.astro';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';
import GitEventToDeploymentMap from '../../../components/lessons/098/1/GitEventToDeploymentMap.astro';
import AliasPointerSwap from '../../../components/lessons/098/1/AliasPointerSwap.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import Sequence from '../../../components/exercises/sequence/Sequence.astro';
import Step from '../../../components/exercises/sequence/Step.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

You ended the last chapter with a green gate in front of a dark site. The four-job CI pipeline proves every commit on `main` typechecks, lints, tests, and builds — but proven code that nobody can reach is just a well-formatted private repo. "Going live" sounds like one big button you press once. On Vercel it isn't a button at all; it's a model, and it's one you already half-know, because the thing that triggers a deploy is a `git push`.

By the end of this lesson you'll be able to look at any `git push` and predict exactly what Vercel does with it and where the result lands — so when you wire up the actual platform in the next lesson, every click confirms something you already understand instead of producing a surprise. This lesson writes no app code. It installs the map; the rest of the chapter walks it.

## Every git event produces a deployment

Start with the part you can see. A junior's first real confusion with Vercel is "why did my pull request ship to production?" — and the cure is to notice that *different git events go to different places*. There are three, and the mapping is exact:

- A push to `main` creates a **production deployment**.
- A push to any other branch *that has an open pull request* creates a **preview deployment** — a build with its own generated `*.vercel.app` URL, posted as a comment on the PR.
- Running the project locally — `vercel dev`, or `pnpm dev` reading `.env.local` — is the **development** environment.

Three git situations, three kinds of deployment. The thing that surprises people is how literal it is: there's no "promote to staging" ceremony, no separate publish step. Where your commit lands is decided entirely by *which branch you pushed to and whether a PR is open*.

<GitEventToDeploymentMap />

Here's the part worth slowing down on, because it carries the rest of the chapter: **the code on all three can be byte-for-byte identical.** The same commit, the same compiled output. What differs between production, preview, and development isn't the code — it's the environment the deployment runs *in*, which is to say the environment variables it reads. A preview and production can be running the very same build and still behave differently because one is talking to a test Stripe key and the other to a live one. Hold onto that distinction — environment is not code. We'll come back to exactly how that split works later; for now just plant the flag.

One more reframe, and it's the one the chapter is named after. You might picture "the deploy" as an action *you* take — a thing you do after the code is ready. It isn't. Vercel installs into your repo as a GitHub App that *watches* for pushes and creates deployments in response. It doesn't push your application code anywhere; your git history is the source of truth, and Vercel reacts to it. The deploy isn't a separate human step you perform after the push — it *is* the consequence of the push. That's the whole idea behind "the push is the deploy."

<Aside type="note">
The GitHub App does hold some write scopes (it can read your CI logs and, if you let it, manage workflow files), so "Vercel can never write to my repo" isn't quite right. The durable way to say it: Vercel doesn't push your application code — it reacts to the commits you push.
</Aside>

<Buckets twoCol instructions="Sort each git action into the deployment environment it triggers. Watch the trap: not every push lands in production.">
  <Bucket name="prod" label="Production" description="The alias the custom domain points at" />
  <Bucket name="preview" label="Preview" description="A throwaway *.vercel.app build per PR branch" />
  <Bucket name="dev" label="Development" description="Your machine, reading .env.local" />

  <Item bucket="prod">Merging a PR into `main`</Item>
  <Item bucket="prod">Pushing a hotfix directly to `main`</Item>
  <Item bucket="preview">Pushing a commit to an open PR branch</Item>
  <Item bucket="preview">A second push to the same PR branch</Item>
  <Item bucket="preview">Opening a new pull request</Item>
  <Item bucket="dev">Running `vercel dev` on your laptop</Item>
  <Item bucket="dev">Reading `.env.local` during `pnpm dev`</Item>
</Buckets>

## Deployments are immutable; production is just an alias

This is the idea everything else in the chapter hangs on, so it's worth the most attention. Take it in two moves: first what a deployment *is*, then what "production" actually points at.

**Every push produces a brand-new deployment.** Its own build, its own packaged output, its own permanent URL of the shape `<hash>-<project>.vercel.app`. Vercel never reaches back into an existing deployment to change it — there's no in-place update, no overwrite. The build you shipped on Tuesday is still sitting there at its original URL on Friday, fully alive, exactly as it was. That's what makes a deployment <Term definition="A build Vercel never overwrites; it stays live on its own URL until you delete it.">immutable</Term> — Vercel treats every build as a finished, frozen thing. And it stays that way until you take it down: the *only* way an old deployment stops being reachable is if you explicitly delete it, which is also the one thing that removes it as a rollback target. Left alone, it lives indefinitely.

So if every push is a new, permanent deployment that never changes — what is "production"? Here's the move that makes everything click: **production is not a deployment at all. It's a pointer.** Your custom domain (`app.example.com`) and the project's main `*.vercel.app` URL are an <Term definition="A domain pointer Vercel aims at one deployment at a time; 'deploying' re-aims it.">alias</Term> — a label aimed at exactly one deployment at a time. "Deploying to production" doesn't mean a build *becomes* production. It means Vercel re-aims that pointer at a new deployment. The swap is atomic and effectively instant: no rebuild, no copy, nothing moves. A name stops pointing at one frozen value and starts pointing at another.

If that shape feels familiar, it should. Way back in "Bindings, not boxes," you learned that a variable in JavaScript isn't a box that holds a value — it's a *name bound to* a value, and reassigning it just re-points the name. Production is the same idea wearing a domain name:

- `app.example.com` is a `let` binding.
- Each deployment is an immutable value — like a frozen object you can't mutate.
- "Deploy" is reassignment: `app.example.com = <new deployment>`.
- The old values don't vanish when you reassign. They're just no longer the one the name points at.

That single analogy is the keystone of the whole chapter. Let it sit for a second, because watching the pointer move while the values stay put is the entire model.

<AliasPointerSwap />

Two practical consequences fall straight out of this picture.

First, the payoff that justifies the whole model: because every previous deployment is still alive at its own URL, **rolling back is just re-aiming the pointer at one of them.** No rebuild, no redeploy, no scramble — Vercel re-aliases the domain to a known-good deployment and you're recovered in seconds. We don't build the rollback flow here; that's its own lesson later in the chapter, on instant rollback. But notice that you've already learned *why* it's possible. Instant rollback isn't a clever feature bolted on — it falls out of immutability plus an alias for free.

Second, a watch-out that bites people constantly: **a `<hash>-<project>.vercel.app` deployment URL is not "the app."** Each of those URLs is pinned to one specific deployment forever. The *production alias* moves between deployments; the deployment URLs never do. So if you copy a `*.vercel.app` URL out of the dashboard and send it to a colleague as "here's our app," you've handed them a frozen snapshot — they'll be staring at whatever build happened to be there that day long after production has moved on. Share the production domain, never a deployment URL.

<Aside type="caution">
By default the production branch is `main`, which is what the course uses — but it's a per-project setting, and the trap is that the production *branch* and the production *URL* are two different things. The branch is a git config (which branch's pushes produce production deployments); the URL is where the alias points. Change the production branch mid-project and nothing redeploys retroactively — the *next* push to the new branch is the first production deploy from it.
</Aside>

## From commit to live: the deploy pipeline

You know now that a push produces an immutable deployment and that going live is a pointer swap. What actually runs *between* the push and the swap? Treat it as a black box with labeled stages — you rarely need to look inside, but you do need to know the order and one relationship that catches almost everyone.

In order, when you push to `main`, Vercel:

1. **Fetches the pushed commit.**
2. **Runs the install** — `pnpm install` — inside a fresh Linux container.
3. **Runs the build** — `pnpm build` / `next build` — in that container, with environment variables scoped to the *target* environment (production vars for a `main` push, preview vars for a PR push).
4. **Packages the output** into Functions <Term definition="The packaged build output Vercel deploys: bundled functions plus static assets.">artifacts</Term> — bundled server functions plus static assets.
5. **Deploys it globally**, live on its own deployment URL.
6. **Re-aliases** the production domain to the new deployment — the pointer swap from the previous section.

Notice where the alias swap sits: dead last, *after* the deployment is already live on its own URL. That gap — "live on a URL" happening before "aliased to production" — is exactly the gap rollback exploits. The deployment exists and works before it's ever the one production points at.

<VideoCallout videoId="AiiGjB2AxqA" videoTitle="Deploying Next.js to Vercel">
Lee Robinson (Vercel) walks the whole model end to end in 6 min — a `git push` triggering the build, a preview deployment per change, and the `vercel` CLI doing the same off the git flow.
</VideoCallout>

Now the relationship that's worth getting crisp, because the wrong version of it is dangerous. You might assume your CI gate from the last chapter *protects production* — that a push to `main` can't ship until the four jobs go green. **It doesn't, and by default it can't.** The Vercel deploy and the GitHub Actions CI run **in parallel, independently.** The deploy does not wait for CI. A push to `main` can re-alias production to a new build *before, or even while,* CI is still running. Green CI is not a precondition for the production swap unless you deliberately make it one.

So what *was* your CI gate protecting? Merges. The branch ruleset blocks a broken PR from merging *into* `main` — and that's what indirectly keeps `main` healthy, which keeps production healthy. The protection is real, but it lives one step upstream of the deploy. When you genuinely need the deploy itself to wait on CI, the tool is Vercel <Term definition="Vercel feature that holds a deployment un-aliased until external checks (e.g. your CI) pass.">Deployment Checks</Term> — they hold a freshly built deployment un-aliased until your external checks pass, so production never points at a build that hasn't gone green. We name it here; wiring it is a later concern. The distinction to carry away: your ruleset gates *merges to `main`*, not the *production alias*.

The other thing this pipeline forces you to understand is *when* an environment variable is read — because it's not always when you'd guess, and the mismatch is the single most common "I changed the env var and nothing happened" bug.

- Some variables are read at **build time** and **baked into the artifact**: anything prefixed `NEXT_PUBLIC_*` (those get inlined directly into the client bundles), the `env.ts` validator, anything used during static generation.
- Other variables are read at **runtime** from the running environment: server-only secrets your server actions and route handlers read when a request comes in.

The consequence is sharp, and it's just the immutability rule again in a new costume. The artifact is frozen. So anything *baked into* it at build time is frozen too. Change a `NEXT_PUBLIC_*` value in the dashboard and the deployments already built still carry the old value, soldered into their bundles — the only way the new value takes effect is a **rebuild**, which means a new deployment. A runtime secret, by contrast, is re-read on the next request, so it can change without a rebuild. When you change a public variable and the live site doesn't budge, you haven't hit a bug — you've hit immutability, and the fix is to ship a new build.

<Aside type="note">
That `env.ts` validator earns its keep right here in the build. If a required server variable is missing or malformed, the build *fails* — the artifact never ships — so a misconfigured environment is caught before it can reach production rather than blowing up at runtime. The full validator schema and the dev/preview/prod scoping that go with it come later in this chapter.
</Aside>

<Sequence instructions="Order what Vercel does, from the moment you push to `main` until users see the new version. Watch the last two: a deployment is live on its own URL *before* the production alias ever moves.">
  <Step>Fetch the pushed commit</Step>
  <Step>Run `pnpm install` in a fresh Linux container</Step>
  <Step>Run `pnpm build` with the target environment's variables</Step>
  <Step>Package the output into Functions artifacts</Step>
  <Step>Deploy the new artifact to its own URL</Step>
  <Step>Re-alias the production domain to the new deployment</Step>
</Sequence>

## What a deploy does *not* do

The fastest way to a bad first week in production is to assume "deploy" means "everything is now in sync." It doesn't. A deploy ships *code* and nothing else, and the gaps between "code shipped" and "system actually consistent" are where the worst incidents live. Three are worth naming outright:

- **No database migration runs.** Drizzle migrations are a separate, deliberate step — the entire next chapter is about running them safely. Shipping code that *expects* a new column does not create the column. The deploy that references it just starts failing against a schema that doesn't have it yet.
- **No external CDN gets purged.** If you put a cache in front of Vercel (a Cloudflare layer, for instance — covered later in this chapter), it has its own cache with its own lifetime. A new deploy doesn't reach through and clear it.
- **No secrets rotate.** A deploy never touches your credentials. Rotating a leaked key is its own action, on its own schedule.

The principle that ties all three together is a senior reflex worth saying plainly: **the deploy ships code; everything stateful is your responsibility.** Databases, caches, secrets, anything with a lifetime of its own — Vercel will happily ship code that assumes those are in a state they're not in. Knowing the gap *before* you ship is the difference between a planned migration and a 2 a.m. page.

<Aside type="caution">
One stateful gap is a security one, and you've already built the defense. That `pnpm install` step runs on Vercel's builder with full access to the *build* environment's variables — so a malicious dependency's install script runs with that same access. This is exactly what the supply-chain hardening from the last chapter is for: SHA-pinned GitHub Actions and the `minimumReleaseAge` quarantine in `.npmrc` (the 24-hour delay before a newly published package version is eligible to install). The deploy doesn't add a new defense here — it's the reason the ones you already shipped matter.
</Aside>

## The Vercel CLI you actually reach for

Git drives the normal path: you push, Vercel deploys, you never open a terminal. The CLI is for the *off-path* moments — the times you need to step outside the git flow on purpose. The useful way to learn it isn't an exhaustive flag reference; it's knowing *when you'd reach for each command*.

```bash
vercel
vercel --prod
vercel env pull .env.local
vercel logs <url>
vercel inspect <url>
vercel promote <url>
```

- `vercel` deploys the current directory as a **preview**, by hand, off the git flow — handy for a quick throwaway build without opening a PR.
- `vercel --prod` deploys to production straight from your machine. Reach for this almost never: it bypasses PR review and CI entirely, putting whatever is on your laptop in front of users with no gate in between. It's listed mainly so you know to recognize it as the wrong tool in normal work.
- `vercel env pull .env.local` syncs the **development**-scoped variables down to your local `.env.local`. This is the command every developer runs right after cloning the repo so their local environment matches. We'll set it up properly in the next lesson.
- `vercel logs <url>` streams a deployment's logs — your first move when a specific deployment is misbehaving.
- `vercel inspect <url>` shows a deployment's metadata and build output — for digging into *why* a build came out the way it did.
- `vercel promote <url>` re-aliases production to a specific existing deployment — the durable, scriptable version of the pointer swap, and the primitive instant rollback is built on. We use it in earnest in the rollback lesson later in the chapter.

The whole list collapses to one rule: reach for the CLI during an **emergency rollback**, **local dev with production-shaped variables**, or **inspecting a failed build** — and otherwise let git drive.

## Why Vercel

You could run this stack elsewhere. Cloudflare Pages with Workers, Netlify, AWS Amplify, and a self-hosted Node server all deploy Next.js, and any of them can host a real SaaS. The course commits to Vercel for one specific reason: in 2026, Next.js 16's runtime features are co-developed with Vercel, so the framework and the platform move together — new framework capabilities land on Vercel first and fit without friction. That tight coupling is the bet, and the course won't re-litigate it from here on.

That's the map. Everything that follows takes this model and wires it into the actual platform: the next lesson lands your first real production URL, then later lessons put the function in the right region, attach a custom domain, give every preview its own database, scope your environment variables, build the instant-rollback flow this lesson promised, and finish with a launch checklist. Each one is a click or a config — and because you have the model now, each one will land as a confirmation instead of a surprise.

## External resources

<CardGrid>
  <ExternalResource
    title="Deploying to Vercel"
    href="https://vercel.com/docs/deployments"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="The official overview of how a git push becomes a deployment — the immutable-deployment and git-event mapping from this lesson."
  />
  <ExternalResource
    title="Environments"
    href="https://vercel.com/docs/deployments/environments"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="Production, preview, and development side by side — the 'environment is not code' split, with the exact rules for which push lands where."
  />
  <ExternalResource
    title="Instant Rollback"
    href="https://vercel.com/docs/instant-rollback"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="The payoff of the alias model: re-aim production at a known-good deployment in seconds. Teases the rollback lesson later in the chapter."
  />
  <ExternalResource
    title="vercel deploy (CLI reference)"
    href="https://vercel.com/docs/cli/deploy"
    icon="simple-icons:vercel"
    iconColor="#000000"
    description="Full reference for the `vercel` and `vercel --prod` commands from the CLI section, including every flag they support."
  />
</CardGrid>
