---
title: What triggers a render
tagline: The first look at React's render model, the mechanism that decides when your components re-run.
chapter-id: 23
course-progress: 0.00005
sidebar:
  order: 1
  label: What triggers a render
---

import Figure from '../../../components/figures/Figure.astro';
import RenderCommitStrip from '../../../components/lessons/023/1/RenderCommitStrip.astro';
import RenderTracking from '../../../components/figures/render-tracking/RenderTracking.astro';
import TrackedNode from '../../../components/figures/render-tracking/TrackedNode.astro';
import Trigger from '../../../components/figures/render-tracking/Trigger.astro';
import Implementation from '../../../components/figures/render-tracking/Implementation.astro';
import CodeTooltips from '../../../components/code/CodeTooltips.astro';
import AnnotatedCode from '../../../components/code/annotated-code/AnnotatedCode.astro';
import AnnotatedStep from '../../../components/code/annotated-code/AnnotatedStep.astro';
import PredictOutput from '../../../components/exercises/predict-output/PredictOutput.astro';
import PredictWhy from '../../../components/exercises/predict-output/PredictWhy.astro';
import Term from '../../../components/ui/Term.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import { CardGrid } from '@astrojs/starlight/components';
import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

Picture a `<UserCard user={...} />` sitting in a dashboard. Above it, a search box. Every time you type a single character into that search box, the `<UserCard>` re-renders — its whole function runs again — even though the user is the same user, with the same name and the same email. Nothing about that card changed on screen, and nothing about its data changed either. So why did it run?

That question trips up almost everyone the first time they meet it, and the answer rewrites how you picture a component. By the end of this lesson you'll know precisely what makes a component re-run, why writing `{ name, email }` inline counts as a brand-new value every single time, and why in 2026 you write the obvious code and let a compiler clean up after you. The last chapter promised that "the function runs again with new props" and then waved it away — this is where that promise gets paid.

## Rendering is React calling your function

Here's the shift the whole chapter rests on. You've been picturing a component as a *template* — a chunk of markup React keeps on screen and quietly edits in place when something changes. Drop that picture. A component is a **function**, and rendering is nothing more exotic than React calling it.

When React renders your component, it invokes the function. The function returns JSX, which — recall from the last chapter — is just a tree of plain JavaScript objects describing what should be on screen. React takes that tree, compares it to the tree from the previous call, and applies the smallest set of DOM changes needed to make the real page match. That's the entire mechanism. Run the function, get a tree, diff it, patch the DOM.

That splits into two phases worth naming once:

- **Render** — React calls your function and gets back the JSX tree. This is pure computation. No DOM is touched. It's just JavaScript building objects.
- **Commit** — React applies the diff to the real DOM. This is the <Term definition="The phase where React applies the computed changes to the real DOM.">commit</Term> phase that actually moves pixels.

Hold onto one consequence, because it seeds everything later in this lesson: **if a render produces the same output it did last time, React commits nothing.** It runs your function, sees the tree matches, and leaves the DOM untouched. The React team guarantees this. The function ran; the screen didn't flicker. That gap between "the function ran" and "the screen changed" is the single most important thing to internalize, and it's why the panic you'll feel later — *won't that re-render everything?* — is mostly misplaced.

<Figure caption="The trigger is the cause; render and commit are the two phases. Render and reconcile are pure computation — only commit touches the DOM.">
  <RenderCommitStrip />
</Figure>

We won't spend more on commit here. It earns its weight in a later chapter, once effects enter and the render/commit split starts mattering for *when* your side effects run. For now, commit is just "the DOM write," and render is the part you can reason about as plain function calls.

Which gives us the frame for the whole chapter. A component is a function, and the function is of one thing: its inputs.

```tsx
function Greeting({ name }: { name: string }) {
  return <h1>Hello {name}</h1>;
}
```

This function *is* what React calls on every render. Give it `name`, it returns a tree. Same `name`, same tree. That's the contract — and it has a name.

Say it out loud: **`UI = f(state)`**. The UI is a function of state. Same inputs in, same JSX out. Your component is the `f`; its props, state, and context are the inputs. Render the same component with the same inputs twice and you get the same tree both times. Keep this phrase nearby — we'll return to it at the end, and it's the thread that ties this whole chapter together.

## The three triggers

So a component re-runs when React calls it again. The obvious next question: what makes React call it again?

There are exactly three reasons. A component re-runs when:

1. **Its own state updated.** You called a `useState` or `useReducer` setter, and React schedules the component to run again with the new value.
2. **An ancestor re-rendered.** When a component re-renders, React re-runs its children by default — and *their* children, all the way down the subtree.
3. **A context it subscribes to changed.** The component reads a shared value with <Term definition="The hook that reads a value shared from an ancestor, covered in detail in a later chapter.">useContext</Term>, and that value updated.

That's the complete list. There is no fourth trigger. And here is the one that catches everyone, so read it twice: **a prop changing is not on the list.**

This is the misconception that quietly wrecks every optimization story, so let's kill it cleanly. When you see a child re-render and its props are different, the instinct is "the prop changed, so the child re-rendered." That's backwards. The prop changing and the child re-rendering are *both* consequences of the same cause: the parent re-ran. When the parent re-runs, it does two things in one stroke — it re-executes the child (trigger 2), *and*, while doing so, it produces the new prop value and hands it down. The prop didn't trigger anything. The parent re-rendering did. The new prop is a passenger, not the driver.

Get this and the rest of the chapter clicks. Miss it and you'll spend hours trying to "stop the prop from changing" when the thing to look at is one level up.

:::note
You may see React's own docs describe only *two* reasons a component renders: the initial render, and "the component's or one of its ancestors' state has been updated." That's the same truth, compressed. An ancestor re-rendering and a context change both come down to *some state somewhere above this component updated, and the update reached it.* The three-trigger split is the practical lens for debugging; the docs' two-reason version is the same rule with the cases folded together. They don't conflict.
:::

The cleanest way to *see* this is to watch which boxes light up. In the tree below, `Dashboard` holds a `SearchBox` and a `UserCard`. Click each trigger and watch the render badges tick. Pay attention to how far the ripple spreads.

<RenderTracking title="What makes a box re-run?">
  <TrackedNode id="app" label="Dashboard">
    <TrackedNode id="search" label="SearchBox" />
    <TrackedNode id="card" label="UserCard" />
  </TrackedNode>

  <Trigger id="state-search" label="type in SearchBox (its own setState)" renders="search" />
  <Trigger id="state-app" label="setState in Dashboard" renders="app,search,card" />
  <Trigger id="rerender-card" label="UserCard's own setState" renders="card" />
</RenderTracking>

There's the answer to the opening puzzle. When you typed in that search box, you weren't typing into `SearchBox` in isolation — the keystroke updated state in `Dashboard` (because that's where the search value lived), `Dashboard` re-rendered, and re-rendering `Dashboard` re-ran every child under it. The `<UserCard>` ran because its parent ran. The user never changed; the parent did.

One more distinction before we go deeper, because it explains *when* this chapter's bugs show up. The very first time a component renders, there's no previous tree to diff against — React is building DOM from nothing. That first render is called a **mount**. Every render after it is an **update**, and an update always has a previous tree to compare to. This matters because nearly every bug in this chapter only appears on updates: the mount has nothing to compare against, so there's nothing to get wrong yet. The trouble starts on the second render.

Let's make trigger 2 concrete with the smallest possible example: a parent that re-renders, and a child whose props never change.

```tsx {6} "I never change"
const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>Clicked {count} times</button>
      <Label text="I never change" />
    </div>
  );
};
```

Click the button and `Label`'s function runs again on every click, even though `text` is the same string it has always been. Trigger 2 fired — the parent re-rendered, so the child re-ran, full stop. (Note `useState` here is borrowed: the setter schedules a re-render; the full hook gets its own treatment in the next chapter, so don't expect depth on it yet.)

Now, `<Label text="I never change" />` running again is harmless — it returns the same tree, React diffs it, sees no difference, commits nothing. No flicker. But it raises the question the rest of this lesson answers: if React *can* tell when a child's output is unchanged, can it skip re-running the child at all? Sometimes, yes. And the rule that decides is worth getting exactly right.

## Reference identity and `Object.is`

When React is allowed to skip re-running a child whose parent just re-rendered — we'll get to *when* in a moment — it has to decide whether the child's props actually changed. To decide, it walks each prop and compares the new value to the old one with a single function: <Term definition="JavaScript's same-value comparison. Like === but NaN equals itself and ±0 differ. React uses it as its one prop-equality rule.">`Object.is`</Term>. This is React's *only* equality rule. There is no other.

You already know most of how `Object.is` behaves, because you know `===`. It splits the world in two:

- **Primitives compare by value.** `Object.is('hi', 'hi')` is `true`. `Object.is(3, 3)` is `true`. Two strings with the same characters are the same value; two equal numbers are the same value. (It's `===` with two edge cases tidied up: `NaN` equals itself, and `+0` and `-0` don't — neither of which is the point here.)
- **Objects, arrays, and functions compare by reference.** `Object.is({}, {})` is `false`. Two object literals with byte-for-byte identical contents are *different values* as far as `Object.is` is concerned, because they're different objects in memory. The comparison asks "are these the same object?", not "do these look the same?"

<CodeTooltips tooltips={{
  'Object.is': "React's equality check. Primitives by value; objects, arrays, and functions by reference.",
  '{}': 'A fresh object — a new reference every time this expression is evaluated.',
}}>
```ts
Object.is('hi', 'hi');   // true  — same string value
Object.is(3, 3);         // true  — same number value
Object.is({}, {});       // false — two different objects
Object.is([1], [1]);     // false — two different arrays
const fn = () => {};
Object.is(fn, fn);       // true  — literally the same function
```
</CodeTooltips>

That last line is the whole game. `Object.is(fn, fn)` is `true` not because the function "looks the same" but because it *is* the same function — the same object in memory, compared to itself. Two functions written out separately, even character-for-character identical, would be `false`. Identity, not appearance.

You might reasonably ask: why doesn't React just compare the *contents* — walk into the object and check each field? Because deep comparison is slow on every prop of every component, and it's usually wrong anyway (how deep? what about functions inside? circular references?). Deep equality isn't automatic in React, and it never will be. If you ever need it, it's manual work you opt into by hand — rare, and easy to get wrong. The default is `Object.is`, and `Object.is` asks one question: same reference?

Now, one guardrail so you don't over-generalize. This comparison only happens **when a child is memoized** — when something has told React "you're allowed to skip this child if its props match." Without that, React doesn't compare props at all. The default behavior from trigger 2 is unconditional: parent re-rendered, child re-runs, no comparison involved. So `Object.is` isn't running on every prop of every render — it runs only at the memoization boundaries, as the gatekeeper deciding whether a skippable child gets to actually skip.

## Inline literals churn identity

Here's where `Object.is` reaches out and bites. The most ordinary, innocent-looking code you'll write produces a brand-new reference on every render — and you'd never guess it from looking.

Watch these three props:

- `<Child style={{ color: 'red' }} />` — that `{ color: 'red' }` is an object literal. Every time the parent renders, that line executes again, and executing an object literal *builds a new object*. New object, new reference.
- `<Child items={[...list, 'extra']} />` — the spread creates a new array each render. Same `list`, same `'extra'`, brand-new array.
- `<Child onClick={() => save(id)} />` — that arrow is a function expression. Each render evaluates it and produces a *new function*. Identical body, different reference.

None of these change anything on screen. The color is still red, the items are still the same items, the click still saves. But under `Object.is`, all three are different from their previous selves on every single render — because each render builds a fresh object, array, or function in memory. Carry this one sentence and you've got the core of the lesson: **any object, array, or function written as a literal in JSX is a new value every render.**

This is *the* reason a memoized child re-renders "for no reason." React reaches the child, runs `Object.is` on the `style` prop, gets a new object versus last render's object — `false`, different — and concludes the prop changed. So the child can't skip. It re-runs. Not because anything is actually different, but because the literal manufactured a new reference.

<AnnotatedCode lang="tsx" code={`
const Profile = ({ user }: { user: User }) => (
  <Avatar
    title="Profile"
    style={{ borderColor: 'red' }}
    tags={[...user.tags, 'verified']}
    onClick={() => openProfile(user.id)}
  />
);
`}>
  <AnnotatedStep meta="{3}" color="green">
    The primitive prop. A plain string, compared by value — so it's the same value every render under `Object.is`. This one is stable, and it's never the reason a child re-renders.
  </AnnotatedStep>

  <AnnotatedStep meta="{4}" color="orange">
    An object literal. Every render executes `{ borderColor: 'red' }` again, building a brand-new object. New reference, `!==` last render's — even though the color never changed.
  </AnnotatedStep>

  <AnnotatedStep meta="{5}" color="orange">
    The spread builds a new array on every render. Same `user.tags`, same `'verified'`, but a fresh array in memory — a different reference each time.
  </AnnotatedStep>

  <AnnotatedStep meta="{6}" color="orange">
    The arrow is a function expression. Each render evaluates it and produces a *new* function — identical body, different reference.
  </AnnotatedStep>
</AnnotatedCode>

Try committing to the rule before we resolve it. Predict what this prints — and notice the instinct you have to fight.

<PredictOutput
  instructions="Object literals are fresh references; primitives compare by value."
  expected={`false
true
true`}
>

```ts
function makeStyle() {
  return { color: 'red' };
}

const a = makeStyle();
const b = makeStyle();

console.log(Object.is(a, b));
console.log(Object.is('red', 'red'));
console.log(Object.is(a, a));
```

<PredictWhy>
`makeStyle()` builds a *new* object each call, so `a` and `b` are different references — `false`, even though their contents are identical. The string `'red'` is a primitive, compared by value — `true`. And `a` compared to itself is the same reference — `true`. This is exactly what happens to an inline `style={{ color: 'red' }}` across two renders: same contents, different object, "changed" to React.
</PredictWhy>

</PredictOutput>

If you predicted `false, true, true`, you've got it. The object built twice is two references; the primitive compared by value is equal; the object compared to itself is equal. That `false` on the first line is the bug in miniature — and now we resolve it.

## The compiler writes the memoization for you

So inline objects churn, memoized children re-render for no real reason, and the natural fix would be to make those references *stable* — somehow hand React the same object across renders so `Object.is` returns `true`. For years, that's exactly what you had to do by hand.

The old way, worth recognizing on sight and nothing more: you'd wrap the object in a hook that <Term definition="Caches a computed value and returns the same one across renders as long as its inputs are unchanged, skipping the recompute.">memoizes</Term> it, and wrap the callback in another. The names were `useMemo` and `useCallback`. They worked, but they spread across the codebase like weeds — every inline object a candidate, every one carrying a dependency array you had to keep in sync by hand. Get the array wrong and you'd cache a stale value. It was a tax you paid on every component, mostly to fix a problem the code didn't really have.

In 2026 you don't pay it. The project ships with the <Term definition="A build-time tool that auto-inserts memoization so you don't write useMemo or useCallback by hand.">React Compiler</Term> turned on — it's a flag in the project config, set once, and from then on it reads your components at build time and inserts that memoization *for* you. The inline object, the array spread, the inline callback — the compiler gives each one a stable identity across renders, automatically, with no hooks and no dependency arrays in your source. It does this on one condition: the component has to be **pure** — same inputs, same output, no surprises. That purity contract is the next lesson's whole subject; here it's just the thing the compiler quietly depends on.

Which collapses to a rule you can adopt today and lean on for the rest of the course:

**Write the natural code. Inline the object, inline the callback. Let the compiler memoize.** Don't reach for `useMemo` or `useCallback` prophylactically — that instinct is a reflex from the pre-compiler era, and in this stack it's noise. Manual memoization survives only as a last resort, for the rare component the compiler decides it can't optimize. There's a DevTools signal that flags those, and the decision of what to do about them comes later — but the default, the thing you do nine times out of ten, is *nothing*. You write the obvious code and move on.

:::tip
The same identity rule shows up with context. A `<Ctx.Provider value={{ user, login }}>` written with an inline object hands every subscriber a new `value` reference on every render — and so re-runs all of them, every time, for the same reason an inline `style` re-runs a child. The compiler memoizes that value too. Without a compiler, you'd wrap it in `useMemo` by hand. Same rule, just applied to context.
:::

Flip between the tabs below to see the asymmetry. Same source on both sides — a `Toolbar` passing an inline callback to a memoized `SaveButton`. With the compiler off, the parent's re-render hands down a fresh callback reference, so the child can't skip and lights up too. With the compiler on, the callback's identity stays stable, so the child stays dark.

<RenderTracking title="Same code, compiler off vs on">
  <TrackedNode id="parent" label="Toolbar">
    <TrackedNode id="child" label="SaveButton" />
  </TrackedNode>

  <Implementation id="without" label="Without compiler">
    <Trigger id="tick" label="parent re-renders" renders="parent,child" />
  </Implementation>

  <Implementation id="with" label="With compiler" default>
    <Trigger id="tick" label="parent re-renders" renders="parent" />
  </Implementation>
</RenderTracking>

## Renders are cheaper than you think

When "a parent re-render re-runs the whole subtree" first lands, the reflex is panic. *The whole subtree? On every keystroke? Isn't that wasteful — won't it tank performance?* It's the most natural worry in the world, and the answer is the one experienced React developers have made their peace with: **mostly, no.**

Here's why. Rendering is calling functions and diffing a tree of plain JavaScript objects. That's fast — JavaScript builds objects by the millions without breaking a sweat. The expensive part of getting something on screen is the **DOM commit**, and React already minimizes that ruthlessly: it touches only the DOM nodes that genuinely differ from last time, and skips the rest. Recall the strip from the top of the lesson — a re-render that produces an unchanged tree commits *nothing*. The function ran; the page didn't move. So "the subtree re-rendered" usually means "some functions ran and React confirmed there was nothing to change" — which costs almost nothing.

This flips the discipline. Optimizing renders is not a default habit you sprinkle everywhere — it's a *last resort*, reached for only after a measurement tells you a specific render is actually too slow. The order is fixed: **profile first, then chase the prop identity that's churning.** React's DevTools Profiler — the DevTools you installed back in the browser chapter — shows you exactly which components rendered and which of their props changed. You don't guess; you measure, find the churning reference, and only then decide whether it's worth pinning down. Measure before you optimize. That's a discipline that outlives any one React API, and it's the senior instinct this whole lesson is quietly training.

Which brings us back where we started: **`UI = f(state)`**. You now know the function — your component. You know the inputs — props, state, context. And you know the three things that make React call the function again — own state, an ancestor, a context. That's the full render trigger model, and it's smaller than the panic suggested.

What we've left untouched is the *return value*. When your function hands React a new tree, how does React figure out which box in the new tree corresponds to which box in the old one — so it knows what to keep, what to throw away, and what to merely update? That matching process is called <Term definition="The algorithm React uses to match a new element tree against the previous one, deciding what to reuse, update, or discard.">reconciliation</Term>, and getting it right — or getting it wrong — is the next lesson.

<VideoCallout videoId="AwW7olQ84Qs" videoTitle="Things you didn't know about re-rendering in React">
  A 5-minute illustrated walkthrough from Code Sketched (building on Josh Comeau's essay) that confirms the same lesson: a component re-renders on its *own* state change, which re-runs every child regardless of props.
</VideoCallout>

## External resources

<CardGrid>
  <ExternalResource
    title="Render and Commit"
    href="https://react.dev/learn/render-and-commit"
    icon="simple-icons:react"
    iconColor="#61DAFB"
    description="The official two-phase model, and the source of the rule that an unchanged render touches no DOM."
  />
  <ExternalResource
    title="Why React Re-Renders"
    href="https://www.joshwcomeau.com/react/why-react-re-renders/"
    icon="lucide:graduation-cap"
    iconColor="#E8590C"
    description="Josh Comeau's interactive deep dive on the state trigger, the props misconception, and reference identity."
  />
  <ExternalResource
    title="Object.is()"
    href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is"
    icon="simple-icons:mdnwebdocs"
    iconColor="#000000"
    description="MDN's reference for the exact equality rule React uses to compare props."
  />
</CardGrid>
