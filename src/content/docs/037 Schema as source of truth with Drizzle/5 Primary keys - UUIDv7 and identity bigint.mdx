---
title: 'Primary keys: UUIDv7 and identity bigint'
tagline: Choosing the right primary key strategy for each Drizzle table, time-sortable UUIDv7, identity bigint, or natural key.
chapter-id: 37
course-progress: 0.00005
sidebar:
  order: 5
  label: Primary keys
---

import CourseProgressBar from '../../../components/ui/CourseProgressBar.astro';
import CodeVariants from '../../../components/code/code-variants/CodeVariants.astro';
import CodeVariant from '../../../components/code/code-variants/CodeVariant.astro';
import Figure from '../../../components/figures/Figure.astro';
import Term from '../../../components/ui/Term.astro';
import ExternalResource from '../../../components/ui/ExternalResource.astro';
import VideoCallout from '../../../components/embeds/VideoCallout.astro';
import { CardGrid } from '@astrojs/starlight/components';
import StateMachineWalker from '../../../components/figures/state-machine-walker/StateMachineWalker.astro';
import Question from '../../../components/figures/state-machine-walker/Question.astro';
import Branch from '../../../components/figures/state-machine-walker/Branch.astro';
import Leaf from '../../../components/figures/state-machine-walker/Leaf.astro';
import DrizzleSchemaCoding from '../../../components/live-coding/DrizzleSchemaCoding/DrizzleSchemaCoding.astro';
import Buckets from '../../../components/exercises/buckets/Buckets.astro';
import Bucket from '../../../components/exercises/buckets/Bucket.astro';
import Item from '../../../components/exercises/buckets/Item.astro';
import UuidByteLayout from '../../../components/lessons/037/5/UuidByteLayout.astro';

<CourseProgressBar value={frontmatter['course-progress']} />

Since your very first table, every row has carried `id: ...primaryKey()` with the fill left blank — a deliberate placeholder this chapter promised to come back and finish. This is where you finish it, and the choice you make here is the most permanent one in the whole schema. A primary key is referenced by every foreign key that points at the row, baked into every URL and every API response that names the row, and effectively un-renameable the moment real data exists. Get a column type wrong and you change it next week; get a primary key wrong and you live with it.

So this is a decision lesson, not a syntax one. The mechanics are three tiny shapes you already half-know. The weight is the question an experienced engineer asks before writing any of them — **which primary-key strategy does this table want, and what does choosing wrong cost later?** By the end you will answer that per table and write the canonical Drizzle shape for each. One concrete fork to hold onto: would you rather ship customers a URL that reads `/invoices/47`, or one that reads `/invoices/0193b5a1-…`? Sit with which one bothers you, and why — the whole lesson is hiding in that choice.

## What a primary key buys you

You have written `.primaryKey()` since the start of the chapter as a piece of syntax. Before we choose what fills it, pin down what it actually does, because the guarantees are doing more work than they look like.

A primary key is the column whose value uniquely names a row in the table. It is the address other tables point at — every foreign key in your schema resolves to exactly one primary-key value, one row. That is its job. And `.primaryKey()` earns that job by bundling three constraints into one call:

```ts
id: uuid().primaryKey(),
```

Three things, free, from one method. The column is implicitly `NOT NULL` (an unnamed row has no identity, so null is impossible). It is implicitly `UNIQUE` (two rows sharing an address is a contradiction). And Postgres builds an index on it (the address has to be fast to look up, because every join uses it). That index is a <Term definition="Postgres's default index structure: a sorted tree. Lookups and ordered inserts are cheap; random inserts can force a page split.">B-tree</Term>, and the fact that it is *sorted* will matter enormously in two sections.

The practical rule that falls out of this is short and worth stating outright, because it shows up as a review smell constantly:

:::caution
Never add a separate `.unique()` or `.index()` to a primary-key column. The uniqueness and the index are already there. A second one is redundant, and seeing it in a diff tells a reviewer the author did not know what `.primaryKey()` includes.
:::

So the slot's guarantees are settled before we start. Everything from here is about choosing what *value* fills a slot whose contract is already written.

## Surrogate or natural: the first fork

There are two questions an experienced engineer asks to land a primary key, and they ask them in a fixed order. Get the order right and the answer usually falls out on its own. The first question is the universal one — it applies to every table before you think about anything else.

**Does the database mint this value, or does the outside world already own it?**

That splits every key into two kinds. A <Term definition="A primary key the database mints — a meaningless, system-owned value like a UUID or an auto-incrementing integer. The course default.">surrogate key</Term> is a value the database makes up. It carries no meaning — it is not the email, not the name, not the slug, just an opaque identifier whose only job is to be unique. A <Term definition="A primary key that is a real domain value — an email, a slug, a country code. The domain owns it, not the database.">natural key</Term> is the opposite: a value that already identifies the thing out in the world. An email address. A URL slug. A country code, a currency code, an ISBN.

The natural key is tempting because it feels like less work. You already have an email; why mint a second identifier? Here is why, and it is the rule to keep:

:::danger
Domain values change. A primary key that changes detonates through every foreign key pointing at it.
:::

Walk through what that means. Suppose `email` is the primary key of your `users` table, and a hundred other rows across the schema — invoices, sessions, audit entries — point at users by their email. A user updates their email, a one-line edit they expect to be routine. But that email *is* the address those hundred rows hold. To keep the data consistent, every one of them has to be rewritten to the new value, in lockstep, or your foreign keys dangle. A routine profile edit just became a schema-wide cascade. The same trap waits on a slug that gets renamed, or a "permanent" external code that gets reissued. Anything the domain owns can change, and a changing primary key is a wound that never heals.

So the senior default is blunt: **surrogate, everywhere a value is user-facing.** The natural-key carve-out is real but narrow — only genuinely *immutable, externally-defined* identifiers earn it. ISO country codes (`'US'`), currency codes (`'USD'`), ISBNs: values that are fixed by an authority outside your app and will never be reissued under you. Even then, only when you are certain you will never want to rename or re-skin them with a display layer.

There is one heuristic that settles the call every time, and it is the single most useful sentence in this section. Before you make any value a primary key, ask:

> Would I be comfortable if this value changed — and every row pointing at it had to change too?

If the answer is anything short of a confident yes, it is a surrogate. A column type is a constraint you tighten later; a primary key is a promise you can't take back.

```ts del={1} ins={2}
slug: text().primaryKey(),                     // bad — a slug gets renamed, and the rename cascades
id: uuid().primaryKey(),                        // good — surrogate id, with a unique on slug instead
```

One smell to name now so you don't reach for it later in this very lesson. You will meet a way to make a primary key span *two* columns in a few minutes. It is tempting to read that and decide an entity like a page should be keyed by `(orgId, slug)` — "a page is unique within its org, so make that pair the key." Resist it. A composite key on a first-class entity is a tenancy-modeling mistake: give the entity a surrogate `id` and add a `unique(orgId, slug)` constraint instead, the pattern the lesson on unique and check constraints will cover. Composite primary keys have exactly one good home, and we will get there.

## Why UUIDv4 is a trap and v7 isn't

Settle that user-facing entities want a surrogate, and one question remains: *which* surrogate? The two candidates are a UUID and a `bigint` integer, and most of this lesson's real intuition lives in understanding the UUID side — specifically, in why the obvious UUID is a trap and a newer one isn't.

This is the one place you should not take a rule on faith. Build the picture instead, because the difference between the two UUIDs is invisible right up until it is catastrophic, and the only defense is seeing *why*.

A <Term definition="Universally Unique Identifier — a 128-bit (16-byte) value, written as 32 hex digits. Generated to be globally unique without a central coordinator.">UUID</Term> is a 16-byte value, written as those familiar 32 hex digits. There are two versions you care about, and at a glance they look identical — same length, same hex, same `-` grouping. The difference is not in how they *look*. It is in **where a new one lands in the index.** Remember the primary-key index is a sorted B-tree, and hold that.

**UUIDv4 is fully random.** Every byte is noise. So every new `id` you generate is a random number, and a random number lands at a *random spot* in a sorted tree. Insert a thousand rows and you have scattered a thousand writes across the entire breadth of the index, each one potentially splitting a page to wedge the new value into its sorted position. At a small table you will never notice — a few hundred rows, the whole index fits in memory, who cares. The trap is that the cost is <Term definition="One logical insert causing many physical page writes, because the new key lands in the middle of the index and forces a page to split.">write amplification</Term> that *compounds with table size*. The bigger the table, the more a random insert thrashes. It is free in development and brutal in a populated production table — expensive at the exact moment you can least afford to change it.

**UUIDv7 fixes this by making the value time-sortable.** It prefixes the 16 bytes with a 48-bit millisecond timestamp, then fills the rest with randomness. The randomness still buys you global uniqueness and the non-guessability of a UUID. But the timestamp prefix means a value minted now sorts *after* every value minted before it. New `id`s no longer scatter — they all land at the *tail* of the index, appended in order, exactly the way an auto-incrementing integer would. You keep everything good about a UUID and recover sequence-like insert performance. The following figure makes the difference concrete; the whole point is in the example values printed beneath each strip, so read those closely.

<Figure caption="Same 16 bytes, same hex — the only difference is where the timestamp lives, and so where new inserts land.">
  <UuidByteLayout />
</Figure>

Two pieces of grounding, one sentence each, because they tell you this is real and current rather than a clever hack. UUIDv7 was standardized by <Term definition="The 2024 IETF specification that standardized UUID versions 6, 7, and 8.">RFC 9562</Term> in May 2024. And Postgres 18, which you put your project on in the previous chapter via Neon, ships a native `uuidv7()` function — no extension to install, it is just there. That native function is what makes the canonical shape in the next section so clean.

One last name to recognize and move past. Before the standard landed, libraries like ULID and KSUID solved this exact problem — time-sortable, globally-unique IDs — and you will still see them in older codebases. They are the pre-standard ancestors of UUIDv7. You do not need to learn them; in 2026, on Postgres 18, `uuidv7()` is the answer.

<VideoCallout videoId="DHtf_46OxY8" videoTitle="UUID vs INT: What's Better For Your Primary Key?">
  Database Star walks auto-increment, UUIDv4, and the time-sortable UUIDv7 in ~10 min — the same v4-scatter-vs-v7-tail story this section just made, with a Postgres demo.
</VideoCallout>

:::caution
Drizzle's `.defaultRandom()` generates a UUIDv4 — the random one, the trap. It is *not* the v7 path. There is an open feature request for a native v7 helper, but until it ships, reach for the SQL default in the next section, never `.defaultRandom()`, for a primary key.
:::

## The three canonical shapes

Time for the mechanics, and there are exactly three shapes this course ships. Each tab below is one strategy with the trigger that should make you reach for it. Read them as "when do I write this," not "how do I write this" — the *how* is one line in every case.

<CodeVariants>
  <CodeVariant label="UUIDv7">
    <div data-mark-color="green">

    ```ts {1}
    id: uuid().primaryKey().default(sql`uuidv7()`),
    ```

    </div>
    **The default — any entity whose id is seen outside the database.** If the id appears in a URL, an API response, or a client payload, it is this. The default fires *SQL-side* via `default(sql\`uuidv7()\`)` rather than in app code, so the id is minted by Postgres itself — which means it also fires for migrations, seed scripts, and a raw `psql` insert, exactly as the previous lesson argued SQL-side defaults should. `sql` is the tagged template you met in the generated-columns work, imported from `drizzle-orm`.
  </CodeVariant>

  <CodeVariant label="identity bigint">
    <div data-mark-color="blue">

    ```ts {1}
    id: bigint({ mode: 'number' }).primaryKey().generatedAlwaysAsIdentity(),
    ```

    </div>
    **The high-volume internal reach.** An auto-incrementing 8-byte integer, minted by Postgres from a sequence. Reach for it only when *all* of these hold: the table is high-volume and internal (an event log, analytics rows, a junction nobody fetches by id), the id never crosses a system boundary, and sharding is not on the roadmap. Why a bigint over a UUID here: 8 bytes instead of 16, a tighter index, and since no outsider ever enumerates these rows, the leak and locality arguments for UUIDs simply do not apply. `mode: 'number'` is required — it maps the column to a JS `number` (safe up to 2⁵³).
  </CodeVariant>

  <CodeVariant label="natural key">
    <div data-mark-color="orange">

    ```ts {1}
    code: text().primaryKey(), // ISO-3166, e.g. 'US' — immutable, externally defined
    ```

    </div>
    **The narrow carve-out.** A real domain value carrying `.primaryKey()` directly, used only for immutable external identifiers. Notice the *kind* of table this belongs to: a small, static reference table — countries, currencies — whose rows are owned by some outside authority and will never be renamed. It is never a user-facing entity. If you find yourself reaching for this on anything a user creates or edits, you want a UUID instead.
  </CodeVariant>
</CodeVariants>

Two of those three are the same decision viewed from different sides. The choice between **UUIDv7** and **identity bigint** is the *second* question — is the id exposed to the world, or internal-only? The **natural key** is the escape hatch from the *first* question, the rare case where the outside world already owns an immutable value worth keying on.

A couple of footnotes so the shapes don't trip you later. If your team is not on Postgres 18 — no native `uuidv7()` to call — the portable fallback is to mint the value in app code: `id: uuid().primaryKey().$defaultFn(() => uuidv7())`, with `uuidv7` from the npm package of the same name. Same logical result, but app-side only, so it will *not* fire for a raw `psql` insert or a seed that bypasses Drizzle — which is exactly why the native SQL-side version is this course's default. And on the bigint: `generatedAlwaysAsIdentity()` is the modern SQL-standard <Term definition="A column whose values Postgres auto-generates from an internal sequence, per the SQL standard.">identity column</Term>. The legacy <Term definition="Postgres's older auto-incrementing 8-byte integer type. Superseded by the SQL-standard GENERATED ALWAYS AS IDENTITY.">bigserial</Term> still works, but `generatedAlwaysAsIdentity` is the shape to write in new code.

## The decision tree

You have the three shapes in hand. Now the part that actually transfers — the *order* an experienced engineer asks the questions in. The shapes are easy to look up; knowing which question to ask first, and what the answer rules out, is the skill. Walk the tree below one click at a time. Commit to an answer at each step before you read the next; the value is in feeling the questions narrow.

<StateMachineWalker title="Which primary key does this table want?">
  <Question id="exposed" prompt="Does this row's id ever leave the database?"
    description="A URL, an API response, a client payload — anywhere an outsider could see or hold the id.">
    <Branch label="Yes — it's exposed somewhere outside the DB" to="owned" />
    <Branch label="No — only the database ever joins on it" to="volume" />
  </Question>

  <Question id="owned" prompt="Is the id an immutable value the outside world already owns?"
    description="A country code, a currency code, an ISBN — fixed by an authority outside your app, never reissued.">
    <Branch label="Yes — it's externally owned and immutable" to="leaf-natural" />
    <Branch label="No — it's ours to mint" to="leaf-uuid" />
  </Question>

  <Question id="volume" prompt="High volume, internal-only, and no sharding planned?"
    description="An event log, analytics rows, a pure junction — rows nobody fetches by id, on a table that won't be sharded.">
    <Branch label="Yes — all three hold" to="leaf-bigint" />
    <Branch label="Not sure / it might get exposed later" to="leaf-uuid-doubt" />
  </Question>

  <Leaf id="leaf-natural" verdict="Natural key — text().primaryKey()">
    The one time a domain value is safe as the key: the world owns it and it never changes. No surrogate to mint — the value *is* the identity. Reserve this for small reference tables (countries, currencies).
  </Leaf>

  <Leaf id="leaf-uuid" verdict="UUIDv7 — uuid().primaryKey().default(sql`uuidv7()`)">
    Exposed but yours to mint, so you want a surrogate that is both non-enumerable *and* time-sortable. This is the day-one default for every user-facing entity — organizations, invoices, users.
  </Leaf>

  <Leaf id="leaf-bigint" verdict="identity bigint — bigint({ mode: 'number' }).primaryKey().generatedAlwaysAsIdentity()">
    Nobody fetches these rows by id, so spend the fewest bytes and keep the tightest index. The internal-counter reach — earned only when all three conditions hold.
  </Leaf>

  <Leaf id="leaf-uuid-doubt" verdict="When in doubt, UUIDv7">
    Exposure has a way of creeping in, and retrofitting a UUID onto an integer primary key *after* launch is painful. The cost of an unneeded UUID is small; the cost of being wrong the other way is a migration on a live table. Default to UUIDv7.
  </Leaf>
</StateMachineWalker>

That last leaf is deliberate, and it is the senior point of the whole tree. The default *has gravity*. UUIDv7 is where you land unless you have a specific, justified reason to step off it; `bigint identity` is an opt-in for a case you can defend, not a coin flip you make per table. When you genuinely can't decide, the tree decides for you — and it decides UUIDv7.

## Composite primary keys, for junction tables only

One mechanic is left, and it is the one I told you to resist reaching for. A primary key can span more than one column. You will need it later in this chapter, for the table that links invoices to tags, so meet the syntax now — and, more importantly, meet the hard boundary around it.

You declare a composite key in the third argument of `pgTable`, the callback that returns table-level constraints:

```ts title="db/schema.ts" {4}
export const invoiceTags = pgTable('invoice_tags', {
  invoiceId: uuid().notNull(),
  tagId: uuid().notNull(),
}, (t) => [primaryKey({ columns: [t.invoiceId, t.tagId] })]);
```

Here `primaryKey` is the standalone import from `drizzle-orm/pg-core`, the table-level helper — distinct from the `.primaryKey()` method you chain on a single column.

`primaryKey({ columns: [...] })` declares a key across the pair: the *combination* of `invoiceId` and `tagId` is what's unique and indexed, with the same three free guarantees as a single-column key. A given invoice can have many tags and a given tag can sit on many invoices, but each specific pairing appears at most once. That is exactly the identity a join row has.

Now the boundary, and it is a hard rule:

:::danger
Composite primary keys belong on junction tables, and almost nowhere else. On a junction the pair *is* the row's identity — there is nothing else to mint. On a first-class entity, a composite key is the modeling mistake from earlier: reach for a surrogate `id` plus a unique constraint instead.
:::

The difference is whether the row has an identity of its own. A junction row exists only to connect two things; "this invoice, that tag" is the whole of what it is. An entity — an invoice, an organization — is a thing in its own right, and a thing in its own right deserves its own opaque, stable id. The full two-foreign-key junction shape, and the moment you should promote a junction into a real entity that carries its own metadata, is the lesson on many-to-many junction tables, later in this chapter.

## Practice: pick the key for each table

The decision is the lesson, so the practice drills the decision before it drills the syntax. Two passes — first sort tables into strategies, then write the shapes.

Start with the cheap one. Sort each table's `id` column into the primary-key strategy it should use. Lean on the two questions in order: is the id exposed, and if not, is this a high-volume internal table?

<Buckets instructions="Sort each table's id column into the primary-key strategy it should use.">
  <Bucket name="uuid" label="UUIDv7" description="Exposed, ours to mint" />
  <Bucket name="bigint" label="identity bigint" description="Internal, high-volume" />
  <Bucket name="natural" label="natural key" description="Immutable, externally owned" />

  <Item bucket="uuid">An `invoices` row shown at `/invoices/:id`</Item>
  <Item bucket="uuid">A `users` row returned in an API response</Item>
  <Item bucket="uuid">An `organizations` row whose id is in every client payload</Item>
  <Item bucket="bigint">An append-only `audit_logs` row nobody fetches by id</Item>
  <Item bucket="bigint">A high-volume `analytics_events` row, internal-only</Item>
  <Item bucket="natural">An ISO-3166 country code (`'US'`)</Item>
  <Item bucket="natural">An ISO-4217 currency code (`'USD'`)</Item>
  <Item bucket="natural">A US state abbreviation (`'CA'`)</Item>
</Buckets>

Now write the shapes. The starter has three tables whose `id` (or `code`) columns are stubbed. Finalize each with the right canonical shape from the variants above: `organizations` is user-facing, `auditLogs` is internal and high-volume, and `countries` is a reference table the outside world owns. Note the sandbox passes explicit snake_case column-name strings — there is no `casing` client in the editor, so the names are written out, the same convention the earlier exercises in this chapter used.

:::note
The checker reads the **shape** of each column — its type, its `.primaryKey()`, and whether it carries a database-side default. That is enough to catch the two real mistakes here: leaving an `id` without a generated default, or keying `audit_logs` on the wrong integer type. It can't, however, see *which* default expression you wrote — `uuidv7()` and the v4 `gen_random_uuid()` look identical to it. So treat the prose above as the grader for that one: `organizations.id` wants `uuidv7()`, never `.defaultRandom()`. (The mini-Postgres running here predates the native `uuidv7()` function, so once your shape is right you'll see a green checklist alongside a "function uuidv7() does not exist" note — that's the sandbox, not your schema. On Neon's Postgres 18 it just works.) Reveal the answer below to check the exact shapes.
:::

<DrizzleSchemaCoding
  instructions="Finalize each id with the primary-key strategy that table wants. organizations is user-facing — its id rides in every URL and API response. auditLogs is internal and high-volume — nobody ever fetches a row by its id. countries.code is an ISO-3166 value the outside world owns and never reissues. Write the canonical Drizzle shape for each; the column names are spelled out in snake_case because there's no casing client in scope."
  maxHeight={400}
  starter={`export const organizations = pgTable('organizations', {
  id: uuid('id').primaryKey(), // TODO: this id appears in URLs and API responses
  name: text('name').notNull(),
});

export const auditLogs = pgTable('audit_logs', {
  id: integer('id').primaryKey(), // TODO: internal, high-volume, never fetched by id
  action: text('action').notNull(),
});

export const countries = pgTable('countries', {
  code: text('code').primaryKey(), // TODO: ISO-3166, e.g. 'US' — already correct?
  name: text('name').notNull(),
});
`}
  requirements={[
    {
      name: 'organizations',
      columns: [
        { name: 'id', type: 'uuid', primaryKey: true, hasDefault: true },
        { name: 'name', type: 'text', notNull: true },
      ],
    },
    {
      name: 'audit_logs',
      columns: [
        { name: 'id', type: 'bigint', primaryKey: true, hasDefault: true },
        { name: 'action', type: 'text', notNull: true },
      ],
    },
    {
      name: 'countries',
      columns: [
        { name: 'code', type: 'text', primaryKey: true },
        { name: 'name', type: 'text', notNull: true },
      ],
    },
  ]}
/>

<details>
<summary>Reveal the answer</summary>

```ts title="db/schema.ts"
export const organizations = pgTable('organizations', {
  // Exposed, ours to mint → UUIDv7. The SQL-side default fires for migrations,
  // seeds, and psql too — not just inserts that go through Drizzle.
  id: uuid('id').primaryKey().default(sql`uuidv7()`),
  name: text('name').notNull(),
});

export const auditLogs = pgTable('audit_logs', {
  // Internal, high-volume, never enumerated by an outsider → identity bigint.
  // 8 bytes, a tighter index, and the id never crosses a system boundary.
  id: bigint('id', { mode: 'number' }).primaryKey().generatedAlwaysAsIdentity(),
  action: text('action').notNull(),
});

export const countries = pgTable('countries', {
  // Immutable, externally owned → the natural key is correct as-is. Don't
  // "fix" it into a surrogate; the ISO code already names the row, forever.
  code: text('code').primaryKey(),
  name: text('name').notNull(),
});
```

`organizations` is the day-one default — a UUIDv7 surrogate, minted SQL-side. `auditLogs` earns the identity bigint because all three conditions hold: internal, high-volume, and no id ever leaves the database. `countries` was already right: a small reference table the world owns is the one place a natural key belongs.

</details>

## Where to read more

The two-version UUID story is moving fast enough that a current, primary source is worth a look. The following links cover the native Postgres 18 support and the locality argument in more depth than this lesson needs to.

<CardGrid>
  <ExternalResource
    title="PostgreSQL 18 UUIDv7 support"
    href="https://neon.com/postgresql/postgresql-18/uuidv7-support"
    icon="simple-icons:neon"
    iconColor="#00E599"
    description="Neon's explainer on the native uuidv7() function, with the v4-scatter-vs-v7-locality argument in depth."
  />
  <ExternalResource
    title="PostgreSQL 18 release announcement"
    href="https://www.postgresql.org/about/news/postgresql-18-released-3142/"
    icon="simple-icons:postgresql"
    iconColor="#336791"
    description="The primary source: the official release note introducing native uuidv7() generation."
  />
  <ExternalResource
    title="UUIDv7 generator & decoder"
    href="https://www.authgear.com/tools/uuidv7-generator/"
    icon="lucide:fingerprint-pattern"
    iconColor="#7C3AED"
    description="Mint a UUIDv7 and watch the tool pull the timestamp back out — the byte layout figure, made interactive."
  />
  <ExternalResource
    title="RFC 9562 — UUID specification"
    href="https://datatracker.ietf.org/doc/html/rfc9562"
    icon="lucide:scroll-text"
    iconColor="#11556E"
    description="The 2024 IETF standard itself; Section 5.7 is the formal UUIDv7 layout this lesson summarized."
  />
</CardGrid>

You have now closed the loop the chapter opened in its second lesson: every `id` slot has a shape, and you can defend each one. From here the schema starts connecting tables to each other — the next lesson is foreign keys, where you finally write the `.references()` that points one row's column at another row's primary key, and decide what happens to the children when the parent is deleted.
