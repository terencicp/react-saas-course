---
// One step inside <AnnotatedCode>. Holds a fenced code block (with EC's native
// line/regex highlights) plus the prose explaining it. The parent toggles
// visibility via [data-active].
---

<div class="ac-step">
  <slot />
</div>
