---
// Inline term with a dashed underline and a hover/focus tooltip showing a short definition.
// Use inside prose for vocabulary the reader can probe in place instead of being sent to a
// separate paragraph or glossary entry.
interface Props {
  definition: string;
}
const { definition } = Astro.props;
---

<span class="term-mark" data-tip={definition} tabindex="0"><slot /></span>

<style is:global>
  .term-mark {
    cursor: help;
    border-bottom: 1px dashed var(--sl-color-gray-3);
    padding: 0 1px;
    margin: 0 -1px;
    border-radius: 2px;
    transition: background-color 0.12s, border-color 0.12s;
  }
  .term-mark:hover,
  .term-mark:focus-visible {
    background: color-mix(in srgb, var(--sl-color-accent) 22%, transparent);
    border-bottom-color: var(--sl-color-accent);
    outline: none;
  }

  .term-tip {
    position: fixed;
    top: 0;
    left: 0;
    background: #1f2937;
    color: #f3f4f6;
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
    padding: 7px 10px;
    border-radius: 6px;
    font: 0.875rem / 1.55 var(--sl-font, ui-sans-serif, system-ui, sans-serif);
    white-space: pre-wrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.12s;
    z-index: 1000;
    max-width: min(420px, calc(100vw - 24px));
    width: max-content;
  }
  .term-tip.is-visible { opacity: 1; }
</style>

<script>
  import { computePosition, flip, shift, offset } from '@floating-ui/dom';

  let tip: HTMLElement | null = null;
  let activeMark: HTMLElement | null = null;

  function ensureTip(): HTMLElement {
    if (!tip) {
      tip = document.createElement('div');
      tip.className = 'term-tip';
      tip.setAttribute('role', 'tooltip');
      document.body.appendChild(tip);
    }
    return tip;
  }

  async function showTooltip(mark: HTMLElement) {
    activeMark = mark;
    const t = ensureTip();
    t.textContent = mark.dataset.tip || '';
    t.classList.add('is-visible');
    const { x, y } = await computePosition(mark, t, {
      placement: 'top',
      strategy: 'fixed',
      middleware: [offset(8), flip(), shift({ padding: 8 })],
    });
    if (activeMark === mark) {
      t.style.transform = `translate(${Math.round(x)}px, ${Math.round(y)}px)`;
    }
  }

  function hideTooltip() {
    activeMark = null;
    tip?.classList.remove('is-visible');
  }

  document.querySelectorAll<HTMLElement>('.term-mark').forEach((el) => {
    el.addEventListener('mouseenter', () => showTooltip(el));
    el.addEventListener('mouseleave', hideTooltip);
    el.addEventListener('focus', () => showTooltip(el));
    el.addEventListener('blur', hideTooltip);
  });
</script>
