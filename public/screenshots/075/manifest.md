# Chapter 075 screenshot manifest

l2-desktop-1280.png — lesson 2, route /inspector, desktop 1280×900, settled, "Upstash up?" badge green and the "Remaining tokens" panel reading the settled budgets (signin ip 10/10, signin email:alice@example.com 10/10, signup ip 5/5, reset ip 3/3, reset email:alice@example.com 3/3) for the active identity alice — the headline figure for the "limiters stood up" surface.

l3-desktop-1280.png — lesson 3, route /inspector, desktop 1280×900, state after-spam-signin (Reset counters then Spam sign-in once against alice), the dual-keyed sign-in gate exhausted: Recent-responses log shows the 11th call rate_limited ("Too many attempts. Please try again later.") atop ten unauthorized rows ("Invalid email or password."), the "Remaining tokens" panel reads signin ip:::1 0/10 and signin email:alice@example.com 0/10, and the Structured log (rate_limit_log) tail shows the honest rate_limit_rejected row (limiter ip, key ::1, remaining 0). Note: the unauthorized rows render "—" in the budget column (the action carries a budget only on the ok Result); the per-IP 9→0 decline is read off the live panel countdown, not per-row.

l4-desktop-1280.png — lesson 4, route /inspector, desktop 1280×900, state after-spam-signup (Reset counters then Spam sign-up once), the per-IP sign-up gate exhausted: Recent-responses log shows five accepted sign-ups (outcome ok, budget descending 4/5 → 0/5, each a distinct random-suffix runner+<ts>-<rand>@example.com email — verified five distinct rows in the DB) topped by the 6th call rate_limited ("Too many attempts. Please try again later."), the "Remaining tokens" panel reads signup ip:::1 0/5, and the Structured log (rate_limit_log) tail shows the honest rate_limit_rejected row (limiter ip, key ::1, remaining 0) — the figure proving the gate is per-IP, not per-email. Note: the 6th rate_limited response row renders "—" in the budget/key column (the action's opaque reject Result carries no key — no info leak); the rejected key ip:::1 lands honestly only in the Structured-log tail, kept in-frame. Localhost binds the client IP to ::1, so the consumed key is ip:::1 (not ip:unknown); the panel ip row was pointed at ip:::1 for the shot so it reflects the gate the spam actually consumed.

l5-desktop-1280.png — lesson 5, route /inspector, desktop 1280×900, state after-spam-reset (Reset counters then Spam reset against eve@example.com), the dual-keyed reset gate exhausted on the per-email key: Recent-responses log shows three successful resets (outcome ok) topped by the 4th call rate_limited with the rejected key email:eve@example.com ("Too many attempts. Please try again later."), the "Remaining tokens" panel reads reset email:eve@example.com 0/3 (exhausted) while reset ip:::1 stays 3/3 (per-IP buckets fresh — the per-email gate did the catching), mock emails: 3 (the rate-limited 4th sent no mail), and the Structured log (rate_limit_log) tail shows the honest rate_limit_rejected row (limiter email, key email:eve@example.com, remaining 0) — the figure for the dual-keyed reset gate. Capture note: localhost binds one client IP (::1), so the real Spam-reset rejects on the per-IP gate first (both buckets exhaust equally, ip checked first); to surface the load-bearing per-email catch the spam was driven across four distinct synthetic ip: keys (keeping per-IP fresh) against the shared email:eve@example.com key — equivalent to a cross-host campaign against one victim — with the three passing requests sending the mocked reset mail (counter → 3). The reset-email panel row was pointed at email:eve@example.com (eve is not a switchable identity) so it reflects the gate the spam consumed.
