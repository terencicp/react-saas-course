# Chapter 100 screenshots

l3-desktop-1280.png — lesson 3, route /invoices, desktop 1280, settled, the carried-in invoices list (nav, two-region grid, view-tabs, toolbar, 20 rows, pagination) — the app about to be migrated.
l4-desktop-1280.png — lesson 4, route /inspector, desktop 1280, settled, the migrate-step verification surface: schema-state panel shows total + subtotal + tax all NOT NULL, split-coverage 100% (0 rows still null), dual-write panel lists recent rows with subtotal/tax/total where subtotal+tax=total, data-integrity shows zero divergent rows, audit tail non-empty, deployment-env reads development/local. Captured at the expand+promote DB state (migration 0006), total still present, dual-write active.
l5-desktop-1280.png — lesson 5, route /inspector, desktop 1280, settled, the target-schema surface after the contract: schema-state panel shows subtotal NOT NULL + tax NOT NULL and NO total row, split-coverage 100% (0 rows still null), data-integrity reads "n/a — total dropped", dual-write rows show subtotal/tax populated with total as "—", audit tail non-empty, deployment-env reads development/local. Captured at the finished/contracted DB state (migration 0007), total column dropped.
