# Chapter 108 screenshots

l2-desktop-1280.png — lesson 2, route /invoices, desktop 1280, settled, the production invoice list (left) beside the live chat rail (right): the "Daily token budget 0 / 100,000" panel above the "Ask your invoices" card with the interactive smoke-test useChat client — enabled textarea and Send button — the surface that proves the streaming /api/chat route is live and wired to its first client. data-testid="invoice-chat".

l5-desktop-1280.png — lesson 5, route /invoices, desktop 1280×900, settled, LIGHT theme, the finished typed chat surface: invoice list (left) beside the assembled right rail (right) — the "Daily token budget" usage panel, and the "Ask your invoices" card (now self-start, sized to its content, no longer stretched to the full list height) showing a "You" question plus an "Assistant" turn rendering the getInvoiceStats stats-card skeleton in its card-shaped loading state (statically composed, deterministic, no live model call), with the chat input + Send below. data-testid="invoice-chat", data-testid="token-usage-panel", data-testid="invoice-stats-skeleton".

l5-mobile-390.png — lesson 5, route /invoices, mobile 390×844 (captured at full content height so the stacked rail is in frame), settled, LIGHT theme, the same finished chat surface on mobile where the lg:grid-cols-[2fr_1fr] rail stacks under the invoice list: the list on top, then the usage panel, the "Ask your invoices" card with the You question and the Assistant stats-card skeleton in its loading shape, and the input + Send at the bottom. Shows the responsive single-column stack. data-testid="invoice-chat", data-testid="token-usage-panel".
