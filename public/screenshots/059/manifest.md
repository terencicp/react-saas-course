# Chapter 059 — screenshot manifest

l2-desktop-1280.png — lesson 2, route /inspector, desktop 1280, settled (signed in as Alice, owner of Acme), the active-org layer resolved: banner shows the active org NAME (Acme) + role (owner), the org switcher (Acme) and acting-user switcher (Alice/Bob/Carol) are populated, and all five inspector panels render.
l4-desktop-1280.png — lesson 4, route /inspector, desktop 1280, settled (acting as Bob, admin of Acme), the finished members + audit surface: members panel lists Alice/Bob/Carol each with a per-row role-select (rendered to every row — owner's has no settable value), and the audit tail shows the seeded member.role-changed row (auditLogs count 1).
l5-desktop-1280.png — lesson 5, route /inspector, desktop 1280, settled (acting as Bob, admin of Acme), the invite + pending surface: the invite panel renders the email input, the role select (member/admin only — no owner), and the Send invitation button; the pending-invitations panel shows the seeded pending invite (newcomer@acme.test · member) with its dev Copy-accept-URL button.
l6-desktop-1280.png — lesson 6, route /accept-invite?id=invitation_acme_pending&token=…&sig=…, desktop 1280, settled (signed in as the invited identity newcomer@acme.test), the accept-invite decision screen branch (A): the consent card shows "Join Acme", "Bob invited you to Acme as member", and the explicit Accept-invitation button (accept-button) — the decision/consent gate, no auto-accept on GET; the invite is still pending.
