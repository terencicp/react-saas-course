# Chapter 065 screenshot manifest

l1-desktop-1280-pro-state.png — lesson 1 (project overview), route /inspector, desktop 1280, state finished-pro end-state for Acme (direct DB write: org.stripeCustomerId populated, plan_entitlements pro/active with subscriptionId sub_1QtAcmeProMonthly + currentPeriodEnd 2026-12-31, one checkout.session.completed processed_events row), the "what you'll have built" figure: header shows Customer cus_QtAcmePro001 with the Manage billing Portal button ENABLED, entitlement panel reads plan `pro`/status `active`, processed events tail shows the landed checkout.session.completed.
l4-desktop-1280-forced-pro.png — lesson 4, route /inspector, desktop 1280, state forced-pro (Force entitlement status → pro/active), the projected entitlement surface: panel reads plan `pro`, status `active`, populated subscriptionId/currentPeriodEnd/seats, with the audit tail showing the baseline transition row.
l5-desktop-1280.png — lesson 5, route /inspector, desktop 1280, state settled (seeded free, Alice owner of Acme), the billing-interface surface: header + Upgrade to Pro / Upgrade to Team Checkout buttons + disabled "Manage billing" Portal button (Customer null) + entitlement panel at plan `free`, status `active`, seats 1.
l5-desktop-1280-pro-only-gate.png — lesson 5, route /inspector/pro-only, desktop 1280, state settled (seeded free), the requirePlan gate fallback: "Upgrade to Pro" card reading "This area requires the Pro plan. Upgrade to continue." with a Back-to-the-inspector button — requirePlan('pro') throwing BillingError against the free row.
