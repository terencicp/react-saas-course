# Chapter 047 screenshot pool

l2-desktop-1280.png — lesson 2, route /invoices/new, desktop 1280, settled (light theme), the create-invoice form: every labeled field (Customer native-select with "Select a customer" placeholder, Number, Status select defaulting to draft, Total, Issued, Due, Currency defaulting to USD), the "Simulate failure" checkbox, and the "Create invoice" submit button, rendered against the seeded customers. Serves Lesson 2's "labeled form" figure.

l2-desktop-1280-field-errors.png — lesson 2 / lesson 1 overview, route /invoices/new, desktop 1280, field-errors state (dark theme), the create form re-rendered after an invalid submit: the Customer select left at "Select a customer" carries a red invalid border with the "Invalid UUID" message beneath it, while Number (INV-09001), Total (1499.00), Issued, Due, and Currency (USD) keep their typed values. Serves the Lesson 1 overview "field errors" tab.

l4-desktop-1280.png — lesson 4, route /invoices/[invoiceId], desktop 1280, settled (light theme), the complete invoice-detail surface for seeded Acme invoice INV-00003: the read-only detail panel (number, status badge, customer, email, issued/due dates, total in USD, line items), the prefilled "Edit invoice" form (every field's defaultValue set from the invoice), the "Save changes" button, the "Delete" dialog trigger, and the inline "Delete invoice" no-JS fallback button. Serves Lesson 4's detail figure AND the deferred Lesson 3 "prefilled edit form" figure.

l4-desktop-1280-delete-dialog-open.png — lesson 4, route /invoices/[invoiceId], desktop 1280, delete-dialog-open (light theme), the open Radix confirmation dialog over the dimmed detail surface: title "Delete invoice INV-00003?", the "This permanently removes the invoice and its line items. This cannot be undone." copy, the close (X) control, and the "Cancel" + destructive "Delete" buttons. Serves Lesson 4's dialog figure.

l5-desktop-1280.png — lesson 5, route /invoices, desktop 1280, settled (light theme), the optimistic-create surface with form and list on one page: the inline "New invoice" create form at the top (Customer select, Number, Status defaulting to draft, Total, Issued, Due, Currency, the "Simulate failure" checkbox, and the "Create invoice" button) followed by the seeded Acme invoice rows below (number, customer, status, total in USD, due date), plus the top-right "New invoice" link to the standalone route. Serves Lesson 5's list figure (the same-page form + list the useOptimistic flow targets).

l5-desktop-1280-optimistic-pending.png — lesson 5 / lesson 1 overview, route /invoices, desktop 1280, optimistic-pending state (dark theme), the optimistic add mid-flight: the inline create form filled (INV-OPT-1, $2,500.00) with a pending "Create invoice" button, and a dimmed (opacity-60) pending row at the very top of the list — INV-OPT-1 with a spinner icon and "—" placeholders for customer/due — painted before the server responds, above the seeded invoice rows. Serves the Lesson 1 overview "optimistic add" tab.

l6-desktop-1280.png — lesson 6, route /invoices?deleted=INV-00042, desktop 1280, settled (light theme), the transactional-delete success surface for a just-deleted invoice: the server-rendered `deleted-banner` ("Invoice INV-00042 deleted", role=status) below the H1 and the JS-enhanced Sonner toast ("Invoice INV-00042 deleted", check icon) at bottom-right, over the inline create form and the seeded Acme invoice rows. Success state flows through the URL `?deleted` param (the SSR banner survives no-JS, the toast is the enhancement). Serves Lesson 6's toast figure.
