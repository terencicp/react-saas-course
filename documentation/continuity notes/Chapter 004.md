# Chapter 004 — Typing values you know

## Lesson 1 — Primitives, literals, and the four corners

- **Taught** — seven TS primitives anchored to `typeof`; literal types as primitives narrowed to one value; literal unions as the senior reach for finite domains; the four corners (`any`/`unknown`/`never`/`void`) with one trigger each; widening on object properties; `unknown` requires narrowing; `never` is what makes exhaustiveness work; `void` means caller ignores return; `enum` non-reach; branded primitives tease.
- **Cut** — outline's third `ExternalResource` swapped (Total TypeScript Essentials chapter + ivov.dev set-theory piece used instead of the originally-listed Pocock `any/unknown/never` writeup); two `VideoCallout` embeds added (Andrew Burgess on `any`/`unknown`/`never`, Matt Pocock on `enum`) not in outline.
- **Debts** — `as const` and `satisfies` (lesson 7); object property widening fix (lesson 7); narrowing surface (lesson 6); `assertNever` / exhaustiveness (chapter 005 lesson 3); branded IDs via `unique symbol` (chapter 005 lesson 4); discriminated unions (lesson 5 + chapter 005 lesson 1); Zod boundary parse (chapter 009 lesson 1); Biome `noExplicitAny` (chapter 024 lesson 5). Refers back to chapter 001 (`typeof null === 'object'`, `const` binds the name, IEEE-754 threshold for `bigint`).
- **Terminology** — "four corners" (the `any`/`unknown`/`never`/`void` set); "four-corners lattice" (visual mental model: `any`/`unknown` at top, `never` at bottom, `void` off-lattice); "inhabitants" (`Term` tooltip — runtime values satisfying a type); "widening" (`Term` tooltip — inferred literal broadened to base primitive in reassignable positions); "the senior reach"; "trigger that earns" (each corner has one trigger); "sound top" (`unknown`) / "unsound escape" (`any`) / "the bottom" (`never`) / "function-return-only" (`void`); rule phrasing: "if the runtime values are finite and known at design time, the type is a literal union, not the primitive."
- **Patterns and best practices** — never write `any`; accept third-party `any` as `unknown` and narrow; reach for literal unions over `string`/`number`/`boolean` for finite known domains; use `unknown` at every external-data boundary (JSON, localStorage, `catch`, untyped SDKs); don't write `Promise<void>` for "no payload" (use `Promise<undefined>` or let inference handle it); write literal unions, not `enum`, in fresh code.
- **Misc.** — Lesson uses arrow functions (`const fn = () => {}`) throughout, not `function` declarations — chapter 002 convention to maintain. Lesson-specific component at `src/components/lessons/004/1/FourCornersLattice.astro`. The closing `Buckets` exercise (8 items, 6 buckets) is the canonical vocabulary test for the chapter's posture shift from "is this a string?" to "what does the string represent?"

## Lesson 2 — Object shapes: type, interface, and field modifiers

- **Taught** — `type` is the default for every alias (objects, primitives, tuples, unions, intersections, generics, conditional, mapped); `interface` earns one trigger — declaration merging on third-party module types; the three non-differences (`extends` vs `&`, semicolons, perf) dispatched once; `?` (optional) vs `string | undefined` vs `?: string | undefined` runtime distinction (`in`, `Object.keys`, `JSON.stringify` behavior); `exactOptionalPropertyTypes` named as the flag that enforces the distinction; `readonly` locks binding not value (field-level analogue of `const`); `readonly T[]` for array immutability (forbids `.push`/`.pop`/`.shift`/`.unshift`/`.splice`/`.sort`/`.reverse`/index-write); `Readonly<T>` as the per-field shorthand (shallow); excess property checks at literal sites vs variable-flow; fix is type the source variable, not `as`.
- **Cut** — none significant; outline's plan landed in full. Five separate `MultipleChoice` cards used (as outline's reworked instruction specified) rather than one multi-select. Single `VideoCallout` added (basarat on `?` vs `| undefined`) — not in outline but pedagogically aligned.
- **Debts** — `as const` for deep/literal-config immutability (lesson 7); `as` as smell (lesson 6); module augmentation full pattern via `declare module` (chapter 006 lesson 4); `exactOptionalPropertyTypes` in full strict tsconfig (chapter 024 lesson 5 — note: lesson body says "lesson 5 of chapter 24" matching outline's "chapter 024 lesson 5"); utility-type surface `Pick`/`Omit`/`Partial`/etc. (chapter 005 lesson 6); dynamic keys / index signatures / `Record<K, V>` (lesson 3 — note: lesson body refers to "next lesson" which is lesson 3, but chapter outline scopes dynamic keys to lesson 4; the lesson 3 outline owns tuples). Refers back to chapter 001 lesson 6 (`const` binds the name, not the value) and lesson 1 of chapter 004 (typo/`any` bugs as paired vocabulary failures).
- **Terminology** — "alias" (`Term` tooltip — name bound to type expression, erased at compile time, structural underneath); "declaration merging" (`Term` tooltip — two same-name declarations in same scope combine, only `interface` allows it); "`exactOptionalPropertyTypes`" (`Term` tooltip — flag enforcing absent-vs-`undefined` distinction); "binding is locked, the value is not" (the field-level `readonly` rule); "the readonly that wasn't" (intro framing for the nested-mutation bug); "two locks, two layers" (field-`readonly` + `readonly T[]` quiz framing); "friendly error" (excess-property check at literal site); "the variable-flow loophole" (excess-property check bypass via inferred-type assignment).
- **Patterns and best practices** — default to `type` for every alias in the codebase; reserve `interface` strictly for `declare module` augmentation of third-party types; under `exactOptionalPropertyTypes` write `?` for absent-or-typed fields and `| undefined` only when API contract requires the key present with `undefined`; don't combine `?` and `| undefined` unless intentional; `readonly` fields by default for row-shaped types (UUIDs, timestamps, fields never reassigned in app code); type arrays as `readonly T[]` whenever caller shouldn't mutate; combine `readonly field: readonly T[]` for full lock; never silence excess-property errors with `as` — type the source variable instead; compose with `&` (not `extends`) since it works for unions too.
- **Misc.** — Lesson stays on objects with **known field names** only; dynamic keys explicitly deferred to "next lesson" (the lesson body says next; the chapter outline plans it for lesson 4 with tuples in lesson 3 — future writers should clarify which lesson owns dynamic keys before lesson 3 ships). Five sequential single-correct MCQ cards (id/email/name/lastSeenAt/overrides) constitute the canonical modifier-matrix test for the chapter. Uses `AnnotatedCode` for the `readonly` walkthrough (three steps: declaration, two failed reassignments, the mutation that slips through). Uses `CodeVariants` for four contrasts: codebase-uses-both intro, two-`type` vs two-`interface`, three `?`/`| undefined` forms, mutable vs immutable array.

## Lesson 3 — Tuples: positions with labels

- **Taught** — Tuple syntax `[T1, T2, ...]` with fixed length and per-position types vs. `(T1 | T2)[]` array contrast; tuples erase to plain arrays at runtime; element labels `[name: type, ...]` as documentation surfaced in tooltips/autocomplete (all-or-nothing, no type-compat effect); `readonly` tuple prefix forbids index-write and mutation methods; `as const` on inline array literal recognized (not authored) as readonly literal-tuple; optional positions `?` and rest positions `...T[]` named for library recognition (non-final rest legal since TS 4.2); the three sites tuples earn their weight — (1) custom hook returns ≤2 elements with destructure-and-rename, (2) `Object.entries`/`Map.entries`/`Set.entries`/`Array.entries` pair iteration, (3) Go-style `[error, value]` read-only recognition; flip rule — past three positions or no destructure-and-rename, reach for an object.
- **Cut** — None significant; the outline landed in full. External resources expanded from 3 to 4 (TS Handbook tuples, TS 4.0 labeled tuples release notes, Total TypeScript `as const` workshop, oida.dev custom-hooks tuples).
- **Debts** — `as const` full treatment (lesson 7); `satisfies` (lesson 7); discriminated `Result<T>` shape seed (lesson 5) and full build (chapter 008 lesson 1); narrowing rules including truthy-on-`err` (lesson 6); index signatures and `Record<K, V>` (lesson 4); `Object.entries` literal-keyed typing via `keyof`/`typeof` (chapter 005 lesson 5); explicit-annotation-vs-inference and `import type` discipline (lesson 8); custom hook authoring rules — `use*` prefix lint, dep arrays (Unit 3); variadic tuple types and tuple manipulation utilities (out of course scope). Refers back to chapter 003 (arrays, `for...of`), chapter 002 (destructuring), lesson 2 of chapter 004 (`readonly T[]`, `?` on optional fields), lesson 1 of chapter 004 (literal unions, four corners).
- **Terminology** — "labeled tuple" (`Term` tooltip — names at type level, tooltip/autocomplete only, no type-compat impact); "destructure-and-rename" (`Term` tooltip — the operation that makes positional shapes work; the reason `useState` returns a tuple); "the three-position rule" / "three positions is the rough boundary"; "the three sites tuples earn their weight"; "the flip rule" (when to reach for an object); "ordered → tuple; named → object" (the project hook-return convention); "fixed-then-rest" (canonical rest-position shape); the bug-anchor phrasing "position is fragile" / "five-position landmine"; phrase "the call-site naming **is** the API" for hook tuple returns.
- **Patterns and best practices** — Default to objects for return shapes; reach for a tuple only at the three named sites; cap tuple hook returns at two elements (past two → object, e.g. `useForm` returns named fields); label every position on any tuple past length 2 (all-or-nothing); annotate exported hook returns explicitly as `readonly [label: T, ...]` rather than relying on `as const` inference when the hook is shared across files; `as const` form acceptable for file-local hooks; for new code prefer discriminated `Result<T>` over Go-style `[error, value]`; iterate `Object.entries`/`Map.entries` with destructuring in the `for...of` head; accept `Object.entries` key widening to `string` for iteration, reach for `Record<LiteralUnion, V>` when typed-key matters at access; never compose optional+rest past two non-fixed elements — switch to object.
- **Misc.** — Lesson resolves the lesson 2 continuity ambiguity: dynamic keys belong to **lesson 4**, tuples to **lesson 3** (matches the chapter outline). Custom hook examples used: `useToggle` (canonical 2-tuple, shown in body) and `useDisclosure` (3-tuple, used in `TypeCoding` exercise — two valid solutions accepted: explicit labeled annotation or `as const`). Hook example bodies use a stubbed `declare function useState<T>(initial: T): [T, (next: T) => void]` to keep the exercise type-only. One `MultipleChoice` (decision test, four options: user-card object/`useToggle` tuple/5-number "coordinates"/`parseDate` Result shape — correct is `useToggle`). `CodeVariants` used for two contrasts: tuple-vs-array syntax, unlabeled-vs-labeled five-tuple. No lesson-specific components added. Closes with the line that `useState` recognition is the seed for future custom-hook authoring in Unit 3.

## Lesson 4 — Dynamic keys: index signatures and Record<K, V>

- **Taught** — The third object shape (objects with dynamic keys, distinct from named-field objects and tuples); the two forms `{ [key: string]: V }` (index signature) and `Record<K, V>` (utility type), interchangeable when `K = string`; index-signature `[keyName: KeyType]: ValueType` parts — keyName is pure documentation, KeyType is the constraint; course prefers `Record<string, V>` for legibility, reserves index-signature syntax for mixed shapes; `Record<LiteralUnion, V>` earns the completeness check (missing-key error at the literal site, same posture as excess-property checks); `noUncheckedIndexedAccess` divergence — open-keyed reads return `V | undefined`, `Record<LiteralUnion, V>` reads return `V` directly; mixed shapes (named fields + index signature) — every named field's type must be assignable to the index signature's value type; senior reach for mixed shapes is to split into a named-field section and a `Record`-typed metadata field; existence checks — `!== undefined` on a captured read narrows to `V`, `in` operator tests runtime existence but does **not** propagate as type narrowing across an index signature (subsequent read still types `V | undefined`).
- **Cut** — None significant. Outline plan landed in full; one `VideoCallout` added (Matt Pocock, "Dynamic objects should NOT be this hard") not in the outline but aligned with the senior call.
- **Debts** — Full narrowing surface — `typeof`, `instanceof`, `Array.isArray`, discriminant equality, custom type predicates (lesson 6); `as const` and `satisfies` for typed-config patterns (lesson 7); unions/intersections including the `string | number` used in mixed-shape value type (lesson 5); annotation-vs-inference rule and `import type` discipline (lesson 8); full strict tsconfig build-out including `noUncheckedIndexedAccess` reaffirmation (chapter 024 lesson 5); mapped types (chapter 005 lesson 6); `keyof`/`typeof` operators (chapter 005 lesson 5); branded IDs (chapter 005 lesson 4); discriminated unions (lesson 5 + chapter 005 lesson 1); Drizzle row return shapes as `Record`-typed surfaces (Unit 5); Better Auth session and `next-intl` messages via module augmentation (chapter 006 lesson 4). Refers back to chapter 003 (the strict tsconfig `noUncheckedIndexedAccess` already on), lesson 1 of chapter 004 (literal unions, `unknown`), lesson 2 of chapter 004 (named-field shapes, excess-property checks at literal sites), lesson 3 of chapter 004 (tuple labels' all-or-nothing documentation rule).
- **Terminology** — "index signature" (`Term` tooltip — object-type member declaring type of every dynamic key, `{ [keyName: KeyType]: ValueType }`); "`Record<K, V>`" (`Term` tooltip — utility type for K-keyed V-valued objects; with `K = string` equivalent to index signature, with `K` a literal union requires every key present); "`PropertyKey`" (`Term` tooltip — `string | number | symbol`, the built-in for any valid object key); "the third object shape" (positions dynamic-keyed objects alongside named-field objects and tuples); "completeness check" (the missing-key error `Record<LiteralUnion, V>` fires at the literal site); "the divergence" (the `noUncheckedIndexedAccess` `V | undefined` vs `V` split between open and finite keys); rule phrasing: "finite keys, `Record<LiteralUnion, V>`; open keys, `Record<string, V>` or the index signature"; "the index signature's value type is a ceiling for every named field on the same object" (mixed-shape constraint).
- **Patterns and best practices** — Default to `Record<string, V>` over the bracket index-signature syntax for legibility; reach for the index-signature form only when the mixed-shape case demands it (named fields coexisting with a dynamic surface); reach for `Record<LiteralUnion, V>` whenever the keys are finite and known at design time (statuses, methods, locales, permission keys); type the key variable as the literal union, not as `string`, to preserve the read-site `V` (typing as `string` reverts to `V | undefined`); narrow open-keyed reads with `!== undefined` on a captured `const` (not `in`, which doesn't propagate); for mixed shapes prefer splitting into `{ name: string; metadata: Record<string, ...> }` rather than authoring an index-signature-on-named-fields type; treat the compile errors a growing literal union creates in every `Record<LiteralUnion, V>` initializer as a refactor feature, not a chore; webhook/wire JSON payloads default to `Record<string, unknown>` (open keys + values unknown until parsed); Drizzle lookup tables default to `Record<string, Row>` (UUID keys are open, value comes from `$inferSelect`).
- **Misc.** — Lesson **does not** use Twoslash machinery — `^?` annotations appear as plain trailing comments naming the inferred type. Uses `@ts-expect-error` line comments to mark intentional errors in the `AnnotatedCode` walkthrough (set the chapter precedent for narrating compile errors inline rather than via prose). `AnnotatedCode` walkthrough centers the completeness-check section (three steps, red/orange/green colors). `CodeVariants` used twice (two-forms section and the `noUncheckedIndexedAccess` divergence section). `CodeTooltips` used once on the mixed-shape `Code` block. `Buckets` exercise (6 items, 2 columns) closes the lesson — open vs finite, with two subtler items (webhook → `Record<string, unknown>`, Drizzle UUID lookup → `Record<string, Row>` both go in the open bucket for different reasons). External resources: 4 cards (TS Handbook index signatures, TS Handbook `Record`, Total TypeScript `noUncheckedIndexedAccess`, dmitripavlutin index signatures) — exceeds the 3-card chapter pattern but stays within outline's allowance. No lesson-specific components added.

## Lesson 5 — Composing types: unions and intersections

- **Taught** — The two composition operators `|` (union) and `&` (intersection) installed via set-theoretic intuition (`|` grows the inhabitant set; `&` on shapes shrinks values but grows the field set); the four union shapes (literal, mixed-primitive, shape, nullable) with the read-rule that only fields common to every variant are readable without narrowing; the **shape-union access rule** with the three-response contrast (widen-wrong / assert-wrong / narrow-right); intersection on shapes as field composition with three production sites (request payload composition, third-party type extension, discriminated-union variants); `'red' & 'blue'` and `string & number` are `never` (one-line); the **discriminated union** shape seeded — union of object variants each carrying a literal-typed discriminant; flag-boolean payload vs discriminated-union contrast (`r.data!.name` smell → discriminant narrowing).
- **Cut** — None significant. Outline plan landed in full. Diagram delivered as a lesson-specific `UnionIntersectionVenn` component rather than inline SVG inside `<Figure>` (outline allowed either). Two `VideoCallout`s added (Andrew Burgess on set-theory frame, Web Dev Simplified on discriminated unions) not in outline.
- **Debts** — Full narrowing surface including `typeof`, `in`, `instanceof`, `Array.isArray`, discriminant equality, custom predicates (lesson 6); the three legitimate triggers for `as` and `!` (lesson 6); `assertNever` and exhaustiveness check (lesson 6 + chapter 005 lesson 3); `as const` for inline-literal discriminants that widen (lesson 7); full discriminated-union surface — fetch results, form states, command/event payloads, `Result<T, E>` (chapter 005 lesson 1); branded IDs as `string & { __brand: unique symbol }` intersection (chapter 005 lesson 4); utility types `Pick`/`Omit`/`Partial`/etc. (chapter 005 lesson 6); generic constraints `T extends U` (chapter 005 lesson 7); declaration merging full pattern (chapter 006 lesson 4 — referenced as the alternative tool for module-wide augmentation). Refers back to chapter 003 (`strictNullChecks` on by default), lesson 1 of chapter 004 (literal unions, property widening, four corners — the `never` corner), lesson 2 of chapter 004 (`?:` field shorthand, declaration merging via `interface`), lesson 4 of chapter 004 (completeness-check posture for missing fields).
- **Terminology** — "union" (`Term` tooltip — `|` operator, value of `A | B` is either an `A` or a `B`, set-theoretic union of inhabitants); "intersection" (`Term` tooltip — `&` operator, value of `A & B` satisfies both; on shapes the value carries fields of both); "shape-union access rule" (`Term` tooltip — only fields common to every variant are readable without narrowing; to read non-shared field, narrow with runtime check the language tracks); "discriminant" (`Term` tooltip — literal-typed field on every variant of a discriminated union identifying which variant a value is); rule phrasings: "Never widen a shape union to read a non-shared field. Never assert past it. Narrow it instead." / "`|` for alternatives, `&` for field composition." / "Read every composed type as a set first." Reading conventions: `|` reads as **or**; `&` (on shapes) reads as **plus**; "shape-union landmine" (named bug class — reading non-shared field on `User | Guest`); "flag-boolean payload" (the bug discriminated unions fix — optional fields + boolean flag, requires `!` assertions); "author, block, narrow" (the three motions the discriminated-`Result<T>` exercise rehearses).
- **Patterns and best practices** — Reach for `|` whenever modeling "one of several alternatives" (literal union, mixed-primitive, shape variant, nullable result, outer shape of a discriminated union); reach for `&` whenever a shape needs fields from multiple sources (payload composition, project-local extension of third-party types, discriminated-union variants); never widen a shape union to silence a compile error and never `as`-assert past it — narrow with a runtime check the language tracks (`'email' in value`, `typeof`, `!== null`); for shape-with-extra-fields against a library type, prefer `&` over `declare module` augmentation unless the augmentation must affect every consumer of the library type; model "this value is in one of N states" as a discriminated union with a literal-typed `status`/`ok`/`kind` discriminant — never as a flag-boolean payload with optional fields and `!` assertions; for `Result<T>` shapes use `ok: true` carrying `value: T` and `ok: false` carrying `error: Error` (course canonical shape, used in chapter 005 build-out); write the discriminant inline-literal or as a `type`-aliased variant so the literal stays literal (won't narrow if widened to `string`); accept the equivalence of `{ status: 'success' } & { data: User }` and `{ status: 'success'; data: User }` — pick the `&` form when the discriminant should be visually distinct from the payload.
- **Misc.** — Lesson-specific component at `src/components/lessons/004/5/UnionIntersectionVenn.astro` (two overlapping circles, set-theoretic visual aid central to the lesson). Uses `CodeVariants` four times (four-union-shapes walk, three-responses-to-shape-union-landmine, flag-boolean-vs-discriminated-union contrast, plus the third one for the discriminated union seed — actually three `CodeVariants` total: four-shapes, three-responses, flag-vs-discriminated). Two `VideoCallout`s embedded (Andrew Burgess set-theory frame, Kyle/Web Dev Simplified discriminated unions). Closing `TypeCoding` exercise authors discriminated `Result<T>` with `ok: true`/`ok: false` shape (course canonical — establishes the convention chapter 005 lesson 1 will build out); exercise uses two `@ts-expect-error` directives (on `r.value` and `r.error` unnarrowed reads) plus one `^?` query inside the `if (r.ok)` branch expecting the inner record type. Closing `Buckets` exercise (6 items, 2 columns) sorts compositions into `|` vs `&` — the subtle item is the discriminated `Result<T>` (outer union, inner intersection-equivalent). External resources: 4 cards (TS Handbook unions, TS Handbook intersections, Total TypeScript discriminated unions by Pocock, ivov.dev TypeScript-and-set-theory) — matches lesson 4's 4-card pattern. The `Result<T>` exercise establishes `ok: true`/`ok: false` (boolean discriminant) as the course canonical Result shape — note this differs from some libraries' `'ok'`/`'err'` string discriminants; future lessons (chapter 005 lesson 1, chapter 008 lesson 1) should maintain `ok: boolean` for consistency.

## Lesson 6 — Narrow, don't assert

- **Taught** — Control-flow narrowing as the senior default for reading union-typed values, framed around the recurring `(value as User).email` bug that compiles and crashes six months later; the six core narrowing forms with the trigger that earns each (`typeof` for mixed primitives, `===` on a literal for finite literal unions, `in` for shape unions without a discriminant, `instanceof` for class branches with one-line cross-realm carve-out, `Array.isArray` for `T | T[]`, `switch` on a discriminant for discriminated unions); custom type predicates as a seventh recognition-only form with the "only as honest as its body" watch-out; the **narrowing scope rule** (narrow holds inside the block where check fired and is invalidated by reassignment or by an intervening function call under strict — fix is `const user = value` capture inside the block); the **three legitimate triggers for `as`** (boundary parse-then-trust where user code writes no `as`, TypeScript-can't-see-what-you-can-prove like immediate Map round-trip, DOM/third-party type gaps for tightly-scoped one-shots); `as unknown as T` is a smell; type assertions don't validate at runtime; `!` is `as` with a narrower trigger (most legitimate use `array.find(...)!`, senior alternative `?? throwError(...)` for production-observable failures); nullable narrowing surface (truthy check works on `User | null` but excludes `0`/`''`/`false` on wider unions — reach for `!= null` or explicit `!== null && !== undefined`; `??` defaults on `null`/`undefined` only; `?.` short-circuits at runtime but does not narrow for subsequent reads — early-return-and-read).
- **Cut** — None significant; outline's plan landed in full. One `VideoCallout` added (Andrew Burgess on narrowing surface, ~12 min) per the outline's optional suggestion — chose narrowing-surface video over an `as`-smell video to reinforce the senior default rather than the escape hatch.
- **Debts** — `assertNever` and the exhaustiveness check at the `switch` `default` branch (chapter 005 lesson 3); assertion functions `asserts value is T` mentioned in one line beside type predicates (chapter 005 lesson 3); `as const` for inline-literal discriminants (lesson 7); `satisfies` (lesson 7); `unknown` narrowing depth in `catch` blocks (chapter 008 lesson 2); cross-realm `instanceof` gotcha at depth (chapter 008 lesson 2); Zod boundary parse pattern full surface (chapter 009 lesson 1); Biome rules around `as`/`!` (chapter 024 lesson 5). Refers back to chapter 002 lesson 5 (the rare legitimate `==` use: `!= null`), lesson 1 (`unknown` as sound top, four corners), lesson 2 (`?` vs `| undefined`), lesson 4 (`noUncheckedIndexedAccess` posture — `!` same three triggers), lesson 5 (shape-union landmine, `'email' in value`, `FetchResult<T>` discriminated-union seed).
- **Terminology** — "control-flow narrowing" (`Term` tooltip — compiler tracks how runtime checks refine a value's type along each branch); "type assertion" (`Term` tooltip — compile-time-only operation, no runtime effect, developer takes responsibility); "discriminant" (`Term` tooltip — literal-typed field on every variant, reused verbatim from lesson 5 per outline); "type predicate" (`Term` tooltip — `value is T` return type, only as honest as its body); "truthy check" (`Term` tooltip — falsy values: `false`, `0`, `''`, `null`, `undefined`, `NaN`, `0n`); load-bearing rule: **"Narrow with a runtime check the language tracks. Never assert past a union without one of three named triggers."**; closing one-liner: **"narrow with the language; reach for `as` only at the boundary, the proof gap, or the DOM seam."**; "the narrowing surface" (the six core forms); "the three legitimate triggers"; "boundary parse-then-trust" / "TypeScript can't see what you can prove" / "the DOM and third-party type gaps" (named trigger labels); "the closure trap" (narrowed-then-callback bug); "the one-line `const` capture" (the senior reflex); "`as unknown as T` is a smell" (loud-on-purpose double-cast).
- **Patterns and best practices** — Default to narrowing with one of the six runtime checks the language tracks; never `as`-assert past a union — every use of `as` must justify against the three named triggers (boundary parse, proof gap, DOM seam) and anything else is a refactor signal; type predicates use `function` declaration form, not arrow (the one carve-out in "arrows by default" — `value is T` only lives on `function`); capture narrowed values with `const user = value` immediately after the check whenever the value crosses into a callback, deferred call, or any code the compiler can't see through; `array.find(...)!` acceptable for test fixtures and one-shot scripts where the prior check is local; reach for `array.find(...) ?? throwError('message')` whenever the failure is production-observable (gives log scanners a name to grep); `!= null` is the rare legitimate `==` use for catching both `null` and `undefined`; truthy checks (`if (user)`) safe on object-or-null unions but wrong on unions including `0`/`''`/`false`; resolve nullable with an early-return narrow at the top of the block when multiple reads follow, not with chained `?.`; the `as` and `!` triggers apply identically — the non-null assertion is `as` with a tighter scope; `as unknown as T` only acceptable for test fixtures and third-party boundaries with no honest signatures.
- **Misc.** — Uses `CodeVariants` twice (the six-form narrowing surface; the three-form nullable surface). Uses `AnnotatedCode` once for the three-trigger `as` walk (single shared file, three colored steps — green/orange/violet — at the three trigger sites). Closing `TypeCoding` exercise refactors a function with two `as` assertions (`(value as User).email` and `roleLabels[roleKey as Role]`) into narrowed form using `in` and a literal-union-typed key; two `@ts-expect-error` directives (one per smell pattern) plus two `^?` queries inside narrowed blocks expecting `email: string` and `narrowedKey: "admin" | "member"`. Closing `Buckets` exercise (8 items, 2 columns: narrow vs assert) is the canonical posture test — five narrow items, three assert items (Zod parse, `querySelector` one-shot, Map round-trip). One `VideoCallout` (Andrew Burgess, video ID `WAlJfSFngxw`, "Type Narrowing in TypeScript"). External resources: 3 cards (TS Handbook narrowing, Total TypeScript on type assertions by Pocock, MDN `instanceof`) — returns to the 3-card pattern after lessons 4/5 used 4. No lesson-specific components added. Lesson body uses bare prose paragraphs (not `:::note` asides) for the `as unknown as T` and "assertions don't validate at runtime" watch-outs, matching the outline's instruction to keep these in the lesson body rather than asides.

## Lesson 7 — Keeping literals narrow with `as const` and `satisfies`

- **Taught** — The property-widening surprise on the `ROUTES` map (parked by lesson 1) lands its fix here; widening rule restated across three reassignable sites (object properties, array element types, `let` bindings) with `const`-on-primitive as the non-widening case; **`as const`** as the value-site freeze with three behaviors (string/number/boolean literals stay literal, properties become `readonly`, arrays become `readonly` tuples of literals); the three production sites `as const` earns its weight (typed config maps + `keyof typeof X` / `(typeof X)[keyof typeof X]` derivations, inline tuple literals from hook returns / `Object.entries`, discriminant values built inline so lesson 5's discriminated union doesn't silently widen); the keyword-collision watch-out (`as const` value-site freeze is sound; `as T` from lesson 6 is a conditional escape hatch — same `as`, different operators); **`satisfies T`** as the contract check that doesn't apply `T` to the value (validates assignability without widening); the annotation-widens-vs-`satisfies` contrast on the same `ROUTES` value; the two `satisfies` triggers (keep narrow + check contract; error at write site not read site); the combined **`as const satisfies T`** idiom with strict order (the `satisfies` reads the post-`as const` inferred type); the four type derivations a `PERMISSIONS` table unlocks (`keyof typeof`, `(typeof X)['admin']` literal tuple, `[keyof typeof X][number]` "every permission ever assigned", `Record<Role, ...>` completeness error at literal site); the three-row contrast table (annotation / `as const` / `satisfies T`) across "applies `T`?" / "catches violations?" / "preserves literals?"; the senior-call rule pairing each scenario with one form.
- **Cut** — None significant; outline plan landed in full. One `VideoCallout` was suggested as optional in the outline; lesson includes two (Matt Pocock on `as const`, Matt Pocock on `satisfies`) to match the chapter's video-callout cadence. External resources stay at 3 cards (TS Handbook `const` assertions, TS 4.9 `satisfies` release notes, Total TypeScript "5 ways to use satisfies" by Pocock).
- **Debts** — `keyof typeof` and `typeof X[K]` operators at full depth (chapter 005 lesson 5 — used here as one-line refresher in the worked example); mapped types and conditional types operating on `as const` outputs (chapter 005 lesson 6); `const` type parameters on generic functions `<const T>` (chapter 005 lesson 7); utility-type surface `Readonly<T>` etc. (chapter 005 lesson 6); annotation-at-boundaries rule and `isolatedDeclarations` (lesson 8); module augmentation via `declare module` (chapter 006 lesson 4); Drizzle schema definitions using `as const satisfies` shapes (Unit 5); Next.js route segment configs (Unit 4); feature-flag and RBAC permission tables (Unit 9). Refers back to lesson 1 (property widening parked there, four corners, literal unions), lesson 2 (`readonly` field modifier — `as const` is the bulk shorthand), lesson 3 (inline tuple `[1, 2, 3] as const` recognition built out here), lesson 4 (`Record<LiteralUnion, V>` completeness check — same contract shape the combined idiom validates against), lesson 5 (discriminant-must-stay-literal — `{ status: 'loading' } as const` is the fix), lesson 6 (`as Type` keyword collision — same `as`, different semantics).
- **Terminology** — "value-site freeze" (`Term` tooltip for `as const` — opts every nested literal out of widening and applies `readonly` to every property); "`satisfies`" (`Term` tooltip — operator that validates a value against a type without applying the type as the value's annotation); "typed config" (`Term` tooltip — module-level constant whose keys and values both carry type information consumed by the rest of the module); "widening" reused verbatim from lesson 1 (`Term` tooltip — inferred literal broadened to base primitive in reassignable positions); load-bearing rules: **"The widening is reversible. `as const` opts every nested literal out of widening; `satisfies` checks against a contract without re-applying it."** / **"`as const` tells TypeScript to freeze every nested literal at its narrowest type and make every property `readonly`."** / **"`satisfies T` validates that a value is assignable to `T` without applying `T` as the value's type."** / **"Lock the literal types with `as const`, then validate against a contract with `satisfies T`. The order matters — `satisfies` reads the post-`as const` inferred type."**; "the senior reach" (for `as const satisfies T`); "fits vs is" tip ("annotation says this value **is** `T`; `satisfies` says this value **fits** `T`"); "the three sites `as const` earns its weight"; "the canonical typed-config idiom" (`as const satisfies T`); "the ceiling vs the value" (contract is the ceiling, frozen value tells you exactly where inside it); "the combined idiom"; phrase "the unfortunate name collision" for `as const` vs `as Type`.
- **Patterns and best practices** — Default to `as const satisfies T` for every module-level typed config (routes maps, feature flags, permission tables, Drizzle schemas, Next.js route segment configs, RBAC tables); reach for `as const` alone when there's no contract to validate against (inline tuple returns from custom hooks, inline discriminant values, positional records destructured at the call site); reach for `satisfies T` (usually with `as const`) when both the literal types and the contract matter; reserve annotations `: T` for cases where the type itself is the public contract (exported function signatures, public type aliases) — lesson 8 owns the full boundary rule; always write inline-literal discriminants with `as const` (`{ status: 'loading' } as const`) to keep the `FetchResult<T>` discriminated-union narrowing intact; SCREAMING_SNAKE_CASE for true module-level compile-time constants (`ROUTES`, `PERMISSIONS`, `FEATURE_FLAGS`); treat the missing-key compile error a growing literal union creates in every `as const satisfies Record<L, V>` as a refactor feature, not a chore; never silence a `satisfies` mismatch with `as` — fix the value.
- **Misc.** — Uses `CodeVariants` twice (three-behaviors-of-`as const` and annotation-widens-vs-`satisfies` contrast). Uses `AnnotatedCode` once on the combined-idiom `PERMISSIONS` worked example — five colored steps (green/blue/orange/violet/red) tracing the single shared file: the `as const satisfies` line, `keyof typeof`, `(typeof X)['admin']` literal tuple, `[keyof typeof X][number]` derivation, and the `@ts-expect-error` completeness check. Two `VideoCallout`s (Matt Pocock `as const` video ID `6M9aZzm-kEc`; Matt Pocock `satisfies` video ID `9F-6mUXuylw`). Closing `TypeCoding` exercise authors a `FEATURE_FLAGS` map with `Flag`/`Stage` literal unions and `as const satisfies Record<Flag, Stage>`; two `^?` queries (one on `keyof typeof FEATURE_FLAGS` expecting the `Flag` union, one on `(typeof FEATURE_FLAGS)['beta-checkout']` expecting the literal `"beta"`); one `@ts-expect-error` directive on a missing-flag case. Closing `MultipleChoice` (single-correct, four scenarios) locks the form-vs-need matching — the routes-map case is correct; the other three (exported function signature, `reduce` local, three-coordinate tuple) reach for annotation, inference, or `as const` alone respectively. Three-row contrast table rendered as plain markdown (no `Figure` component). External resources: 3 cards. No lesson-specific components added. Lesson is mostly value declarations (few function-shaped examples); the one function example uses arrow form bound to `const` per chapter conventions. `^?` queries appear as plain trailing comments throughout (matches lesson 4's no-Twoslash convention).

## Lesson 8 — Annotate the boundaries, infer the inside

- **Taught** — The chapter's posture lesson resolving the over-annotated vs under-annotated anti-extremes; the load-bearing rule **"annotate at the boundaries; infer everywhere else"**; three sites annotations earn their weight (function parameters always, exported APIs as documentation surface, return types where inference produces unintended type — interface-satisfying callbacks, complex-conditional returns, recursive functions inferring `any`); three sites inference wins (local variables, inline callbacks via contextual inference flowing from array element type, internal-helper return types); the spatial mental model ("types live at the perimeter; inference fills the interior") via a `PerimeterDiagram` showing module rectangle with annotated edges and inferred interior; `isolatedDeclarations` named in one forward sentence (stable in TS 6.0 March 2026, opt-in not default, trajectory for monorepos/library publishers); the `import type` discipline under `verbatimModuleSyntax: true` — compiler emits imports/exports exactly as written, no elision heuristic, `import type` erased entirely, bare imports preserved verbatim; the per-name mixed form `import { type Foo, bar }`; the two failure modes `verbatimModuleSyntax` prevents (side-effect modules silently tree-shaken when imported only for types — Drizzle relations registration, Better Auth singleton wiring; circular type-imports written as value-imports deadlocking module init — `User`/`Invoice` mutual type reference).
- **Cut** — None significant. Outline plan landed in full. Outline allowed an optional `VideoCallout`; lesson includes one (Matt Pocock on return types, video ID `nwSe95uFN8E`). External resources expanded from 3 to 4 cards (TS Handbook `verbatimModuleSyntax`, TS Handbook `isolatedDeclarations`, typescript-eslint Consistent Type Imports, Total TypeScript Annotations and Assertions).
- **Debts** — `isolatedDeclarations` mechanics in depth (deferred to wherever exported-API lessons surface it; not the course's single-app default); full module-graph mechanics — live bindings, depth-first evaluation, dynamic `import()`, `"use client"` boundary, `import 'server-only'` (chapter 006); module augmentation via `declare module` (chapter 006 lesson 4); full strict tsconfig build-out including `verbatimModuleSyntax` reaffirmation (chapter 024 lesson 5 referenced as "the strict tsconfig"); Biome `useImportType` lint rule auto-fix (chapter 024 lesson 5); `import type * as X` and `export type { X }` re-export forms (out of scope); project references and monorepo topology (out of scope — single-app SaaS course); Drizzle `$inferSelect`/`$inferInsert`, Zod `z.infer`, TanStack typed query keys all rely on this rule (forward links). Refers back to lesson 5 (`Result<T>` shape used in the exported-API return-type example), lesson 6 (control-flow narrowing — discriminated-union narrowing the explicit `Result<T>` return-type locks), lesson 7 (`as const satisfies` used in the exported `ROUTES` exercise item).
- **Terminology** — "implicit any" (`Term` tooltip — error TypeScript fires under `noImplicitAny` when parameter has no annotation and no contextual inference, defaulting to unsafe top type); "contextual inference" (`Term` tooltip — TypeScript inferring parameter's type from surrounding call site rather than function body); "`verbatimModuleSyntax`" (`Term` tooltip — TS flag emitting each import statement exactly as written and requiring `import type` for type-only imports, removing old type-elision heuristic); "`isolatedDeclarations`" (`Term` tooltip — opt-in flag stable as of TS 6.0 requiring exported APIs to declare types explicitly so downstream tools build `.d.ts` without running type checker); load-bearing rules: **"Annotate at the boundaries; infer everywhere else."** / **"A type annotation is a contract with someone who won't read your code."** / **"Annotations are documentation and structural enforcement; inference is concision and refactoring resilience. The boundary rule gives you both — contracts at the seams, freedom inside."**; "boundary" / "the seams" / "the perimeter" / "the interior" (spatial vocabulary for module shape); "the asymmetric posture" (typed edge, unannotated middle — the convergent shape of well-maintained TS codebases); "the chapter's posture lesson"; "this lesson installs the rule; every code sample in the rest of the course follows it without comment"; "vocabulary failures" (frames both anti-extremes); "side-effect modules silently tree-shaken" and "circular type-imports masquerading as value-imports" (named failure modes); "explicit about intent" (the `import type` design rationale).
- **Patterns and best practices** — Annotate every function parameter without exception (the lone exception that proves the rule: contextual inference on inline callbacks); annotate every exported function's return type, every exported `type` alias, every exported `const`; let inference handle every local variable, every inline `.map`/`.filter`/`.reduce` callback parameter, every internal-helper return type unless inference would be wrong; reach for an explicit return type on a non-exported function only in three cases (callback satisfying a wider `void`-returning interface, complex conditional return type the consumer shouldn't depend on, recursive function that would otherwise infer `any`); the `as const satisfies T` form (lesson 7) counts as "annotation at the boundary" for exported typed configs; under `verbatimModuleSyntax` write `import type` for every purely type-only import and the per-name `type` modifier (`import { type Foo, bar }`) for mixed imports — never two import lines; for side-effecting modules without a value to grab, write a bare `import './path'` to preserve the module-graph edge; for cross-module type references that would form a cycle, mark both sides `import type` so the cycle stays type-level and never becomes runtime; never use `import type * as X` or the niche re-export forms — canonical form is `import type { X }` and `import { type Foo, bar }`; quote of the canonical Code conventions rule: "annotate parameters and return types of exported functions; let inference handle locals, intermediate values, and internal-helper returns."
- **Misc.** — Lesson-specific component at `src/components/lessons/004/8/PerimeterDiagram.astro` (the spatial mental model — module rectangle with three regions: "Exports (annotate)" left edge, "Internal body (infer)" middle, "Parameters of every function (annotate)" right edge). Introduction uses a one-shot `function` declaration in the under-annotated counterexample (deliberate violation of "arrows by default" — the implicit-any failure reads identically against either form; commented inline as the failure case the rule prevents); every correct-shape snippet uses `const`-bound arrows. Uses `CodeVariants` once for the wrong-vs-right `import type` contrast (red value-import error / green type-only import); uses plain `Code` blocks elsewhere (three for annotation sites, three for inference sites, one for mixed imports, one each for the two failure modes). One `VideoCallout` (Matt Pocock on return types, video ID `nwSe95uFN8E`) placed within the explicit-return-type subsection. Closing `Buckets` exercise (10 items, three buckets — Annotate / Infer / Type import — twoCol layout) is the canonical posture install: 5 Annotate items, 3 Infer items, 2 type-import items; includes one subtle item ("the `satisfies Record<RouteName, string>` clause on `export const ROUTES = ...`") that maps to Annotate as the exported-boundary contract. External resources: 4 cards (returns to lesson 4/5's 4-card pattern, exceeds typical 3-card cadence). The `Buckets` exercise establishes the bucket name `type-import` and label "Type import" for the third category. Lesson explicitly states it's "the chapter's posture lesson" — future lessons across the course assume this rule is installed and will follow it without comment.
