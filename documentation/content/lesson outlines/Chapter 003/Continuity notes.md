# Chapter 003 — Picking the right container

## Lesson 1 — The object as workhorse record

- **Taught** — Dot-default/bracket-on-trigger access (3 triggers: non-identifier key, key in variable, computed key); shorthand/computed-keys/spread construction with "right-most key wins"; `in` vs `Object.hasOwn` vs `??` presence checks; `Object.keys/values/entries`, `Object.fromEntries`, `Object.assign`, `Object.freeze`/`isFrozen`, `Object.groupBy`; prototype-chain stated and parked with `Object.create(null)` for prototype-less lookup.
- **Cut** — Did not show `arr.reduce((acc,[k,v]) => ({...acc,[k]:v}), {})` as a worked rewrite (only mentioned as retired); `Object.fromEntries(new URLSearchParams(...))` parsing example mentioned only in prose, no snippet; no MDN "Further reading" h2 heading — cards placed under `## External resources` instead.
- **Debts** — `Record<K, V>` mapped-type form deferred to Ch 004 L4 (referenced via `<Term>`); `readonly` modifier to Ch 004 L2; `Map.groupBy` and non-string-key grouping to Ch 003 L4; `for...of` over objects to Ch 003 L5; React state spread pattern to Unit 3/Ch 023; Server Actions options-object input to Ch 030 L4; Zod `.parse()` to Unit 6; Drizzle row shapes to Unit 5; `JSON.stringify` serialization to Ch 009 L1.
- **Terminology** — "record" (known, named-field shape) vs future "dictionary" (Ch 003 L4); "the three triggers" (non-identifier / variable key / computed) reused for both bracket reads and computed-key writes; "right-most key wins" for spread merge; "iteration triad" for keys/values/entries; `noUncheckedIndexedAccess` defined via `<Term>` here for chapter-wide reuse.
- **Patterns and best practices** — Dot access by reflex; `Object.hasOwn` over `'key' in obj` for user-controlled input; spread (`{...a, ...b}`) over `Object.assign` for non-mutating merge; `Object.fromEntries(Object.entries(x).map(...))` round-trip over `.reduce`-with-spread (which is O(n²)); `Object.groupBy` over hand-rolled reduce; defensive handling of `byStatus.x ?? []` because `Object.groupBy` returns `Partial<Record<K, T[]>>`; treat `Object.keys` as `string[]` not `keyof T`.
- **Misc.** — Seed domain continued: `invoice`, `customer`, `amountCents`, integer cents. Components used: `CodeTooltips`, `CodeVariants`, `AnnotatedCode`, `Term`, `TSPlaygroundCallout`, `Buckets`, `CardGrid` + `ExternalResource`. `<Buckets twoCol>` closing exercise with 4 buckets / 8 intent-phrased items. No video (enumerated surface). The lesson opens cold with two bug snippets (no intro h2).

## Lesson 2 — Arrays and the non-mutating update

- **Taught** — Indexing under `noUncheckedIndexedAccess` (`arr[i]` returns `T | undefined`) with `?? fallback` and temp-binding-narrow patterns; `.at(0)`/`.at(-1)` for positional access; the four ES2023 non-mutating pairs (`sort`/`toSorted`, `reverse`/`toReversed`, `splice`/`toSpliced`, `arr[i]=v`/`with(i,v)`); ownership rule "mutate locally, replace when shared"; `.push`/`.pop`/`.shift`/`.unshift` still earn their place inside function-owned arrays; spread `[...arr, x]` for boundary changes and `.slice()` for shallow copy; `Array.from(new Set(arr))` dedup idiom; `Array.of` vs `Array(n)` sparse-array trap.
- **Cut** — Closing `ReactCoding` exercise has only two simple tests (first item smallest after click, initial item is largest) rather than the more elaborate two-click structural check originally floated; second predict-output card swapped from `.splice`/`.toSpliced` to a direct `.toSorted`/`.sort` contrast for tighter A/B framing; no MDN "Indexed collections" overview card (replaced with TS docs for `noUncheckedIndexedAccess` and TC39 spec proposal).
- **Debts** — Full `useState<T[]>` mechanics + updater-function form deferred to Ch 023 (the React snippets here are taken on faith); element-by-element array surface (`.map`/`.filter`/`.reduce`/`.find` family) is Lesson 3's job; `Set` rationale beyond dedup goes to Ch 003 L4; `Symbol.iterator`/iteration protocol behind `Array.from` lands in Ch 003 L5; `Array.from(formData.entries())` in Server Actions to Ch 030 L4; Drizzle result-array transforms to Unit 5.
- **Terminology** — "Four pairs" / "non-mutating twin" / "ownership rule" (function-owned vs shared); "the boundary" (spread) vs "the middle" (`.with`/`.toSpliced`); ".slice reads, .splice mutates" footgun line; `noUncheckedIndexedAccess` re-used via `<Term>` from L1; `??` recalled from Ch 002 L5; shallow-copy rule recalled from Ch 001 L1.
- **Patterns and best practices** — `.at(-1)` over `arr[arr.length - 1]` by reflex; `const first = arr[0]; if (first === undefined) return;` over non-null assertion `arr[0]!`; never `if (arr.length > 0) arr[0]` (TS won't narrow); `.toSorted`/`.toReversed`/`.toSpliced`/`.with` whenever the array escapes the function; `[...arr, x]` for append (not `arr.concat(x)`); `setItems(prev => [...prev, x])` not `prev.push(x)`; `arr = []`/`setArr([])` never `arr.length = 0`; `Array.from(new Set(arr))` for primitive dedup; write `[3]` not `Array(3)`.
- **Misc.** — Seed domain: invoices with `id`, `amountCents`, `status` (paid/pending/overdue). Components used: `CodeTooltips`, `CodeVariants`+`CodeVariant`, `Term`, `ExternalResource`, `PredictOutput`+`PredictWhy`, `ReactCoding`, `VideoCallout`, `CardGrid`. Lesson opens cold (no intro h2) with the React `.sort()` no-re-render bug; React used as failure visualization only (not taught). One `VideoCallout` (ColorCode immutability video). React-coding exercise uses direct `setInvoices(invoices.toSorted(...))` form, NOT the updater-function form — that's deliberate (updater form is Ch 023's job). Node 24 LTS named as course-pinned runtime (forward-ref to Ch 003 L8).

## Lesson 3 — The array method surface

- **Taught** — Eight array methods grouped by four output shapes (transform: `.map`/`.flatMap`; subset: `.filter`; fold: `.reduce`/`.reduceRight`; search/test: `.find`/`.findIndex`/`.findLast`/`.findLastIndex`/`.some`/`.every`; side-effect: `.forEach`); `.filter` narrowing surface with four shapes (`.filter(Boolean)` no-narrow footgun, TS 5.5+ inferred predicate as daily reach, explicit `(x): x is T => ...`, reusable `isPresent` helper with `<T,>` arrow-generic form); `.reduce` rules (always pass initial value, prefer specialized methods); reduce-to-object O(n²) trap with `Object.fromEntries(arr.map(...))` linear fix; `.forEach` almost-never rule with async footgun; four `for...of` triggers (async sequencing, early termination, multi-statement bodies, index+value via `.entries()`); parallel vs sequenced async (`Promise.all(arr.map(async ...))` vs `for...of`+`await`); name-the-intermediate rule (4+ chain links → named const); Set-for-membership foreshadow.
- **Cut** — The fourth `for...of` trigger reframed from "need both index and value with custom step" (dropping C-style `for(let i=0; ...)` for non-1 steps) to just "need both index and value via `.entries()`" — custom-step C-style loops not covered; `.indexOf`/`.includes`/`.concat`/`.join`/`.split` only named in passing; `.push`-inside-`.forEach` anti-pattern not explicitly shown.
- **Debts** — `Set` semantics and full Set/Map surface + `Map.groupBy` to L4; `Symbol.iterator`/iteration protocol and lazy `Iterator.prototype` helpers to L5; `for await...of` and `Promise.all` deliberate fan-out to Ch 007 L3; user-defined type guards beyond `.filter` predicates to Ch 005 L3; React list rendering with `key` to Ch 022; TanStack Query `data?.map(...)` to Unit 15; Drizzle result-set transforms to Unit 5; `useState<T[]>` mechanics to Ch 023.
- **Terminology** — "Four output shapes" (transform/subset/fold/search-test, plus side-effect); "type predicate" defined via `<Term>` as `x is T` return annotation (reused from `.filter` to `.find`); "the daily reach" vs "the conditional reach" framing for inferred vs explicit predicates; "name the intermediate" as refactor rule; "four triggers" for `for...of`; "the spectrum" (under-using methods ↔ over-applying chains) as lesson framing; `isPresent` as the canonical reusable narrowing helper.
- **Patterns and best practices** — `.reduce((sum, i) => sum + i.amountCents, 0)` over hand-rolled C-style sum loops; always pass initial value to `.reduce`; `Object.fromEntries(arr.map((x) => [x.id, x]))` over `.reduce((acc,x) => ({...acc, [x.id]: x}), {})`; `.some(fn)` over `.filter(fn).length > 0`; `.every` for invariants; `(x) => x !== null` over `.filter(Boolean)` when narrowing; explicit `(x): x is T` or `isPresent` helper for complex narrowing; `for...of`+`await` for sequenced async, `Promise.all(arr.map(async ...))` for deliberate parallel, never `.forEach(async ...)`; `for (const [i, item] of arr.entries())` when both index and value needed; name intermediates as `const` once chain hits 4+ links; build a `Set` outside `.filter` when inner check is `.includes`/membership; `.flatMap((x) => cond ? [x.id] : [])` to collapse `.filter().map()` when steps share per-item work; `.findLast`/`.findLastIndex` over `.reverse().find(...)`.
- **Misc.** — Seed domain continued: invoices `{id, amountCents, status, customerId, dueDate}`. Components used: `CodeVariants`+`CodeVariant`, `AnnotatedCode`+`AnnotatedStep`, `Term`, `ExternalResource`, `TypeCoding`, `CodeReview`+`ReviewFile`+`ReviewIssue`+`ReviewWhy`, `CardGrid`. Opens cold (no intro h2) with two contrasting bug snippets (under-use vs over-use). No video. Closing `CodeReview` has two `ReviewIssue` plants (one async `.forEach`, one chain-with-dead-`.map`); the deliberately-bad PR file is a pedagogical plant, not production-shape code to imitate. `TypeCoding` uses two `^?` query targets to contrast `.filter(Boolean)` (stays `(string|null)[]`) vs the fixed form (`string[]`). TS 5.5+ is the pinned version that enables inferred type predicates.

## Lesson 4 — When Set and Map earn their weight

- **Taught** — Four-way container pick (object/array/`Set`/`Map`) keyed by triggers; `Set` for dedup, membership-in-a-loop, and ES2025 set algebra (`union`/`intersection`/`difference`/`symmetricDifference`/`isSubsetOf`/`isSupersetOf`/`isDisjointFrom`); `Map` for non-string keys, frequent insert/delete, insertion-order-as-contract; `Map.groupBy` as `Object.groupBy`'s non-string-key cousin; full daily API surface for both (`add`/`set`/`get`/`has`/`delete`/`size`/`for...of`/iterator views) with explicit generic on construction; `Map.get` always returns `V | undefined` (same discipline as `noUncheckedIndexedAccess`) handled via `??` or temp-binding-narrow; `WeakMap` at the narrow edge for object-keyed GC-coupled caches (DOM-element example); `JSON.stringify(new Map())` and `JSON.stringify(new Set())` both return `'{}'`, fix by converting at the boundary (`Array.from`, `Object.fromEntries`).
- **Cut** — Did not turn the closing `<Buckets>` into a five-bucket layout (kept four buckets; item 6 lands in `map` with a prose note that `WeakMap` is the better production fit); no `<Aside type="caution">` wrapper around the serialization section (prose-led instead); no inline annotated `// → ...` return comments on the surface blocks (returns explained in trailing prose instead); the "set-like" point on Set methods kept to one sentence, not elaborated.
- **Debts** — Iteration protocol/`Symbol.iterator`, `for...in` ban, ES2025 `Iterator.prototype` lazy helpers to L5; deep serialization (`structuredClone`, `Date`/`Temporal`/`BigInt` round-trips, RSC wire's `Map`/`Set` acceptance) to Ch 009 L1; class private fields (`#field`) replacing `WeakMap`-for-private-state to Ch 009 L2 (classes); React `useState` mechanics for `Set`/`Map` (the `new Set(prev).add(x)` / `new Map(prev).set(k, v)` shape) to Ch 023; LRU cache idiom (`map.delete(k); map.set(k, v)` touch-to-MRU) to Unit 14; Drizzle result-set dedup/lookup transforms to Unit 5; TanStack Query's `WeakMap`-keyed internal cache to Unit 15 (named only); DOM API to next unit (the `getRect` snippet is recognition-only).
- **Terminology** — "Dictionary" (dynamic/unbounded keyspace, → `Map`) vs "record" (fixed known fields, → object literal) defined via `<Term>` — the pair that drives the four-way pick; "set-like" object (has `.size`, `.has`, `.keys()`) as the operand type Set methods accept; `SameValueZero` defined via `<Term>` as the equality semantics for both `Set` values and `Map` keys (NaN matches NaN; objects by reference, not shape); "named threshold" framing for when defaults flip to conditional containers; `noUncheckedIndexedAccess` re-used via `<Term>` from L1.
- **Patterns and best practices** — Always annotate generic at construction (`new Set<string>()`, `new Map<string, Invoice>()`) — empty initializer otherwise widens to `Set<unknown>`/`Map<any, any>`; build a `Set` once outside any loop that does membership check, never `.includes` inside `.filter`/`.map`/`for...of`; reach for ES2025 native set methods over `lodash.intersection`/`lodash.union` — delete the import; `Map.groupBy` when the grouping key isn't a string (Date, object, tuple), `Object.groupBy` when it is — never coerce a Date into a stringified group key; `.get(id) ?? fallback` as the default miss-handler, temp-binding-narrow (`const x = m.get(id); if (x === undefined) return; x.field`) when absence must short-circuit; prefer `.get` + null check over `.has` + `.get` sequence (one lookup, not two); convert `Map`/`Set` to array/object at every `JSON.stringify` boundary (`Array.from(map)`, `Object.fromEntries(map)`, `Array.from(set)`); use `WeakMap` for object-keyed metadata caches (cache dies with the object) — never reach for `WeakMap` for private state (use `#field`); ownership rule from L2 carries — mutate locally, build fresh instance when shared (React state).
- **Misc.** — Seed domain continued: invoices `{id, amountCents, status, customerId, dueDate}` plus new shapes — `Set<string>` of selected invoice IDs, `Map<string, Invoice>` ID lookup, `Map<Date, Invoice[]>` due-date grouping, `Map<Customer, number>` outstanding-by-customer (recognition-only — production code keys by string ID). Components used: `Term`, `ExternalResource`, `Figure` (with Mermaid `flowchart LR` for the four-way decision tree), `CodeVariants`+`CodeVariant`, `Buckets`+`Bucket`+`Item`, `CardGrid`. Opens cold (no intro h2) with three contrasting bug snippets (prototype leak / nested includes / Map-over-wire). No video, no `AnnotatedCode`. Closing `<Buckets twoCol>` has 4 buckets / 8 items; items 4 (lookup in render loop → `map`) and 8 (word count → `map`, not `obj`) are the deliberate traps that test the dictionary-vs-record install. RSC wire's structured-clone serializer named as the exception to the `JSON.stringify` rule (forward-ref to Next.js chapters). Node 24 LTS recalled as the runtime that makes ES2025 Set methods universally available; ES2024 `Map.groupBy` baseline since end of 2024.

## Lesson 5 — Iteration and the lazy helpers

- **Taught** — Iteration protocol (`[Symbol.iterator]()` + `.next()` returning `{ value, done }`) as recognition-only contract; `for...of` as the consumer of the protocol with five things array methods can't do (`break`/`continue`/`return`, `await`, destructure-in-binding, `.entries()` index+value, multi-statement bodies); the four object-iteration entry points (`Object.entries`/`.keys`/`.values` + the **banned** `for...in` with prototype-chain leak and array-indices-as-strings rationale); generators in one snippet (`function*`/`yield`, returns iterator that is also iterable) — recognition only; ES2025 `Iterator.prototype` helper surface split into lazy (`.map`/`.filter`/`.take`/`.drop`/`.flatMap`) and terminal (`.toArray`/`.reduce`/`.forEach`/`.some`/`.every`/`.find`) groups, with `Iterator.from(iterable)` as entry point; three triggers for the helpers (lazy source / large size / short-circuit); pull-through pipeline model (terminal pulls, lazy stages wire); `for await...of` foreshadowed in one paragraph.
- **Cut** — Forward-link to `Iterator.from(formData.entries())` in Server Actions (Ch 030 L4) dropped from prose; TC39 proposal external-resource card replaced with web.dev "Iterator helpers reach Baseline" card; Node.js async-iterators forward link dropped from `CardGrid`; chapter-outline framing of generators as "stateful resumable computations" not retained (lesson scoped generators to "lazy source for helper chain").
- **Debts** — `for await...of` and async-iteration mechanics to Ch 007 L3; `Promise.all`+`.map` deliberate fan-out to Ch 007 L3; `ReadableStream`/SSE consumption to Ch 015 L1/L2; full async-generator surface (`async function*`, `Symbol.asyncIterator`) to Ch 007 L3 / Ch 015; `Array.from(formData.entries())` in Server Actions to Ch 030 L4; `useState` mechanics for iterables to Ch 023; deep serialization (`structuredClone`) to Ch 009 L1; class private fields (`#field`) replacing `WeakMap` to Ch 009 L2.
- **Terminology** — "Iterable" (has `[Symbol.iterator]()` returning iterator) vs "iterator" (has `.next()` returning `{ value, done }`) defined via `<Term>` as the protocol pair; "own property" `<Term>` re-used from L1; "the protocol" as the seam between container surface and consumer surface; "lazy" vs "terminal" as the two groups of helpers; "pull-through pipeline" as the mental model (terminal pulls one value at a time back through the chain); "trigger before tool" reused for the helper-vs-array-methods decision; "observable signal of laziness" = `console.log` inside `yield`; "the senior reach" framing for `Iterator.from(...).filter(...).take(n).toArray()`.
- **Patterns and best practices** — `for...of` over `.forEach` by reflex; `for (const [key, value] of Object.entries(obj))` as default object iteration; never `for...in` (configurable Biome lint as one-liner); `Object.hasOwn` for the own-key check when not iterating; `function*` for any custom lazy source (don't hand-roll `[Symbol.iterator]`); `Iterator.from(source).filter(...).take(n).toArray()` only when source is lazy/large/short-circuiting — wrap an in-memory 50-row array and you've added noise for zero benefit; `.take(n)` followed by `.toArray()` pulls exactly `n` items plus the upstream waste needed to find them; rule: "if you can already point at the array, don't wrap it; if the source yields over time, do"; `for await (const chunk of response.body)` shape for async sources (full mechanics later).
- **Misc.** — Seed domain continued: invoices `{id, amountCents, status, customerId, dueDate}`, plus streamed `invoiceEvents()` generator yielding `{id, severity}` for the lazy-helper demo. Components used: `Term`, `ExternalResource`, `CodeVariants`+`CodeVariant`, `AnnotatedCode`+`AnnotatedStep`, `PredictOutput`+`PredictWhy`, `ScriptCoding`, `VideoCallout`, `CardGrid`, plus lesson-specific `PullThroughPipeline` SVG at `src/components/lessons/003/5/PullThroughPipeline.astro`. Opens cold (no intro h2) with two contrasting bug snippets (eager waste / prototype leak). Two `VideoCallout`s (Joshua Morony on generators; Better Stack on iterator helpers). `ScriptCoding` rewrite exercise uses a yield-counter pattern (`yieldCount++` inside generator) to assert laziness — eager `[...source]` fails the count check (1000 yields), lazy `Iterator.from(...).take(5).toArray()` passes (401 yields, threshold 425). Cold-open variant A and starter use intentionally bad shapes (`[...gen].filter(...)`, `for...in`) — pedagogical plants, flagged in prose. The generator's `console.log` inside `yield` is non-idiomatic in production, intentional here to make laziness observable. TS 6.0 (May-2026 stable) puts iterator helper typings in `lib.es2025.iterator.d.ts` so the chain is fully typed under default course `tsconfig`. Node 24 LTS recalled as the runtime making `Iterator.prototype` helpers universally available.

## Lesson 6 — Regex: the modern flavor

- **Taught** — Two construction forms (literal default vs `new RegExp(...)` constructor when pattern is dynamic, with the doubled-backslash watch-out and a 3-line `escapeRegex` helper); the six daily flags (`g`/`i`/`m`/`s`/`u`/`v`) with the `u`-vs-`v` mutual-exclusivity rule; named capture groups `(?<name>...)` taught *before* indexed groups as the senior default, accessed via `.match(...)?.groups` (typed as `{ [key: string]: string } | undefined`); property escapes `\p{Letter}`/`\p{Number}`/`\p{White_Space}`/`\p{Emoji}`/`\p{Script=...}` as the reflex over `[a-zA-Z]` for human text; `v`-flag set ops `&&`/`--`/nested-union as recognition-only; the four-method result surface (`.test`/`.match`/`.matchAll`/`.replaceAll`) and the `.match`-with-`g`-drops-groups asymmetry; the four lookaround forms as a recognition surface keyed to the "context is condition not capture" trigger; the "drop the regex" decision rule with a parser table (email→`z.email()`, URL→`URL.canParse`/`new URL`, JSON→`JSON.parse`, HTML→`DOMParser`, ISO date→`Temporal.PlainDate.from`); ReDoS in one sentence (no nested quantifiers, length-cap untrusted input).
- **Cut** — `\k<name>` backreference example collapsed to one sentence (no worked snippet); the sticky `y` flag and `d`/`hasIndices` flag not mentioned at all (chapter outline already parked them as one-liners); the function-form `.replaceAll` only shown with positional `(_full, _year, num) =>` — the "named groups as trailing object" form named but not exampled; no explicit ReDoS code example, just the avoidance rule.
- **Debts** — Zod 4 `z.email()`/`z.url()`/`z.iso.datetime()`/`z.string().regex(...)` forward-linked to Unit 6 (Ch 042–047) — `.regex(...)` accepts a `RegExp` directly so flags travel with the pattern; `new URL(input)` and `URL.canParse(input)` to Ch 012 (HTTP); `JSON.parse` to Ch 009 L1; `Temporal.PlainDate.from` to Ch 009 / Unit 17; `DOMParser` to Unit 2 (DOM).
- **Terminology** — "Unicode mode" (`u`) and "Unicode sets mode" (`v`) via `<Term>` as the mutually-exclusive pair; "property escape" via `<Term>` for `\p{...}`; "lookaround" via `<Term>` as the position-asserting non-consuming construct; "ReDoS" via `<Term>`; "the asymmetry trap" = `.match` with `g` returns `string[]` (no groups), `.matchAll` returns iterator of full match objects; "the two-step gate" before any regex (structured format? → parser; unreadable? → small parser); `[a-zA-Z]` only lives in "ASCII-by-contract data" (hex, Base64, protocol identifiers).
- **Patterns and best practices** — Literal form `/pattern/flags` by reflex; `new RegExp` only when pattern is dynamic, with `escapeRegex` for user substrings; `u` always on for human text, `v` only when set ops or emoji sequences; named capture groups always over indexed `match[1]`/`match[2]`; `match?.groups` guard handles both null match and TS narrowing; `\p{Letter}` over `[a-zA-Z]` for any human input by reflex; `.test` for booleans (never with `g` if reused — `lastIndex` state); `.matchAll` (not `.match` with `g`) when both all matches and captures are needed; `.replaceAll` requires `g` on regex args (TS errors, runtime throws); `URL.canParse(input)` preferred over `try { new URL(...) } catch` for the boolean form; never hand-roll regex against email/URL/JSON/HTML/CSV/Markdown/ISO date — use the parser; don't nest quantifiers; length-cap untrusted input before regex.
- **Misc.** — Seed domain continued: invoices `{id, amountCents, status, customerId, dueDate}` with regex landing on invoice-number format `INV-2026-0042` (year/num named groups) plus user-input shapes (username, email, hex color, px values). Components used: `Term`, `ExternalResource`, `CodeVariants`+`CodeVariant`, `AnnotatedCode`+`AnnotatedStep`, `PredictOutput`+`PredictWhy`, `CodeReview`+`ReviewFile`+`ReviewIssue`+`ReviewWhy`, `CardGrid`. Opens cold (no intro h2) with two bug snippets wrapped in `<div data-mark-color="red">` (Latin-only username, hand-rolled email in Server Action). No `VideoCallout`, no `Figure`/diagram, no `Buckets`. Closing `CodeReview` mirrors L3's Decision-archetype close — `validateContactInput` with two seeded `ReviewIssue` plants (hand-rolled email regex, hand-rolled URL regex), each replaced by parser (`z.email()`, `URL.canParse`). Node 24 LTS recalled as the runtime making `v` flag and `\p{...}` universally available. The constructor-form snippet uses an inline `escapeRegex` (`.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')`) — sketched not promoted to an exported helper (utility files land in Unit 3 projects).

## Lesson 7 — VS Code as a team artifact

- **Taught** — VS Code commitment (Cursor/Zed/Neovim parked with one-line triggers); six-extension minimum set (`biomejs.biome`, `bradlc.vscode-tailwindcss`, `EditorConfig.EditorConfig`, `eamodio.gitlens`, `usernamehw.errorlens`, `yoavbls.pretty-ts-errors`); three repo-owned files (`.editorconfig` at root with 6 rules; `.vscode/extensions.json` with `recommendations` array; `.vscode/settings.json` with `editor.formatOnSave`, `editor.defaultFormatter: biomejs.biome`, `[css]` per-language override, `editor.codeActionsOnSave` with `source.fixAll.biome` + `source.organizeImports.biome` both as `"explicit"`, and `typescript.tsdk: node_modules/typescript/lib`); workspace-vs-user partition (team artifact vs personal preference); `typescript.tsdk` pre-answers the "Use Workspace Version" toast.
- **Cut** — Side-by-side `<Tabs>` view of the three files (outline floated this; lesson kept the linear `<Steps>` walk); explicit mention of `Cmd/Ctrl+Shift+P` / command palette; explicit mention of Settings Sync as the counter-pattern.
- **Debts** — `biome.json` rule configuration to Ch 028 L5; `tsconfig.json` authoring to Ch 028 L4; `.mise.toml` and Node version pinning to Ch 003 L8 (next lesson); `package.json` + pnpm install (which creates `node_modules/typescript`) to Ch 028 L2; Tailwind v4 (where the `[css]` Biome formatter override pays off) to Ch 018+.
- **Terminology** — "Team artifact" as the lesson's keystone phrase; "workspace" vs "user" (VS Code-native vocabulary, preferred over "global"); `<Term>` introduced for "LSP" (Language Server Protocol); "pin" as the verb for committing a tool decision to the repo (sets up L8's `mise use --pin` framing); `"explicit"` string form for `codeActionsOnSave` (mandatory since VS Code 1.83, replaces older `true`/`false`).
- **Patterns and best practices** — `.editorconfig` is the single source of truth for indent/line-endings (Biome reads it, no duplication in `biome.json`); `.vscode/extensions.json` provides structural enforcement via the clone-time install prompt; never push theme/font/keybindings/mini-map preferences to `.vscode/settings.json`; always pin `typescript.tsdk` to `node_modules/typescript/lib` so editor and build use the same `tsc`; install extensions via marketplace click or `code --install-extension <id>` — either works.
- **Misc.** — Setup/wiring lesson (the only non-language lesson in Ch 003) sitting between L6 (regex) and L8 (local `.ts` execution). Components used: `Steps`, `Card`+`CardGrid`, `Aside` (`type="tip"` via `:::tip` markdown form), `Term`, `ExternalResource`, `Buckets`+`Bucket`+`Item`. Opens cold (no intro h2) with the whitespace-storm PR failure mode. No video, no diagram, no live coding. Closing `<Buckets twoCol>` has 2 buckets / 6 items partitioning workspace vs user settings. Icons used: `biome`, `seti:css`, `seti:config`, `seti:git`, `warning`, `seti:typescript` for extension cards; `simple-icons:visualstudiocode` (with `iconColor="#007ACC"`), `simple-icons:biome`, `lucide:file-code-2` for external resources. The `node_modules/typescript/lib` path is pinned even though the directory doesn't exist yet — harmless, picked up the moment Ch 028 L2 installs deps.

## Lesson 8 — Run TypeScript locally

- **Taught** — Runtime pinning principle (runtime as property of repo) with mise as the 2026 default (Rust, polyglot, reads `.tool-versions`/`.nvmrc`); Node 24 LTS lifecycle (Active LTS through Oct 2026, Maintenance through Apr 2028); four-step mise install procedure (`brew install mise` / `curl https://mise.run | sh` → `eval "$(mise activate zsh)"` in rc → `mise use --pin node@24` writes `.mise.toml` with full version string → `mise install` + `mise current`); the three-path partition for `.ts` execution gated by trigger-before-tool (native `node` as default — strips types, doesn't read tsconfig, no transform, no type-check; `tsx` past five triggers — path aliases, JSX, decorators, `enum`, `namespace` — reads tsconfig, uses esbuild, dev-only never prod; `tsc --noEmit` for type-check, `tsc` for library publish emit); `pnpm dlx` / `npx` as one-shot install pattern before `package.json` exists; `ts-node` flagged as legacy.
- **Cut** — `--experimental-transform-types` flag dropped (not mentioned at all, no forward-pointer); `tsx watch` mode named in one clause but not demonstrated; the `<CodeVariants>` tabbed summary of the three invocations not used (kept linear `<Steps>` walk); `ts-node` mention compressed to one paragraph rather than a labeled callout.
- **Debts** — `pnpm` install and `package.json` to Ch 028 L2 (forward-linked twice; switches `pnpm dlx tsx` → `pnpm tsx`); full `tsconfig.json` authoring (strictness flags, target, lib) to Ch 028 L4 — the worked example uses a minimal 6-line tsconfig with `module: "preserve"`, `moduleResolution: "bundler"`, `baseUrl: "."`, `paths: { "@/*": ["src/*"] }`; Next.js owns application `.ts` compilation pipeline from Ch 028 onward; Biome/ESLint config to Ch 028 L5; CI gating on `tsc --noEmit` to the project chapters.
- **Terminology** — "Trigger before tool" recurring framing (carries from Ch 003 L1's "the three triggers", L4's "named threshold"); "the partition" / "three paths" for `.ts` execution; "the five triggers" that flip native `node` to `tsx`; `LTS` defined via `<Term>` (Long-Term Support, Active vs Maintenance phases); "type-stripper" as native Node's mechanism; "the runtime is a property of the repo" as keystone phrase (parallel to L7's "team artifact"); `.mise.toml` as the committed pinning artifact (alongside `.editorconfig`/`.vscode/*` from L7).
- **Patterns and best practices** — Commit `.mise.toml` at repo root with `mise use --pin` (full version string, not loose major); `eval "$(mise activate <shell>)"` in shell rc as mandatory activation step; native `node file.ts` by reflex for plain scripts (relative imports, no JSX/decorators/enums/namespaces); `tsx` only past one of the five triggers and never as a production runtime; `tsc --noEmit` as the dedicated type-checker (silent success convention — no output = pass); `pnpm dlx <tool>` for one-shot pre-`package.json` invocation, `pnpm <tool>` once installed; `moduleResolution: "bundler"` is the canonical fit for `tsx`/esbuild; never reach for `ts-node` in 2026; relative imports (`./lib/x.ts`) or bare specifiers only when running with native `node`; path aliases (`@/...`) require `tsx` or a bundler.
- **Misc.** — Closes Chapter 003's teaching content (L9 is the chapter quiz). Components used: `Steps` (mise install + worked example), `Figure` + Mermaid `flowchart LR` (the three-path decision tree — single diagram in the lesson), `FileTree` (post-step-3 layout in worked example), `Term` (`LTS`), `ExternalResource` + `CardGrid` (4 cards: Node TypeScript docs, mise getting-started, tsx README, Node release schedule), `Matching` + `Pair` (closing 5-pair reflex-check; duplicate right-side values intentional). Aside `:::caution` for `tsx`-not-in-prod, `:::note` for the partition one-liner. Opens cold (no intro h2) with two contributor/CI failure modes. No video, no live coding. Seed file is `hello.ts` with `greet(name: string): string` — no invoice domain (the lesson is tooling, not language). Worked example uses `pnpm dlx tsx` and `pnpm dlx tsc --noEmit` (pre-package.json forms). Node 24 LTS framing corrected from the chapter outline's looser "maintenance through late 2027" to "Active LTS through Oct 2026, Maintenance through Apr 2028"; future lessons should follow this exact framing. Sub-version specificity ("Node 24.12 forward" for native strip-types) deliberately relaxed to "stable in Node 24 LTS, no flag" — the safer claim.
