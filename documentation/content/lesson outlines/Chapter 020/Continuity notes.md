# Chapter 020 — Layout and sizing

## Lesson 1 — The box model and the inline/block axis

**Taught.** The four boxes (content/padding/border/margin); `border-box` (Preflight default) makes widths compose by addition (`w-64 p-4 border` = 256px); the `--spacing` scale (`calc(var(--spacing)*n)`, default `0.25rem`/4px, 4px grid) feeds every `p-*`/`m-*`/`gap-*`; margin collapse as recognition-only smell (collapses to larger value, only block elements in normal flow on vertical axis); the inline/block axis and logical utilities (`ps-*`/`pe-*`, `ms-*`/`me-*`, block-axis `pbs-*`/`pbe-*`/`mbs-*`/`mbe-*`); `mx-auto max-w-*` centering pairing; DevTools box-model panel.

**Cut.** The `outline`/`focus-visible:outline-2` watch-out (outline takes no layout space) was dropped — register it elsewhere if a focus-ring lesson needs it; not yet taught.

**Debts.** Forward-references this chapter: `gap` full treatment + sibling-margin-forbidden rule (L6), flex centering `justify-center` (L3), grid `place-items-center` (L4), `max-w-3xl`/sizing model (L5), logical `inset-s-*`/`inset-e-*`/`inset-bs-*`/`inset-be-*` positioning behavior (L7). Assumes from prior chapters: Preflight set `box-sizing: border-box` + stripped margins (019.3), `@theme`/`--spacing`/three-tier tokens (018.2/019.4), utility+variant syntax and `cn()` (018).

**Terminology.** "Four boxes" named inside-out: content / padding / border / margin. DevTools overlay color mapping (intentional coupling): content=blue, padding=green, border=yellow, margin=orange/tan — the opening SVG uses the same mapping; reuse it for any later box-model visual. "inline axis" = text-flow direction (horizontal/LTR in English); "block axis" = perpendicular (vertical). "normal flow", "RTL" defined via `Term`. `mx-auto` = `margin-inline: auto`.

**Patterns and best practices.** Never override Preflight's `box-sizing` with a custom `*` rule. Stay on the spacing scale; rescale via `@theme --spacing`, don't litter arbitrary values (`p-[17px]` is a smell). Logical utilities (`ps-*`/`pe-*`) over physical from day one in any RTL-bound project. Space siblings with `gap` on a flex/grid parent, not margins (avoids collapse). `mx-auto max-w-*` only for a lone block in normal flow — flex/grid parents center via the container. Negative margins are a deliberate rare effect, not a spacing patch.

**Misc.** Hard-coded accent colors inside `ParamPlayground`/SVG previews are a deliberate divergence from the OKLCH-token convention (illustrations, not project code) — don't route through `@theme`. No DevTools screenshot was included for the box-model panel (prose + color-matched SVG carry it); a downstream screenshot could still land it. Examples authored at a single breakpoint (no responsive variants — those are Chapter 021).

## Lesson 2 — Display modes and the hide decision tree

**Taught.** `display` as outer-role (block-level vs inline-level) × inner-formatting-context (normal flow / flex / grid) — `inline-flex`/`inline-grid` explained as the cross-product, not magic keywords; the modes a senior reaches for (`block`, `inline`, `inline-block`, `flex`, `inline-flex`, `grid`, `inline-grid`, `contents`, `hidden`→`none`) each by *when you reach for it*; `inline-block` reach is narrow (in-text sized thing) and usually really wants flex-on-parent; `contents` removes the wrapper's box so children promote to grandparent's flex/grid (semantic-wrapper-breaking-flex fix, Fragment-first ordering); Preflight does NOT normalise `display`; the two-tree model (layout tree × accessibility tree) and the three hide tools — `display:none`/`hidden` (both trees, ≡ conditional render which also tears down state), `visibility:hidden`/`invisible` (keeps box+space, drops a11y; flagged as usually-a-sizing-smell), `aria-hidden="true"` (visible+space, drops a11y only; decorative-icon-beside-label case).

**Cut.** `list-item` display value dropped entirely (outline listed it recognition-only); two-value `display` syntax (`block flow`/`inline flex`) kept as name-recognition only; floats/clears mention dropped (outline's "floats are dead" watch-out not carried — register elsewhere if a float lesson ever needs it).

**Debts.** Defers to this chapter: flex/grid alignment mechanics `items-center`/`gap`/`justify-*` (L3/L4), the `invisible` "reserve space" anti-pattern's real fix `min-h-*`/`aspect-ratio`/grid sizing (L5). Defers forward: `sr-only` internals + full `sr-only`/`aria-hidden`/`hidden` ARIA decision + live regions (Ch027 L3 owns — this lesson only names `sr-only` as a decision-tree leaf); responsive display toggling `hidden md:block`/`md:flex` (Ch021 L6). Assumes: HTML semantics + `aria-label` on icon-only buttons (Ch017 L4, used as contrast), Fragments + `&&` zero-trap conditional render (Ch017 L1), Preflight (Ch019.3 / Ch020 L1), `border-box`+box model (Ch020 L1).

**Terminology.** "outer role / inner formatting context" is the load-bearing two-axis frame for `display` — reuse it. "layout tree" and "accessibility tree" + "the two trees" / "hide from what?" 2×2 — the namesake mental model; reuse for any later a11y/hiding content. "phantom control" = `aria-hidden` on a focusable element (forbidden). "assistive technology / AT" defined via `Term`. Icon treatment rule to keep consistent: icon-only → `aria-label`; decorative-icon-beside-visible-label → `aria-hidden="true"`.

**Patterns and best practices.** Pick the semantic element first, then set its display — `display` never changes meaning. For icon-and-text buttons write `inline-flex items-center gap-2` explicitly (Preflight leaves `<button>`'s quirky default). Prefer a React Fragment to group children; reach for `display: contents` only when a real DOM node must exist (semantics/ref/attribute) and its box is unwanted — and test with a screen reader. Hiding decision order: ask "is it even present right now?" first → conditional render (`flag && <Node/>`, real-boolean left side) for state/route-driven presence; only then which-tree questions. Never `aria-hidden` on an interactive/focusable element. Treat `visibility:hidden`/`invisible` as a smell signalling a sizing fix.

**Misc.** Tailwind v4 source-order edge (fact-checked Jun 2026): `hidden` sits mid-cascade, so don't ship examples relying on `hidden` overriding a *later* inline-level display utility on the same element (bites `hidden md:inline-flex` reorderings — Ch021 territory). Reuses L1's hard-coded-accent `ParamPlayground` divergence and single-breakpoint convention. New lesson components: `src/components/lessons/020/2/DisplayOuterInnerMatrix.astro` (outer×inner grid) and `TwoTreeHideMatrix.astro` (2×2 layout×a11y) — reuse the two-tree matrix if a later a11y lesson wants the same visual. `ReactCoding` starter uses `export function App()` (sanctioned exception to the arrow-fn default for exercise scaffolds).

## Lesson 3 — Flexbox, the 1D primitive

**Taught.** Flex container × flex item (direct children only, no per-child opt-in); the two-axis model (main = flow direction, follows `direction`/RTL; cross = perpendicular) with the load-bearing reflex *name the axis, then pick the property*; `flex-row`/`flex-col` and `-reverse` (recognition-only, a11y trap); `flex-wrap` + `gap-y-*`-becomes-row-gap (light); the three item-sizing shorthands `flex-1` (grow+shrink from 0 basis → equal columns) / `flex-auto` (grow+shrink from content basis) / `flex-none`, plus `shrink-0` (items shrink by default — the avatar-squash fix), with longhand `flex-grow`/`flex-shrink`/`flex-basis` named once at the call site; alignment as axis×value — main `justify-*` (`start`/`end`/`center`/`between`/`around`/`evenly`, `between`=nav workhorse), cross `items-*` (`stretch` default explains full-height children / `start`/`end`/`center`/`baseline`), `items-baseline` for mixed-font-size text, `self-*` and `content-*` recognition-only; `gap-*` as the assumed spacing default (between-items only, no edge collision); the `min-w-0` fix (flex items default `min-width:auto` clamps at min-content → row blows out; `min-w-0`+`truncate` is the real recipe for a flexible text region); five canonical layouts (nav bar, form row, vertical card stack, toolbar `flex-1` spacer, pinned footer `flex flex-col h-full`+`flex-1` body); DevTools flex overlay.

**Debts.** Defers to this chapter: grid / flex-vs-grid decision (L4, named once as "next lesson"); sizing utilities `h-full`/`max-w-*` used unexplained in canonical layouts (L5 owns); full `gap` vs `space-x`/margin comparison (L6, only assumed here). Assumes: `display: flex`/`inline-flex` + outer-role×inner-context frame (L2, explicitly picked up in intro), box model + `border-box` (L1), `--spacing` scale feeding `gap-*` (L1), sibling-margins-forbidden (L1/conventions, used not litigated). `truncate` named lightly as a text utility (deferred typography lesson Ch021 L1 owns it).

**Terminology.** "flex container" / "flex item" / "main axis" / "cross axis" / "min-content" defined via `Term` — reuse these definitions; the main/cross frame is load-bearing for L4's grid axes too. "name the axis, then pick the property" is the lesson's spine reflex. "leftover space as a layout tool" = the `flex-1` spacer / pinned-footer idea — reuse the phrasing. Recipe to keep consistent: a flexible text region in a row is `flex-1 min-w-0` (+`truncate` for ellipsis), never `flex-1` alone; fixed-size items (avatars/icons/labels) get `shrink-0`.

**Patterns and best practices.** Every flex container with >1 item uses `gap-*`, never sibling margins. `flex-1 min-w-0` for fillable text regions; `shrink-0` on anything whose width is the design (avatars, icons, fixed labels, sidebars). Prefer `order-*` on one item over `flex-row-reverse`/`flex-col-reverse` (reverse breaks tab/source order — a11y defect). `items-baseline` (not `items-center`) when aligning text of mixed sizes in a row. Reach for the five canonical layouts rather than ad-hoc CSS. Open the DevTools flex overlay before guessing utilities when alignment looks wrong.

**Misc.** Reuses L1/L2 hard-coded-accent `ParamPlayground`/SVG divergence and single-breakpoint convention (no `md:` responsive variants — Ch021). `ReactCoding` starters/targets use `export function App()`. Main axis "follows `direction`" noted to keep RTL story honest without introducing logical inset utilities (L7). New lesson components: `src/components/lessons/020/3/AxisFrame.astro` (row/column axis arrows, real flex), `BaselineAlign.astro` (items-center vs items-baseline contrast), `FlexLayout.astro` (renders the five canonical patterns by `pattern` prop — reuse if a later lesson wants a live flex pattern). Includes a `VideoCallout` (videoId `53kRTGGDYd8`).

## Lesson 4 — Grid, the 2D primitive

**Taught.** Grid container × grid item (reuses L3's container×item frame; grid *defines* 2D tracks, items snap into cells auto-placed left-to-right/top-to-bottom unless they declare a spot); track sizing menu (fixed length, `fr`, content keywords `auto`/`min-content`/`max-content`/`fit-content()`, `minmax(min,max)`); the load-bearing v4 fact — numeric `grid-cols-N`/`grid-rows-N` compile to `repeat(N, minmax(0,1fr))` not `repeat(N,1fr)`, so the `minmax(0,…)` floor lets columns shrink below content (same trap as L3's `min-w-0`); only the hand-written bracket `grid-cols-[repeat(3,1fr)]` form bites with overflow; the `fr` unit (= one share of leftover space); `repeat(auto-fit,minmax(16rem,1fr))` for breakpoint-free reflow (vs `md:`/`lg:` counts when the design names counts) + `auto-fit` (collapses empty trailing tracks, the card default) vs `auto-fill` (keeps phantom tracks); named template areas via bracket form `[grid-template-areas:'…']` + `[grid-area:name]` (underscore=space, adjacent row strings, rectangular template); `subgrid` (`grid-rows-subgrid`/`grid-cols-subgrid` + the card must `row-span-N` the parent's tracks) for cross-card internal alignment; alignment shorthands `place-items-*` (item within its cell, longhands `items-*`/`justify-items-*`) vs `place-content-*` (whole track group within container, longhands `content-*`/`justify-content-*`) + `place-self-*`; item placement — span (`col-span-*`/`row-span-*`/`col-span-full`, clamps if span > track count) and pin (`col-start-*`/`col-end-*`, lines numbered from 1, N+1 lines for N columns) + named lines (name-check only); a glance table of grid utility groups; five canonical layouts (card grid breakpoints / card grid auto-fit / page shell / centered hero `grid place-items-center min-h-dvh` / dashboard featured tile `col-span-2 row-span-2`); flex-vs-grid decision (second-axis-first heuristic; "most SaaS UIs are a grid of flex compositions"); DevTools grid overlay.

**Cut.** `grid-flow-row`/`grid-flow-col`/`grid-flow-dense` and `auto-cols-*`/`auto-rows-*` appear in the glance table with the `dense` a11y caveat but get no worked example (table-only); named grid lines deliberately name-checked, not taught.

**Debts.** Defers forward: container queries `@container` (Ch021 L7, named once as the component-scoped cousin of `auto-fit`); responsive-variant *syntax* `md:`/`lg:` (Ch021 L6 — used here with a one-line aside, read as "more columns when wider"); full `gap` vs `space-x`/margin comparison (L6, only assumed). Uses-but-defers within chapter: `min-h-dvh`/`h-dvh`/`dvh` viewport unit named only (L5 owns sizing). Assumes: `display: grid`/`inline-grid` + outer-role×inner-context frame (L2), L3's full flex vocabulary + container×item + main/cross axes + `min-w-0` story (explicitly bridged), `gap` as spacing default + box model (L1), Tailwind utility/variant/bracket syntax (Ch018).

**Terminology.** `grid container` / `grid item` / `track` / `cell` / `fr unit` / `app shell` / `breakpoint` / `media query` / `Baseline` defined via `Term` — reuse. "flex *distributes* leftover space on one axis; grid *defines* a structure on two and items snap in" is the chapter's two-primitive split line. Reuses L3's "leftover space as a layout tool" for `fr`. Bracket-area authoring gotchas to keep consistent: underscore = space, adjacent row strings (`'header_header''sidebar_main'`), names must match exactly + template rectangular. Heuristic spine: "does a second axis need to line up?" → grid, else flex.

**Patterns and best practices.** Equal/exact columns, page shells, dashboards, cross-card alignment, responsive column *count* → grid; one row/column of variable-content items → flex; nest grid (skeleton) over flex (flesh). Use numeric `grid-cols-*` (never hand-written `repeat(N,1fr)`) so the `minmax(0,…)` floor is baked in; if you must hand-write, write `minmax(0,1fr)` yourself. `auto-fit minmax` when only a min card width matters; `md:`/`lg:` counts when the spec names them. Canonical v4 page shell is the bracket `[grid-template-areas:…]` + `[grid-area:…]` form (no plugin — core Tailwind ships no grid-area utility); prefer plain `col-span-full` over named areas for a sidebar-less header/main/footer shell. Subgrid is Baseline — ship without fallback. `grid place-items-center min-h-dvh` is the centering one-liner. Match `col-span-N` to track count. Open the DevTools grid overlay before guessing utilities.

**Misc.** **Deliberate convention divergence (flagged for downstream): the card-grid pattern uses `md:`/`lg:` responsive variants despite the chapter's single-breakpoint convention — grid's headline use case is meaningless without them; do not strip, the Ch021-syntax aside is intentional.** **Chapter-outline correction folded in: core Tailwind v4 has no `grid-areas-*`/`area-*` utilities (plugins only) — do not reintroduce them.** Reuses hard-coded-accent `ParamPlayground`/SVG divergence and `export function App()` scaffolds. New lesson components under `src/components/lessons/020/4/`: `GridLayout.astro` (renders the five canonical patterns by `pattern` prop — mirrors L3's `FlexLayout.astro`, reuse for any later live-grid need), plus `RaggedFlexVsEvenGrid`, `AutoPlacementCells`, `ShellLayout` (variant desktop/mobile), `SubgridBeforeAfter` (variant with/without), `PlaceItemsVsPlaceContent`, `GridLineNumbers`. Includes two `VideoCallout`s (subgrid `APxt2mKOsss`, flex-vs-grid `aEj6k-gi9-s`).

## Lesson 5 — Sizing, viewport units, and aspect-ratio

**Taught.** The one model — every dimension is **content-driven (intrinsic)** or **forced (extrinsic)**; the load-bearing asymmetry "a block element is forced on width, content-driven on height" (explains why `width:100%` is usually redundant and `height:100%` usually no-ops on a `<div>`); display mode already claims one axis, a sizing utility only bites on the free axis; content-keyword vocab `min-content`/`max-content`/`fit-content` (Tailwind `w-min`/`w-max`/`w-fit`, recognition + `w-fit` in practice); forcing utilities `w-*`/`h-*` on the `--spacing` scale, `w-full`(=100%), fractions `w-1/2`, arbitrary `w-[640px]` (smell), `w-screen` recognition-only (scrollbar-gutter overflow), the v4 `size-*` square shortcut (square→`size-*`, rectangle→`w-*`+`h-*`); `min-*`/`max-*` as post-algorithm clamps — `max-w-prose`/`max-w-[65ch]`+`mx-auto` reading-width cap (~65ch sweet spot, `ch`=width of "0" glyph), `min-w-0` re-explained through the model (flex item's default min is `min-content`, `min-w-0` drops the floor so extrinsic flex sizing wins), `max-h-dvh overflow-y-auto` tall-panel shape, `w-fit` to opt one `items-stretch` child back to content-driven; the viewport-unit family `lvh`(=`vh`,largest)/`svh`(smallest)/`dvh`(dynamic), `min-h-dvh` as THE reflex (what `min-h-screen` should be), `h-dvh` for exact-fill hero, `svh` when stillness matters (the `dvh` re-measure-mid-scroll tradeoff), legacy `*-screen`→`dvh` mechanical fix, `*-dvw` cousins rare; `aspect-ratio` (`aspect-square`/`aspect-video`=16/9/`aspect-[4/3]`/`aspect-auto`) + `object-cover` for zero-CLS media, derives height from width, replaces `padding-bottom:56.25%` hack (recognition-only), flex/grid collapse watch-out fixed by `min-w-0`; `clamp(min,preferred,max)` named once (+`min()`/`max()` one-sided cousins), one form, forward-pointed; unit decision table (`rem`=default via scale, `dvh`=viewport-fill, `ch`=reading width, `px`=hairlines, `fr`=grid only, `%`/`em` rare).

**Cut.** Nothing of downstream consequence — the chapter-outline scope was delivered nearly whole; `dvw` and `em`/`%` kept to a single sentence each (intentional), no worked examples for them.

**Debts.** Forward: container queries `@container`/`cqi` + **fluid typography with `clamp()`** (Ch021 L7 — `clamp()` named here, deep dive deferred); responsive variants `md:`/`lg:` (Ch021 L6 — all examples single-breakpoint); typography `text-*`/`truncate`/`text-wrap` (Ch021 L1 — `truncate` used unexplained in `min-w-0` recipe); `overflow-*`/scroll containers (Ch020 L8 — `overflow-y-auto` named, shape only); `next/image`/`srcset`/`object-*` depth (out of scope, `object-cover` named once). Assumes: `--spacing` scale + `mx-auto`/`max-w-*` + `border-box` (L1), inline ignores width/height (L2), `min-w-0`/`items-stretch` flex-collapse story (L3, explicitly re-explained through the model), `grid place-items-center`/`min-h-dvh`/`dvh` named-not-taught (L4 deferred dvh definition to here — now paid off), Tailwind utility/bracket syntax (Ch018).

**Terminology.** "content-driven vs. forced" = the lesson's plain-words frame; `intrinsic`/`extrinsic` are the proper names (defined via `Term`, recognition depth). The spine reflex: **"is this dimension content-driven or forced?"** — reuse for any later sizing debugging. "the display mode already decided which axis is forced" — reuse. `CLS` and `viewport` defined via `Term` (load-bearing); `ch`, `dvh`/`svh`/`lvh`, `min-content`/`max-content`/`fit-content` via `Term`. Recipes to keep consistent: square→`size-*`; reading width→`max-w-prose`; fill viewport→`min-h-dvh` (never `min-h-screen`); late-loading media→`aspect-*`+`object-cover`; fluid size→`clamp()`; shrinking flex text→`flex-1 min-w-0`.

**Patterns and best practices.** `min-h-dvh` not `min-h-screen` on every full-height shell (the single most important sizing fix; `h-screen`/`min-h-screen` are everywhere in old code — recognize and convert). `size-*` for any square element. `max-w-prose`/`max-w-[65ch]`+`mx-auto` to cap reading width. Reserve late-loading media with `aspect-*`+`object-cover` (zero CLS). Stay on the `--spacing` scale; `mt-[37px]`-style arbitrary values are a smell — rescale `--spacing` in `@theme` instead (reinforces L1). Prefer `min-*`/`max-*` clamps (work with the algorithm) over forcing sizes. `w-full` inside a constrained container, never `w-screen` (scrollbar overflow). Reach for `rem`/the scale by default; every other unit has one specific job. `clamp()` for a size that breathes between two bounds (responsive without breakpoints — same instinct as grid `auto-fit minmax`).

**Misc.** Reuses hard-coded-accent `ParamPlayground`/SVG divergence (OKLCH playground accents are illustrations, not project code) and `export function App()` exercise scaffold. **All examples authored at a single breakpoint** — the L4 card-grid `md:`/`lg:` divergence does NOT extend here (sizing examples are meaningful without responsive variants). New lesson components under `src/components/lessons/020/5/`: `IosVhGap.astro` (phone-frame showing the `100vh` dead-strip/hidden-CTA bug), `IntrinsicExtrinsicAxes.astro` (axis-coloring diagram for block/inline/flex-item — **new color mapping distinct from L1's box-model four**, reuse if a later lesson revisits the model), `ViewportUnitSim.astro` (toggle address bar, watch `vh`/`lvh`/`svh`/`dvh` gap/overflow/track). Three `ParamPlayground`s carry reading-width, aspect-ratio, and the unit feel. Includes one `VideoCallout` (Kevin Powell dvh/svh/lvh, `ru3U8MHbFFI`). Capstone `ReactCoding` is target-match (visual + AI feedback, not brittle pixel asserts) building a zero-shift media card combining `size-*`/`aspect-video`/`max-w-prose`/`min-h-dvh grid place-items-center`.

## Lesson 6 — Gap, the universal spacing default

**Taught.** The **padding / gap / margin spatial model** ("where does this space sit relative to the element?" — padding = inside, gap = between flex/grid siblings, margin = outside/push from a non-sibling), the lesson's anchor and the senior conclusion *2026 writes padding+gap constantly, margin almost never*; `gap` as the universal default (one property on a flex/grid/multi-col parent, compiles to `gap: calc(var(--spacing)*n)`, between-items-only/no edge collision — parent `padding` owns edges, reflows on wrap and count change, `gap-x-*`/`gap-y-*` = column-gap/row-gap split, plain `gap` = shorthand for both); the "gap doesn't work in flexbox" worry is dead (universal support since 2021, ship without fallback); why `gap` beats the legacy patterns via the shared per-child-by-DOM-position mechanism — four failure modes (wrap breakage, **hidden/reordered children**, margin collapse, RTL physical-margin non-flip); `divide-y-*`/`divide-x-*` for visible hairlines between children (composes with parent `border`+`rounded-*` for the bordered-list-card, `divide-color-*` to tint, *not* a substitute for `gap` — divide draws a line, gap adds space, often both); the gap-vs-margin decision rule (gap between flex/grid siblings ~90%, margin only to push from something *outside* the container or where there's no flex/grid parent, never margin between flex/grid siblings).

**Cut.** Multi-column `gap` reduced to a half-sentence (recognition-only, per outline); `grid-gap` legacy-form name and the "don't mix `gap` and `space-x` on one container" watch-out from the outline were dropped (low-value, not load-bearing). Nothing a later lesson plausibly depends on.

**Debts.** Assumes: `--spacing` scale + margin collapse + `mx-auto` (L1, reused not re-taught), `flex`/`flex-col`/`order-*` (L3), `grid`/`grid-cols-*` (L4), `hidden`→`display:none` (L2), `@theme`/utility syntax (Ch018). Forward: responsive spacing variants `md:gap-*` (Ch021 L6 — all examples single-breakpoint); logical-inset RTL depth (L7); `<hr>` semantic break vs `divide` decorative rule (Ch017 L6, not raised here).

**Terminology.** "padding / gap / margin — one spatial model" (inside / between / outside) is the load-bearing frame — reuse it for any later spacing decision. "siblings" = direct children of one parent, defined via `Term` (gap spaces siblings, not grandchildren or the parent's outer edges). The legacy mechanism shape to keep consistent: `space-*` and `divide-*` both compile to `:where(& > :not(:last-child))` → `margin-bottom`/`border-bottom` on **all-but-the-last** child (Tailwind v4; do NOT regress to v3's all-but-the-first/`margin-top`). The "smell" phrasing: a sibling `margin` inside a flex/grid container is the smell, fix = delete it and put `gap` on the parent.

**Patterns and best practices.** `gap` on a flex/grid parent for between-sibling spacing; parent `padding` for edge spacing — the canonical container is `flex flex-col gap-* p-*`. Never sibling margins inside a flex/grid container (re-affirms L1/L3). `space-x-*`/`space-y-*` and the `* + *` "lobotomized owl" are **recognition-only** (dead for new code, alive in legacy + AI output trained on it); the fix is almost always `flex flex-col`+one `gap`. `divide-*` for visible separators, composes with `gap`/`border`/`rounded-*`. The hidden-child failure is demonstrated with the `hidden` class (`display:none`) on the **last** child that stays in the DOM (NOT React conditional rendering — a v4-shaped phantom-trailing-gap, since the hidden node remains the structural last child and the now-visually-last row keeps the between-margin); `gap` is immune because `display:none` children are pulled out of layout. `divide-*` carries the same hidden-last-child / `order-*` sensitivity (accepted because visible-separator-between-dynamic-items is rare and has no gap-equivalent) — flagged in a `:::caution`, not avoided.

**Misc.** Reuses the hard-coded-accent `ParamPlayground`/SVG illustration divergence and `export function App()` scaffold conventions; single-breakpoint throughout (no `md:`). New lesson component: `src/components/lessons/020/6/SpatialModelDiagram.astro` (the padding/gap/margin centerpiece — reuse if a later lesson revisits the three-slot model). Interactivity weight: one gap+direction `ParamPlayground`, two `CodeVariants` (legacy-vs-modern for wrap and hidden-child, shared `syncKey="legacy-vs-gap"`), one `ReactCoding` target-match (convert `space-y-4`→`flex flex-col gap-4` with a `hidden` last child), one three-way padding/gap/margin `Buckets` drill. Includes a `VideoCallout` (Kevin Powell, `Gx0iZrN-0L4`, "gap is NOT a replacement for margins" — the margin-for-prose nuance).

## Lesson 7 — Position and inset utilities

**Taught.** The five `position` modes framed by *what each is positioned against* (not as vocab): `static` (flow default, offsets inert), `relative` (in flow, offsets nudge visually but its real 2026 job is to *open a containing block* for `absolute` children), `absolute` (out of flow, anchors to nearest positioned ancestor), `fixed` (out of flow, anchors to viewport, survives scroll), `sticky` (hybrid — relative-in-flow until scroll hits its offset, then fixed-but-only-within-parent-bounds); the in-flow/out-of-flow binary (static/relative/sticky-before-threshold stay, absolute/fixed leave); the **containing block** model — `absolute` offsets measured from the nearest ancestor with `position` set (relative/absolute/fixed/sticky), falling back to the **initial containing block** (≈viewport) if none → the canonical bug (badge flies to page corner) fixed by the **`relative` parent + `absolute` child** reflex (the lesson's spine); the `fixed`-captured-by-a-`transform`/`filter`/`perspective`-ancestor trap (recognition-only, fix = portal, deferred to L9/Ch022 L5); the `inset-*` family in tiers — single-edge `top-*`/`right-*`/`bottom-*`/`left-*` (+`top-full`, fractions, **negative** `-top-2` for overhang), axis pairs `inset-x-*`/`inset-y-*`, all-four `inset-*` with `inset-0` = "fill the containing block" (the full-cover/backdrop pattern), logical `inset-s-*`/`inset-e-*`/`inset-bs-*`/`inset-be-*` (RTL-aware, replaced deprecated `start-*`/`end-*`); the three `sticky` preconditions (offset set, scrollable ancestor, parent tall enough); five canonical layouts (badge on card, sticky section header, toast `fixed bottom-4 right-4 z-50`, side drawer `fixed inset-y-0 right-0 w-72`, full-cover overlay `absolute/fixed inset-0`); a two-question decision walker (leave flow? → how on scroll?); `pb-[env(safe-area-inset-bottom)]` one-line iOS watch-out; CSS Anchor Positioning + Popover API as forward-reference orientation (recognize the names, shadcn wraps them, don't hand-roll).

**Cut.** The `pointer-events-none` click-through-overlay variant (outline named it on the full-cover pattern) was dropped — register elsewhere if a later overlay/hover lesson needs it. `top-full`/fractional offsets mentioned but not worked. `CodeTooltips` underlying-CSS pass (optional in outline) skipped.

**Debts.** Defers to this chapter: `z-50`/stacking-context numerics + the `transform`/`filter` containing-block trap's z-index half (L9 — `z-50` named once at the toast, trap named once pointing forward); overflow modes + scroll-container mechanics that `sticky` depends on + `overflow-hidden`-clips-the-overhang nod (L8). Defers forward: React Portals / portal-to-`<body>` as the `fixed`-trap fix (Ch022 L5, named not implemented); shadcn Popover/Dropdown/Tooltip wrapping anchor positioning + Popover API + portaling (Ch027 L1); `transform`/`translate-*` motion (Ch021 L5, named only as a containing-block trigger); responsive variants `md:fixed`/`lg:sticky` (Ch021 L6 — single-breakpoint throughout); `useLockBodyScroll` (project Ch028 L8); a11y depth for the badge count `aria-label`/`sr-only` (Ch027 L3, one-line nod). Assumes: normal flow (L1/L2, refreshed one line), `--spacing` scale feeding `inset-*` (L1), logical-property concept `ps-*`/`pe-*` (L1, extended to insets), `size-*` (L5) + `flex-1 min-w-0`+`truncate` (L3) reused unexplained in the capstone, utility/bracket syntax (Ch018).

**Terminology.** "what is this element positioned against?" = the load-bearing frame for the five modes — reuse. "containing block" (rectangle an out-of-flow element's offsets resolve against — for `absolute`, the padding box of the nearest positioned ancestor) and "initial containing block" (viewport-sized fallback) defined via `Term`. "normal flow", "in flow / out of flow", "inset" (shorthand for the four offset properties), "top layer" (browser-managed layer above all content where popovers/dialogs render, escaping every stacking context) defined via `Term`. The spine reflex: **`relative` parent + `absolute` child is the atomic unit of corner-positioning** — reuse the phrasing. Recipes to keep consistent: corner badge = `relative` parent + `absolute -top-* -right-*` child; fill a region = `absolute/fixed inset-0`; full-height edge panel = `fixed inset-y-0 {edge}-0 w-*`; toast = `fixed bottom-4 right-4 z-50`.

**Patterns and best practices.** Treat `position` as a small/surgical tool — most layout stays flex/grid. `relative` on the parent the instant you write `absolute` on a child (the reflex). Logical insets `inset-s-*`/`inset-e-*` over physical `left-*`/`right-*` from day one in any RTL-bound project (mirrors L1's stance); physical acceptable in LTR-only with mechanical migration later. Author `inset-s-*`/`inset-e-*` not the deprecated `start-*`/`end-*` (both compile). Stay on the `--spacing` scale; `pb-[env(safe-area-inset-bottom)]` and genuine one-off arbitrary insets are the sanctioned exceptions. Don't hand-roll floating-UI positioning math — reach for the shadcn wrapped component (it also portals to `<body>` to dodge the stacking/containing-block traps). Semantic elements in the canonical layouts (`<article>`/`<aside>`); a corner count badge needs an `aria-label` to reach AT.

**Misc.** Reuses the hard-coded-accent `ParamPlayground`/SVG divergence and `export function App()` scaffold; single-breakpoint throughout. **Author-flagged figure constraint (honor downstream): `fixed`/`sticky` patterns inside figure cards are faked with framed `absolute inset-0` containers so they don't pin to the real viewport and break the page — captions say "in your app this is `fixed`; here it's framed."** Playground `select` deliberately restricted to `static`/`relative`/`absolute` (fixed/sticky are awkward in a small preview frame — demonstrated in the galleries instead). Fact-checks held (Jun 2026): `inset-s-*` family landed Tailwind v4.2 replacing `start-*`/`end-*`; CSS Anchor Positioning Baseline 2026 (Chrome 125+, Firefox 147+, Safari 26+). New lesson components under `src/components/lessons/020/7/`: `ContainingBlockDiagram.astro` (the absolute-child-climbs-the-tree before/after illustration), `InsetUtilityReference.astro` (inset family reference), `PositionLayouts.astro` (renders the five canonical patterns by `pattern` prop — mirrors L3's `FlexLayout`/L4's `GridLayout`, reuse for any later live position pattern). Interactivity: one position-mode `ParamPlayground` (centerpiece), one `Buckets` (in-flow vs out-of-flow), one `CodeVariants` (badge escape/fix, `syncKey="badge-fix"`), one `TabbedContent` gallery (`syncKey="position-layouts-gallery"`), one `StateMachineWalker` (kind="decision"), one `ReactCoding` target-match (notification card — badge `absolute -top-1 -right-1` inside `relative` `<article>`). Includes a `VideoCallout` (Kevin Powell sticky-vs-fixed, `8MaCTDkoVd8`).

## Lesson 8 — Overflow and scroll containers

**Taught.** The **scroll container** as the lesson's central noun (a box that clips overflow and owns its own scrolling; the page `<body>` is one, an inner box becomes one given `overflow` + a height it can exceed); the five `overflow` modes framed by *two* questions — does it clip? does it create a scroll container? — `visible` (default, spills, no container), `hidden` (clips, **silently creates a scroll container** — the side effect that hosts a `sticky` child and clips L7's badge), `clip` (clips with **no** scroll container, can't be scrolled even by JS — the 2026 side-effect-free clipper), `auto` (scrollbar only when overflowing — the senior default for any scroll region), `scroll` (always-visible bars, recognition-only); per-axis `overflow-x-*`/`overflow-y-*` with the two canonical pairings (`overflow-x-auto overflow-y-hidden` horizontal scroller, `overflow-y-auto overflow-x-hidden` vertical panel) and the spec rule that one axis `visible` + the other a scrolling value silently promotes `visible`→`auto`; `overscroll-behavior` as the scroll-chain/edge control — `overscroll-contain` (stop the chain, keep native bounce — the reach for every modal/drawer/dialog body/inner sidebar), `overscroll-none` (stop chain + kill bounce, full-screen surfaces), `overscroll-auto` (default), per-axis variants — fixing the iOS pull-to-refresh + page-behind-modal bug in one class; the **sticky-inside-overflow** canonical pattern paying off L7's deferred precondition (`h-dvh overflow-y-auto overscroll-contain` box + `sticky top-0` header — the two are a pair, pull either and it breaks); `scrollbar-gutter-stable` (reserve the gutter so a conditionally-appearing scrollbar doesn't jolt content) + `-auto`/`-both`; the **page-scroll vs. app-shell-scroll** architectural decision (page = `<body>` scrolls, the 2026 default, gives scroll restoration/back-forward/anchors free; app-shell = inner `<main>` scrolls with chrome fixed, dashboards, costs manual scroll-restoration); `scroll-snap-*` carousels with no JS library — `snap-x`/`snap-y` + `snap-mandatory`/`snap-proximity` on the container, `snap-start`/`snap-center`/`snap-end` on each child, `scroll-px-*` (scroll-padding) to inset snap points past edges/sticky headers; the dead `-webkit-overflow-scrolling: touch` hack (recognition/delete-it only).

**Cut.** Nothing of downstream consequence — the chapter-outline scope was delivered whole. The `useLockBodyScroll` body-lock pattern was named (and *why iOS makes it hard*) but deliberately deferred to project Ch028 L8 per outline, not built here.

**Debts.** Pays off L7's deferred "`sticky` needs a scrollable ancestor" (the ancestor *is* a scroll container) and L5's named-but-undefined `overflow-y-auto`. Forward: `useLockBodyScroll` hook implementation + the iOS touch-event handling (project Ch028 L8 — pattern + reason named, not implemented); React Portals as the `fixed`-trap/modal fix (Ch022 L5); shadcn Dialog/Drawer/ScrollArea wrap these scroll/overscroll patterns (Ch027 L1); accessible-carousel depth — items need per-card focusable elements (Ch027, one-line nod here); stacking context / `z-*` for overlays (L9, not raised); responsive variants `md:overflow-*` (Ch021 L6 — single-breakpoint throughout). Assumes: `sticky`/`fixed` modes + "needs scrollable ancestor" (L7), `h-dvh`/`min-h-dvh` + intrinsic-vs-extrinsic (L5), `flex`/`flex-col` + flex-items-shrink-by-default + `min-w-0` (L3), `gap` (L6), `--spacing` scale feeding `scroll-px-*`/insets (L1), utility/bracket syntax (Ch018).

**Terminology.** "scroll container", "scroll chaining", "pull-to-refresh", "scrollbar gutter", "scroll snapping" defined via `Term` — reuse. The load-bearing distinction to keep consistent: **`hidden` creates a scroll container, `clip` doesn't** (the section-1 payoff; why `hidden` can host `sticky` and `clip` can't). The spine question: **"who owns this scroll, and what happens when it reaches the end?"** Recipes to keep consistent: scrollable inner region = `overflow-y-auto overscroll-contain` on a height-constrained box; modal/drawer body = add `overscroll-contain` the moment you type `overflow-y-auto`; internally-scrolling sidebar = `h-dvh overflow-y-auto overscroll-contain` + `sticky top-0` header (+ `min-h-0` if it's a flex child); card carousel = `flex gap-4 overflow-x-auto snap-x snap-mandatory scroll-px-4` row + `w-* shrink-0 snap-start` cards.

**Patterns and best practices.** `overflow-y-auto overscroll-contain` on the scrollable element of every overlay (modal/drawer/dialog body, inner sidebar) — `overscroll-contain` is the one-class fix for the scroll-chain/pull-to-refresh bug, ship directly (Baseline universal, no fallback). Reach for `clip` over `hidden` when you want pure clipping with no scroll-container side effects; reach for `auto` over `scroll` always. `scrollbar-gutter-stable` on any container that conditionally overflows on interaction (search results, modal body, expandable panel) to kill the scrollbar jolt; leave native scrollbars unstyled by default (custom scrollbar utilities `scrollbar-thin`/`scrollbar-none`/`scrollbar-thumb-*`/`scrollbar-track-*` exist but are dashboard-chrome nicety, not baseline). Page-scroll (`<body>`) is the default — only take app-shell scroll when chrome must stay fixed *and* you'll own scroll restoration. Horizontal snap children need `shrink-0` (or fixed width) or flex squashes them and there's nothing to scroll (same family as L3's `min-w-0`/shrink story). `overflow-x`/`overflow-y` are the **deliberate physical exception** to the chapter's logical-first stance (they map to viewport axes, don't flip under RTL — there is no `overflow-inline`); state this once so the inconsistency isn't a surprise. Semantic `<aside>`/`<main>` in the app shell — the sidebar's scroll region is a real landmark; a genuinely-overflowing scroll container is keyboard-focusable by default but carousel *items* still need focusable links/buttons. Delete any `-webkit-overflow-scrolling: touch` found in old code.

**Misc.** **The `min-h-0`/`min-w-0`/`minmax(0,…)` through-line completes here** — this is its third appearance (L3 inline axis, L4 grid, here the block axis for app-shell scroll: a flex/grid child won't shrink below content *height* without `min-h-0`, so an inner `<main>`/sidebar never becomes scrollable). Name it as "the same floor, now on the vertical axis"; don't re-derive. **Fact-check correction folded in (corrects the chapter outline's stale "`scrollbar-gutter` via bracket form"): Tailwind v4.3 (May 8, 2026) ships first-party `scrollbar-gutter-stable`/`-auto`/`-both` plus scrollbar-styling utilities — author the first-party utility, not bracket form.** Reuses the hard-coded-accent `ParamPlayground` divergence and `export function App()` scaffold; single-breakpoint throughout. New lesson components under `src/components/lessons/020/8/`: `OverflowModesTable.astro` (the five-modes clips?/creates-container? table), `StickySidebarScroll.astro` (discrete `step={1|2|3}` frames for the sticky-inside-overflow `DiagramSequence` — the L7 framed-scroll-state fallback applied), `SnapCarousel.astro` (a real-CSS, devtools-inspectable snapping carousel — reuse for any later live scroll-snap need). Interactivity: one overflow-mode `ParamPlayground` (centerpiece, real CSS, with a "scroll container? yes/no" `Readout` surfacing the invisible `hidden`-vs-`clip` difference), one `Buckets` (creates-a-container vs not), one `CodeVariants` (scroll-chain bug/fix, `syncKey="overscroll-fix"`), one `DiagramSequence` (3-step sidebar scroll), one `StateMachineWalker` (kind="decision", page-vs-app-shell), one `Figure` (live carousel), one `ReactCoding` target-match (the internally-scrolling dashboard sidebar — the chapter's most-reused real layout). Includes a `VideoCallout` (Kevin Powell `overflow:hidden` alternatives, `72pUm4tQesw`).

## Lesson 9 — Stacking context and z-index

**Taught.** The two prerequisite facts gating everything: (1) `z-index` is inert on `static` elements — only bites on positioned (`relative`/`absolute`/`fixed`/`sticky`) elements and flex/grid items, (2) every page is a *tree* of stacking contexts (root on `<html>`), each a sealed "envelope" whose members sort internally then the whole context places as one unit in its parent — **z-index only competes inside the same context**; the browser's two-step sort (sort members within each context, then place each context as a unit among siblings) as the legible name for every "bigger number loses" bug; the canonical trapped-overlay bug (parent `opacity-95` creates a context → child `z-50` sealed inside, parent participates in root at `auto`≈0, sibling `z-40` beats it and paints over the whole envelope, child and all); the trigger list as *recognition by shape* not memorization — Always (`fixed`/`sticky`, no z-index needed), Positioned+real-z-index (`relative z-0` creates one, bare `relative` does not), the cosmetic villains (`opacity<1` headline, `transform`≠none, `filter`/`backdrop-filter`, `mix-blend-mode`≠normal, `contain: layout/paint/strict`, `will-change` of a context property), the deliberate side-effect-free one (`isolation: isolate`), one-clause nods (`container-type` → Ch021 L7; top-layer elements escape every context); the diagnostic reflex (don't bump the number — walk *up* the DOM for the first ancestor with `opacity<1`/`transform`/`filter`/`fixed`/`sticky`/`isolate`; that's the trap) + Chrome DevTools Layers panel (Cmd+Shift+P → "Show Layers"); the three fixes ordered by 2026 frequency — portal to `<body>` (escape the context; shadcn Dialog/Popover/Tooltip/Dropdown do this), `isolation: isolate` (deliberately *create* a context to contain layering, inverse of portal, zero visual effect, replaces `transform: translateZ(0)` hack), restructure the DOM (lift above the trapping ancestor when it's incidental, not when `opacity`/`transform` is load-bearing); the anti-pattern stated bluntly — `z-9999` is not a fix, a trap can't be beaten from inside it; the named z-tier convention (Tailwind's scale: `z-10`/`z-20` content, `z-30`/`z-40` chrome, `z-50` overlays/backdrop — finally explains L7's `z-50` toast) + the reframe that *portaling replaces the number race with DOM order* (last-rendered overlay paints last); `-z-10` puts a child behind its parent's own background (decorative layer), pair with `isolate`. The `transition: opacity` mid-fade flicker gotcha (element is briefly a context during the fade → portal it, or pair with `transform: scale`).

**Cut.** Nothing of downstream consequence — chapter-outline scope delivered whole. `perspective`/3D transforms and `will-change` performance tuning stayed name-only (intentional, niche). The `mix-blend-mode`-needs-`isolate` reach (outline named it under the `isolation` property) folded into the general isolate framing, not worked separately.

**Debts.** **Pays off L7's two deferred promises:** the `z-50` toast numeric (now "the overlay tier") and the `transform`/`filter` ancestor capturing a `fixed` child (now explained — that ancestor created a context + containing block; fix = portal). Closes Chapter 020's teaching arc. Forward (named, not implemented): React Portals / `createPortal` (Ch022 L5 — *the* fix and why shadcn floating UI works, recognize the shape only); shadcn Dialog/Popover/Tooltip/Dropdown portaling internals (Ch027 L1); `transform`/`transition` motion mechanics (Ch021 L5 — appear only as context triggers); `container-type` context creation (Ch021 L7, named once so it isn't a surprise there); responsive `md:z-*` (Ch021 L6 — single-breakpoint throughout). Assumes: the five `position` modes + containing-block model + the `fixed`-captured-by-`transform` trap + "z-index needs a position" + top-layer `Term` (L7, heavily leaned on, reused not re-taught), `transform`/`opacity`/`filter` exist as CSS properties (L7), flex/grid items (L3/L4), `--spacing`/utility/variant/bracket syntax + `cn()` (Ch018).

**Terminology.** "stacking context" = the load-bearing `Term` (self-contained z-axis group, sorted internally, placed as one unit in parent — the **sealed envelope** metaphor: shuffle pages inside, but the envelope is filed in one drawer slot; reuse the envelope metaphor for any later z-axis visual). "paint order" `Term` (later-painted covers earlier; "higher on z-axis" = "painted later" — used interchangeably with stacking). "the two-step sort" = the browser's sort-within-then-place-as-unit algorithm (reuse the phrasing). "portal" `Term` (render a node elsewhere in the tree, typically `<body>`, while staying logically in the React parent — full treatment Ch022 L5). "compositing layer" `Term` (DevTools Layers panel surface, *roughly* maps to stacking contexts — say "rough correspondence", not exact). `isolate` `Term` (Tailwind for `isolation: isolate`). top-layer reused from L7. The spine reframe: **z-index is local ordering scoped to a context, not a global "bring to front" dial** — `9999` loses because the envelope it's trapped in lost the contest that mattered. The diagnostic mantra: **"stop typing bigger numbers, start walking up the tree."** Senior pairing to hold together: *portal escapes a context; `isolate` creates one on purpose.*

**Patterns and best practices.** When z-index "doesn't work," walk up the DOM for the trapping ancestor — never escalate the number (`z-9999` is a reviewer-flagged smell). Portal floating UI (modal/popover/tooltip/dropdown/toast) to `<body>` — or reach for the shadcn component, which already portals. `isolate` on any component (card/section) whose internal layering must stay sealed from the page; pair `-z-10` decorative layers with `isolate` on the parent. Adopt Tailwind's default scale as a *named tier convention* the whole team shares (`z-10`/`z-20` content, `z-30`/`z-40` chrome, `z-50` overlay) rather than ad-hoc numbers; once portaled, lean on DOM paint order instead of high z-index. Pair opacity-fade transitions with `transform: scale` (or portal) to dodge the mid-fade flicker. **The project codebase's floating UI should never carry four-digit z-index — portaling + the tier scale is the standard.**

**Misc.** Reuses the hard-coded-accent `ParamPlayground`/SVG illustration divergence (illustrations, not project code — not routed through `@theme`) and `export function App()` exercise scaffold; single-breakpoint throughout. Honors L7's framed-figure honesty caption convention for any `fixed`/`sticky` overlay that would otherwise pin to the real viewport. Fact-check held (Jun 2026, MDN "Stacking context"): trigger list current — `fixed`/`sticky` create a context with no z-index; `relative`/`absolute` need a z-index other than `auto` (so `relative z-0` qualifies, bare `relative` does not); `opacity<1`, `transform`/`filter`/`backdrop-filter` (non-none), `mix-blend-mode` (non-normal), `isolation: isolate`, `will-change` of a context property, `contain: layout/paint`, `container-type: size/inline-size`, top-layer all qualify. New lesson components under `src/components/lessons/020/9/`: `StackingContextStack.astro` (discrete `step={1-4}` frames for the build-the-model `DiagramSequence` — mirrors L8's `StickySidebarScroll`, reuse for any later z-axis visual), `OpacityTrapPreview.astro` (the live opacity-trap `ParamPlayground` preview, real CSS reading the opacity param), `ZIndexTierScale.astro` (the named-tier reference table). Interactivity: one `ParamPlayground` (drag parent opacity, watch child snap behind sibling — real CSS), one `DiagramSequence` (4-step model + canonical bug), one `CodeVariants` (broken `opacity-95` trap vs portal fix, `syncKey="zindex-trap"`), one `Buckets` (creates-a-context vs not), one `StateMachineWalker` (kind="decision", "my z-index doesn't work → which fix?"), one `ReactCoding` target-match capstone (reproduce-then-fix the trapped `opacity-95` overlay, no mega-z-index allowed). Includes a `VideoCallout` (Kevin Powell offset-parents/stacking-context, `GS6b9p6edfk`, jump to 12:53) + two `ExternalResource`s (MDN Stacking context, Josh Comeau "What the heck, z-index??").
