# Chapter 19.6 prerequisites review

## Missing prerequisites

- Lesson 19.6.2 — async-local-storage shim threading `tx` into a route handler under test. Quote: "The starter wires async-local-storage so a request begun inside `withRollback`'s callback sees the same `tx`. This is a 19.3.1 technique; the lesson does not re-teach it, but does name the wiring file (`src/db/test-tx-context.ts`) so the student knows where the seam lives." Suggested source: 19.3.1 (extend to cover ALS-based `tx` threading as the explicit-handle alternative for route handlers that cannot accept a `db` parameter) or a new lesson in Unit 19 between 19.3.1 and 19.3.2. Severity: medium. Rationale: 19.3.1 explicitly teaches the *opposite* discipline ("Course default: explicit handle") and names ALS only as a dismissed alternative without explaining its mechanics or wiring. 19.6 then uses ALS as the load-bearing isolation seam for integration-testing route handlers, calling it a "19.3.1 technique" while the student has never seen it implemented. A student following the course sequentially would arrive at 19.6 knowing only explicit-handle threading and encounter a starter file (`src/db/test-tx-context.ts`) whose mechanism was never taught.
